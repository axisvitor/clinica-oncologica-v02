═══════════════════════════════════════════════════════════════
FRONTEND ENVIRONMENT VARIABLES ANALYSIS
═══════════════════════════════════════════════════════════════

File: frontend-hormonia/.env
Environment: PRODUCTION
Date: 2025-10-04

═══════════════════════════════════════════════════════════════
1. API CONFIGURATION
═══════════════════════════════════════════════════════════════

✓ Backend API URLs (all VITE_ prefixed - PUBLIC):
  - VITE_API_URL: https://clinica-oncologica-v02-production.up.railway.app
  - VITE_API_BASE_URL: https://clinica-oncologica-v02-production.up.railway.app
  - VITE_API_BASE_PATH: /api/v1
  - VITE_API_TIMEOUT: 30000

✓ Consistency Check:
  - VITE_API_URL matches backend FRONTEND_API_URL ✓
  - VITE_API_BASE_URL matches backend URL ✓

═══════════════════════════════════════════════════════════════
2. WEBSOCKET CONFIGURATION
═══════════════════════════════════════════════════════════════

✓ WebSocket URLs:
  - VITE_WS_URL: wss://clinica-oncologica-v02-production.up.railway.app/ws/connect
  - VITE_WS_BASE_URL: wss://clinica-oncologica-v02-production.up.railway.app/ws/connect

⚠️ DISCREPANCY:
  - Frontend: /ws/connect
  - Backend expected: /ws
  - Needs verification: Is backend configured for /ws/connect route?

═══════════════════════════════════════════════════════════════
3. FIREBASE AUTHENTICATION
═══════════════════════════════════════════════════════════════

✓ Firebase Web Config (PUBLIC keys):
  - VITE_FIREBASE_API_KEY: AIzaSyDbZHMNV2eZQty03TgA4yNo_3L6UDSpHdI
  - VITE_FIREBASE_AUTH_DOMAIN: sistema-oncologico-auth.firebaseapp.com
  - VITE_FIREBASE_PROJECT_ID: sistema-oncologico-auth
  - VITE_FIREBASE_STORAGE_BUCKET: sistema-oncologico-auth.firebasestorage.app
  - VITE_FIREBASE_MESSAGING_SENDER_ID: 608742835827
  - VITE_FIREBASE_APP_ID: 1:608742835827:web:fa12840b0bd4949b7c8c06
  - VITE_FIREBASE_MEASUREMENT_ID: G-2QZQFKJMH2

✓ Cross-reference with backend:
  - VITE_FIREBASE_API_KEY matches FIREBASE_WEB_API_KEY ✓
  - VITE_FIREBASE_PROJECT_ID matches FIREBASE_WEB_PROJECT_ID ✓
  - VITE_FIREBASE_APP_ID matches FIREBASE_WEB_APP_ID ✓
  - VITE_FIREBASE_AUTH_DOMAIN matches FIREBASE_AUTH_DOMAIN ✓

⚠️ NOTE: Frontend has additional config not in backend .env:
  - VITE_FIREBASE_STORAGE_BUCKET (not critical, public config)
  - VITE_FIREBASE_MEASUREMENT_ID (Google Analytics, optional)

═══════════════════════════════════════════════════════════════
4. SUPABASE CONFIGURATION
═══════════════════════════════════════════════════════════════

✓ Supabase Client Config (PUBLIC keys):
  - VITE_SUPABASE_URL: https://rszpypytdciggybbpnrp.supabase.co
  - VITE_SUPABASE_ANON_KEY: eyJhbGc... [matches backend]
  - VITE_SUPABASE_AUTH_ENABLED: true
  - VITE_SUPABASE_REALTIME_ENABLED: true

✓ Cross-reference with backend:
  - VITE_SUPABASE_URL matches SUPABASE_URL ✓
  - VITE_SUPABASE_ANON_KEY matches SUPABASE_ANON_KEY ✓

✅ SECURITY CHECK:
  - Frontend does NOT have SUPABASE_SERVICE_ROLE_KEY (correct!)
  - Frontend does NOT have SUPABASE_BYPASS_RLS (correct!)

═══════════════════════════════════════════════════════════════
5. APPLICATION SETTINGS
═══════════════════════════════════════════════════════════════

✓ Environment:
  - VITE_ENVIRONMENT: production
  - VITE_DEBUG_MODE: false
  - VITE_APP_NAME: Hormonia - Sistema de Gestão Oncológica
  - VITE_APP_VERSION: 2.0.0

✓ Feature Flags:
  - VITE_ENABLE_WHATSAPP_INTEGRATION: true
  - VITE_ENABLE_AI_CHAT: true
  - VITE_ENABLE_EVOLUTION: true
  - VITE_ENABLE_DEBUG_TOOLS: false (production)
  - VITE_USE_MOCK_DATA: false (production)
  - VITE_USE_MOCK_API: false (production)
  - VITE_USE_MOCK_AUTH: false (production)

✓ Security Settings:
  - VITE_ENABLE_CSP: true
  - VITE_FORCE_HTTPS: true
  - VITE_SECURITY_HEADERS_ENABLED: true

═══════════════════════════════════════════════════════════════
6. HARDCODED URLs IN FRONTEND CODE
═══════════════════════════════════════════════════════════════

⚠️ CRITICAL - HARDCODED LOCALHOST URLs:
  File: src/pages/medico/PacientesList.tsx (Line 31)
  - 'http://localhost:3003/api/pacientes'
  ❌ MUST BE CHANGED to use environment variable

  File: src/pages/medico/ProntuarioView.tsx (Lines 42, 56)
  - 'http://localhost:3003/api/pacientes/${pacienteId}'
  - 'http://localhost:3003/api/consultas/paciente/${pacienteId}'
  ❌ MUST BE CHANGED to use environment variable

⚠️ FALLBACK URLs in Runtime Config:
  File: src/lib/runtime-config.ts
  - Line 61-63: Hardcoded production fallback
    * VITE_API_URL: 'https://backend-production-e0bd.up.railway.app'
    * VITE_API_BASE_URL: 'https://backend-production-e0bd.up.railway.app'
    * VITE_WS_BASE_URL: 'wss://backend-production-e0bd.up.railway.app/ws'

  ⚠️ MISMATCH: Fallback URL differs from .env URL!
    - Fallback: backend-production-e0bd.up.railway.app
    - .env: clinica-oncologica-v02-production.up.railway.app
    ❌ MUST BE UPDATED to match current deployment

  File: src/lib/runtime-config.ts (Lines 129-131)
  - Development fallbacks:
    * 'http://127.0.0.1:8000' (API)
    * 'ws://127.0.0.1:8000/ws' (WebSocket)
  ✓ Acceptable for development

  File: src/lib/api-client.ts (Line 47)
  - Fallback: 'http://localhost:8000'
  ✓ Acceptable for development

  File: src/lib/config-initializer.tsx (Lines 70-72)
  - Development fallbacks using 127.0.0.1
  ✓ Acceptable for development

  File: src/hooks/useWebSocket.ts (Line 25)
  - Fallback: 'ws://localhost:8080/ws'
  ⚠️ INCONSISTENT: Uses port 8080 instead of 8000

  File: src/services/whatsapp/WhatsAppService.ts (Line 82)
  - Fallback: 'http://localhost:8000'
  ✓ Acceptable for development

⚠️ MOCK DATA URLs (not critical, used in mock mode only):
  - src/mocks/quiz.mock.ts: 'https://sistema.com/quiz/...'
  - src/lib/mock-api-handler.ts: 'https://sistema.com/quiz/...'
  - src/lib/mock-api-handler.ts: 'http://localhost' (URL parsing only)
  ✓ These are mock data, not production concerns

═══════════════════════════════════════════════════════════════
7. VITE CONFIGURATION
═══════════════════════════════════════════════════════════════

✓ vite.config.ts Runtime Config Injection:
  - Plugin: 'runtime-config-injection'
  - Generates: config.js for runtime environment loading
  - Fallback strategy in place

✓ Development Proxy (only active in dev mode):
  - '/api' → proxies to VITE_API_URL or 'http://127.0.0.1:8000'
  - '/ws' → proxies to VITE_WS_BASE_URL or 'ws://127.0.0.1:8000'

✓ Build Configuration:
  - All VITE_ variables are PUBLIC and embedded in build
  - Runtime config loader provides flexibility for Railway

═══════════════════════════════════════════════════════════════
SUMMARY - FRONTEND CONFIGURATION STATUS
═══════════════════════════════════════════════════════════════

✅ CORRECT:
  1. All Firebase credentials match backend ✓
  2. Supabase credentials match backend ✓
  3. API URLs correctly configured in .env ✓
  4. Security flags appropriate for production ✓
  5. No sensitive backend keys exposed ✓

❌ CRITICAL ISSUES:
  1. HARDCODED localhost:3003 in medico pages (2 files)
     → BREAKS production deployment
     → MUST use VITE_API_URL environment variable

⚠️ WARNINGS:
  1. WebSocket path discrepancy: /ws/connect vs /ws
  2. Runtime config fallback URL outdated
  3. useWebSocket.ts has wrong fallback port (8080 instead of 8000)

💡 RECOMMENDATIONS:
  1. FIX IMMEDIATELY: Replace all hardcoded localhost:3003 URLs
  2. UPDATE runtime-config.ts fallback URLs to match current deployment
  3. VERIFY WebSocket endpoint on backend (/ws or /ws/connect)
  4. Standardize all fallback ports to 8000
  5. Remove or update outdated Railway URLs in fallbacks
