# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M013 planning | security/scope | Escopo de remediação do M013 | Priorizar F-01..F-11 críticos/altos em M013; diferir findings médios e proof gaps independentes para M014/M015 provisionais. | Os riscos críticos/altos concentram exposição imediata de PHI, controle de WhatsApp, IDOR/BOLA e serving público de arquivos privados. Incluir todos os médios agora diluiria a prova e aumentaria risco de uma remediação larga demais. | Yes | collaborative |
| D002 | M013 planning | architecture/security | Arquitetura de controle de autorização para M013 | Usar dependências/helpers compartilhados de auth, role e patient ownership em vez de patches locais endpoint por endpoint. | O relatório mostra falhas repetidas de autorização em vários routers. Controles compartilhados reduzem inconsistência, melhoram testabilidade e seguem padrões já existentes no projeto em `app.dependencies` e `_quiz_shared.py`. | Yes | collaborative |
| D003 | M013 planning | security/error-handling | Estratégia de tratamento de falhas em fronteiras PHI | Falhar fechado para autenticação, ownership, SSRF, arquivos privados e quiz inválido; registrar diagnósticos estruturados sem PHI, tokens ou segredos. | Em um sistema com PHI/LGPD, resposta permissiva ou fallback público é mais perigoso que negação segura. Diagnósticos precisam ajudar investigação sem criar novo vazamento. | Yes | collaborative |
| D004 | M013 planning | architecture/storage-security | Estratégia de serving para arquivos privados e relatórios | Tratar uploads/reports privados como recursos de aplicação servidos por endpoint autenticado/ownership-checked ou URL assinada curta, não por `StaticFiles` público. | `is_public=false` como metadado não protege quando o diretório inteiro é montado em `/uploads`. Relatórios de paciente concentram PHI e não devem depender de obscuridade de filename. | Yes | collaborative |
| D005 | M013 planning | architecture/quiz-security | Modelo de segurança de link/sessão do quiz público | Quiz público deve validar estado opaco/assinado ligado a token, sessão, paciente, expiração e revogação antes de aceitar submit. | O quiz precisa ser público para pacientes, mas session IDs ou cookies controláveis não são prova de autorização. A validação vinculada preserva o fluxo legítimo e bloqueia submissões forjadas ou cruzadas. | Yes | collaborative |
| D006 | M013/S01 planning | security/whatsapp | S01 WhatsApp management auth boundary | Protect every `/api/v2/whatsapp` management endpoint with the existing admin session dependency while keeping the non-PHI `/api/v2/whatsapp/health` check public. | The finding targets anonymous management operations that send/read messages, contacts, queues and instances. Using the canonical admin session dependency fails closed before service construction, while preserving a lightweight health probe avoids breaking monitoring that does not expose PHI or control surfaces. | Yes | agent |
| D007 | M013/S01 planning | security/wuzapi | S01 WuzAPI media SSRF guard seam | Add a reusable WuzAPI media SSRF guard that validates scheme, userinfo, host/port, DNS/IP classification and every manual redirect before `fetch_and_encode_media` performs outbound requests. | Media URLs are attacker-controlled inputs reused by multiple WhatsApp send paths. A single guard module with injectable DNS resolution gives deterministic tests, blocks private/internal destinations before network access, and avoids duplicating SSRF checks in service code. | Yes | agent |
| D008 | M013/S02 planning | security/authorization | S02 patient ownership denial semantics | Return 403 for authenticated doctors attempting to access known patients or patient-bound resources assigned to another doctor; reserve 404 for nonexistent resources and keep denial details generic/no PHI. | Existing patient RBAC tests expect 403 for cross-doctor access, and deterministic 403 assertions prove the ownership boundary without relying on anti-enumeration ambiguity. Generic denial messages maintain fail-closed behavior without leaking PHI. | Yes | agent |
| D009 | M013 S03 planning | security/public-quiz | S03 public quiz authorization model | Use a shared public quiz security seam that validates signed access tokens against the stored quiz session, token hash, link status, patient/template binding, and effective expiration; compatibility endpoints must also require a signed HttpOnly session-state cookie instead of trusting raw quiz_session_id alone. | The quiz flow must remain publicly accessible to patients, but raw session IDs and signature-only JWT checks allow replay, mismatch, expired-link, and cross-patient submissions. Centralizing validation keeps GET/submit/access/session/submit/logout behavior consistent and supports PHI-free denial diagnostics. | Yes, if a deliberate token-rotation grace-period policy is designed and covered by tests. | agent |
| D010 | M013/S04 planning | architecture/storage-security | S04 local upload and report storage layout | Mount only the public upload subdirectory at `/uploads`, keep private uploads and generated patient report artifacts under an unmounted private subdirectory, and serve private files through `GET /api/v2/upload/{upload_id}/download` with authenticated owner/admin checks. | This concretizes D004 for the existing local filesystem implementation: moving the static mount to public-only removes unauthenticated access to PHI while preserving an optional public asset path, and a gated download endpoint gives tests a real authorized retrieval path without relying on filename obscurity or long-lived signed URLs. | Yes, if a later storage backend introduces short-lived signed URLs with equivalent ownership and expiry tests. | agent |
| D011 | M013/S04/T02 | backend/upload-security | Separate local upload storage into public static assets and private API-gated artifacts. | Mount only the public upload root at `/uploads`; store new private uploads under an unmounted sibling private root with `private/...` logical storage paths and serve them only via `/api/v2/upload/{upload_id}/download`. | A public-root-only static mount prevents generated private files from becoming reachable through `/uploads`, while visibility-prefixed logical storage paths let handlers resolve paths safely and fail closed for prefix mismatches or traversal. | Yes | agent |
| D012 | M013/S05 planning | security/report-authorization | How cache-backed report resources are authorized in M013/S05 | Authorize report downloads, exports, sharing, history, and restore operations using raw report/export metadata: admin may access existing resources; non-admin users must match a raw owner field such as generated_by/created_by or prove assignment to every patient_id on the raw resource. Missing, malformed, or normalization-derived ownership evidence fails closed. | Report APIs are Redis/cache-heavy and normalization helpers can default missing created_by to the current requester, so authorization must run before normalization and cannot treat absent owner metadata as ownership. This preserves legitimate owner/admin behavior while preventing report UUID enumeration, owner laundering, and cross-doctor PHI exposure. | Yes | agent |
| D013 | M013/S06 planning | security/report-artifacts | How S06 closes remaining R007 generated report artifact/log leakage risk | Generated patient report task artifacts should use opaque report_id-only PDF filenames, and task diagnostics should omit raw/sanitized free-form report_type or map it only to a strict non-PHI category such as other. | S06 research and current tests show sanitized free-form report_type can preserve patient names or other PHI in private artifact filenames and logs. Opaque report_id-only filenames plus allowlisted diagnostics preserve debuggability without treating sanitization as redaction. | Yes, if a future product requirement needs human-readable non-PHI report categories backed by a reviewed allowlist. | agent |
| D014 | M013/S06 integrated security proof | security/observability | Generated patient report artifacts and report-task diagnostics must not expose free-form report_type input. | Use report-id-only opaque `{report_id}.pdf` filenames under the private report artifact root; omit raw/sanitized report_type from Taskiq diagnostics while retaining task_name, report_id, status, reason, and failure_type. | Report types can contain patient-identifying, token-like, or traversal-shaped input. Opaque filenames and allowlisted diagnostics close the R007 PHI/log leakage gap without losing failure visibility for future debugging. | Yes, if a future allowlisted report-type taxonomy is introduced without free-form values. | agent |
| D015 | M014 plan-milestone | planning | Ordem e decomposição estrutural de M014 | Planejar M014 em cinco slices verticais: S01 ingress/replay/rate-limit, S02 ADK ownership, S03 browser PHI cache/quiz, S04 upload stored-XSS/private serving, S05 JWT/config posture + matriz/regressão. | A pesquisa e o contexto já priorizam controles de entrada e prova controlada. Essa ordem aposenta primeiro riscos externos antes de aprofundar ownership, persistência no browser, serving de arquivos e fechamento documental, enquanto S05 impede que itens R012/R013 sumam sem evidência ou owner. | Yes | collaborative |
| D016 | M014 plan-milestone | security-proof | Limite de prova operacional de M014 | Tratar JWT multi-worker, deployment secrets, DB TLS/RLS e dependências WuzAPI/Gemini como provas controladas/posture checks em M014; qualquer harness produção-like permanece explicitamente em R014/M015. | M014 precisa fechar hardening médio e proof gaps sem introduzir segredos, serviços vivos ou dados reais. Classificar itens de postura evita superprometer garantias runtime que a matriz controlada não exercita e mantém a milestone compatível com LGPD/PHI-safe evidence. | Yes | collaborative |
| D017 | M014/S01 plan-slice | M014/S01 ingress security | S01 ingress hardening strategy | Use shared fail-closed seams for browser CSRF contraction, password-reset token JTI consumption, webhook HMAC/timestamp/idempotency checks, and trusted-proxy-aware rate-limit identity instead of route-local permissive fallbacks. | S01 spans multiple externally reachable state-changing boundaries. Shared guards keep denial ordering testable before DB, queue, or provider effects, reduce duplicated allow-on-error behavior, and preserve PHI-safe diagnostics for the reviewer evidence matrix. | Yes | agent |
| D018 | M014/S01/T01 | security/ingress | Rate-limit client identity and infrastructure-failure policy for M014/S01 | Use a shared trusted-proxy resolver for rate-limit/audit identity and fail closed on Redis/rate-limit infrastructure failures for ingress paths. | Externally reachable endpoints must not let callers spoof X-Forwarded-For or continue into DB/queue/provider work when rate-limit infrastructure is unavailable; a shared helper and fail-closed defaults make the denial semantics consistent and testable while PHI-safe logs preserve diagnostics. | Yes, if a non-ingress background workload needs explicit fail-open behavior with separate tests. | agent |
| D019 | M014/S01/T04 | security/webhooks | Webhook replay and idempotency failure semantics | Fail closed at webhook ingress: invalid HMAC/timestamp denies before parsing/processing, duplicate idempotency denies 409, and Redis/DB/idempotency uncertainty denies 503 with PHI-safe structured diagnostics. | External webhook retries are state-changing ingress and cannot safely proceed when authenticity or uniqueness cannot be proven. Honest 4xx/5xx status codes prevent replay-as-success behavior while protecting DB, queue, DLQ, provider, and flow side effects from duplicate or unauthenticated requests. | Yes | agent |
| D020 | M014/S02 plan-slice | M014/S02 security/ADK | ADK endpoint and lifecycle identity authority | Require canonical session-authenticated user identity at `/api/v2/adk/run`, reject payload or nested context user mismatches before wrapper/runtime execution, and enforce stored session/invocation `user_id` ownership for resume, close, and cancel. | The current ADK route is included without an auth dependency and forwards payload-supplied identity, while runtime state already stores owners but does not check them. A route gate plus runtime ownership seam fails closed before Gemini/tool side effects and prevents guessed session or invocation IDs from crossing users under controlled fixtures. | Yes, if ADK later gets a separate non-cookie service-account auth contract with equivalent ownership proof and tests. | agent |
| D021 | M014/S03 planning | browser-cache-persistence | M014/S03 browser PHI cache and persistence hardening strategy | Classify browser-sensitive backend responses as no-store and bypass HTTP cache, persist React Query state only through an explicit non-PHI allowlist, and remove quiz answer/free-text/patient-label localStorage persistence in favor of HttpOnly cookie plus backend session recovery. | The confirmed code paths cache session-cookie GETs as public responses and persist dashboard/quiz patient-bound data in IndexedDB/localStorage. A deny-by-default persistence contract closes R012/R013 proof gaps while preserving intentional non-PHI cache/performance behavior under controlled tests. | Yes — if product later requires offline/resume beyond backend state, it must be redesigned around non-PHI metadata and covered by equivalent no-PHI persistence tests. | agent |
| D022 | M014/S04 planning | security/upload-artifacts | M014/S04 active web-document uploads and legacy artifact serving policy | Reject active web-document upload shapes before durable persistence and serve any legacy/private active-looking artifacts only through authenticated owner/admin routes as non-executable attachments with nosniff and safe content type fallback. | The existing private/public storage split prevents direct path exposure, but stored-XSS proof still requires closing spoofed HTML/SVG/XML/script uploads and ensuring already persisted active-looking artifacts cannot execute when retrieved. A shared fail-closed policy keeps upload, avatar, private download, and report/export artifact behavior consistent while preserving controlled fixture-based proof. | Yes, if a future product requirement explicitly needs HTML/SVG upload support with sanitization and a separate sandboxed serving domain. | agent |
| D023 | M015 Runtime Security Validation | planning | M015 risk-first slice order | Plan S01 as the runtime harness plus DB TLS/RLS proof, then S02 session/queue, S03 provider stubs, S04 private artifacts, and S05 final runner/evidence closure. | The local TLS PostgreSQL and harness substrate are the most brittle unknowns and unlock every downstream seam. Session/queue behavior must precede provider and artifact checks that rely on authenticated runtime clients, while the evidence matrix belongs last so it validates fresh seam evidence instead of becoming report-only. | Yes, if S01 implementation reveals a different dependency bottleneck during refine-slice or execution. | agent |
| D024 | M015 Runtime Security Validation | tooling | M015 validation entrypoint shape | Use one seam-filterable M015 validation runner that can execute individual seam checks during slice work and the full matrix at final closure. | A single runner satisfies the user-visible outcome and prevents final integration drift, while seam filters keep each slice demoable and debuggable without waiting for all seams to exist. This also makes CI promotion easier after M015 without adding live secrets or provider dependencies. | Yes, if the runner becomes too complex and a thin orchestrator over smaller scripts is needed while preserving the single public entrypoint. | agent |
| D025 | M015/S01 planning | tooling/runtime-harness | M015 S01 harness file layout and TLS proof boundary | Use a root `scripts/security/verify-m015-runtime-security.sh` entrypoint, committed harness assets under `scripts/security/m015-runtime/`, sanitized evidence under `backend-hormonia/docs/reports/security/m015/`, ignored generated runtime material under `.m015-runtime/`, and fix the backend async DB TLS handling before claiming the FastAPI process participates in a strict TLS DB runtime. | The roadmap promises one seam-filterable runner from the repository root while the current backend compose stack lacks PostgreSQL and uses live-provider-oriented wiring. Keeping harness assets outside the backend default compose avoids destabilizing local development, preserving evidence under docs makes reviewer artifacts durable, and strict TLS in the async engine prevents S01 from proving TLS only through a side-channel psycopg probe while the actual API would silently weaken certificate verification. | Yes, if implementation proves Docker/CI constraints require a different internal layout while preserving the same public entrypoint and evidence/redaction contract. | agent |
