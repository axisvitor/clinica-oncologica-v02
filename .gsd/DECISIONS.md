# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| 1 | 2026-03-14 | M004/S03/T04 | Canonical frontend admin/user contract | Drop Firebase-shaped admin/user fields (`firebase_uid`, provider-era metadata) from the official frontend/shared type surfaces and normalizers; only keep minimal routed mock cleanup when a source file still needs to satisfy the narrowed type | The shipped frontend auth path is already backend-cookie session-first, so preserving Firebase-shaped canonical fields only keeps dead semantics alive in types, mocks, and narrative surfaces | Yes — if a routed runtime consumer later proves it still needs a backend field, reintroduce it from observed usage instead of legacy inertia |
| 2 | 2026-03-14 | M004/S03/T05 | Post-cut residue publication | Keep the S01 `frontend` scopes in `runtime-residue-allowlist.json` with `approved: []` once S03 removes all official frontend residue; do not delete the scopes or rename the categories | Empty approved sets preserve the existing category/scope vocabulary and turn any frontend auth/session regression into an explicit verifier failure instead of silent drift or ad-hoc bookkeeping | Yes — if the verifier later gains a clearer first-class zero-scope representation, the encoding can change without changing the boundary meaning |
| 3 | 2026-03-14 | M004/S04/T01 | Official staff session transport | Treat the session cookie as the only accepted transport at the auth chokepoints; ignore or explicitly reject `X-Session-ID`, session-as-Bearer, and websocket `session_id` query/header handshakes instead of preserving precedence rules | S04 only becomes real when the live resolver/extractor layer stops honoring legacy fallback transport; keeping precedence logic anywhere leaves a bypass path even if the frontend stopped sending it | Yes — if a future runtime introduces a new official transport, add it as a new canonical contract with its own focused proof instead of reviving the legacy ones |
| 4 | 2026-03-14 | M004/S04/T03 | Root `/session/*` retirement boundary | Keep root `/session/*` mounted only as an explicit 410 tombstone with stable diagnostics, and move that surface out of the S01 residue allowlist into focused pytest | The route is intentionally dead but still mounted, so treating it as approved verifier debt obscures the difference between live residue and explicit retirement; focused proof makes revival, disappearance, and stale bookkeeping separate failure modes | Yes — if the runtime later deletes the tombstone entirely or introduces a new explicit retirement mechanism, republish the proof surface instead of re-approving the old route |
| 5 | 2026-03-14 | M004/S04/T02 | Legacy transport residue in helper/admin wrappers | Helper and admin wrapper seams may still observe `X-Session-ID` or session-as-Bearer only for rejection/detection or test-bypass suppression, but they must never forward those values into accepted staff session resolution | The hard cut is not real if downstream wrappers silently keep honoring legacy transport; keeping only detection scaffolding preserves diagnostics and honest tests without reopening the canonical cookie-only contract | Yes — once the remaining detection scaffolding is proven unnecessary, delete it instead of treating it as compatibility |
| 6 | 2026-03-14 | M004/S05 | Adjacent Firebase residue proof shape | Execute S05 in ordered passes (auth syntax/core Redis session contract → shared login/cache adapters → audit/admin/docs surfaces → adjacent frontend types → residue republication) and pair the S01 residue guard with focused audit/docs/type proof instead of treating the guard as sufficient by itself | The remaining Firebase problem spans both live runtime identity pivots and adjacent surfaces that the S01 verifier intentionally excludes, so one blended proof would hide which boundary still treats Firebase as live | Yes — if a later verifier version covers these adjacent surfaces directly, the proof pack can be collapsed without changing the slice goal |
| 7 | 2026-03-14 | M004/S05/T01 | Core staff-session identity pivot | Treat Redis session creation, rehydration, listing, and bulk invalidation as `user_id`-only operations; any leftover Firebase-era identity is compatibility data outside the live pivot, and the legacy Firebase module must load lazily so session-first imports stay usable while later cleanup continues | This cuts the deepest runtime dependency without forcing T01 to finish every adjacent Firebase surface, and it keeps auth/session unit proof importable even while later slice tasks still own remaining legacy files and websocket/test cleanup | Yes — if a later slice removes the remaining compatibility metadata and lazy legacy seam entirely, the import and payload contracts can narrow further |
| 8 | 2026-03-14 | M004/S05/T03 | Adjacent audit/admin/docs runtime contract | Treat runtime audit context, admin audit serialization/export, and routed operator docs/examples as canonical `user_id` + cookie-backed session surfaces; strip `firebase_uid` from emitted runtime payloads/metadata and stop documenting manual session-header transport | The auth/session cut is still operationally ambiguous if audit output, admin exports, or operator guidance keep advertising Firebase-era identity and header-based session semantics after the live runtime already moved to canonical cookie-backed behavior | Yes — if M005 later promotes a different historical/export contract, add it explicitly instead of leaking legacy identity back into the live runtime story |
| 9 | 2026-03-14 | M004/S05/T04 | Adjacent frontend auth type boundary | Remove dead auth-provider enums from frontend RBAC barrels entirely and pin the narrowed user/medico contracts with type-level proof instead of keeping provider-era compatibility exports | The official frontend runtime is already session-first, so preserving `AuthProvider` or Firebase-claims naming in adjacent type barrels would keep stale imports compiling and quietly reintroduce the wrong contract | Yes — if a future runtime genuinely needs a provider discriminator again, add a new canonical surface from observed usage rather than reviving the legacy enum |
| 10 | 2026-03-14 | M004/S05/T05 | Post-S05 residue boundary meaning | Republish S01 so surviving verifier hits after S05 are treated as passive compatibility/rejection bookkeeping only; delete approvals for cleaned shared-auth/Redis/auth-user-adapter surfaces and keep those cuts under focused proof instead of padding the allowlist | Once S05 removed live `firebase_uid` emission and adjacent Firebase narrative from the canonical runtime story, leaving dead approvals in the allowlist would make the boundary lie; the durable contract is a smaller runtime residue map plus focused proof packs for what left it | Yes — if a later slice deletes the remaining approved hits or folds the focused proof into the verifier, republish the boundary again rather than preserving stale bookkeeping |
| 11 | 2026-03-14 | M004/S05/T02 | Shared auth/cache payload sanitization | Treat shared session restore and cache adapters as canonical `user_id` surfaces only, and sanitize stale cached payloads before returning them instead of preserving `firebase_uid` in emitted runtime auth state | The live contract is still Firebase-shaped if stale cache/session payloads keep echoing `firebase_uid` back into login/restore/websocket-adjacent flows even after the core session pivot moved to `user_id` | Yes — if a future migration deletes the legacy cache fields entirely, the sanitization path can be removed along with the remaining compatibility readers |
| 12 | 2026-03-14 | M004/S06 | Mounted no-Firebase proof topology | Keep S06 narrow and replayable: reuse the published no-Firebase stack launch contract, drive the existing Chromium auth acceptance on the live stack, add only a thin routed smoke for `/dashboard`, `/admin`, and `/whatsapp`, and publish the replay path as the slice evidence | Broad repo-wide sweeps are noisy and dishonest here because historical suites and dormant files still carry pre-cut assumptions; S06 needs mounted proof of the canonical runtime, not generic cleanup coverage | Yes — if later infrastructure provides a cleaner assembled-stack verifier, the proof pack can collapse into that entrypoint without widening scope |
| 13 | 2026-03-14 | M004/S06/T01 | Proof-user redaction contract | Keep only masked proof artifacts in `/tmp` and generate real `E2E_SESSION_FIRST_*` exports on demand through a reseeding bootstrap helper instead of persisting plaintext credentials or reset tokens | S06 needs replayable auth fixtures for Playwright and browser smoke, but the durable inspection surface must stay masked and outside the repo; reseeding on demand keeps the runtime proof repeatable without leaving secrets at rest | Yes — if a later secret-safe local fixture store exists, the bootstrap helper can target it instead of reseeding from the database each run |
| 14 | 2026-03-14 | M004/S06/T02 | Mounted preflight probe truthfulness | A successful `run-mounted-proof.sh --preflight` should keep the mounted backend/frontend alive only briefly for immediate follow-up health/config curls, then auto-clean the listeners instead of tearing the stack down before the published verification command can run | The slice’s written verification and observability contract includes live post-preflight probes; short-lived preservation keeps that command truthful without leaving long-lived local residue or changing the auth/session runtime boundary | Yes — if a later runner can expose the same live probes through a cleaner persistent-process contract, this short hold window can be removed or replaced |
| 15 | 2026-03-14 | M005 planning | M005 slice ordering | Execute M005 as Alembic operability → explicit historical Firebase boundary → canonical head convergence → assembled upgrade/runtime proof; do not start live schema removal before the graph is independently traversable | The first hard risk is that Alembic itself still depends on unrelated runtime secrets, so downstream schema cleanup cannot be proven honestly until the migration control plane works on its own | Yes — if execution shows the graph becomes operable after a smaller contained fix, later slices can collapse without changing the dependency principle |
| 16 | 2026-03-14 | M005 planning | Firebase-era data retention boundary | Any Firebase-era data kept after M005 must live behind an explicit archival/historical boundary and must not remain part of the canonical live model contract | This preserves forensic value where needed without leaving `firebase_uid` or sync-era structures alive as hidden runtime dependencies | Yes — if execution proves a retained field is still operationally required, add a new decision before promoting it back into a live contract |
| 17 | 2026-03-14 | M005 planning | Historical Alembic revision policy | Do not rewrite merge/no-op/one-way revisions for cosmetic linearity; only patch old revisions when necessary to remove runtime side effects that currently block traversal or upgrade, preserving their database semantics | The graph already contains honest scars, and rewriting history to look cleaner risks breaking upgrade semantics while hiding why the graph looks the way it does | Yes — if execution uncovers a revision whose stored semantics are actually wrong rather than operationally entangled, add a new corrective decision before changing it |
| 18 | 2026-03-14 | M005/S01/T01 | Alembic bootstrap import boundary | Keep declarative metadata and migration bootstrap helpers in settings-free `app.db` modules, and make package-level `app.utils` / `app.integrations*` exports lazy so deep model imports do not bootstrap runtime settings | Alembic control-plane commands need to load model metadata and resolve the DB URL without touching `app.config.settings`; lazy package exports preserve public import surfaces while removing accidental runtime side effects from package initialization | Yes — if the project later introduces a slimmer dedicated migrations model surface, the helper modules and lazy export shims can be narrowed again without restoring runtime-coupled imports |
| 19 | 2026-03-14 | M005/S01/T02 | Historical migration helper import seam | Keep migration-only helper source alongside the revisions in `backend-hormonia/alembic/runtime_helpers.py`, but import it through a backend-root shim `backend-hormonia/alembic_runtime_helpers.py` instead of `alembic.*` | The installed Alembic package already owns the `alembic` import name, so a thin shim preserves helper ownership next to the migration graph without shadowing upstream Alembic or reintroducing runtime-coupled imports | Yes — if the project later gains an importable local migrations package/namespace, the shim can be removed and the revisions can import that namespace directly |
| 20 | 2026-03-14 | M005/S01/T03 | Clean-replay migration operability guardrails | Runtime-coupled backfill revisions must prove pending rows before importing app services, and clean-replay alignment revisions may guard missing legacy-only DB objects instead of failing the entire `upgrade head` path | S01 needs Alembic control-plane commands to work on a fresh Postgres with only database config; importing runtime encryption services on empty backfills or assuming legacy-only enum objects on the clean path turns upgrade replay into a false dependency on unrelated runtime history | Yes — S03 can tighten or replace these guards once canonical clean-schema convergence is implemented and proven |
| 21 | 2026-03-14 | M005/S02/T01 | Firebase sync history publication | Publish `user_sync_log` as the explicit archival table `firebase_sync_history`, and model it as append-only history without `updated_at` or live-domain exports | The preserved Firebase sync rows still matter for replay and forensics, but keeping them under a live-looking `user_sync_log` model — especially one that advertised a non-existent `updated_at` column — obscures the boundary and makes runtime writes dishonest | Yes — if S03 later proves the retained legacy residue columns can be safely collapsed or moved again, republish the history surface instead of reviving `user_sync_log` as a live contract |
| 22 | 2026-03-14 | M005/S02/T02 | Canonical `firebase_uid` quarantine | Strip `firebase_uid` from canonical audit writes and official user/admin/physician payloads even when legacy callers or stale caches still carry it; keep any preserved audit residue behind historical/read-only admin filters only | The slice boundary lies if old writers, serializer helpers, or cached payloads can quietly republish `firebase_uid` as live contract data after the historical boundary was published | Yes — if S03 later removes the remaining historical residue entirely, the quarantine sanitizers and filtered read-only seam can be deleted with that proof |
| 23 | 2026-03-15 | M005/S02/T03 | Historical audit fixture truthfulness | Shared Postgres test schema guards may patch only live `audit_logs` columns; they must not recreate `firebase_uid` or `idx_audit_firebase_time`, and the migration proof must exercise both shared conftest guards against a stripped audit table | The historical boundary is dishonest if tests silently backfill Firebase-era audit residue into old schemas; explicit proof turns future fixture drift into a named failure instead of a hidden mutation | Yes — if a later slice intentionally republishes Firebase audit residue as live schema, update the guards and the fixture-drift proof together instead of letting fixtures diverge |
| 24 | 2026-03-15 | M005/S03 planning | Canonical head convergence scope | Converge S03 with new linear alignment revisions only over `users`, `audit_logs`, and `firebase_sync_history`; republish still-live Firebase-named profile/settings data under neutral canonical storage, keep `firebase_uid` and `auth_provider` only as explicit compatibility residue while current readers/tests still require them, and remove `audit_logs.firebase_uid` plus transitional sync columns from the canonical head contract instead of manufacturing them into clean replay | This closes the honest-head contradiction without widening S03 into repo-wide metadata cleanup or inventing fresh live Firebase residue just to satisfy stale ORM expectations | Yes — if execution proves a preserved field is still operationally required or safe to delete outright, publish a narrower superseding decision instead of silently drifting the head contract |
| 25 | 2026-03-15 | M005/S03/T01 | Canonical users contract transition shape | Treat `users` canonical profile/settings storage as the live contract immediately, but keep route/helper writes dual-writing the old Firebase-named columns/claims until the remaining readers and fixtures are removed | The new neutral columns need to become the source of truth now, but hard-cutting the legacy storage in the same task would break existing helpers, sync paths, and shared test fixtures before the rest of S03/T02 can republish them cleanly | Yes — once the remaining readers are migrated or deleted, the legacy mirrors can be removed instead of expanded |
