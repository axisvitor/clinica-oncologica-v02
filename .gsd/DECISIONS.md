# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| 1 | 2026-03-14 | M004/S03/T04 | Canonical frontend admin/user contract | Drop Firebase-shaped admin/user fields (`firebase_uid`, provider-era metadata) from the official frontend/shared type surfaces and normalizers; only keep minimal routed mock cleanup when a source file still needs to satisfy the narrowed type | The shipped frontend auth path is already backend-cookie session-first, so preserving Firebase-shaped canonical fields only keeps dead semantics alive in types, mocks, and narrative surfaces | Yes — if a routed runtime consumer later proves it still needs a backend field, reintroduce it from observed usage instead of legacy inertia |
| 2 | 2026-03-14 | M004/S03/T05 | Post-cut residue publication | Keep the S01 `frontend` scopes in `runtime-residue-allowlist.json` with `approved: []` once S03 removes all official frontend residue; do not delete the scopes or rename the categories | Empty approved sets preserve the existing category/scope vocabulary and turn any frontend auth/session regression into an explicit verifier failure instead of silent drift or ad-hoc bookkeeping | Yes — if the verifier later gains a clearer first-class zero-scope representation, the encoding can change without changing the boundary meaning |
| 3 | 2026-03-14 | M004/S04/T01 | Official staff session transport | Treat the session cookie as the only accepted transport at the auth chokepoints; ignore or explicitly reject `X-Session-ID`, session-as-Bearer, and websocket `session_id` query/header handshakes instead of preserving precedence rules | S04 only becomes real when the live resolver/extractor layer stops honoring legacy fallback transport; keeping precedence logic anywhere leaves a bypass path even if the frontend stopped sending it | Yes — if a future runtime introduces a new official transport, add it as a new canonical contract with its own focused proof instead of reviving the legacy ones |
| 4 | 2026-03-14 | M004/S04/T03 | Root `/session/*` retirement boundary | Keep root `/session/*` mounted only as an explicit 410 tombstone with stable diagnostics, and move that surface out of the S01 residue allowlist into focused pytest | The route is intentionally dead but still mounted, so treating it as approved verifier debt obscures the difference between live residue and explicit retirement; focused proof makes revival, disappearance, and stale bookkeeping separate failure modes | Yes — if the runtime later deletes the tombstone entirely or introduces a new explicit retirement mechanism, republish the proof surface instead of re-approving the old route |
| 5 | 2026-03-14 | M004/S04/T02 | Legacy transport residue in helper/admin wrappers | Helper and admin wrapper seams may still observe `X-Session-ID` or session-as-Bearer only for rejection/detection or test-bypass suppression, but they must never forward those values into accepted staff session resolution | The hard cut is not real if downstream wrappers silently keep honoring legacy transport; keeping only detection scaffolding preserves diagnostics and honest tests without reopening the canonical cookie-only contract | Yes — once the remaining detection scaffolding is proven unnecessary, delete it instead of treating it as compatibility |
| 6 | 2026-03-14 | M004/S05 | Adjacent Firebase residue proof shape | Execute S05 in ordered passes (auth syntax/core Redis session contract → shared login/cache adapters → audit/admin/docs surfaces → adjacent frontend types → residue republication) and pair the S01 residue guard with focused audit/docs/type proof instead of treating the guard as sufficient by itself | The remaining Firebase problem spans both live runtime identity pivots and adjacent surfaces that the S01 verifier intentionally excludes, so one blended proof would hide which boundary still treats Firebase as live | Yes — if a later verifier version covers these adjacent surfaces directly, the proof pack can be collapsed without changing the slice goal |
| 7 | 2026-03-14 | M004/S05/T01 | Core staff-session identity pivot | Treat Redis session creation, rehydration, listing, and bulk invalidation as `user_id`-only operations; any leftover Firebase-era identity is compatibility data outside the live pivot, and the legacy Firebase module must load lazily so session-first imports stay usable while later cleanup continues | This cuts the deepest runtime dependency without forcing T01 to finish every adjacent Firebase surface, and it keeps auth/session unit proof importable even while later slice tasks still own remaining legacy files and websocket/test cleanup | Yes — if a later slice removes the remaining compatibility metadata and lazy legacy seam entirely, the import and payload contracts can narrow further |
| 8 | 2026-03-14 | M004/S05/T03 | Adjacent audit/admin/docs runtime contract | Treat runtime audit context, admin audit serialization/export, and routed operator docs/examples as canonical `user_id` + cookie-backed session surfaces; strip `firebase_uid` from emitted runtime payloads/metadata and stop documenting manual session-header transport | The auth/session cut is still operationally ambiguous if audit output, admin exports, or operator guidance keep advertising Firebase-era identity and header-based session semantics after the live runtime already moved to canonical cookie-backed behavior | Yes — if M005 later promotes a different historical/export contract, add it explicitly instead of leaking legacy identity back into the live runtime story |
| 9 | 2026-03-14 | M004/S05/T04 | Adjacent frontend auth type boundary | Remove dead auth-provider enums from frontend RBAC barrels entirely and pin the narrowed user/medico contracts with type-level proof instead of keeping provider-era compatibility exports | The official frontend runtime is already session-first, so preserving `AuthProvider` or Firebase-claims naming in adjacent type barrels would keep stale imports compiling and quietly reintroduce the wrong contract | Yes — if a future runtime genuinely needs a provider discriminator again, add a new canonical surface from observed usage rather than reviving the legacy enum |
| 10 | 2026-03-14 | M004/S05/T05 | Post-S05 residue boundary meaning | Republish S01 so surviving verifier hits after S05 are treated as passive compatibility/rejection bookkeeping only; delete approvals for cleaned shared-auth/Redis/auth-user-adapter surfaces and keep those cuts under focused proof instead of padding the allowlist | Once S05 removed live `firebase_uid` emission and adjacent Firebase narrative from the canonical runtime story, leaving dead approvals in the allowlist would make the boundary lie; the durable contract is a smaller runtime residue map plus focused proof packs for what left it | Yes — if a later slice deletes the remaining approved hits or folds the focused proof into the verifier, republish the boundary again rather than preserving stale bookkeeping |
| 11 | 2026-03-14 | M004/S05/T02 | Shared auth/cache payload sanitization | Treat shared session restore and cache adapters as canonical `user_id` surfaces only, and sanitize stale cached payloads before returning them instead of preserving `firebase_uid` in emitted runtime auth state | The live contract is still Firebase-shaped if stale cache/session payloads keep echoing `firebase_uid` back into login/restore/websocket-adjacent flows even after the core session pivot moved to `user_id` | Yes — if a future migration deletes the legacy cache fields entirely, the sanitization path can be removed along with the remaining compatibility readers |
