# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M009/S01/T02 | M009/S01 | Taskiq health check strategy during Celery coexistence | Health endpoints check Taskiq first (async Redis ping), then Celery (control.inspect), report both; workers check passes if either is healthy | During migration (S02-S04), both Celery and Taskiq may be running simultaneously. The readiness probe must not report unhealthy just because one system is down while the other handles tasks. Taskiq check is async + fast (Redis ping), Celery check is thread-based + slow (inspect with 0.5s timeout). | Yes — once Celery is fully removed (after S04), drop Celery checks entirely |
| D002 | M009/S01/T01 | M009/S01 | Taskiq broker type selection | ListQueueBroker over RedisStreamBroker | Simpler FIFO dispatch with no ack/consumer-group overhead. Tasks already have their own retry logic via SmartRetryMiddleware, so stream acknowledgement adds complexity without benefit. Dragonfly compatibility is proven with ListQueueBroker. | Yes — revisit if task loss becomes a concern under heavy load |
| D003 | M009/S01/T01 | M009/S01 | Taskiq broker module import strategy | Broker reads Redis URL from env vars directly (TASKIQ_BROKER_URL → CELERY_BROKER_URL → REDIS_URL → default), not from app.config.settings | Worker processes and standalone tests need to import the broker without triggering the full settings validation chain (which requires DATABASE_URL, WuzAPI tokens, etc). Keeps the broker module lightweight and independently importable. | No — fundamental constraint for worker process startup |
| D004 | M009/S01/T01 | M009/S01 | Redis client version range for Taskiq compatibility | Bump redis from <7.0.0 to <8.0.0 in requirements.txt | taskiq-redis requires redis 7.x (specifically pulls 7.3.0). The previous cap at <7.0.0 blocked installation. Verified Dragonfly compatibility with redis 7.x client. | Yes — pin tighter if redis 7.x introduces issues with Dragonfly |
| D005 | M009/S01/T02 | M009/S01 | DB session strategy for Taskiq tasks | AsyncSession via TaskiqDepends(get_db_session) replacing Celery sync get_scoped_session() | Taskiq is async-native, so tasks use AsyncSession directly — no sync-to-async bridging needed. Session created fresh per task execution, auto-rollback on exception, auto-close after completion. | No — core pattern for all subsequent task migrations |
| D006 | M009/S02/T01 | M009/S02 | ETA/delayed dispatch replacement for Celery .apply_async(eta=datetime) | ListRedisScheduleSource from taskiq-redis for dynamic one-shot scheduling via schedule_by_time() | Taskiq's LabelScheduleSource (used for cron/interval) raises NotImplementedError on add_schedule(). ListRedisScheduleSource stores one-shot schedules in Redis and fires them at specified time — direct replacement for Celery's eta parameter. Three call sites need this: send_bulk_messages, task_scheduler.py, retry_handler.py. The scheduler process must be running to fire these schedules (same operational requirement as Celery beat). | Yes — could switch to Redis delayed queue if ListRedisScheduleSource proves unreliable with Dragonfly |
| D007 | M009/S02 | M009/S02 | Messaging task migration strategy: parallel module vs in-place replacement | Create `messaging_taskiq.py` alongside existing `messaging.py` — Celery tasks remain intact for non-messaging callers (flow_automation.py, batch_tasks.py) | The slice scope explicitly states "Celery e Taskiq coexistem — apenas messaging usa Taskiq." External call sites in flow_automation.py (S03) and batch_tasks.py (S03) still use send_scheduled_message.delay(). Replacing the Celery task in-place would break those .delay() calls. A parallel module keeps Celery tasks alive for non-messaging callers while messaging-domain callers (API routes, task_scheduler, retry_handler) switch to .kiq(). S03 migrates flow callers; S05 deletes the Celery versions. | No — this is the migration coexistence strategy for S02-S04 |
| D008 | M009/S02/T01 | M009/S02 | ETA/delayed dispatch replacement for Celery .apply_async(eta=datetime) | ListRedisScheduleSource from taskiq-redis for dynamic one-shot scheduling via schedule_by_time() | Taskiq's LabelScheduleSource (used for cron/interval) raises NotImplementedError on add_schedule(). ListRedisScheduleSource stores one-shot schedules in Redis and fires them at specified time — direct replacement for Celery's eta parameter. Three call sites need this: send_bulk_messages, task_scheduler.py, retry_handler.py. The scheduler process must be running to fire these schedules (same operational requirement as Celery beat). | Yes — could switch to Redis delayed queue if ListRedisScheduleSource proves unreliable with Dragonfly |
| D009 | M009/S03 | M009/S03 | How to handle sync-only ORM services (FlowStateRepository, FlowManagementService, SequentialMessageHandler) inside async Taskiq tasks | Wrap sync services in get_scoped_session() context manager within async task body — do not rewrite deep service code to async | These services use sync SQLAlchemy ORM throughout (db.query(), db.commit()). Rewriting them to async would cascade through dozens of files and is out of scope for the Celery→Taskiq migration. The sync session in an async task is safe because the sync ORM calls are I/O-blocking but brief. Pattern proven by 14 tasks in flows_taskiq.py. | Yes — could convert services to async ORM in a future milestone if performance warrants it |
| D010 | M009/S03/T04 | M009/S03 | How to handle recovery.py attempt_recovery() which dispatches via Celery .delay() from a sync call chain | Keep Celery .delay() intact during coexistence with TODO(S05) marker — defer migration to S05 Celery removal slice | attempt_recovery() is a sync function called from detect_stuck_flows via sync find_stuck_flows/attempt_recovery chain. Converting to async would require making the entire chain async, which cascades into the stuck detection service layer. Since Celery still exists during S03 coexistence, the .delay() call works. S05 removes Celery and must convert this call site. | No — S05 must resolve this before Celery removal |
| D011 |  | M009/S04 | How to handle MonitoringTask class hierarchy (8 subclasses with inheritance) in Taskiq migration | Fully flatten 8 MonitoringTask subclasses into 8 standalone @broker.task() async functions — eliminate class hierarchy, self.log/self.create_success_result patterns | Taskiq tasks are async functions, not class-based. The MonitoringTask hierarchy (BaseMixin→MonitoringTask→8 subclasses) added complexity for shared logging/result patterns that are already provided by taskiq_base helpers (log_task_start/success/error). Flattening simplifies each task to a standalone function with structured logging — no inheritance, no self references. | No — the flattened tasks are simpler and more maintainable |
| D012 |  | M009/S04 | How to structure quiz_flow Taskiq tasks — keep 4-file subpackage or consolidate into single module | Consolidate 4 Celery subpackage files (cleanup_tasks.py, trigger_tasks.py, response_tasks.py, question_tasks.py) into single quiz_flow_taskiq.py module | The Celery quiz_flow subpackage split was driven by class-based task organization. With Taskiq's function-based tasks and a total of 8 tasks, a single module is clearer — all quiz_flow lifecycle (cleanup, triggers, responses, questions) visible in one file. Cross-dispatch within module is simpler (no cross-file imports). Follows the pattern of all other *_taskiq.py modules being single files. | Yes — could re-split if the module grows significantly |
| D013 | M009/S05/T04 | M009/S05 | How to handle Celery task revoke/cancel calls after Celery removal | Replace celery_app.control.revoke() and AsyncResult.revoke() with logged no-ops — emit logger.warning with task_id for audit trail, skip actual cancellation | Taskiq ListQueueBroker doesn't support per-message cancellation (no ack/consumer-group model). Flow pause guards prevent delivery of cancelled messages anyway. The admin API cancel endpoints are rarely used. Logging preserves audit trail for operations. | Yes — revisit if Taskiq adds cancellation support or if cancel operations become critical |
| D014 | M009/S05/T01 | M009/S05 | How to preserve pure helper functions when deleting Celery task modules | Extract 40+ helper functions into app/tasks/helpers/{domain}_helpers.py modules and redirect all Taskiq module imports there before deleting Celery sources | Taskiq modules imported helper functions (not tasks) from Celery source files. Deleting Celery files without first relocating helpers would break all 10 Taskiq modules at import time. The helpers package provides a clear, domain-organized home for shared functions. | No — helpers package is the permanent home for shared task functions |
| D015 | M009/S05/T04 | M009/S05 | How to handle function names containing "celery" in surviving code after Celery removal | Rename function identifiers containing "celery" to use "backend" instead (e.g. get_task_by_celery_id → get_task_by_backend_id) because AST import scan catches names containing "celery" | The AST zero-import scan catches any imported name containing "celery" — including function names. Renaming to "backend" makes the scan pass and is semantically correct since these functions now operate against the Taskiq backend, not Celery specifically. | No — naming is settled |
| D016 | M010 | arch | Physician dashboard primary view strategy | Patient-centric list (flow phase, day, last contact, attention flags per patient row) replaces analytics-centric risk assessment table as the primary view | The doctor's morning workflow is "which patients need attention" — not "what are the risk scores." A patient-centric view with clinical context per row serves this workflow directly. Risk data is still available but not the primary organizing principle. | Yes — revisit if risk scoring becomes the primary clinical workflow |
| D017 | M010 | arch | Physician dashboard and admin dashboard separation | Keep /dashboard (admin) and /physician/dashboard (physician) as separate routes and components — do not unify by role | Admin needs operational metrics (system health, task queues, user management). Physician needs clinical workflow (patient flow state, AI summaries, alerts). Mixing creates noise for both audiences. | Yes — could unify if roles converge |
| D018 | M010 | api | Physician patient list API strategy | New dedicated endpoint GET /api/v2/physician/patients with JOINed flow state data, rather than enriching existing risk-assessments endpoint | The risk-assessments endpoint is focused on AI risk scoring. The physician list needs patient + flow state + alert counts — different data shape, different query pattern. Separate endpoints keep each focused and independently cacheable. | Yes — could merge if data needs converge |
| D019 | M011 | pattern | Backend caching strategy for physician hot paths | @cache_response per-endpoint (60s physician, 120s dashboard) on top of CacheMiddleware (90s auth TTL) | CacheMiddleware is generic HTTP-level with ETag/conditional. @cache_response is endpoint-specific with semantic key_prefix, allowing targeted invalidation. Both layers complement — middleware handles conditional requests, decorator handles Redis storage. | No |
| D020 | M011 | pattern | Frontend staleTime discipline | staleTime ≥ 60s for dashboard/patient hooks, ≥ 120s for admin, Infinity for static config. refetchInterval ≥ 120s or removed. | Data at 10-30s staleTime generates redundant requests — clinical data doesn't change that fast. Aligning frontend staleTime with backend cache TTL avoids fetching the same cached data repeatedly. | Yes — tighten if real-time monitoring is added |
| D021 | M012 | data | Override persistence strategy for per-patient flow customization | Dedicated `patient_flow_overrides` table (FK to patient_flow_states) instead of JSONB inside step_data | step_data already stores runtime state (current_day, awaiting_response, recovery_attempts). Mixing config overrides with runtime state makes querying, auditing, and debugging harder. Separate table is queryable, indexable, and has clean lifecycle. | No |
| D022 | M012 | pattern | Override immutability on global template changes | Overrides are fixed — global template changes do not overwrite existing per-patient overrides. Days without overrides inherit updated global. | Simplifies implementation (no version tracking across templates), and matches user expectation: "I customized this patient's flow, don't undo my work." Merge happens at read-time, not write-time. | No |
| D023 | M012 | pattern | Override editability window | Only future days (not yet sent) are editable. Past days are read-only in the override editor. | Editing a day already sent creates confusing state — the message is gone. Future-only editing keeps the contract clear: override affects what will be sent, not what was sent. | Yes — if historical annotation is added |
| D024 | M012 | pattern | Override granularity | Full override capability: edit content, message_type, expects_response, add extra days, skip/disable days | Partial override (content-only) would frustrate doctors who need to adjust message timing (expects_response) or skip irrelevant days for a specific patient. Full capability matches the existing DayConfigEditor feature set. | No |
| D025 | M012/S02/T02 | M012/S02 | Whether override content should be personalized by AI before sending | Override content bypasses AI personalization entirely — physician-authored content is used as-is, with message metadata marking `personalized: false` and `override: true` for audit | When a physician writes custom override content for a specific patient, that content is intentionally authored. Running it through Gemini personalization would alter the physician's intended message, defeating the purpose of the override. The metadata markers enable downstream analysis of override vs. AI-personalized message effectiveness. | Yes — revisit if physicians request optional AI enhancement of their overrides |
| D026 | M012/S02/T01 | M012/S02 | Override cache behavior for patients with no overrides | Empty dict `{}` cached as miss sentinel with same 3600s TTL — prevents repeated DB queries for patients without overrides | Without a miss sentinel, every message send for patients without overrides would hit the DB. Since most patients won't have overrides, the sentinel converts the common case from DB query to Redis cache hit. The 3600s TTL matches global template cache for consistency. | Yes — could shorten TTL if override creation needs faster visibility |
