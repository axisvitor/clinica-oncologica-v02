---
phase: 03-operational-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/tasks/flows/flow_tasks.py
  - backend-hormonia/app/tasks/flows/base.py
autonomous: true
requirements:
  - ASYNC-04

must_haves:
  truths:
    - "grep -r 'asyncio.run(' app/tasks/ returns zero results in backend-hormonia"
    - "All Celery tasks calling async functions use async_to_sync from asgiref"
    - "send_critical_alert_sync no longer uses ThreadPoolExecutor or loop detection boilerplate"
  artifacts:
    - path: "backend-hormonia/app/tasks/flows/flow_tasks.py"
      provides: "Daily flow processing task using async_to_sync instead of asyncio.run"
      contains: "async_to_sync(process_daily_flows_async)"
    - path: "backend-hormonia/app/tasks/flows/base.py"
      provides: "send_critical_alert_sync using async_to_sync instead of loop detection + ThreadPoolExecutor"
      contains: "async_to_sync(manager.process_alert)"
  key_links:
    - from: "backend-hormonia/app/tasks/flows/flow_tasks.py"
      to: "asgiref.sync.async_to_sync"
      via: "import and invocation wrapping process_daily_flows_async"
      pattern: "async_to_sync\\(process_daily_flows_async\\)"
    - from: "backend-hormonia/app/tasks/flows/base.py"
      to: "asgiref.sync.async_to_sync"
      via: "import and invocation wrapping manager.process_alert"
      pattern: "async_to_sync\\(manager\\.process_alert\\)"
---

<objective>
Replace `asyncio.run()` with `asgiref.sync.async_to_sync` in the two remaining Celery task files that still use the deprecated pattern, eliminating memory leak risk from event loop creation per invocation.

Purpose: `asyncio.run()` creates a new event loop per call and does not clean it up properly in Celery's worker threading model, causing gradual memory leaks. The `async_to_sync` pattern is already established in 15+ other task locations in this codebase.

Output: Two modified files with zero `asyncio.run()` calls remaining in `app/tasks/`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-operational-stability/03-RESEARCH.md
@backend-hormonia/app/tasks/flows/flow_tasks.py
@backend-hormonia/app/tasks/flows/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace asyncio.run() with async_to_sync in flow_tasks.py</name>
  <files>backend-hormonia/app/tasks/flows/flow_tasks.py</files>
  <action>
In `flow_tasks.py`, replace the `asyncio.run()` call at line 326 with `async_to_sync`:

1. Add import at top of file (alongside existing imports): `from asgiref.sync import async_to_sync`
2. Replace line 326: `results = asyncio.run(process_daily_flows_async(limit))` with `results = async_to_sync(process_daily_flows_async)(limit)`
3. Keep the `import asyncio` at the top — it is still needed by `process_daily_flows_async` which uses `asyncio.Semaphore`, `asyncio.gather`, and `asyncio.sleep` internally.
4. Do NOT modify `process_daily_flows_async` itself — only the sync entry point that calls it.

The pattern matches 15+ other tasks in this codebase (e.g., `app/tasks/flow_automation.py`, `app/tasks/follow_up.py`).
  </action>
  <verify>
Run: `cd backend-hormonia && grep -n "asyncio.run(" app/tasks/flows/flow_tasks.py` — must return zero results.
Run: `cd backend-hormonia && grep -n "async_to_sync" app/tasks/flows/flow_tasks.py` — must show the import and the call site.
Run: `cd backend-hormonia && python -c "from app.tasks.flows.flow_tasks import process_daily_flows"` — must not raise ImportError.
  </verify>
  <done>flow_tasks.py uses async_to_sync(process_daily_flows_async)(limit) instead of asyncio.run(); the asyncio import remains for internal async primitives; module imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Replace asyncio.run() and loop detection in base.py send_critical_alert_sync</name>
  <files>backend-hormonia/app/tasks/flows/base.py</files>
  <action>
In `base.py`, replace the entire loop-detection + ThreadPoolExecutor block in `send_critical_alert_sync` (lines 55-73) with a single `async_to_sync` call:

1. Add `from asgiref.sync import async_to_sync` to the imports at the top of the file.
2. Replace the entire block from `try: loop = asyncio.get_event_loop()` through `loop.run_until_complete(manager.process_alert(alert))` (lines 55-73) with: `async_to_sync(manager.process_alert)(alert)`
3. Check if `import asyncio` is still needed anywhere else in `base.py` (by the `FlowTaskBase` class or other functions). If the `asyncio` import is used only in the removed block, remove it. If other code in the file uses `asyncio`, keep it.
4. The `import concurrent.futures` inside the function body (line 65) will be removed as part of the block deletion.
5. Keep the outer `try/except Exception as e: logger.error(...)` — only the inner try/except and loop-detection block is replaced.

The result should collapse ~18 lines of loop-detection boilerplate into 1 line.
  </action>
  <verify>
Run: `cd backend-hormonia && grep -n "asyncio.run(" app/tasks/flows/base.py` — must return zero results.
Run: `cd backend-hormonia && grep -n "get_event_loop\|run_until_complete\|ThreadPoolExecutor" app/tasks/flows/base.py` — must return zero results.
Run: `cd backend-hormonia && grep -n "async_to_sync" app/tasks/flows/base.py` — must show import and call.
Run: `cd backend-hormonia && python -c "from app.tasks.flows.base import send_critical_alert_sync"` — must not raise ImportError.
  </verify>
  <done>send_critical_alert_sync uses async_to_sync(manager.process_alert)(alert); loop detection boilerplate and ThreadPoolExecutor removed; module imports cleanly.</done>
</task>

</tasks>

<verification>
Run from backend-hormonia/:
1. `grep -rn "asyncio.run(" app/tasks/` — must return zero results (success criterion for ASYNC-04)
2. `grep -rn "async_to_sync" app/tasks/flows/flow_tasks.py app/tasks/flows/base.py` — both files show the pattern
3. `python -c "from app.tasks.flows.flow_tasks import process_daily_flows; from app.tasks.flows.base import send_critical_alert_sync, FlowTaskBase; print('OK')"` — imports succeed
</verification>

<success_criteria>
- `grep -r "asyncio.run(" app/tasks/` in backend-hormonia returns zero results
- Both files import and use `async_to_sync` from `asgiref.sync`
- Both files import cleanly without errors
- No other code in the two files was modified beyond the targeted replacements
</success_criteria>

<output>
After completion, create `.planning/phases/03-operational-stability/03-01-SUMMARY.md`
</output>
