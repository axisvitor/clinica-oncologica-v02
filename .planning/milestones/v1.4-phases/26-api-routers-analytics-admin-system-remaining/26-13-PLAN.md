---
phase: 26-api-routers-analytics-admin-system-remaining
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/api/v2/routers/tasks/dependencies.py
  - backend-hormonia/app/api/v2/routers/tasks/endpoints/crud.py
  - backend-hormonia/app/api/v2/routers/tasks/endpoints/operations.py
  - backend-hormonia/app/api/v2/routers/tasks/endpoints/monitoring.py
  - backend-hormonia/app/api/v2/routers/tasks/endpoints/bulk.py
autonomous: true
gap_closure: true
requirements:
  - API-09

must_haves:
  truths:
    - "tasks/dependencies.py _get_current_user_simple uses Depends(get_async_db) with zero Depends(get_db)"
    - "tasks/endpoints/crud.py uses Depends(get_async_db) in all 3 handlers with zero Depends(get_db)"
    - "tasks/endpoints/operations.py uses Depends(get_async_db) in both handlers with zero Depends(get_db)"
    - "tasks/endpoints/monitoring.py uses Depends(get_async_db) in all 3 handlers with zero Depends(get_db)"
    - "tasks/endpoints/bulk.py uses Depends(get_async_db) in both handlers with zero Depends(get_db)"
    - "API contracts unchanged -- same endpoints, same request/response shapes"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/tasks/dependencies.py"
      provides: "AsyncSession-backed task dependency module"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/tasks/endpoints/crud.py"
      provides: "AsyncSession-backed task CRUD router"
      contains: "Depends(get_async_db)"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/tasks/endpoints/crud.py"
      to: "backend-hormonia/app/api/v2/routers/tasks/dependencies.py"
      via: "_get_current_user_simple dependency"
      pattern: "Depends\\(_get_current_user_simple\\)"
---

<objective>
Migrate the tasks sub-router package from sync Session to AsyncSession.

Purpose: The `tasks/` package has 5 files with `Depends(get_db)`: the shared `dependencies.py` (1 usage in `_get_current_user_simple`) and 4 endpoint files (crud.py with 3, operations.py with 2, monitoring.py with 3, bulk.py with 2). These are Celery task management endpoints, but they run as API handlers, not Celery workers. The `db` parameter is used primarily for the `_get_current_user_simple` auth dependency which queries the session store, and in some endpoints for task metadata storage. Most task operations delegate to Celery/Redis and use `db` minimally.
Output: All 5 files with zero `Depends(get_db)` and `Depends(get_async_db)`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-api-routers-analytics-admin-system-remaining/26-RESEARCH.md
@backend-hormonia/app/api/v2/routers/tasks/dependencies.py
@backend-hormonia/app/api/v2/routers/tasks/endpoints/crud.py
@backend-hormonia/app/api/v2/routers/tasks/endpoints/operations.py
@backend-hormonia/app/api/v2/routers/tasks/endpoints/monitoring.py
@backend-hormonia/app/api/v2/routers/tasks/endpoints/bulk.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
    # Yields AsyncSession for request-scoped DI
```

CONTEXT:
1. `tasks/dependencies.py` has `_get_current_user_simple(db=Depends(get_db), ...)`. This dependency validates user sessions via Redis/headers, and `db` may or may not be used for DB lookups. The function body needs inspection during execution — if `db` is not used in the body at all (auth is purely Redis-based), just swap the DI. If `db.query(...)` is present, convert to async.
2. The 4 endpoint files use `db=Depends(get_db)` in handler signatures. Most task operations are Celery/Redis-backed. DB usage is typically limited to looking up task metadata or user verification. Convert any `db.query(...)` calls to async select patterns.
3. These endpoints handle Celery task management from the API layer. The `db` is NOT used for Celery task execution — it's for API-layer metadata queries.

Pattern for task endpoints:
```python
from app.core.database.async_engine import get_async_db
from sqlalchemy.ext.asyncio import AsyncSession

async def handler(
    db: AsyncSession = Depends(get_async_db),
    current_user=Depends(_get_current_user_simple),
    ...
):
    # Most task operations use Celery API, not db directly
    # If db is used: await db.execute(select(...)) for reads
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate tasks/dependencies.py to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/tasks/dependencies.py
  </files>
  <action>
    **tasks/dependencies.py (150 lines, 1 Depends(get_db))**

    1. Replace `from app.database import get_db` with:
       ```python
       from app.core.database.async_engine import get_async_db
       from sqlalchemy.ext.asyncio import AsyncSession
       ```
    2. Update `_get_current_user_simple` signature:
       `db=Depends(get_db)` → `db: AsyncSession = Depends(get_async_db)`
    3. Read the function body. If it uses `db.query(...)` anywhere, convert to `await db.execute(select(...))`. If it only uses `db` for passing to sub-functions, those sub-functions must also handle AsyncSession.
    4. The function primarily validates sessions via Redis headers (`resolve_session_id`, `get_user_data_from_session`). If `db` is only declared but not used in the body, the swap is sufficient with no query changes.
    5. Add `from sqlalchemy import select` only if db queries exist.

    Compile check.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/tasks/dependencies.py && python3 -c "
import importlib, inspect
m = importlib.import_module('app.api.v2.routers.tasks.dependencies')
s = inspect.getsource(m)
assert 'Depends(get_db)' not in s, 'Depends(get_db) found'
print('PASS: tasks/dependencies.py migrated')
"</automated>
  </verify>
  <done>tasks/dependencies.py uses `Depends(get_async_db)`. Zero `Depends(get_db)`. File compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate tasks endpoint routers (crud, operations, monitoring, bulk) to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/tasks/endpoints/crud.py
    backend-hormonia/app/api/v2/routers/tasks/endpoints/operations.py
    backend-hormonia/app/api/v2/routers/tasks/endpoints/monitoring.py
    backend-hormonia/app/api/v2/routers/tasks/endpoints/bulk.py
  </files>
  <action>
    For each of the 4 files, apply the same migration pattern:

    **crud.py (392 lines, 3 Depends(get_db))**

    1. Replace `from app.database import get_db` with:
       ```python
       from app.core.database.async_engine import get_async_db
       from sqlalchemy.ext.asyncio import AsyncSession
       ```
    2. Update all 3 handler signatures: `db=Depends(get_db)` → `db: AsyncSession = Depends(get_async_db)`.
    3. These handlers primarily use Celery APIs and in-memory task registries. If any `db.query(...)` calls exist, convert to `await db.execute(select(...))`.
    4. Add `from sqlalchemy import select` if db queries exist.

    **operations.py (228 lines, 2 Depends(get_db))**

    1. Same import replacement.
    2. Update both handler signatures.
    3. Convert any `db.query(...)` to async.

    **monitoring.py (327 lines, 3 Depends(get_db))**

    1. Same import replacement.
    2. Update all 3 handler signatures.
    3. Convert any `db.query(...)` to async.

    **bulk.py (245 lines, 2 Depends(get_db))**

    1. Same import replacement.
    2. Update both handler signatures.
    3. Convert any `db.query(...)` to async.

    Compile check all 4 files.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/tasks/endpoints/crud.py && python3 -m py_compile app/api/v2/routers/tasks/endpoints/operations.py && python3 -m py_compile app/api/v2/routers/tasks/endpoints/monitoring.py && python3 -m py_compile app/api/v2/routers/tasks/endpoints/bulk.py && python3 -c "
import importlib, inspect
for mod_path in [
    'app.api.v2.routers.tasks.endpoints.crud',
    'app.api.v2.routers.tasks.endpoints.operations',
    'app.api.v2.routers.tasks.endpoints.monitoring',
    'app.api.v2.routers.tasks.endpoints.bulk',
]:
    m = importlib.import_module(mod_path)
    s = inspect.getsource(m)
    assert 'Depends(get_db)' not in s, f'Depends(get_db) found in {mod_path}'
print('PASS: all 4 task endpoint routers have zero Depends(get_db)')
"</automated>
  </verify>
  <done>All 4 task endpoint routers use `Depends(get_async_db)`. Zero `Depends(get_db)`. Zero `db.query(`. Files compile.</done>
</task>

</tasks>

<verification>
- All 5 files compile
- Source inspection confirms zero `Depends(get_db)` in all 5 files
- `_get_current_user_simple` in dependencies.py correctly declares AsyncSession
</verification>

<success_criteria>
- All 5 files compile without errors
- Zero `Depends(get_db)` across all 5 files
- API endpoint contracts unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/26-api-routers-analytics-admin-system-remaining/26-13-SUMMARY.md`
</output>
