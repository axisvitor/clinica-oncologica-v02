---
phase: 26-api-routers-analytics-admin-system-remaining
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/api/v2/routers/debug/common.py
  - backend-hormonia/app/api/v2/routers/debug/auth.py
  - backend-hormonia/app/api/v2/routers/debug/database.py
  - backend-hormonia/app/api/v2/routers/debug/environment.py
  - backend-hormonia/app/api/v2/routers/patients/base.py
autonomous: true
gap_closure: true
requirements:
  - API-09

must_haves:
  truths:
    - "debug/common.py uses Depends(get_async_db) with zero Depends(get_db) and zero db.query("
    - "debug/auth.py uses Depends(get_async_db) in all 4 handlers with zero Depends(get_db) and zero db.query("
    - "debug/database.py uses Depends(get_async_db) in both handlers with zero Depends(get_db)"
    - "debug/environment.py uses Depends(get_async_db) with zero Depends(get_db)"
    - "patients/base.py uses Depends(get_async_db) with zero Depends(get_db) and zero db.query("
    - "API contracts unchanged -- same endpoints, same request/response shapes"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/debug/common.py"
      provides: "AsyncSession-backed debug common dependency"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/debug/auth.py"
      provides: "AsyncSession-backed debug auth router"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/patients/base.py"
      provides: "AsyncSession-backed patients base dependency"
      contains: "Depends(get_async_db)"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/debug/auth.py"
      to: "backend-hormonia/app/api/v2/routers/debug/common.py"
      via: "get_admin_user and log_debug_operation dependencies"
      pattern: "Depends\\(get_admin_user\\)"
    - from: "backend-hormonia/app/api/v2/routers/patients/base.py"
      to: "backend-hormonia/app/models/patient.py"
      via: "async patient lookup for base validation"
      pattern: "select\\(Patient\\)"
---

<objective>
Migrate debug sub-routers and patients/base.py from sync Session to AsyncSession.

Purpose: The `debug/` package has 4 files with `Depends(get_db)`: common.py (shared dependency with `db.query(AuditLog)`), auth.py (4 handlers), database.py (2 handlers with raw SQL), environment.py (1 handler). Additionally, `patients/base.py` has 1 `Depends(get_db)` in a dependency/utility plus `db.query(User)` and `db.query(Patient)` calls. All must be migrated to close the global gap.
Output: All 5 files with zero `Depends(get_db)` and zero `db.query(`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-api-routers-analytics-admin-system-remaining/26-RESEARCH.md
@backend-hormonia/app/api/v2/routers/debug/common.py
@backend-hormonia/app/api/v2/routers/debug/auth.py
@backend-hormonia/app/api/v2/routers/debug/database.py
@backend-hormonia/app/api/v2/routers/debug/environment.py
@backend-hormonia/app/api/v2/routers/patients/base.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
    # Yields AsyncSession for request-scoped DI
```

CONTEXT:
1. **debug/common.py** (232 lines, 1 Depends(get_db)): Has `get_admin_user` or a similar admin dependency that uses `db=Depends(get_db)`. Also has `log_debug_operation` that may write `AuditLog` records via `db.query()`.
2. **debug/auth.py** (491 lines, 4 Depends(get_db)): Auth debug endpoints. Handlers use `db=Depends(get_db)` and `db.query(User)` for user lookups.
3. **debug/database.py** (256 lines, 2 Depends(get_db)): Database diagnostics. Uses `db.execute(text(...))` for raw SQL — must `await` these.
4. **debug/environment.py** (122 lines, 1 Depends(get_db)): Environment info endpoint. Light DB usage.
5. **patients/base.py** (171 lines, 1 Depends(get_db)): Base module for patient routers. Has a dependency with `db: Session = Depends(get_db)` and `db.query(User)` / `db.query(Patient)` in utility functions.

Pattern for debug raw SQL:
```python
# BEFORE
result = db.execute(text("SELECT 1")).fetchone()
# AFTER
result = await db.execute(text("SELECT 1"))
row = result.fetchone()
```

Pattern for debug audit logging:
```python
# BEFORE
audit = AuditLog(...)
db.add(audit)
db.commit()
# AFTER
audit = AuditLog(...)
db.add(audit)
await db.commit()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate debug sub-routers to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/debug/common.py
    backend-hormonia/app/api/v2/routers/debug/auth.py
    backend-hormonia/app/api/v2/routers/debug/database.py
    backend-hormonia/app/api/v2/routers/debug/environment.py
  </files>
  <action>
    **debug/common.py (232 lines, 1 Depends(get_db))**

    1. Replace `from app.database import get_db` with:
       ```python
       from app.core.database.async_engine import get_async_db
       from sqlalchemy.ext.asyncio import AsyncSession
       from sqlalchemy import select
       ```
    2. Find the dependency function with `db=Depends(get_db)` and update to `db: AsyncSession = Depends(get_async_db)`.
    3. Convert `db.query(AuditLog)` or `db.query(User)` calls to `await db.execute(select(...))`.
    4. If `log_debug_operation` writes audit records, await `db.commit()`.
    5. Make any DB-touching utility functions `async def`.

    **debug/auth.py (491 lines, 4 Depends(get_db))**

    1. Replace `from app.database import get_db` with async imports.
    2. Update all 4 handler signatures: `db=Depends(get_db)` → `db: AsyncSession = Depends(get_async_db)`.
    3. Convert `db.query(User)` calls to `await db.execute(select(User).where(...))`.
    4. Await any write ops.

    **debug/database.py (256 lines, 2 Depends(get_db))**

    1. Replace `from app.database import get_db` with async imports.
    2. Update both handler signatures.
    3. Convert `db.execute(text(...))` to `result = await db.execute(text(...))` — these are raw SQL diagnostics.
    4. Keep sync engine pool access unchanged (same pattern as database_health.py).

    **debug/environment.py (122 lines, 1 Depends(get_db))**

    1. Replace `from app.database import get_db` with async imports.
    2. Update handler signature.
    3. Convert any DB operations to async (likely minimal).

    Compile check all 4 files.
  </action>
  <verify>
    <automated>cd backend-hormonia && for f in app/api/v2/routers/debug/common.py app/api/v2/routers/debug/auth.py app/api/v2/routers/debug/database.py app/api/v2/routers/debug/environment.py; do python3 -m py_compile "$f" || exit 1; done && python3 -c "
import importlib, inspect
for mod_path in [
    'app.api.v2.routers.debug.common',
    'app.api.v2.routers.debug.auth',
    'app.api.v2.routers.debug.database',
    'app.api.v2.routers.debug.environment',
]:
    m = importlib.import_module(mod_path)
    s = inspect.getsource(m)
    assert 'Depends(get_db)' not in s, f'Depends(get_db) found in {mod_path}'
print('PASS: all 4 debug routers migrated')
"</automated>
  </verify>
  <done>All 4 debug router files use `Depends(get_async_db)`. Zero `Depends(get_db)`. Zero `db.query(`. Files compile.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate patients/base.py to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/patients/base.py
  </files>
  <action>
    **patients/base.py (171 lines, 1 Depends(get_db))**

    1. Replace `from app.database import get_db` with:
       ```python
       from app.core.database.async_engine import get_async_db
       from sqlalchemy.ext.asyncio import AsyncSession
       from sqlalchemy import select
       ```
    2. Remove `from sqlalchemy.orm import Session`.
    3. Find the function/dependency with `db: Session = Depends(get_db)` and update to `db: AsyncSession = Depends(get_async_db)`.
    4. Convert `db.query(User)` (line ~118) to:
       ```python
       result = await db.execute(select(User).where(...))
       user = result.scalar_one_or_none()
       ```
    5. Convert `db.query(Patient)` (line ~148) similarly.
    6. Make affected utility functions `async def` if they use DB operations.
    7. Update callers in other patients/ router files (crud.py, integrity.py, etc.) to `await` the now-async utility functions if they call them. Note: The patients/crud.py and friends were migrated in Phase 24 — verify their calls to base.py functions already use `await` or need updating.

    Compile check.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/patients/base.py && python3 -c "
import importlib, inspect
m = importlib.import_module('app.api.v2.routers.patients.base')
s = inspect.getsource(m)
assert 'Depends(get_db)' not in s, 'Depends(get_db) found in patients/base.py'
assert 'db.query(' not in s, 'db.query( found in patients/base.py'
print('PASS: patients/base.py migrated')
"</automated>
  </verify>
  <done>patients/base.py uses `Depends(get_async_db)`. Zero `Depends(get_db)`. Zero `db.query(`. File compiles. Patient utility functions are async-safe.</done>
</task>

</tasks>

<verification>
- All 5 files compile
- Source inspection confirms zero `Depends(get_db)` and zero `db.query(` in all 5 files
- Debug endpoints still function with async session
- patients/base.py callers are compatible
</verification>

<success_criteria>
- All 5 files compile without errors
- Zero `Depends(get_db)` across all 5 files
- Zero `db.query(` across all 5 files
- API endpoint contracts unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/26-api-routers-analytics-admin-system-remaining/26-15-SUMMARY.md`
</output>
