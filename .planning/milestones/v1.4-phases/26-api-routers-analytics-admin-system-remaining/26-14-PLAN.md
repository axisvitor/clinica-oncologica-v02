---
phase: 26-api-routers-analytics-admin-system-remaining
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/api/v2/routers/system/metrics.py
  - backend-hormonia/app/api/v2/routers/system/initialization.py
  - backend-hormonia/app/api/v2/routers/system/components.py
  - backend-hormonia/app/api/v2/routers/system/health.py
  - backend-hormonia/app/api/v2/routers/health/metrics.py
  - backend-hormonia/app/api/v2/routers/health/core.py
  - backend-hormonia/app/api/v2/routers/health/test.py
  - backend-hormonia/app/api/v2/routers/localization.py
  - backend-hormonia/app/api/v2/routers/webhooks.py
  - backend-hormonia/app/api/v2/routers/ai/insights.py
autonomous: true
gap_closure: true
requirements:
  - API-06
  - API-08
  - API-09

must_haves:
  truths:
    - "system/metrics.py uses Depends(get_async_db) with zero Depends(get_db)"
    - "system/initialization.py uses Depends(get_async_db) with zero Depends(get_db)"
    - "system/components.py uses Depends(get_async_db) with zero Depends(get_db)"
    - "system/health.py uses Depends(get_async_db) with zero Depends(get_db)"
    - "health/metrics.py uses Depends(get_async_db) in all 3 handlers with zero Depends(get_db) and zero db.query("
    - "health/core.py uses Depends(get_async_db) in both handlers with zero Depends(get_db)"
    - "health/test.py uses Depends(get_async_db) with zero Depends(get_db)"
    - "localization.py uses Depends(get_async_db) in all 3 handlers with zero Depends(get_db)"
    - "webhooks.py service factory uses Depends(get_async_db) with zero Depends(get_db)"
    - "ai/insights.py uses Depends(get_async_db) in all handlers with zero Depends(get_db)"
    - "API contracts unchanged -- same endpoints, same request/response shapes"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/health/metrics.py"
      provides: "AsyncSession-backed health metrics router"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/health/core.py"
      provides: "AsyncSession-backed core health router"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/localization.py"
      provides: "AsyncSession-backed localization router"
      contains: "Depends(get_async_db)"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/health/metrics.py"
      to: "backend-hormonia/app/models/user.py"
      via: "async user/patient count queries for system metrics"
      pattern: "select\\(func\\.count"
    - from: "backend-hormonia/app/api/v2/routers/health/core.py"
      to: "backend-hormonia/app/api/v2/routers/health/database_health.py"
      via: "check_database_health delegation"
      pattern: "check_database_health"
---

<objective>
Migrate system, health, localization, webhooks, and AI insight routers from sync Session to AsyncSession.

Purpose: This plan covers 10 remaining router files across multiple sub-packages:
- 4 system routers (metrics.py, initialization.py, components.py, health.py) with 1-2 Depends(get_db) each
- 3 health routers (metrics.py, core.py, test.py) with 1-3 Depends(get_db) each, health/metrics.py has 4 db.query calls
- localization.py (766 lines, 3 Depends(get_db)) - the largest file
- webhooks.py (315 lines, 1 Depends(get_db)) - service factory pattern
- ai/insights.py (335 lines, 3 Depends(get_db))
Most are stub/Redis-backed or have light DB usage. health/metrics.py has the most db.query calls (4).
Output: All 10 files with zero `Depends(get_db)`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-api-routers-analytics-admin-system-remaining/26-RESEARCH.md
@backend-hormonia/app/api/v2/routers/system/metrics.py
@backend-hormonia/app/api/v2/routers/system/initialization.py
@backend-hormonia/app/api/v2/routers/system/components.py
@backend-hormonia/app/api/v2/routers/system/health.py
@backend-hormonia/app/api/v2/routers/health/metrics.py
@backend-hormonia/app/api/v2/routers/health/core.py
@backend-hormonia/app/api/v2/routers/health/test.py
@backend-hormonia/app/api/v2/routers/localization.py
@backend-hormonia/app/api/v2/routers/webhooks.py
@backend-hormonia/app/api/v2/routers/ai/insights.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
    # Yields AsyncSession for request-scoped DI
```

Standard patterns:
```python
from app.core.database.async_engine import get_async_db
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, func, text

db: AsyncSession = Depends(get_async_db)

# Health check raw SQL
result = await db.execute(text("SELECT 1"))
row = result.fetchone()

# Count query
result = await db.execute(select(func.count(User.id)))
user_count = result.scalar() or 0
```

CONTEXT:
1. **system/ routers** (metrics.py 278 lines, initialization.py 230 lines, components.py 265 lines, health.py 169 lines): These are system management endpoints. `db` is used for DB connectivity checks and some metadata queries. Some use `db.execute(text(...))` for raw SQL health checks.
2. **health/metrics.py** (192 lines, 3 handlers, 4 db.query calls): Has `db.query(User)`, `db.query(func.count(...))` for system metrics. All must convert to async select.
3. **health/core.py** (213 lines, 2 handlers): Uses `db: Session = Depends(get_db)` and delegates to `check_database_health(db)`. Since database_health.py was already migrated in Phase 26-05, need to check if `check_database_health` now expects AsyncSession.
4. **health/test.py** (91 lines, 1 handler): Manual health test, uses `db: Session = Depends(get_db)` and delegates to health check functions.
5. **localization.py** (766 lines, 3 handlers): Large but DB usage is light — primarily Redis-backed i18n with occasional user preference lookups.
6. **webhooks.py** (315 lines, 1 factory): `get_webhook_service(db=Depends(get_db))` creates `WebhookService(db)`.
7. **ai/insights.py** (335 lines, 3 handlers): Uses `db=Depends(get_db)` and passes to patient services.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate system/ and health/ sub-routers to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/system/metrics.py
    backend-hormonia/app/api/v2/routers/system/initialization.py
    backend-hormonia/app/api/v2/routers/system/components.py
    backend-hormonia/app/api/v2/routers/system/health.py
    backend-hormonia/app/api/v2/routers/health/metrics.py
    backend-hormonia/app/api/v2/routers/health/core.py
    backend-hormonia/app/api/v2/routers/health/test.py
  </files>
  <action>
    For each of the 7 files, apply the standard migration:

    **system/metrics.py (278 lines, 1 Depends(get_db))**
    1. Replace `from app.database import get_db` with `from app.core.database.async_engine import get_async_db` + `from sqlalchemy.ext.asyncio import AsyncSession`.
    2. Update handler: `db=Depends(get_db)` → `db: AsyncSession = Depends(get_async_db)`.
    3. Convert any `db.execute(text(...))` to `await db.execute(text(...))`. Convert any `db.query(...)` to async select.

    **system/initialization.py (230 lines, 1 Depends(get_db))**
    1. Same import swap.
    2. Update handler signature.
    3. Convert `db.execute(text(...))` to `await db.execute(text(...))`.

    **system/components.py (265 lines, 2 Depends(get_db))**
    1. Same import swap.
    2. Update both handler signatures.
    3. Convert any DB operations to async.

    **system/health.py (169 lines, 1 Depends(get_db))**
    1. Same import swap.
    2. Update handler signature.
    3. Convert any DB operations to async.

    **health/metrics.py (192 lines, 3 Depends(get_db), 4 db.query)**
    1. Replace `from app.database import get_db` with async imports.
    2. Remove `from sqlalchemy.orm import Session`.
    3. Update all 3 handler signatures.
    4. Convert 4 `db.query(...)` calls:
       - `db.query(User).count()` → `result = await db.execute(select(func.count(User.id))); count = result.scalar() or 0`
       - Similar for other count/list queries.
    5. Add `from sqlalchemy import select, func` if not present.

    **health/core.py (213 lines, 2 Depends(get_db))**
    1. Same import swap. Remove `from sqlalchemy.orm import Session`.
    2. Update both handler signatures.
    3. `check_database_health(db)` is called — this function was migrated in Plan 26-05 to accept AsyncSession. If it now expects `await`, update the call site.
    4. Convert any `db.execute(text("SELECT 1"))` to `await db.execute(text("SELECT 1"))`.

    **health/test.py (91 lines, 1 Depends(get_db))**
    1. Same import swap. Remove `from sqlalchemy.orm import Session`.
    2. Update handler signature.
    3. Health check delegation calls may now need `await` if the underlying functions are async.

    Compile check all 7 files.
  </action>
  <verify>
    <automated>cd backend-hormonia && for f in app/api/v2/routers/system/metrics.py app/api/v2/routers/system/initialization.py app/api/v2/routers/system/components.py app/api/v2/routers/system/health.py app/api/v2/routers/health/metrics.py app/api/v2/routers/health/core.py app/api/v2/routers/health/test.py; do python3 -m py_compile "$f" || exit 1; done && python3 -c "
import importlib, inspect
for mod_path in [
    'app.api.v2.routers.system.metrics',
    'app.api.v2.routers.system.initialization',
    'app.api.v2.routers.system.components',
    'app.api.v2.routers.system.health',
    'app.api.v2.routers.health.metrics',
    'app.api.v2.routers.health.core',
    'app.api.v2.routers.health.test',
]:
    m = importlib.import_module(mod_path)
    s = inspect.getsource(m)
    assert 'Depends(get_db)' not in s, f'Depends(get_db) found in {mod_path}'
print('PASS: all 7 system/health routers migrated')
"</automated>
  </verify>
  <done>All 7 system and health router files use `Depends(get_async_db)`. Zero `Depends(get_db)`. Zero `db.query(`. Files compile.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate localization.py, webhooks.py, and ai/insights.py to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/localization.py
    backend-hormonia/app/api/v2/routers/webhooks.py
    backend-hormonia/app/api/v2/routers/ai/insights.py
  </files>
  <action>
    **localization.py (766 lines, 3 Depends(get_db))**

    1. Replace `from app.database import get_db` with:
       ```python
       from app.core.database.async_engine import get_async_db
       from sqlalchemy.ext.asyncio import AsyncSession
       from sqlalchemy import select
       ```
    2. Update all 3 handler signatures: `db=Depends(get_db)` → `db: AsyncSession = Depends(get_async_db)`.
    3. Convert any `db.query(...)` calls to async select. The i18n system is primarily Redis-backed; DB calls are for user language preferences.
    4. Await any write ops (`db.commit()`, `db.refresh()`).

    **webhooks.py (315 lines, 1 Depends(get_db))**

    1. Replace `from app.database import get_db` with async imports.
    2. Update the service factory:
       ```python
       async def get_webhook_service(db: AsyncSession = Depends(get_async_db)) -> WebhookService:
           return WebhookService(db)
       ```
    3. Check if `WebhookService` uses `db.query()` internally. If yes, the service will fail with AsyncSession — note for future cleanup. If the service is primarily Redis/HTTP-backed, the swap is sufficient.

    **ai/insights.py (335 lines, 3 Depends(get_db))**

    1. Replace `from app.database import get_db` with async imports.
    2. Update all 3 handler signatures: `db=Depends(get_db)` → `db: AsyncSession = Depends(get_async_db)`.
    3. The handlers use `get_patient_service` and `validate_patient_access` from `app.dependencies`. Check if these accept AsyncSession (they should after Phase 23 service migration). If they do, the swap is sufficient.
    4. Convert any direct `db.query(...)` in the router to async select.

    Compile check all 3 files.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/localization.py && python3 -m py_compile app/api/v2/routers/webhooks.py && python3 -m py_compile app/api/v2/routers/ai/insights.py && python3 -c "
import importlib, inspect
for mod_path in [
    'app.api.v2.routers.localization',
    'app.api.v2.routers.webhooks',
    'app.api.v2.routers.ai.insights',
]:
    m = importlib.import_module(mod_path)
    s = inspect.getsource(m)
    assert 'Depends(get_db)' not in s, f'Depends(get_db) found in {mod_path}'
print('PASS: localization + webhooks + ai/insights migrated')
"</automated>
  </verify>
  <done>localization.py, webhooks.py, and ai/insights.py all use `Depends(get_async_db)`. Zero `Depends(get_db)`. Files compile.</done>
</task>

</tasks>

<verification>
- All 10 files compile
- Source inspection confirms zero `Depends(get_db)` in all 10 files
- health/metrics.py has zero `db.query(`
</verification>

<success_criteria>
- All 10 files compile without errors
- Zero `Depends(get_db)` across all 10 files
- Zero `db.query(` in health/metrics.py
- API endpoint contracts unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/26-api-routers-analytics-admin-system-remaining/26-14-SUMMARY.md`
</output>
