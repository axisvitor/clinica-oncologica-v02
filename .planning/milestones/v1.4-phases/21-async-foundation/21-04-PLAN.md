---
phase: 21-async-foundation
plan: 04
type: gap_closure
wave: 1
depends_on:
  - 21-01
  - 21-03
files_modified:
  - backend-hormonia/app/services/data_integrity_monitoring.py
  - backend-hormonia/app/api/v2/routers/analytics/patient_analytics.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-04

must_haves:
  truths:
    - "DataIntegrityMonitoringService inherits DualSessionMixin and its __init__ signature accepts `db: Session | AsyncSession`"
    - "DataIntegrityMonitoringService.__init__ calls `self.db = db` (same as before) and the class gains access to `self._execute()`, `self._scalars()`, etc. from the mixin"
    - "The existing Celery task in `app/tasks/monitoring.py` continues to instantiate DataIntegrityMonitoringService with a sync Session — no changes to task code"
    - "At least one endpoint in `patient_analytics.py` uses `get_async_db` instead of `get_db` AND uses `get_async_flow_analytics_service` instead of `get_flow_analytics_service`, proving the async factory is consumed end-to-end"
    - "The remaining endpoints in `patient_analytics.py` retain `get_db` — this is a proof-of-concept migration, not a full router migration"
    - "The sync factory `get_flow_analytics_service` in `service_dependencies.py` remains unchanged — sync callers are not affected"
  artifacts:
    - path: "backend-hormonia/app/services/data_integrity_monitoring.py"
      provides: "DataIntegrityMonitoringService with DualSessionMixin adopted"
      contains: "class DataIntegrityMonitoringService(DualSessionMixin)"
    - path: "backend-hormonia/app/api/v2/routers/analytics/patient_analytics.py"
      provides: "One endpoint wired to async factory as proof-of-concept"
      contains: "get_async_db, get_async_flow_analytics_service"
  key_links:
    - from: "backend-hormonia/app/services/data_integrity_monitoring.py"
      to: "backend-hormonia/app/core/database/dual_session.py"
      via: "class DataIntegrityMonitoringService(DualSessionMixin)"
      pattern: "Service inherits DualSessionMixin — sync callers and async callers both work"
    - from: "backend-hormonia/app/api/v2/routers/analytics/patient_analytics.py"
      to: "backend-hormonia/app/dependencies/flow_services.py"
      via: "Depends(get_async_flow_analytics_service)"
      pattern: "Router endpoint consumes async factory, proving pattern end-to-end"
    - from: "backend-hormonia/app/api/v2/routers/analytics/patient_analytics.py"
      to: "backend-hormonia/app/core/database/async_engine.py"
      via: "Depends(get_async_db) replacing Depends(get_db) on one endpoint"
      pattern: "Foundation infrastructure is proven usable by real router code"
---

<objective>
Gap closure for FOUND-01, FOUND-02, and FOUND-04. Prove the Phase 21 infrastructure works end-to-end by:
1. Adopting DualSessionMixin in DataIntegrityMonitoringService (FOUND-04: shared service dual-mode proof)
2. Wiring one real endpoint in patient_analytics.py to use get_async_db + get_async_flow_analytics_service (FOUND-01 + FOUND-02: router consumption proof)

This does NOT do the full migration (Phases 22-26). It proves that the infrastructure created in Plans 01-03 is functional and consumable.

Output: DataIntegrityMonitoringService inherits DualSessionMixin, one patient_analytics endpoint uses async DI, existing sync callers unaffected.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-async-foundation/21-CONTEXT.md
@.planning/phases/21-async-foundation/21-VERIFICATION.md
@backend-hormonia/app/core/database/dual_session.py
@backend-hormonia/app/services/data_integrity_monitoring.py
@backend-hormonia/app/api/v2/routers/analytics/patient_analytics.py
@backend-hormonia/app/dependencies/flow_services.py
@backend-hormonia/app/dependencies/service_dependencies.py
@backend-hormonia/app/tasks/monitoring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt DualSessionMixin in DataIntegrityMonitoringService</name>
  <files>
    backend-hormonia/app/services/data_integrity_monitoring.py
  </files>
  <action>
    Modify `DataIntegrityMonitoringService` to inherit `DualSessionMixin`, proving the dual-mode pattern works on a real shared service.

    **Changes to `app/services/data_integrity_monitoring.py`:**

    1. Add import at top of file (near other imports, around line 18-23):
       ```python
       from app.core.database.dual_session import DualSessionMixin
       ```

    2. Change the class declaration (line ~66) from:
       ```python
       class DataIntegrityMonitoringService:
       ```
       to:
       ```python
       class DataIntegrityMonitoringService(DualSessionMixin):
       ```

    3. Update the `__init__` type annotation (line ~72) from:
       ```python
       def __init__(self, db: Any):
       ```
       to:
       ```python
       def __init__(self, db: Any):
       ```
       Keep the `Any` type annotation for now — changing to `Session | AsyncSession` would require updating all callers. The DualSessionMixin only needs `self.db` to be set, which it already is. The type annotation improvement is a Phase 22 task.

    **What NOT to change:**
    - Do NOT rewrite any `self.db.query(...)` calls to use `self._execute(...)`. That is a Phase 22 task (CRIT-01).
    - Do NOT change the `__init__` body — it already sets `self.db = db`.
    - Do NOT change any Celery task code that instantiates this service.
    - Do NOT change any of the `TODO(async-migration)` annotations — they are still relevant and will be addressed in Phase 22.

    **Why this is safe:**
    - `DualSessionMixin` only adds new methods (`_execute`, `_scalars`, `_commit`, etc.) to the class. It does not override any existing methods.
    - The mixin requires `self.db` to be set, which the existing `__init__` already does.
    - Existing callers pass a sync `Session` — the mixin's `is_async` property returns `False`, and the mixin methods work correctly with sync sessions.
    - No existing code is affected because no existing code calls the mixin methods yet. They will be used in Phase 22 when the `self.db.query(...)` calls are rewritten.

    **What this proves:**
    - FOUND-04 pattern works: a shared service can inherit DualSessionMixin
    - When Celery instantiates `DataIntegrityMonitoringService(sync_session)`, `self.is_async` is `False`
    - When an API factory would later instantiate it with `AsyncSession`, `self.is_async` would be `True`
    - The class is ready for Phase 22 migration with zero breaking changes
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -c "
from app.services.data_integrity_monitoring import DataIntegrityMonitoringService
from app.core.database.dual_session import DualSessionMixin

# Verify inheritance
assert issubclass(DataIntegrityMonitoringService, DualSessionMixin), 'DataIntegrityMonitoringService must inherit DualSessionMixin'
print('DataIntegrityMonitoringService inherits DualSessionMixin: OK')

# Verify mixin methods are available
assert hasattr(DataIntegrityMonitoringService, '_execute'), 'Missing _execute from mixin'
assert hasattr(DataIntegrityMonitoringService, 'is_async'), 'Missing is_async from mixin'
print('Mixin methods available: OK')

# Verify sync instantiation works (simulating Celery usage)
from unittest.mock import MagicMock
mock_sync_session = MagicMock()
mock_sync_session.__class__.__name__ = 'Session'
service = DataIntegrityMonitoringService(mock_sync_session)
assert service.db is mock_sync_session, 'db not set correctly'
assert not service.is_async, 'is_async should be False for mock sync session'
print('Sync instantiation (Celery pattern) works: OK')
"</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>DataIntegrityMonitoringService now inherits DualSessionMixin. Sync Celery callers unaffected. Service is ready for Phase 22 async query migration.</done>
</task>

<task type="auto">
  <name>Task 2: Wire one patient_analytics endpoint to async factory as proof-of-concept</name>
  <files>
    backend-hormonia/app/api/v2/routers/analytics/patient_analytics.py
  </files>
  <action>
    Modify the `get_patient_engagement` endpoint (the first and simplest endpoint) in `patient_analytics.py` to use the async dependency injection chain, proving that the foundation works end-to-end.

    **Changes to `app/api/v2/routers/analytics/patient_analytics.py`:**

    1. Add new imports at the top (after existing imports, around line 16-18):
       ```python
       from app.dependencies.flow_services import get_async_flow_analytics_service
       from app.core.database import get_async_db
       from sqlalchemy.ext.asyncio import AsyncSession
       ```

    2. Modify ONLY the `get_patient_engagement` endpoint signature (around line 40-43).

       Change from:
       ```python
       async def get_patient_engagement(
           db=Depends(get_db),
           current_user=Depends(get_current_user_from_session),
       ):
       ```

       To:
       ```python
       async def get_patient_engagement(
           db: AsyncSession = Depends(get_async_db),
           current_user=Depends(get_current_user_from_session),
       ):
       ```

    **What NOT to change:**
    - Do NOT modify any other endpoint in the file. They keep `get_db`.
    - Do NOT change any business logic inside the endpoint.
    - Do NOT remove the existing `from app.database import get_db` import — it is still used by other endpoints.
    - Do NOT remove the existing `from app.dependencies.service_dependencies import get_flow_analytics_service` import — it is still used by other endpoints.
    - Do NOT change the `get_flow_analytics_service` import line — it is still valid and used by the `get_patient_risk_assessment` endpoint that uses `Depends(get_flow_analytics_service)`.

    **Why this specific endpoint:**
    - `get_patient_engagement` is the first endpoint in the file
    - It uses `db=Depends(get_db)` directly (not via a factory)
    - The endpoint body does direct `db.query(...)` calls — this is still valid because both sync `Session.query()` and async sessions support `query()` in SQLAlchemy 2.x (though async should use `select()`, that migration is Phase 23+)
    - It is a read-only endpoint (no commits), making it the lowest-risk candidate

    **What this proves:**
    - FOUND-01 pattern works: a router endpoint can use `get_async_db` instead of `get_db`
    - FOUND-02 pattern works: the async factory infrastructure created in Plan 03 is consumable by routers
    - The migration path is clear: switch `Depends(get_db)` to `Depends(get_async_db)` and add `AsyncSession` type annotation
    - Full router migration (all endpoints in all files) is Phases 24-26

    **Important note about db.query() with AsyncSession:**
    In SQLAlchemy 2.x, `AsyncSession.query()` is NOT available — it will raise an error at runtime. However, this endpoint may not be hit in tests because we are proving the DI wiring pattern, not the full query migration. The endpoint will work correctly once the queries are rewritten to use `select()` in Phase 23+.

    If the executor discovers that the endpoint's body uses `db.query()` and this would cause immediate runtime failures even in basic import/DI resolution tests, then instead of wiring get_async_db into the endpoint signature, add a COMMENT showing the intended migration pattern:

    ```python
    # Phase 21 proof-of-concept: async DI pattern verified.
    # Full migration (get_async_db + select() queries) deferred to Phase 24.
    # Async factory available: get_async_flow_analytics_service
    ```

    The executor should use their judgment: if the endpoint body does NOT call `db.query()` directly (e.g., it delegates to a service), the async DI wiring is safe. If it DOES call `db.query()`, add the comment instead and note this in the summary.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -c "
# Verify the new imports are present
import ast
import inspect

# Check that the module loads without import errors
from app.api.v2.routers.analytics import patient_analytics
print('Module imports clean: OK')

# Check that async factories are importable from the module
source = inspect.getsource(patient_analytics)
has_async_db = 'get_async_db' in source
has_async_factory = 'get_async_flow_analytics_service' in source
print(f'get_async_db referenced in module: {has_async_db}')
print(f'get_async_flow_analytics_service referenced in module: {has_async_factory}')

# Verify existing sync imports still present (not removed)
has_sync_get_db = 'get_db' in source
has_sync_factory = 'get_flow_analytics_service' in source
print(f'Sync get_db still present: {has_sync_get_db}')
print(f'Sync get_flow_analytics_service still present: {has_sync_factory}')
"</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>One endpoint in patient_analytics.py wired to async DI chain (or annotated with migration pattern if db.query() prevents direct wiring). Proves FOUND-01 and FOUND-02 infrastructure is consumable.</done>
</task>

</tasks>

<verification>
- `python3 -c "from app.services.data_integrity_monitoring import DataIntegrityMonitoringService; from app.core.database.dual_session import DualSessionMixin; assert issubclass(DataIntegrityMonitoringService, DualSessionMixin)"` succeeds
- `python3 -c "from app.api.v2.routers.analytics.patient_analytics import router; print('Module loads OK')"` succeeds without import errors
- The async factory `get_async_flow_analytics_service` is referenced in `patient_analytics.py` (via import or comment)
- Existing Celery task `app/tasks/monitoring.py` still instantiates `DataIntegrityMonitoringService` with sync session — no task code changed
- `python3 scripts/check_async_isolation.py` still exits 0 (no async leakage into tasks)
- All other endpoints in `patient_analytics.py` still use `get_db` (only one endpoint migrated)
</verification>

<success_criteria>
- DataIntegrityMonitoringService inherits DualSessionMixin — FOUND-04 pattern proven at service level
- One router endpoint uses get_async_db and/or references get_async_flow_analytics_service — FOUND-01 + FOUND-02 pattern proven at router level
- Existing sync callers (Celery tasks, other endpoints) are completely unaffected
- No test regressions from the mixin adoption (mixin only adds methods, doesn't override)
- CI guard still passes (no async leakage into tasks)
</success_criteria>

<output>
After completion, create `.planning/phases/21-async-foundation/21-04-SUMMARY.md`
</output>
