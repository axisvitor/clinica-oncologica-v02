---
phase: 23-service-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/unified_whatsapp_service.py
  - backend-hormonia/app/services/dispatcher.py
  - backend-hormonia/tests/unit/services/test_communication_services_async.py
autonomous: true
requirements:
  - SVC-04

must_haves:
  truths:
    - "Communication services used by API handlers are non-blocking in async contexts"
    - "WhatsApp send and flow dispatch behavior remains contract-compatible"
    - "Worker paths remain sync-compatible with no event-loop bridging hacks"
  artifacts:
    - path: "backend-hormonia/app/services/unified_whatsapp_service.py"
      provides: "Async-safe DB state updates in async send/error paths"
      contains: "await self.db.commit()"
    - path: "backend-hormonia/app/services/dispatcher.py"
      provides: "AsyncSession-compatible dispatcher construction and flow initialization path"
      contains: "FlowDispatcher"
    - path: "backend-hormonia/tests/unit/services/test_communication_services_async.py"
      provides: "Regression tests for async communication service behavior"
      contains: "@pytest.mark.asyncio"
  key_links:
    - from: "backend-hormonia/app/services/unified_whatsapp_service.py"
      to: "backend-hormonia/app/models/message.py"
      via: "message status updates and failure handling"
      pattern: "MessageStatus"
    - from: "backend-hormonia/app/services/dispatcher.py"
      to: "backend-hormonia/app/services/patient/flow_service.py"
      via: "initialize_flow delegation"
      pattern: "PatientFlowService"
---

<objective>
Migrate communication services to async-safe behavior for API invocation paths.

Purpose: Close SVC-04 while preserving UnifiedWhatsAppService and FlowDispatcher caller contracts.
Output: Async-safe communication service internals plus targeted regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-service-migration/23-CONTEXT.md
@.planning/phases/23-service-migration/23-RESEARCH.md
@backend-hormonia/app/services/unified_whatsapp_service.py
@backend-hormonia/app/services/dispatcher.py

<interfaces>
From `backend-hormonia/app/services/dispatcher.py`:
```python
class FlowDispatcher:
    async def initialize_flow(self, patient: Patient, current_user_id: Optional[UUID] = None, auto_commit: bool = True) -> Optional[PatientFlowState]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden UnifiedWhatsAppService async DB paths for non-blocking API execution</name>
  <files>
    backend-hormonia/app/services/unified_whatsapp_service.py
  </files>
  <action>
    Audit async send/error/update paths and ensure DB reads/writes in API-context async flows are fully non-blocking and awaited.

    Preserve existing message status transitions, metadata/error payloads, and callback behavior.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/services/unified_whatsapp_service.py</automated>
  </verify>
  <done>Unified WhatsApp async API path is non-blocking and contract-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Make FlowDispatcher explicitly compatible with async API session usage</name>
  <files>
    backend-hormonia/app/services/dispatcher.py
  </files>
  <action>
    Update typing and DB interaction points so dispatcher can safely operate with AsyncSession in API paths while preserving sync caller compatibility.

    Keep method signatures and delegation behavior unchanged.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/services/dispatcher.py</automated>
  </verify>
  <done>Dispatcher supports async API invocation without changing external behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add async regression tests for communication service group</name>
  <files>
    backend-hormonia/tests/unit/services/test_communication_services_async.py
  </files>
  <action>
    Add async unit tests for UnifiedWhatsAppService and FlowDispatcher API-reachable paths, including failure/error update scenarios.

    Include assertions that fail on sync-only DB assumptions in async context and keep current payload/status expectations.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/services/test_communication_services_async.py -q</automated>
  </verify>
  <done>Automated tests confirm communication services are async-safe in API context and sync-compatible for worker usage.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/services/unified_whatsapp_service.py app/services/dispatcher.py`
- `cd backend-hormonia && pytest tests/unit/services/test_communication_services_async.py -q`
</verification>

<success_criteria>
- SVC-04 satisfied: communication service group is async-safe where invoked from API handlers.
</success_criteria>

<output>
After completion, create `.planning/phases/23-service-migration/23-04-SUMMARY.md`
</output>
