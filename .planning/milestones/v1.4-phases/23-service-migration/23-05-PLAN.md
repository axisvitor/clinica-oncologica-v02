---
phase: 23-service-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/firebase_user_sync_service.py
  - backend-hormonia/app/services/session_service.py
  - backend-hormonia/tests/unit/services/test_auth_session_services_async.py
autonomous: true
requirements:
  - SVC-05

must_haves:
  truths:
    - "Firebase user sync and session service DB operations complete asynchronously in API context"
    - "Auth/session semantics (role checks, sync/link behavior, session payloads) remain stable"
    - "No async auth path depends on sync-only ORM calls"
  artifacts:
    - path: "backend-hormonia/app/services/firebase_user_sync_service.py"
      provides: "Async-safe user lookup/create/update/link DB paths"
      contains: "await self.db.execute(select(...))"
    - path: "backend-hormonia/app/services/session_service.py"
      provides: "Async-safe session/user DB interactions"
      contains: "await self.db.execute"
    - path: "backend-hormonia/tests/unit/services/test_auth_session_services_async.py"
      provides: "Regression tests for async auth/session behavior"
      contains: "@pytest.mark.asyncio"
  key_links:
    - from: "backend-hormonia/app/services/firebase_user_sync_service.py"
      to: "backend-hormonia/app/models/user.py"
      via: "firebase user synchronization and role validation"
      pattern: "UserRole"
    - from: "backend-hormonia/app/services/session_service.py"
      to: "backend-hormonia/app/core/redis_manager"
      via: "session lifecycle with Redis-backed storage"
      pattern: "create_session_from_firebase_token"
---

<objective>
Migrate auth/session service group to async-safe DB operations for API usage.

Purpose: Close SVC-05 with strict behavior parity for authentication/session contracts.
Output: Async-safe auth/session services plus regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-service-migration/23-CONTEXT.md
@.planning/phases/23-service-migration/23-RESEARCH.md
@backend-hormonia/app/services/firebase_user_sync_service.py
@backend-hormonia/app/services/session_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert FirebaseUserSyncService async methods to async-safe DB execution</name>
  <files>
    backend-hormonia/app/services/firebase_user_sync_service.py
  </files>
  <action>
    Migrate all async-reachable DB paths that still use sync ORM patterns in `FirebaseUserSyncService`.

    Required methods include sync/link/create/update/validate operations currently marked with async-migration TODO markers.
    Preserve existing security validation, role mapping, audit logging semantics, and return contracts.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/services/firebase_user_sync_service.py</automated>
  </verify>
  <done>Firebase user sync async methods are non-blocking and behavior-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Verify SessionService async DB paths and align any remaining sync assumptions</name>
  <files>
    backend-hormonia/app/services/session_service.py
  </files>
  <action>
    Audit SessionService for async API paths and update any remaining sync DB assumptions while preserving current session/token semantics and payload shape.

    Keep Redis/session behavior unchanged.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/services/session_service.py</automated>
  </verify>
  <done>SessionService remains contract-compatible and fully async-safe in API context.</done>
</task>

<task type="auto">
  <name>Task 3: Add async regression tests for auth/session services</name>
  <files>
    backend-hormonia/tests/unit/services/test_auth_session_services_async.py
  </files>
  <action>
    Add async unit tests covering Firebase user sync create/update/link/validate flows and session user lookup/create flow.

    Tests must assert behavior parity and fail if sync-only DB access is used in async methods.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/services/test_auth_session_services_async.py -q</automated>
  </verify>
  <done>Automated tests verify auth/session services are async-safe with unchanged business/security behavior.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/services/firebase_user_sync_service.py app/services/session_service.py`
- `cd backend-hormonia && pytest tests/unit/services/test_auth_session_services_async.py -q`
</verification>

<success_criteria>
- SVC-05 satisfied: auth/session services complete DB reads/writes asynchronously in API context.
</success_criteria>

<output>
After completion, create `.planning/phases/23-service-migration/23-05-SUMMARY.md`
</output>
