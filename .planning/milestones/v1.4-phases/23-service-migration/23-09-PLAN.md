---
phase: 23-service-migration
plan: 09
type: execute
wave: 3
depends_on:
  - 23-06
  - 23-08
files_modified:
  - backend-hormonia/app/services/lgpd/consent_service.py
  - backend-hormonia/tests/unit/services/test_infrastructure_services_async.py
autonomous: true
gap_closure: true
requirements:
  - SVC-01
  - SVC-02
  - SVC-03
  - SVC-04
  - SVC-05
  - SVC-06
  - SVC-07

must_haves:
  truths:
    - "Infrastructure services called by API handlers perform non-blocking DB operations, including LGPDAuditService methods"
    - "LGPD audit history and access-log behavior remains parity-compatible after async-safe migration"
    - "Phase 23 cross-group async regression still shows zero MissingGreenlet evidence"
  artifacts:
    - path: "backend-hormonia/app/services/lgpd/consent_service.py"
      provides: "AsyncSession-safe LGPDAuditService operations with preserved method contracts"
      contains: "await self._resolve(self.db.execute(select(LGPDAuditLog)"
    - path: "backend-hormonia/tests/unit/services/test_infrastructure_services_async.py"
      provides: "Regression guards for async-safe LGPDAuditService methods"
      contains: "sync db.query should not be used"
  key_links:
    - from: "backend-hormonia/app/services/lgpd/consent_service.py"
      to: "backend-hormonia/app/models/lgpd_audit.py"
      via: "LGPDAuditService select/execute query paths"
      pattern: "select\(LGPDAuditLog\)"
    - from: "backend-hormonia/tests/unit/services/test_infrastructure_services_async.py"
      to: "backend-hormonia/app/services/lgpd/consent_service.py"
      via: "async regression tests calling LGPDAuditService methods"
      pattern: "LGPDAuditService"
---

<objective>
Close the Phase 23 verification gap in infrastructure async safety by fixing `LGPDAuditService` sync-in-async behavior.

Purpose: Satisfy the failed SVC-06 truth while preserving caller compatibility, API/Celery coexistence rules, and end-of-phase async regression guarantees.
Output: Updated `LGPDAuditService` async-safe DB operations plus regression coverage proving no sync ORM usage in async methods.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-service-migration/23-CONTEXT.md
@.planning/phases/23-service-migration/23-RESEARCH.md
@.planning/phases/23-service-migration/23-VERIFICATION.md
@.planning/phases/23-service-migration/23-06-SUMMARY.md
@.planning/phases/23-service-migration/23-08-SUMMARY.md
@backend-hormonia/app/services/lgpd/consent_service.py
@backend-hormonia/tests/unit/services/test_infrastructure_services_async.py

<interfaces>
From `backend-hormonia/app/services/lgpd/consent_service.py`:
```python
class LGPDAuditService:
    def __init__(self, db: Session):
    async def log_data_access(...) -> LGPDAuditLog:
    async def get_patient_access_history(...) -> List[LGPDAuditLog]:
    async def get_user_access_history(...) -> List[LGPDAuditLog]:
    async def get_failed_access_attempts(...) -> List[LGPDAuditLog]:
```

From `backend-hormonia/tests/unit/services/test_infrastructure_services_async.py`:
```python
class _QueueAsyncSession:
    async def execute(self, statement): ...
    async def commit(self): ...
    async def refresh(self, instance): ...
    def query(self, *args, **kwargs):
        raise AssertionError("sync db.query should not be used")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert LGPDAuditService methods to AsyncSession-safe execution</name>
  <files>
    backend-hormonia/app/services/lgpd/consent_service.py
  </files>
  <action>
    Migrate `LGPDAuditService` to dual-mode session typing (`Session | AsyncSession`) and convert its async methods to non-blocking DB execution.

    Implement awaited execution/transaction helpers (matching the existing dual-mode style in this module) and replace async-method sync ORM/query chains with `select(...)` + awaited `execute(...)` + `scalars().all()`.

    Preserve method signatures, return shapes, filtering semantics (`start_date`, `end_date`, `hours`, `limit`), ordering (`created_at.desc()`), and error behavior. Use this async-safe path per locked API/Celery coexistence decision; do not add event-loop bridging hacks.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/services/lgpd/consent_service.py</automated>
  </verify>
  <done>`LGPDAuditService` async methods no longer call sync `query()/commit()/refresh()` directly and remain contract-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression guards for LGPDAuditService and re-verify infrastructure async safety</name>
  <files>
    backend-hormonia/tests/unit/services/test_infrastructure_services_async.py
  </files>
  <action>
    Extend infrastructure async regression tests to cover `LGPDAuditService`:
    - `log_data_access` uses async-safe commit/refresh path and returns created audit log
    - `get_patient_access_history`, `get_user_access_history`, and `get_failed_access_attempts` use execute/select paths (not `db.query`)
    - filter and limit inputs preserve behavior parity

    Keep existing sync-query guard behavior so tests fail immediately if async methods regress to sync ORM calls. After test updates, run both infrastructure unit checks and phase-level integration harness to preserve SVC-01..SVC-07 evidence.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/services/test_infrastructure_services_async.py tests/integration/test_phase23_service_async_missinggreenlet.py -q</automated>
  </verify>
  <done>Gap truth for infrastructure async safety is verifiably closed and cross-group MissingGreenlet evidence remains clean.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/services/lgpd/consent_service.py`
- `cd backend-hormonia && pytest tests/unit/services/test_infrastructure_services_async.py tests/integration/test_phase23_service_async_missinggreenlet.py -q`
</verification>

<success_criteria>
- SVC-06 gap is closed: `LGPDAuditService` no longer uses sync-only DB operations inside async methods.
- Infrastructure async-safety truth in `23-VERIFICATION.md` is satisfiable as VERIFIED.
- Cross-group async regression still reports zero `MissingGreenlet` logs for Phase 23 migrated services.
</success_criteria>

<output>
After completion, create `.planning/phases/23-service-migration/23-09-SUMMARY.md`
</output>
