---
phase: 23-service-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/patient/sync_service.py
  - backend-hormonia/app/services/patient/validation_service.py
  - backend-hormonia/tests/unit/services/test_patient_service_async.py
autonomous: true
requirements:
  - SVC-01

must_haves:
  truths:
    - "Patient duplicate checks and merge flows run from async API contexts without MissingGreenlet"
    - "Patient sync/validation service behavior and error messages remain compatible with current callers"
    - "Celery/sync callers keep working without event-loop bridging hacks"
  artifacts:
    - path: "backend-hormonia/app/services/patient/sync_service.py"
      provides: "Async-safe DB execution in async methods and dual-mode session compatibility"
      contains: "select(...) plus awaited async path"
    - path: "backend-hormonia/app/services/patient/validation_service.py"
      provides: "Async-safe doctor/duplicate validation query path"
      contains: "select(User)"
    - path: "backend-hormonia/tests/unit/services/test_patient_service_async.py"
      provides: "Regression tests that fail on sync-only query usage in async paths"
      contains: "@pytest.mark.asyncio"
  key_links:
    - from: "backend-hormonia/app/services/patient/sync_service.py"
      to: "backend-hormonia/app/models/patient.py"
      via: "merge/relationship migration path"
      pattern: "merge_patients"
    - from: "backend-hormonia/app/services/patient/validation_service.py"
      to: "backend-hormonia/app/models/user.py"
      via: "doctor existence validation"
      pattern: "select\(User\)"
---

<objective>
Migrate patient shared services to async-safe DB behavior for API callers while preserving sync/Celery compatibility.

Purpose: Close SVC-01 with behavior parity and no event-loop blocking in patient service paths.
Output: Async-safe patient service internals plus focused regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-service-migration/23-CONTEXT.md
@.planning/phases/23-service-migration/23-RESEARCH.md
@backend-hormonia/app/services/patient/sync_service.py
@backend-hormonia/app/services/patient/validation_service.py

<interfaces>
From `backend-hormonia/app/services/patient/sync_service.py`:
```python
class PatientSyncService:
    async def merge_patients(self, primary_patient_id: UUID, duplicate_patient_id: UUID) -> Patient: ...
```

From `backend-hormonia/app/services/patient/validation_service.py`:
```python
class PatientValidationService:
    def validate_patient_data(self, patient_data: PatientCreate | PatientUpdate, ...) -> Dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert patient sync/validation async-reachable DB paths to async-safe execution</name>
  <files>
    backend-hormonia/app/services/patient/sync_service.py
    backend-hormonia/app/services/patient/validation_service.py
  </files>
  <action>
    Keep constructor signatures and public method contracts unchanged, but migrate async-reachable DB operations to dual-mode-safe execution.

    Required scope:
    - `PatientSyncService.merge_patients`, `_migrate_patient_relationships`, `_soft_delete_patient`
    - `PatientValidationService._validate_doctor_exists`

    Use explicit async-safe SQLAlchemy execution for AsyncSession paths (select/execute and awaited commit/refresh where applicable), while preserving sync Session behavior for Celery-only callers. Do not change error categories/messages unless required to keep existing behavior correct.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/services/patient/sync_service.py app/services/patient/validation_service.py</automated>
  </verify>
  <done>Patient shared services support AsyncSession callers without blocking and preserve current caller-facing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add patient async regression coverage for duplicate checks and merge path</name>
  <files>
    backend-hormonia/tests/unit/services/test_patient_service_async.py
  </files>
  <action>
    Add async unit tests that cover duplicate/doctor validation and merge relationship migration paths with AsyncSession-style fakes.

    Tests must fail if async methods regress to sync-only DB patterns (for example, unguarded `.query()` usage in AsyncSession path), and must assert behavior parity for successful merge and error handling paths.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/services/test_patient_service_async.py -q</automated>
  </verify>
  <done>Automated tests prove patient services are async-safe for API context and remain compatible for existing callers.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/services/patient/sync_service.py app/services/patient/validation_service.py`
- `cd backend-hormonia && pytest tests/unit/services/test_patient_service_async.py -q`
</verification>

<success_criteria>
- SVC-01 satisfied: `sync_service.py` and `validation_service.py` run in async API contexts without blocking DB calls.
- Caller compatibility preserved: argument shapes, return payloads, and error semantics remain stable.
</success_criteria>

<output>
After completion, create `.planning/phases/23-service-migration/23-01-SUMMARY.md`
</output>
