---
phase: 24-api-routers-auth-patients-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - backend-hormonia/app/api/v2/routers/patients/crud.py
  - backend-hormonia/app/api/v2/routers/patients/integrity.py
  - backend-hormonia/app/api/v2/routers/patients/import_export.py
  - backend-hormonia/app/api/v2/routers/patients/flow.py
  - backend-hormonia/app/api/v2/routers/physicians/crud.py
  - backend-hormonia/tests/api/v2/test_phase24_patients_physicians_async.py
autonomous: true
requirements:
  - API-02

must_haves:
  truths:
    - "Patient and physician router handlers execute with AsyncSession dependencies in request scope"
    - "Patient import/export endpoints are included in async migration validation, not skipped"
    - "No async handler in patients/physicians routers uses sync ORM query chaining (db.query) after migration"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/patients/crud.py"
      provides: "AsyncSession-backed patient CRUD handlers"
      contains: "db: AsyncSession = Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/patients/import_export.py"
      provides: "AsyncSession-backed patient import/export handlers"
      contains: "await db.execute("
    - path: "backend-hormonia/app/api/v2/routers/patients/flow.py"
      provides: "AsyncSession-backed patient flow-state handlers"
      contains: "select(Patient"
    - path: "backend-hormonia/app/api/v2/routers/physicians/crud.py"
      provides: "AsyncSession-backed physician CRUD handlers"
      contains: "select(User"
    - path: "backend-hormonia/tests/api/v2/test_phase24_patients_physicians_async.py"
      provides: "Automated regression checks for API-02 async migration and parity"
      contains: "Depends(get_db)"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/patients/crud.py"
      to: "backend-hormonia/app/services/patient/crud_service.py"
      via: "AsyncSession propagation through patient CRUD orchestration"
      pattern: "PatientCRUDService"
    - from: "backend-hormonia/app/api/v2/routers/patients/import_export.py"
      to: "backend-hormonia/app/models/patient.py"
      via: "Async import/export filtering and persistence queries"
      pattern: "select\\(Patient"
    - from: "backend-hormonia/app/api/v2/routers/physicians/crud.py"
      to: "backend-hormonia/app/models/user.py"
      via: "Async physician list/detail/update queries"
      pattern: "select\\(User"
---

<objective>
Migrate the patient and physician router group to AsyncSession-only request handling.

Purpose: Satisfy API-02 in the locked migration order (auth/users/roles -> patients/physicians -> flow routers), including explicit import/export route validation and contract stability.
Output: AsyncSession-based patient/physician routers with full-group regression evidence covering CRUD, integrity, flow-state, and import/export endpoints.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-api-routers-auth-patients-flow/24-CONTEXT.md
@.planning/phases/23-service-migration/23-01-SUMMARY.md
@.planning/phases/23-service-migration/23-08-SUMMARY.md
@backend-hormonia/app/core/database/async_engine.py
@backend-hormonia/app/api/v2/routers/patients/crud.py
@backend-hormonia/app/api/v2/routers/patients/integrity.py
@backend-hormonia/app/api/v2/routers/patients/import_export.py
@backend-hormonia/app/api/v2/routers/patients/flow.py
@backend-hormonia/app/api/v2/routers/physicians/crud.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
```

From `backend-hormonia/app/api/v2/routers/patients/crud.py`:
```python
@router.get("/")
async def list_patients(..., db: Session = Depends(get_db), ...):

@router.post("/", status_code=201)
async def create_patient(..., db: AsyncSession = Depends(get_async_db), ...):
```

From `backend-hormonia/app/api/v2/routers/patients/import_export.py`:
```python
@router.get("/export")
async def export_patients(..., db: Session = Depends(get_db), ...):

@router.post("/import")
async def import_patients(..., db: Session = Depends(get_db), ...):
```

From `backend-hormonia/app/api/v2/routers/physicians/crud.py`:
```python
@router.get("/")
async def list_physicians(..., db: Session = Depends(get_db), ...):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert patient CRUD and integrity handlers to AsyncSession-safe execution</name>
  <files>
    backend-hormonia/app/api/v2/routers/patients/crud.py
    backend-hormonia/app/api/v2/routers/patients/integrity.py
  </files>
  <action>
    Migrate patient CRUD and integrity endpoints to `AsyncSession` request dependencies (`Depends(get_async_db)`) and replace sync ORM query chains with async-safe SQLAlchemy execution patterns.

    Preserve endpoint URLs, response models, permission checks, and saga integration semantics. Where blocking migration issues appear, apply the minimum fix needed to unblock API-02 progress; defer unrelated non-blocking issues per locked decision.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/patients/crud.py app/api/v2/routers/patients/integrity.py</automated>
  </verify>
  <done>Patient CRUD/integrity endpoints are AsyncSession-based with no async-path sync `db.query()` usage.</done>
</task>

<task type="auto">
  <name>Task 2: Convert patient import/export, patient flow, and physician CRUD handlers</name>
  <files>
    backend-hormonia/app/api/v2/routers/patients/import_export.py
    backend-hormonia/app/api/v2/routers/patients/flow.py
    backend-hormonia/app/api/v2/routers/physicians/crud.py
  </files>
  <action>
    Migrate patient import/export endpoints, patient flow-state endpoints, and physician CRUD endpoints to AsyncSession-safe request handling.

    Ensure import/export routes are fully included in migration and verification (locked decision). Preserve status codes, payload schemas, cursor behavior, and existing route paths while replacing sync query chains in async handlers.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/patients/import_export.py app/api/v2/routers/patients/flow.py app/api/v2/routers/physicians/crud.py</automated>
  </verify>
  <done>All API-02 patient/physician router files are AsyncSession-driven and contract-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Add API-02 group regression coverage with import/export inclusion</name>
  <files>
    backend-hormonia/tests/api/v2/test_phase24_patients_physicians_async.py
  </files>
  <action>
    Create deterministic regression tests for the full API-02 router group:
    - verify migrated patient/physician handlers depend on `get_async_db`
    - verify async handlers do not use sync `db.query(...)`
    - include explicit assertions for patient import/export endpoints in the validation set
    - add contract parity checks for representative patient and physician routes

    Keep validation broad enough to cover all changed endpoints in this group and concise enough for repeatable execution.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_phase24_patients_physicians_async.py tests/api/v2/test_patients.py tests/api/v2/test_physicians.py -q</automated>
  </verify>
  <done>Automated evidence confirms API-02 migration across patient/physician routes, including import/export coverage and contract stability.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/patients/crud.py app/api/v2/routers/patients/integrity.py app/api/v2/routers/patients/import_export.py app/api/v2/routers/patients/flow.py app/api/v2/routers/physicians/crud.py`
- `cd backend-hormonia && pytest tests/api/v2/test_phase24_patients_physicians_async.py tests/api/v2/test_patients.py tests/api/v2/test_physicians.py -q`
</verification>

<success_criteria>
- API-02 is met: patient and physician router handlers use AsyncSession dependencies.
- Patient import/export handlers are explicitly validated as part of migration acceptance.
- No async handler in API-02 router files depends on sync `get_db` or uses sync ORM query chaining.
</success_criteria>

<output>
After completion, create `.planning/phases/24-api-routers-auth-patients-flow/24-02-SUMMARY.md`
</output>
