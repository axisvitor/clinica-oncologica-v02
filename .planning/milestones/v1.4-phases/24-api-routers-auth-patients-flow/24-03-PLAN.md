---
phase: 24-api-routers-auth-patients-flow
plan: 03
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - backend-hormonia/app/api/v2/routers/flows.py
  - backend-hormonia/app/api/v2/routers/flow_templates.py
  - backend-hormonia/tests/api/v2/test_phase24_flows_async.py
autonomous: true
requirements:
  - API-03

must_haves:
  truths:
    - "Flow router handlers execute with AsyncSession dependencies in request scope"
    - "Flow advanced/state/templates/analytics handler sections in the consolidated flow router are async-safe"
    - "Flow and flow-template endpoint contracts (paths, methods, payload shapes) remain stable after migration"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/flows.py"
      provides: "AsyncSession-backed flow router handlers (including advanced/state/templates/analytics sections)"
      contains: "await db.execute("
    - path: "backend-hormonia/app/api/v2/routers/flow_templates.py"
      provides: "AsyncSession-backed template and flow-kind handlers"
      contains: "select(FlowTemplateVersion"
    - path: "backend-hormonia/tests/api/v2/test_phase24_flows_async.py"
      provides: "Automated regression checks for API-03 async migration and parity"
      contains: "Depends(get_db)"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/flows.py"
      to: "backend-hormonia/app/services/flow_service.py"
      via: "get_flow_service_dependency AsyncSession wiring"
      pattern: "get_flow_service_dependency"
    - from: "backend-hormonia/app/api/v2/routers/flows.py"
      to: "backend-hormonia/app/models/flow.py"
      via: "Async flow-state and template queries"
      pattern: "select\\(PatientFlowState"
    - from: "backend-hormonia/app/api/v2/routers/flow_templates.py"
      to: "backend-hormonia/app/models/flow.py"
      via: "Async template and kind queries"
      pattern: "select\\(FlowTemplateVersion"
---

<objective>
Migrate the flow router group to AsyncSession-only request handling and async-safe query execution.

Purpose: Satisfy API-03 while keeping endpoint contracts stable and completing the locked migration sequence after auth/users/roles and patients/physicians.
Output: AsyncSession-based flow and flow-template routers with regression evidence covering all changed flow handler sections.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-api-routers-auth-patients-flow/24-CONTEXT.md
@.planning/phases/22-critical-async-fixes/22-03-SUMMARY.md
@.planning/phases/23-service-migration/23-08-SUMMARY.md
@backend-hormonia/app/core/database/async_engine.py
@backend-hormonia/app/api/v2/routers/flows.py
@backend-hormonia/app/api/v2/routers/flow_templates.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
```

From `backend-hormonia/app/api/v2/routers/flows.py`:
```python
async def get_flow_service_dependency(
    async_db: AsyncSession = Depends(get_async_db),
    db=Depends(get_db),
) -> FlowService:

@router.get("/analytics")
async def get_flow_analytics(..., db=Depends(get_db), ...):

@router.get("/")
async def list_flows(..., db=Depends(get_db), ...):
```

From `backend-hormonia/app/api/v2/routers/flow_templates.py`:
```python
@router.get("/flows")
async def list_flow_templates(..., db=Depends(get_db), ...):

@router.post("/flows")
async def create_flow_template(..., db=Depends(get_db), ...):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert consolidated flow router handlers to AsyncSession-safe execution</name>
  <files>
    backend-hormonia/app/api/v2/routers/flows.py
  </files>
  <action>
    Migrate flow router request dependencies from sync `get_db` to async `get_async_db` for all migrated handlers and update query logic to async-safe SQLAlchemy execution patterns.

    Treat the roadmap flow sub-groups (`advanced`, `state`, `templates`, `analytics`) as the handler sections inside the consolidated `flows.py` file and ensure each section is covered.

    Preserve existing endpoints, request/response schema shapes, and timeout/flow-service wiring behavior.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/flows.py</automated>
  </verify>
  <done>Flow router handlers in `flows.py` use AsyncSession-safe DB execution across analytics/state/template/advanced sections.</done>
</task>

<task type="auto">
  <name>Task 2: Convert flow template router handlers to AsyncSession-safe execution</name>
  <files>
    backend-hormonia/app/api/v2/routers/flow_templates.py
  </files>
  <action>
    Migrate flow template and flow-kind handlers in `flow_templates.py` to AsyncSession dependencies and async-safe select/execute query patterns.

    Keep cursor pagination, cache behavior, and template audit logging semantics unchanged while preserving endpoint contracts.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/flow_templates.py</automated>
  </verify>
  <done>`flow_templates.py` handlers execute with AsyncSession and preserve contract parity.</done>
</task>

<task type="auto">
  <name>Task 3: Add API-03 regression coverage for full flow-router migration scope</name>
  <files>
    backend-hormonia/tests/api/v2/test_phase24_flows_async.py
  </files>
  <action>
    Create deterministic regression tests that validate all changed flow-router handlers:
    - migrated flow and flow-template handlers depend on `get_async_db`
    - async handlers avoid sync `db.query(...)`
    - coverage explicitly includes analytics/state/template/advanced handler sections in consolidated `flows.py`
    - route/response parity checks confirm contract stability

    Keep tests focused on migration behavior and compatibility guarantees.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_phase24_flows_async.py tests/api/v2/test_flows.py tests/api/v2/test_flows_advance.py tests/unit/test_flow_templates_repro.py -q</automated>
  </verify>
  <done>Automated evidence confirms API-03 migration across all changed flow handlers with stable endpoint contracts.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/flows.py app/api/v2/routers/flow_templates.py`
- `cd backend-hormonia && pytest tests/api/v2/test_phase24_flows_async.py tests/api/v2/test_flows.py tests/api/v2/test_flows_advance.py tests/unit/test_flow_templates_repro.py -q`
</verification>

<success_criteria>
- API-03 is met: flow router handlers run with AsyncSession dependencies and async-safe query execution.
- Consolidated flow-router sections (advanced/state/templates/analytics) are all included in migration validation.
- Flow and flow-template endpoint contracts remain stable (no endpoint removals or renames).
</success_criteria>

<output>
After completion, create `.planning/phases/24-api-routers-auth-patients-flow/24-03-SUMMARY.md`
</output>
