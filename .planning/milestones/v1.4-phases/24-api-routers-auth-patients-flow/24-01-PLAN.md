---
phase: 24-api-routers-auth-patients-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/api/v2/routers/auth.py
  - backend-hormonia/app/api/v2/routers/users.py
  - backend-hormonia/app/api/v2/routers/roles/endpoints.py
  - backend-hormonia/tests/api/v2/test_phase24_auth_users_roles_async.py
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - "Auth, user-profile, and role/permission endpoints execute with AsyncSession dependencies in request scope"
    - "No async handler in auth/users/roles uses sync ORM query chaining (db.query) after migration"
    - "Endpoint paths, methods, and response schema contracts for auth/users/roles remain stable"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/auth.py"
      provides: "AsyncSession-backed auth/session handlers"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/users.py"
      provides: "AsyncSession-backed profile/preferences/session handlers"
      contains: "await db.execute("
    - path: "backend-hormonia/app/api/v2/routers/roles/endpoints.py"
      provides: "AsyncSession-backed role and permission handlers"
      contains: "select(User)"
    - path: "backend-hormonia/tests/api/v2/test_phase24_auth_users_roles_async.py"
      provides: "Automated regression checks for API-01 async migration and contract stability"
      contains: "Depends(get_db)"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/auth.py"
      to: "backend-hormonia/app/core/database/async_engine.py"
      via: "FastAPI dependency injection"
      pattern: "Depends\\(get_async_db\\)"
    - from: "backend-hormonia/app/api/v2/routers/users.py"
      to: "backend-hormonia/app/models/session.py"
      via: "Async select/execute session listing and revocation"
      pattern: "select\\(Session"
    - from: "backend-hormonia/app/api/v2/routers/roles/endpoints.py"
      to: "backend-hormonia/app/models/user.py"
      via: "Async role assignment and statistics queries"
      pattern: "select\\(User"
---

<objective>
Migrate the auth, users, and role-management router group to AsyncSession-only request handling.

Purpose: Satisfy API-01 while honoring locked decisions for contract stability, status-code corrections when encountered, and full-group validation (including role/permission routes).
Output: AsyncSession-based auth/users/roles routers plus targeted regression tests that prove no sync-in-async query usage and stable API contracts.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-api-routers-auth-patients-flow/24-CONTEXT.md
@.planning/phases/21-async-foundation/21-04-SUMMARY.md
@.planning/phases/23-service-migration/23-05-SUMMARY.md
@backend-hormonia/app/core/database/async_engine.py
@backend-hormonia/app/api/v2/routers/auth.py
@backend-hormonia/app/api/v2/routers/users.py
@backend-hormonia/app/api/v2/routers/roles/endpoints.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
```

From `backend-hormonia/app/api/v2/routers/auth.py`:
```python
@router.post("/verify-token")
async def verify_firebase_token(..., db=Depends(get_db), ...):

@router.post("/login")
async def login(..., db=Depends(get_db), ...):
```

From `backend-hormonia/app/api/v2/routers/users.py`:
```python
@router.get("/me")
async def get_current_user_profile(..., db=Depends(get_db), ...):

@router.get("/sessions")
async def list_sessions(..., db=Depends(get_db), ...):
```

From `backend-hormonia/app/api/v2/routers/roles/endpoints.py`:
```python
@router.get("/")
async def get_available_roles(..., db: Session = Depends(get_db)):

@router.post("/{user_id}/assign")
async def assign_role_to_user(..., db: Session = Depends(get_db), ...):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert auth and users routers to AsyncSession-safe handlers</name>
  <files>
    backend-hormonia/app/api/v2/routers/auth.py
    backend-hormonia/app/api/v2/routers/users.py
  </files>
  <action>
    Replace sync request-scope DB dependencies with `AsyncSession` and `Depends(get_async_db)` in all migrated auth/users endpoints.

    Convert sync ORM query chains to SQLAlchemy 2.0 async execution (`select(...)`, `await db.execute(...)`, `scalars()/scalar_one_or_none()`) while preserving response payload shape, cache behavior, and current endpoint paths.

    If status-code inconsistencies are discovered in these endpoints, correct them in this phase (per locked decision) without changing endpoint contracts.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/auth.py app/api/v2/routers/users.py</automated>
  </verify>
  <done>Auth and users handlers use AsyncSession in request scope and contain no async-path sync `db.query()` usage.</done>
</task>

<task type="auto">
  <name>Task 2: Convert role-management endpoints to AsyncSession-safe queries</name>
  <files>
    backend-hormonia/app/api/v2/routers/roles/endpoints.py
  </files>
  <action>
    Migrate role and permission endpoints to `AsyncSession` with async-safe query execution, including user lookups, role counts, admin-lockout checks, and statistics aggregation.

    Preserve RBAC rules, audit logging behavior, route signatures, and response schema compatibility. Keep write-operation rate limits and cache decorators intact.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/roles/endpoints.py</automated>
  </verify>
  <done>Role endpoints execute via AsyncSession and keep existing role/permission contract semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add API-01 regression coverage for async migration and contract parity</name>
  <files>
    backend-hormonia/tests/api/v2/test_phase24_auth_users_roles_async.py
  </files>
  <action>
    Create focused regression tests that validate the entire auth/users/roles group after migration:
    - all migrated handlers in these router files depend on `get_async_db` (not `get_db`)
    - async handlers do not use sync `db.query(...)`
    - auth/users/roles endpoint contracts remain stable via targeted route/response assertions

    Include coverage for role and permission routes explicitly (locked decision). Keep tests deterministic and fast.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_phase24_auth_users_roles_async.py tests/api/v2/test_auth_route_corrections.py tests/security/test_rbac_authorization.py -q</automated>
  </verify>
  <done>Automated evidence confirms API-01 migration for all changed auth/users/roles endpoints with contract stability preserved.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/auth.py app/api/v2/routers/users.py app/api/v2/routers/roles/endpoints.py`
- `cd backend-hormonia && pytest tests/api/v2/test_phase24_auth_users_roles_async.py tests/api/v2/test_auth_route_corrections.py tests/security/test_rbac_authorization.py -q`
</verification>

<success_criteria>
- API-01 is met: auth/users/roles request handlers use AsyncSession dependencies.
- No async handler in the API-01 router group performs sync ORM query chaining.
- Contract parity holds: paths/methods/response schema expectations for auth/users/roles remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/24-api-routers-auth-patients-flow/24-01-SUMMARY.md`
</output>
