---
phase: 24-api-routers-auth-patients-flow
plan: 05
type: execute
wave: 2
depends_on:
  - 24-04
files_modified:
  - backend-hormonia/app/api/v2/routers/patients/crud.py
  - backend-hormonia/app/api/v2/routers/patients/integrity.py
  - backend-hormonia/app/api/v2/routers/patients/import_export.py
  - backend-hormonia/app/api/v2/routers/patients/flow.py
  - backend-hormonia/app/api/v2/routers/physicians/crud.py
  - backend-hormonia/tests/api/v2/test_phase24_patients_physicians_async.py
autonomous: true
requirements:
  - API-02
gap_closure: true

must_haves:
  truths:
    - "All API-02 routers inject AsyncSession via get_async_db in request scope"
    - "Patient import/export routes are included in async migration and verification evidence"
    - "Patient/physician async handlers execute without sync query chaining regressions"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/patients/crud.py"
      provides: "AsyncSession-backed patient CRUD handlers"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/patients/import_export.py"
      provides: "AsyncSession-backed import/export handlers"
      contains: "await db.execute("
    - path: "backend-hormonia/app/api/v2/routers/patients/flow.py"
      provides: "AsyncSession-backed patient flow handlers"
      contains: "select(Patient"
    - path: "backend-hormonia/app/api/v2/routers/physicians/crud.py"
      provides: "AsyncSession-backed physician CRUD handlers"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/tests/api/v2/test_phase24_patients_physicians_async.py"
      provides: "API-02 async dependency and contract regression evidence"
      contains: "import_export"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/patients/crud.py"
      to: "backend-hormonia/app/orchestration/saga_orchestrator"
      via: "patient-create saga path using async request dependency"
      pattern: "Saga|onboarding"
    - from: "backend-hormonia/app/api/v2/routers/patients/import_export.py"
      to: "backend-hormonia/app/models/patient.py"
      via: "async select-based import/export query paths"
      pattern: "select\(Patient"
    - from: "backend-hormonia/app/api/v2/routers/physicians/crud.py"
      to: "backend-hormonia/app/models/user.py"
      via: "async physician listing/filtering"
      pattern: "select\(User"
---

<objective>
Close the API-02 migration gap by finishing AsyncSession router migration for patients and physicians.

Purpose: Deliver full patient/physician async safety after blocker remediation, while preserving contract compatibility and locked import/export coverage.
Output: AsyncSession-based API-02 routers with passing async regression checks across CRUD, integrity, flow, and import/export endpoints.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-api-routers-auth-patients-flow/24-CONTEXT.md
@.planning/phases/24-api-routers-auth-patients-flow/24-VERIFICATION.md
@.planning/phases/24-api-routers-auth-patients-flow/24-02-SUMMARY.md
@backend-hormonia/app/api/v2/routers/patients/crud.py
@backend-hormonia/app/api/v2/routers/patients/integrity.py
@backend-hormonia/app/api/v2/routers/patients/import_export.py
@backend-hormonia/app/api/v2/routers/patients/flow.py
@backend-hormonia/app/api/v2/routers/physicians/crud.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
```

From `backend-hormonia/app/api/v2/routers/patients/crud.py`:
```python
@router.get("/")
async def list_patients(..., db: Session = Depends(get_db), ...):
```

From `backend-hormonia/app/api/v2/routers/patients/import_export.py`:
```python
@router.get("/export")
async def export_patients(..., db: Session = Depends(get_db), ...):
```

From `backend-hormonia/app/api/v2/routers/physicians/crud.py`:
```python
@router.get("/")
async def list_physicians(..., db: Session = Depends(get_db), ...):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate patient CRUD/integrity and physician CRUD handlers to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/patients/crud.py
    backend-hormonia/app/api/v2/routers/patients/integrity.py
    backend-hormonia/app/api/v2/routers/physicians/crud.py
  </files>
  <action>
    Replace request-scope sync dependencies (`Session`, `Depends(get_db)`) with `AsyncSession` + `Depends(get_async_db)` and migrate handler query paths to `select(...)/await db.execute(...)` patterns.

    Keep endpoint paths, request/response schemas, permissions, and physician filtering behavior unchanged. Use minimum local hotfixes if migration-specific blockers appear (per locked decision).
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/patients/crud.py app/api/v2/routers/patients/integrity.py app/api/v2/routers/physicians/crud.py</automated>
  </verify>
  <done>CRUD/integrity/physician API-02 handlers use AsyncSession with no async-path `db.query(...)` calls.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate patient import/export and patient-flow routers with contract parity</name>
  <files>
    backend-hormonia/app/api/v2/routers/patients/import_export.py
    backend-hormonia/app/api/v2/routers/patients/flow.py
  </files>
  <action>
    Convert import/export and flow endpoints to AsyncSession-safe query execution, including bulk lookup paths and onboarding/flow status read paths.

    Include the full import/export route set explicitly in migration scope (locked decision). Preserve response/status semantics unless a status correction is required for consistency.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/patients/import_export.py app/api/v2/routers/patients/flow.py</automated>
  </verify>
  <done>All API-02 patient router files are AsyncSession-based, and import/export endpoints remain contract-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run API-02 verification suite with explicit import/export coverage</name>
  <files>
    backend-hormonia/tests/api/v2/test_phase24_patients_physicians_async.py
  </files>
  <action>
    Update/extend API-02 regression tests to assert:
    - all API-02 handlers depend on `get_async_db`
    - no async handler uses sync `db.query(...)`
    - import/export endpoints are explicitly included in validation
    - route/response contracts are unchanged
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_phase24_patients_physicians_async.py tests/api/v2/test_patients.py tests/api/v2/test_physicians.py -q</automated>
  </verify>
  <done>API-02 verification command passes with full patient/physician coverage including import/export routes.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/patients/crud.py app/api/v2/routers/patients/integrity.py app/api/v2/routers/patients/import_export.py app/api/v2/routers/patients/flow.py app/api/v2/routers/physicians/crud.py`
- `cd backend-hormonia && pytest tests/api/v2/test_phase24_patients_physicians_async.py tests/api/v2/test_patients.py tests/api/v2/test_physicians.py -q`
</verification>

<success_criteria>
- API-02 is fully met: all patient/physician routers use AsyncSession in request scope.
- Import/export endpoints are migrated and validated, not deferred.
- API-02 tests pass without adapter/saga migration blockers.
</success_criteria>

<output>
After completion, create `.planning/phases/24-api-routers-auth-patients-flow/24-05-SUMMARY.md`
</output>
