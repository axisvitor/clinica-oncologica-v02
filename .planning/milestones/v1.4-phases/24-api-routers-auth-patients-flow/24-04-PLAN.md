---
phase: 24-api-routers-auth-patients-flow
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/tests/conftest.py
  - backend-hormonia/tests/api/critical/conftest.py
  - backend-hormonia/tests/api/v2/test_phase24_auth_users_roles_async.py
autonomous: true
requirements:
  - API-01
gap_closure: true

must_haves:
  truths:
    - "Auth verification suite runs without sessions.session_token UndefinedColumn failures"
    - "Async adapter-backed tests no longer fail because awaited flush/commit paths are sync-only"
    - "API-01 evidence remains contract-safe after blocker fixes"
  artifacts:
    - path: "backend-hormonia/tests/conftest.py"
      provides: "Root-suite schema and async adapter compatibility guards"
      contains: "SyncToAsyncSessionAdapter"
    - path: "backend-hormonia/tests/api/critical/conftest.py"
      provides: "Critical-suite schema and async adapter compatibility guards"
      contains: "SyncToAsyncSessionAdapter"
    - path: "backend-hormonia/tests/api/v2/test_phase24_auth_users_roles_async.py"
      provides: "Regression assertion that API-01 async dependency and contract checks still hold"
      contains: "get_async_db"
  key_links:
    - from: "backend-hormonia/tests/conftest.py"
      to: "backend-hormonia/tests/api/v2/test_auth_route_corrections.py"
      via: "test-engine schema guard during fixture bootstrap"
      pattern: "ALTER TABLE sessions|information_schema.columns"
    - from: "backend-hormonia/tests/api/critical/conftest.py"
      to: "backend-hormonia/tests/api/v2/test_patients.py"
      via: "awaitable adapter methods used by async router dependencies"
      pattern: "class SyncToAsyncSessionAdapter"
---

<objective>
Close the cross-cutting verification blockers preventing reliable auth and async-adapter evidence in Phase 24.

Purpose: Remove schema/adapter failures that block gap-closure verification while preserving all existing auth/user/role API contracts.
Output: Fixture-level compatibility guards and adapter behavior updates that let API-01 validation run cleanly.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-api-routers-auth-patients-flow/24-CONTEXT.md
@.planning/phases/24-api-routers-auth-patients-flow/24-VERIFICATION.md
@.planning/phases/24-api-routers-auth-patients-flow/24-01-SUMMARY.md
@backend-hormonia/tests/conftest.py
@backend-hormonia/tests/api/critical/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sessions schema compatibility guards for verification environments</name>
  <files>
    backend-hormonia/tests/conftest.py
    backend-hormonia/tests/api/critical/conftest.py
  </files>
  <action>
    Add idempotent Postgres schema guard logic for the `sessions` table so auth verification never fails due to legacy column drift (`sessions.session_token` mismatch).

    Keep fixes minimal and scoped to test bootstrap (locked decision: minimum unblock fix for migration blockers). Do not change runtime API codepaths or endpoint contracts.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_auth.py::TestSessionManagement::test_list_sessions_success -q</automated>
  </verify>
  <done>Session-management auth test passes without `UndefinedColumn` on `sessions.session_token`.</done>
</task>

<task type="auto">
  <name>Task 2: Make sync-backed async test adapter flush/commit behavior await-safe</name>
  <files>
    backend-hormonia/tests/conftest.py
    backend-hormonia/tests/api/critical/conftest.py
  </files>
  <action>
    Update `SyncToAsyncSessionAdapter` behavior so async call sites that await DB write paths (`flush`/`commit` equivalents) work against sync transactional sessions without throwing adapter incompatibility errors.

    Preserve transaction isolation and rollback guarantees currently provided by fixture transaction wrappers.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_patients.py::TestPatientCRUD::test_create_patient_success -q</automated>
  </verify>
  <done>Patient create path no longer fails from adapter flush incompatibility in async dependency override scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run API-01 verification set after blocker fixes</name>
  <files>
    backend-hormonia/tests/api/v2/test_phase24_auth_users_roles_async.py
  </files>
  <action>
    Adjust/extend API-01 regression assertions only as needed to reflect stabilized fixture behavior while keeping existing contract-parity checks intact.

    Ensure full auth/users/roles verification evidence includes role/permission routes per locked decision.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_phase24_auth_users_roles_async.py tests/api/v2/test_auth_route_corrections.py tests/security/test_rbac_authorization.py -q</automated>
  </verify>
  <done>API-01 verification command passes with no schema or adapter blocker failures.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && pytest tests/api/v2/test_auth.py::TestSessionManagement::test_list_sessions_success -q`
- `cd backend-hormonia && pytest tests/api/v2/test_phase24_auth_users_roles_async.py tests/api/v2/test_auth_route_corrections.py tests/security/test_rbac_authorization.py -q`
</verification>

<success_criteria>
- Auth/session verification no longer fails from `sessions.session_token` schema drift.
- Async fixture adapters support awaited write-path behavior used by migrated routers.
- API-01 verification evidence remains green with contract stability preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/24-api-routers-auth-patients-flow/24-04-SUMMARY.md`
</output>
