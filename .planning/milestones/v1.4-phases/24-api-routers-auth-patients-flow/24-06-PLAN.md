---
phase: 24-api-routers-auth-patients-flow
plan: 06
type: execute
wave: 3
depends_on:
  - 24-05
files_modified:
  - backend-hormonia/app/api/v2/routers/flows.py
  - backend-hormonia/tests/api/v2/test_phase24_flows_async.py
autonomous: true
requirements:
  - API-03
gap_closure: true

must_haves:
  truths:
    - "Consolidated flow router handlers use AsyncSession across analytics/state/templates/advanced sections"
    - "No async flow handler keeps sync `get_db` or `db.query(...)` codepaths"
    - "Flow endpoint contracts remain unchanged after final migration"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/flows.py"
      provides: "AsyncSession-backed consolidated flow router"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/tests/api/v2/test_phase24_flows_async.py"
      provides: "API-03 async migration and contract parity regression checks"
      contains: "flows.py"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/flows.py"
      to: "backend-hormonia/app/services/flow_service.py"
      via: "get_flow_service_dependency async wiring"
      pattern: "get_flow_service_dependency"
    - from: "backend-hormonia/app/api/v2/routers/flows.py"
      to: "backend-hormonia/app/models/flow.py"
      via: "async select-based analytics/template/state queries"
      pattern: "select\(PatientFlowState|select\(FlowTemplateVersion"
---

<objective>
Close the final API-03 gap by converting remaining consolidated flow-router query paths to AsyncSession.

Purpose: Finish Phase 24 router migration in locked order after auth and patient groups are stable.
Output: Fully AsyncSession-based `flows.py` plus regression evidence proving section-level coverage and contract parity.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-api-routers-auth-patients-flow/24-CONTEXT.md
@.planning/phases/24-api-routers-auth-patients-flow/24-VERIFICATION.md
@.planning/phases/24-api-routers-auth-patients-flow/24-03-SUMMARY.md
@backend-hormonia/app/api/v2/routers/flows.py
@backend-hormonia/tests/api/v2/test_phase24_flows_async.py

<interfaces>
From `backend-hormonia/app/api/v2/routers/flows.py`:
```python
async def get_flow_service_dependency(
    async_db: AsyncSession = Depends(get_async_db),
    db=Depends(get_db),
) -> FlowService:

@router.get("/analytics")
async def get_flow_analytics(..., db=Depends(get_db), ...):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert remaining consolidated flow handlers to AsyncSession queries</name>
  <files>
    backend-hormonia/app/api/v2/routers/flows.py
  </files>
  <action>
    Replace remaining sync dependency/query paths in `flows.py` with AsyncSession-safe logic for all sections in scope (analytics, state, templates, advanced).

    Preserve current endpoints and payload schemas exactly (locked contract decision). If consistency fixes are needed, apply only contract-preserving corrections.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/flows.py</automated>
  </verify>
  <done>`flows.py` has no API handler using sync `get_db` or `db.query(...)` in async request paths.</done>
</task>

<task type="auto">
  <name>Task 2: Align flow service dependency wiring for non-blocking request execution</name>
  <files>
    backend-hormonia/app/api/v2/routers/flows.py
  </files>
  <action>
    Ensure `get_flow_service_dependency` wiring keeps async request handlers non-blocking and consistent with Phase 23 dual-mode service patterns.

    Keep legacy Celery-oriented sync paths outside this router migration scope unchanged.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_phase24_flows_async.py::test_flow_router_uses_async_dependencies -q</automated>
  </verify>
  <done>Flow service dependency wiring supports AsyncSession request handling without regressing existing flow behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run API-03 regression and contract checks</name>
  <files>
    backend-hormonia/tests/api/v2/test_phase24_flows_async.py
  </files>
  <action>
    Update flow regression tests so section-level coverage enforces the completed migration in consolidated `flows.py` and confirms contract parity against existing flow tests.

    Validation must cover analytics/state/templates/advanced sections explicitly.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_phase24_flows_async.py tests/api/v2/test_flows.py tests/api/v2/test_flows_advance.py tests/unit/test_flow_templates_repro.py -q</automated>
  </verify>
  <done>API-03 verification command passes and confirms full AsyncSession migration for flow routers.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/flows.py`
- `cd backend-hormonia && pytest tests/api/v2/test_phase24_flows_async.py tests/api/v2/test_flows.py tests/api/v2/test_flows_advance.py tests/unit/test_flow_templates_repro.py -q`
</verification>

<success_criteria>
- API-03 is fully met in the consolidated flow router implementation.
- Route contracts remain stable (no endpoint removals/renames or payload breakage).
- Phase 24 gap truths for flow router migration move to verified.
</success_criteria>

<output>
After completion, create `.planning/phases/24-api-routers-auth-patients-flow/24-06-SUMMARY.md`
</output>
