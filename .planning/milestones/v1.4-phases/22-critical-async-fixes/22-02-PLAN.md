---
phase: 22-critical-async-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/flow_alerts.py
  - backend-hormonia/tests/unit/services/test_flow_alerts_async.py
autonomous: true
requirements:
  - CRIT-02

must_haves:
  truths:
    - "Flow alert evaluation completes in async contexts without MissingGreenlet"
    - "All five CRIT-02 async methods in flow_alerts use async-safe SQL execution"
    - "Alert payload format and processing semantics remain compatible with AlertManager"
  artifacts:
    - path: "backend-hormonia/app/services/flow_alerts.py"
      provides: "Async-safe alert query methods for completion, duration, inconsistent state, and inactive templates"
      contains: "await self.db.execute(select(...))"
    - path: "backend-hormonia/tests/unit/services/test_flow_alerts_async.py"
      provides: "Regression tests for async alert query paths and payload compatibility"
      contains: "@pytest.mark.asyncio"
  key_links:
    - from: "backend-hormonia/app/services/flow_alerts.py"
      to: "backend-hormonia/app/dependencies/flow_services.py"
      via: "get_async_flow_alerts_service AsyncSession injection"
      pattern: "FlowAlertsService\(db\)"
    - from: "backend-hormonia/app/services/flow_alerts.py"
      to: "app.services.alerts.get_alert_manager"
      via: "alert processing loop in evaluate_alerts"
      pattern: "await self\.alert_manager\.process_alert"
---

<objective>
Repair CRIT-02 by removing sync DB primitives from async alert generation paths in `flow_alerts.py`.

Purpose: Prevent MissingGreenlet in alert-heavy request paths under async load while keeping business behavior and alert contracts stable.
Output: Async-safe alert service plus focused regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-critical-async-fixes/22-CONTEXT.md
@backend-hormonia/app/services/flow_alerts.py
@backend-hormonia/app/dependencies/flow_services.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert flow alerts async methods to SQLAlchemy 2.0 async execution</name>
  <files>
    backend-hormonia/app/services/flow_alerts.py
  </files>
  <action>
    Migrate all CRIT-02 async method DB access to async-safe execution.

    Required async targets:
    - `evaluate_alerts`
    - `_completion_rate_alerts`
    - `_duration_alerts`
    - `_inconsistent_state_alerts`
    - `_inactive_template_alerts`

    Replace sync query chaining with `select(...)` statements executed via `await self.db.execute(...)`, preserving grouping/filtering logic and emitted alert payloads.
    Keep alert severity/title/message/context behavior unchanged.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/services/flow_alerts.py</automated>
  </verify>
  <done>CRIT-02 methods are async-safe and still produce equivalent alert outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add async regression coverage for flow alert generation</name>
  <files>
    backend-hormonia/tests/unit/services/test_flow_alerts_async.py
  </files>
  <action>
    Add unit tests for async alert generation using AsyncSession mocks/fakes.

    Validate:
    - completion-rate threshold logic
    - duration-threshold logic
    - inconsistent state detection
    - inactive template detection
    - evaluate_alerts processing loop still calls AlertManager

    Include at least one concurrency-oriented test (multiple async evaluations) to guard against MissingGreenlet regressions under load-like conditions.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/services/test_flow_alerts_async.py -q</automated>
  </verify>
  <done>Automated tests confirm async-safe flow alerts execution and unchanged alerting contracts.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && ! rg -n "db\.query\(" app/services/flow_alerts.py`
- `cd backend-hormonia && pytest tests/unit/services/test_flow_alerts_async.py -q`
</verification>

<success_criteria>
- CRIT-02 is satisfied: all 5 async methods in `flow_alerts.py` use async-safe DB operations.
- No sync query primitives remain in async execution paths of this module.
- Async load-like test coverage exists for alert evaluation.
</success_criteria>

<output>
After completion, create `.planning/phases/22-critical-async-fixes/22-02-SUMMARY.md`
</output>
