---
phase: 22-critical-async-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/data_integrity_monitoring.py
  - backend-hormonia/tests/unit/services/test_data_integrity_monitoring_async.py
autonomous: true
requirements:
  - CRIT-01

must_haves:
  truths:
    - "Integrity scan endpoints that rely on DataIntegrityMonitoringService complete without MissingGreenlet in async execution paths"
    - "All five async methods in data_integrity_monitoring use async-safe SQLAlchemy 2.0 execution (no sync query chaining)"
    - "Celery/sync callers continue to instantiate DataIntegrityMonitoringService without behavior changes"
  artifacts:
    - path: "backend-hormonia/app/services/data_integrity_monitoring.py"
      provides: "Async-safe implementations for _scan_patient_integrity, _scan_flow_integrity, _scan_message_integrity, _check_patient_orphaned_relationships, get_integrity_dashboard"
      contains: "await self._execute(select(...))"
    - path: "backend-hormonia/tests/unit/services/test_data_integrity_monitoring_async.py"
      provides: "Regression coverage proving no sync query usage in async methods"
      contains: "@pytest.mark.asyncio"
  key_links:
    - from: "backend-hormonia/app/services/data_integrity_monitoring.py"
      to: "backend-hormonia/app/core/database/dual_session.py"
      via: "DualSessionMixin _execute/_scalars dispatch"
      pattern: "await self\\._execute\(select\("
    - from: "backend-hormonia/app/services/data_integrity_monitoring.py"
      to: "backend-hormonia/app/dependencies/patient_services.py"
      via: "get_async_data_integrity_service AsyncSession injection"
      pattern: "DataIntegrityMonitoringService\(db\)"
---

<objective>
Convert the CRIT-01 hot path to fully async-safe DB access in `data_integrity_monitoring.py`.

Purpose: Eliminate production MissingGreenlet failures from the highest-risk integrity scan flows while preserving existing behavior and Celery sync compatibility.
Output: Async-safe service methods plus focused regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-critical-async-fixes/22-CONTEXT.md
@backend-hormonia/app/services/data_integrity_monitoring.py
@backend-hormonia/app/core/database/dual_session.py
@backend-hormonia/app/dependencies/patient_services.py

<interfaces>
From `backend-hormonia/app/core/database/dual_session.py`:
```python
class DualSessionMixin:
    @property
    def is_async(self) -> bool: ...
    def _execute(self, stmt, **kwargs) -> Any: ...
    def _scalars(self, stmt, **kwargs) -> Any: ...
```

From `backend-hormonia/app/dependencies/patient_services.py`:
```python
async def get_async_data_integrity_service(
    db: AsyncSession = Depends(get_async_db),
) -> DataIntegrityMonitoringService:
    return DataIntegrityMonitoringService(db)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sync ORM calls in the five async integrity methods</name>
  <files>
    backend-hormonia/app/services/data_integrity_monitoring.py
  </files>
  <action>
    Refactor all CRIT-01 async targets to SQLAlchemy 2.0 async-safe patterns using `select(...)` + `await self._execute(...)` or `await self._scalars(...)`.

    Required methods:
    - `_scan_patient_integrity`
    - `_scan_flow_integrity`
    - `_scan_message_integrity`
    - `_check_patient_orphaned_relationships`
    - `get_integrity_dashboard`

    Keep behavior and returned payload shapes unchanged. Keep constructor and sync compatibility unchanged. Remove sync query-chaining from these async methods (`db.query(...).all()/first()/count()`), and avoid introducing silent fallbacks.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/services/data_integrity_monitoring.py</automated>
  </verify>
  <done>All five async CRIT-01 methods use async-safe statement execution and preserve existing outputs/error semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add async regression tests for integrity monitoring paths</name>
  <files>
    backend-hormonia/tests/unit/services/test_data_integrity_monitoring_async.py
  </files>
  <action>
    Create focused async unit tests that exercise each migrated method with AsyncSession-compatible mocks/fakes and verify no sync-query assumptions remain.

    Include coverage for:
    - scan methods returning expected `entities_scanned` and issue aggregation behavior
    - orphaned doctor detection path
    - dashboard health score calculation path
    - failure path preserving logged error semantics

    Tests must fail if implementation regresses to sync query-only behavior in async code paths.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/services/test_data_integrity_monitoring_async.py -q</automated>
  </verify>
  <done>Automated tests prove CRIT-01 async methods run without sync DB operations and without MissingGreenlet regressions.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && ! rg -n "db\.query\(" app/services/data_integrity_monitoring.py`
- `cd backend-hormonia && pytest tests/unit/services/test_data_integrity_monitoring_async.py -q`
- `cd backend-hormonia && pytest tests/integration/test_celery_async_isolation.py::TestCeleryAsyncIsolation::test_shared_service_sync_mode_via_dual_session_mixin -q`
</verification>

<success_criteria>
- CRIT-01 is satisfied: all 5 async methods in `data_integrity_monitoring.py` use async-safe DB execution.
- No sync query primitives remain in async execution paths of this module.
- Sync/Celery usage remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/22-critical-async-fixes/22-01-SUMMARY.md`
</output>
