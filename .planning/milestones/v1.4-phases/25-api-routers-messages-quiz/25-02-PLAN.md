---
phase: 25-api-routers-messages-quiz
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/api/v2/_quiz_shared.py
  - backend-hormonia/app/api/v2/routers/monthly_quiz_operations/_shared.py
  - backend-hormonia/app/api/v2/routers/quiz_templates.py
  - backend-hormonia/app/api/v2/routers/monthly_quiz_management.py
autonomous: true
requirements:
  - API-05

must_haves:
  truths:
    - "Shared quiz helpers export get_async_db (and temporarily keep get_db for scheduling.py/public.py until Plan 25-04) and _check_patient_access is async"
    - "quiz_templates.py uses AsyncSession for all CRUD operations"
    - "monthly_quiz_management.py uses AsyncSession for all operations including _get_monthly_quiz_or_404"
    - "API contract is unchanged -- same endpoints, same request/response shapes"
  artifacts:
    - path: "backend-hormonia/app/api/v2/_quiz_shared.py"
      provides: "Async _check_patient_access helper and updated imports"
      contains: "async def _check_patient_access"
    - path: "backend-hormonia/app/api/v2/routers/monthly_quiz_operations/_shared.py"
      provides: "get_async_db re-export added alongside get_db (get_db kept for scheduling.py/public.py until Plan 25-04)"
      contains: "get_async_db"
    - path: "backend-hormonia/app/api/v2/routers/quiz_templates.py"
      provides: "AsyncSession-backed quiz template CRUD router"
      contains: "Depends(get_async_db)"
    - path: "backend-hormonia/app/api/v2/routers/monthly_quiz_management.py"
      provides: "AsyncSession-backed monthly quiz management router"
      contains: "Depends(get_async_db)"
  key_links:
    - from: "backend-hormonia/app/api/v2/_quiz_shared.py"
      to: "backend-hormonia/app/models/patient.py"
      via: "async select-based patient access check"
      pattern: "select\\(Patient"
    - from: "backend-hormonia/app/api/v2/routers/monthly_quiz_operations/_shared.py"
      to: "backend-hormonia/app/core/database/async_engine.py"
      via: "re-export of get_async_db"
      pattern: "get_async_db"
---

<objective>
Migrate quiz shared helpers and simple quiz routers to AsyncSession.

Purpose: Two shared helper files (`_quiz_shared.py` and `monthly_quiz_operations/_shared.py`) re-export `get_db` and provide sync helpers consumed by multiple quiz routers. These MUST be converted first. Then the two simplest quiz routers (`quiz_templates.py` with 5 db.query, `monthly_quiz_management.py` with 4 db.query) are migrated.
Output: All four files using AsyncSession, shared helpers async-compatible, simple quiz routers fully migrated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-api-routers-messages-quiz/25-RESEARCH.md
@backend-hormonia/app/api/v2/_quiz_shared.py
@backend-hormonia/app/api/v2/routers/monthly_quiz_operations/_shared.py
@backend-hormonia/app/api/v2/routers/quiz_templates.py
@backend-hormonia/app/api/v2/routers/monthly_quiz_management.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
```

From `backend-hormonia/app/api/v2/_quiz_shared.py` (BEFORE migration):
```python
from sqlalchemy.orm import Session
def _check_patient_access(db: Session, current_user: User, patient_id: UUID) -> Patient:
    patient = db.query(Patient).filter(Patient.id == patient_id).first()
    ...
```

AFTER migration target:
```python
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy.future import select
async def _check_patient_access(db: AsyncSession, current_user: User, patient_id: UUID) -> Patient:
    result = await db.execute(select(Patient).where(Patient.id == patient_id))
    patient = result.scalar_one_or_none()
    ...
```

From `backend-hormonia/app/api/v2/routers/monthly_quiz_operations/_shared.py` (BEFORE migration):
```python
from app.database import get_db
# Re-exports get_db for crud.py consumers
```

AFTER migration target:
```python
from app.database import get_db  # kept for scheduling.py/public.py (Plan 25-04)
from app.core.database.async_engine import get_async_db  # new: for crud.py consumers
# Re-exports both get_db and get_async_db until Plan 25-04 completes
```

CRITICAL: After making `_check_patient_access` async, ALL callers must use `await _check_patient_access(...)`. Callers in `quiz_responses.py` and `quiz_alerts.py` will be updated in Plan 25-03. For this plan, only update callers within `quiz_templates.py` and `monthly_quiz_management.py` if they call this function (research shows they do not).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert shared helper files to async</name>
  <files>
    backend-hormonia/app/api/v2/_quiz_shared.py
    backend-hormonia/app/api/v2/routers/monthly_quiz_operations/_shared.py
  </files>
  <action>
    **File 1: `app/api/v2/_quiz_shared.py`**

    1. Replace `from sqlalchemy.orm import Session` with `from sqlalchemy.ext.asyncio import AsyncSession`
    2. Add `from sqlalchemy.future import select`
    3. Convert `_check_patient_access` to async:
       - Change `def _check_patient_access(db: Session, ...)` to `async def _check_patient_access(db: AsyncSession, ...)`
       - Replace `patient = db.query(Patient).filter(Patient.id == patient_id).first()` with:
         ```python
         result = await db.execute(select(Patient).where(Patient.id == patient_id))
         patient = result.scalar_one_or_none()
         ```
    4. If there are any other sync DB calls in this file, convert them similarly
    5. Leave `_get_current_user_simple` unchanged (already async-safe, no DB call)

    **File 2: `app/api/v2/routers/monthly_quiz_operations/_shared.py`**

    1. Add `from app.core.database.async_engine import get_async_db` (keep the existing `from app.database import get_db` for now -- `scheduling.py` and `public.py` still import `get_db` from this module and will be migrated in Plan 25-04)
    2. Add `from sqlalchemy.ext.asyncio import AsyncSession` (for consumers that import it from `_shared`)
    3. Update `__all__` list: ADD `"get_async_db"` and `"AsyncSession"` (keep `"get_db"` and `"Session"` until Plan 25-04 removes them)
    4. IMPORTANT: Do NOT remove `get_db` or `Session` yet -- `scheduling.py` and `public.py` still depend on them and will be migrated in Plan 25-04

    Compile check both files.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/_quiz_shared.py && python3 -m py_compile app/api/v2/routers/monthly_quiz_operations/_shared.py</automated>
  </verify>
  <done>Both shared helper files compile. `_check_patient_access` is async with `AsyncSession`. `_shared.py` re-exports `get_async_db` alongside `get_db` (backward compat for scheduling.py/public.py until Plan 25-04).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate quiz_templates.py to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/quiz_templates.py
  </files>
  <action>
    In `backend-hormonia/app/api/v2/routers/quiz_templates.py`:

    1. Replace `get_db` import with `get_async_db`:
       - Remove: `from app.database import get_db` (or wherever imported)
       - Add: `from app.core.database.async_engine import get_async_db`
       - Add: `from sqlalchemy.ext.asyncio import AsyncSession`
       - Add: `from sqlalchemy.future import select`

    2. Update ALL handler signatures:
       - Change `db=Depends(get_db)` or `db: Session = Depends(get_db)` to `db: AsyncSession = Depends(get_async_db)`
       - There are approximately 6 `Depends(get_db)` occurrences

    3. Convert all 5 `db.query(...)` calls to async select/execute:
       - `db.query(QuizTemplate).filter(...)` -> `result = await db.execute(select(QuizTemplate).where(...))` then `result.scalar_one_or_none()` or `result.scalars().all()`
       - Replace `db.query(QuizTemplate).get(template_id)` with `result = await db.execute(select(QuizTemplate).where(QuizTemplate.id == template_id))` then `result.scalar_one_or_none()` (`.get()` is deprecated in async)

    4. Await all write operations:
       - `db.commit()` -> `await db.commit()`
       - `db.flush()` -> `await db.flush()`
       - `db.refresh(obj)` -> `await db.refresh(obj)`
       - `db.delete(obj)` -> `await db.delete(obj)`
       - `db.add(obj)` stays sync (correct for AsyncSession)

    5. `TemplateAuditLogger` has no DB calls (just logging) -- leave unchanged.

    6. Remove `from sqlalchemy.orm import Session` if no longer used.

    Compile check.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/quiz_templates.py && python3 -c "import inspect; import app.api.v2.routers.quiz_templates as m; s=inspect.getsource(m); assert 'db.query(' not in s, 'db.query found'; assert 'Depends(get_db)' not in s, 'get_db found'; print('PASS: quiz_templates.py fully async')"</automated>
  </verify>
  <done>quiz_templates.py uses `Depends(get_async_db)` in all handlers. Zero `db.query(` calls. All write ops awaited. File compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate monthly_quiz_management.py to AsyncSession</name>
  <files>
    backend-hormonia/app/api/v2/routers/monthly_quiz_management.py
  </files>
  <action>
    In `backend-hormonia/app/api/v2/routers/monthly_quiz_management.py`:

    1. Replace `get_db` import with `get_async_db`:
       - Remove: `from app.database import get_db` (or wherever imported)
       - Add: `from app.core.database.async_engine import get_async_db`
       - Add: `from sqlalchemy.ext.asyncio import AsyncSession`
       - Add: `from sqlalchemy.future import select`

    2. Update ALL handler signatures:
       - Change `db=Depends(get_db)` to `db: AsyncSession = Depends(get_async_db)`
       - There are approximately 7 `Depends(get_db)` occurrences

    3. Convert `_get_monthly_quiz_or_404` helper to async:
       - Change `def _get_monthly_quiz_or_404(db, quiz_id)` to `async def _get_monthly_quiz_or_404(db: AsyncSession, quiz_id)`
       - Replace `db.query(QuizTemplate).filter(QuizTemplate.id == quiz_id).first()` with:
         ```python
         result = await db.execute(select(QuizTemplate).where(QuizTemplate.id == quiz_id))
         quiz = result.scalar_one_or_none()
         ```
       - Update ALL 3 call sites to use `await _get_monthly_quiz_or_404(db, quiz_id)`

    4. Convert all remaining `db.query(...)` calls (4 total) to async select/execute:
       - Same pattern as quiz_templates.py

    5. Handle JSONB `tags` mutation pattern:
       - The handler mutates `quiz.tags` then calls `db.commit()` -- this works with AsyncSession
       - Ensure `await db.commit()` after mutation
       - If `flag_modified` is used for JSONB change tracking, keep it (it's sync-compatible)

    6. Await all write operations:
       - `db.commit()` -> `await db.commit()`
       - `db.flush()` -> `await db.flush()`
       - `db.refresh(obj)` -> `await db.refresh(obj)`
       - `db.add(obj)` stays sync

    7. Remove `from sqlalchemy.orm import Session` if no longer used.

    Compile check.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/monthly_quiz_management.py && python3 -c "import inspect; import app.api.v2.routers.monthly_quiz_management as m; s=inspect.getsource(m); assert 'db.query(' not in s, 'db.query found'; assert 'Depends(get_db)' not in s, 'get_db found'; print('PASS: monthly_quiz_management.py fully async')"</automated>
  </verify>
  <done>monthly_quiz_management.py uses `Depends(get_async_db)` in all handlers. Zero `db.query(` calls. `_get_monthly_quiz_or_404` is async. All write ops awaited. File compiles.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/api/v2/_quiz_shared.py`
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/monthly_quiz_operations/_shared.py`
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/quiz_templates.py`
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/monthly_quiz_management.py`
- Source-level assertions confirm zero `db.query(` and zero `Depends(get_db)` in quiz_templates.py and monthly_quiz_management.py
</verification>

<success_criteria>
- `_quiz_shared.py` has `async def _check_patient_access` with `AsyncSession` parameter
- `monthly_quiz_operations/_shared.py` re-exports `get_async_db` (and temporarily keeps `get_db` for scheduling.py/public.py until Plan 25-04)
- `quiz_templates.py` has zero `Depends(get_db)` and zero `db.query(` calls
- `monthly_quiz_management.py` has zero `Depends(get_db)` and zero `db.query(` calls
- `_get_monthly_quiz_or_404` is async and all 3 call sites use `await`
- All four files compile without errors
- API endpoints unchanged (same paths, methods, request/response shapes)
</success_criteria>

<output>
After completion, create `.planning/phases/25-api-routers-messages-quiz/25-02-SUMMARY.md`
</output>
