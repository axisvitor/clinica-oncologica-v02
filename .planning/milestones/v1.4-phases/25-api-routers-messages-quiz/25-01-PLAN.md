---
phase: 25-api-routers-messages-quiz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/api/v2/routers/messages.py
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "All message router handlers use AsyncSession via Depends(get_async_db) instead of sync get_db"
    - "Handlers that previously called sync MessageRepository/MessageService/PatientRepository now inline async select/execute queries"
    - "Stub handlers that declared Depends(get_db) but performed no DB work use get_async_db or have the dependency removed"
    - "API contract is unchanged -- same endpoints, same request/response shapes"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/messages.py"
      provides: "Fully AsyncSession-backed message router"
      contains: "Depends(get_async_db)"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/messages.py"
      to: "backend-hormonia/app/models/message.py"
      via: "async select-based queries replacing sync repo calls"
      pattern: "select\\(Message"
    - from: "backend-hormonia/app/api/v2/routers/messages.py"
      to: "backend-hormonia/app/models/patient.py"
      via: "inlined async patient lookup replacing PatientRepository"
      pattern: "select\\(Patient"
---

<objective>
Migrate the messages router from sync Session to AsyncSession.

Purpose: `messages.py` (947 lines) is the sole file for API-04. It contains 12 `Depends(get_db)` occurrences across 10+ handlers. Of these, 6 are stubs returning empty/mock data (no real DB work), and the remaining handlers call sync-only repositories (`MessageRepository`, `MessageService`, `PatientRepository`) that cannot receive `AsyncSession`. The migration must inline async queries for real DB handlers and swap the dependency for stubs.
Output: `messages.py` with zero `Depends(get_db)`, zero `db.query(`, all write ops awaited.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-api-routers-messages-quiz/25-RESEARCH.md
@backend-hormonia/app/api/v2/routers/messages.py
@backend-hormonia/app/domain/messaging/core/message_service/service.py

<interfaces>
From `backend-hormonia/app/core/database/async_engine.py`:
```python
async def get_async_db() -> AsyncGenerator[AsyncSession, None]:
    # Yields AsyncSession for request-scoped DI
```

Migration patterns (from Phase 24, verified):
```python
# DI swap
from app.core.database.async_engine import get_async_db
db: AsyncSession = Depends(get_async_db)

# Read single
result = await db.execute(select(Model).where(Model.id == id))
obj = result.scalar_one_or_none()

# Read list
result = await db.execute(select(Model).where(...))
items = result.scalars().all()

# Count
result = await db.execute(select(func.count(Model.id)))
count = result.scalar_one()

# Bulk update
await db.execute(update(Message).where(...).values(...))
await db.commit()

# Write
db.add(obj)
await db.flush()
await db.commit()
await db.refresh(obj)
```

CRITICAL: Do NOT pass AsyncSession to `MessageRepository(db)`, `MessageService(db)`, or `PatientRepository(db)`. These are sync-only repos (typed `db: Session`). Inline the async queries directly in the handler instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace get_db with get_async_db and update imports in messages.py</name>
  <files>
    backend-hormonia/app/api/v2/routers/messages.py
  </files>
  <action>
    In `backend-hormonia/app/api/v2/routers/messages.py`:

    1. Replace the `get_db` import with `get_async_db`:
       - Remove: `from app.database import get_db` (or wherever get_db is imported from)
       - Add: `from app.core.database.async_engine import get_async_db`
       - Add: `from sqlalchemy.ext.asyncio import AsyncSession`
       - Add: `from sqlalchemy.future import select` (if not present)
       - Add: `from sqlalchemy import update, func` (if not present for bulk update and count)

    2. For ALL handler function signatures that have `db=Depends(get_db)` or `db: Session = Depends(get_db)`:
       - Change to `db: AsyncSession = Depends(get_async_db)`
       - There are approximately 12 occurrences

    3. For STUB handlers that declare `Depends(get_db)` but perform NO actual database work (return empty lists, zero-filled stats, mock responses, or 501 Not Implemented):
       - `list_scheduled_messages` -- returns empty list stub
       - `get_patient_message_stats` -- returns zero-filled stub
       - `send_bulk_messages` -- returns mock response
       - Simply swap `Depends(get_db)` to `Depends(get_async_db)` in the signature. No body changes needed since these handlers don't touch db.

    4. Remove any `from sqlalchemy.orm import Session` import that is no longer used (only if ALL usages are removed).

    5. Do NOT touch handlers that have no `Depends(get_db)` (template endpoints returning 501, analytics stubs returning zeros).

    Compile check: `python3 -m py_compile app/api/v2/routers/messages.py`
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/messages.py</automated>
  </verify>
  <done>All handler signatures use `Depends(get_async_db)` with `AsyncSession` type. No `Depends(get_db)` remains in messages.py. File compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Inline async DB operations for real-work handlers in messages.py</name>
  <files>
    backend-hormonia/app/api/v2/routers/messages.py
  </files>
  <action>
    Convert the handler bodies that perform actual database work. These handlers currently call sync repositories that cannot accept AsyncSession. Inline the async queries directly.

    **Handler: `list_messages`** (calls `MessageRepository(db).list_v2(...)`)
    - Remove `repo = MessageRepository(db)` and `results = repo.list_v2(...)` call
    - Inline: Build a `select(Message).where(...)` query with the same filters (patient_id, direction, status, date range) that `list_v2` applies
    - Apply `.limit(limit).offset(offset)` or cursor-based pagination matching existing behavior
    - Execute: `result = await db.execute(stmt)` then `messages = result.scalars().all()`
    - If the handler returns a count, add: `count_result = await db.execute(select(func.count()).select_from(sub))` using the same filter conditions

    **Handler: `list_conversations`** (3 `db.query()` calls including N+1)
    - Replace `db.query(func.distinct(Message.patient_id))...` with `await db.execute(select(func.distinct(Message.patient_id)).where(...))`
    - Replace per-patient `db.query(Message)` lookups with a batch query using `.in_()` or a single windowed query
    - Replace count query with `await db.execute(select(func.count(...)))`

    **Handler: `get_conversation_unread_count`** (1 `db.query()` count)
    - Replace `db.query(func.count(Message.id)).filter(...)` with `result = await db.execute(select(func.count(Message.id)).where(...))` then `count = result.scalar_one() or 0`

    **Handler: `mark_conversation_read`** (1 bulk update + commit)
    - Replace `db.query(Message).filter(...).update({...})` with `await db.execute(update(Message).where(...).values(...))`
    - Replace `db.commit()` with `await db.commit()`

    **Handler: `get_message`** (single query)
    - Replace `db.query(Message).filter(Message.id == message_id).first()` with `result = await db.execute(select(Message).where(Message.id == message_id))` then `message = result.scalar_one_or_none()`

    **Handler: `send_message`** (calls `PatientRepository(db)` + `MessageService(db)`)
    - Remove `repo = PatientRepository(db)` and `patient = repo.get_by_id(pid)`
    - Inline: `result = await db.execute(select(Patient).where(Patient.id == pid))` then `patient = result.scalar_one_or_none()`
    - Remove `message_service = MessageService(db)` and `message = message_service.schedule_message(...)`
    - Inline the message creation: `new_message = Message(patient_id=pid, content=..., ...)` then `db.add(new_message)`, `await db.commit()`, `await db.refresh(new_message)`
    - If `schedule_message` does more than create+commit (check the method body first), replicate the essential logic inline

    **Handler: `mark_message_as_read`** (calls `MessageService(db).get_message()` + `update_message()`)
    - Replace `service.get_message(mid)` with `result = await db.execute(select(Message).where(Message.id == mid))` then `message = result.scalar_one_or_none()`
    - Replace `service.update_message(...)` with direct attribute assignment + `await db.commit()` + `await db.refresh(message)`

    **Handler: `delete_message`** (calls `MessageService(db).get_message()` + `update_message()`)
    - Same pattern as `mark_message_as_read`: inline the lookup and update

    **Handler: `get_patient_conversation`** (calls `MessageRepository(db).list_v2()`)
    - Same inlining pattern as `list_messages` with patient_id filter pre-applied

    For ALL handlers: ensure every `db.commit()`, `db.flush()`, `db.refresh()`, `db.delete()`, `db.rollback()` has `await` prefix. `db.add()` stays sync.

    Compile check after all changes.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m py_compile app/api/v2/routers/messages.py && python3 -c "import inspect; import app.api.v2.routers.messages as m; s=inspect.getsource(m); assert 'db.query(' not in s, 'db.query found'; assert 'Depends(get_db)' not in s, 'get_db found'; assert 'MessageRepository(' not in s, 'sync repo found'; assert 'MessageService(' not in s, 'sync service found'; print('PASS: messages.py fully async')"</automated>
  </verify>
  <done>All real-work handlers inline async select/execute queries. No sync repo instantiation (`MessageRepository`, `MessageService`, `PatientRepository`) remains. All write ops are awaited. File compiles and passes source-level assertions.</done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python3 -m py_compile app/api/v2/routers/messages.py` -- compiles without errors
- `cd backend-hormonia && python3 -c "import inspect; import app.api.v2.routers.messages as m; s=inspect.getsource(m); assert 'db.query(' not in s; assert 'Depends(get_db)' not in s; assert 'MessageRepository(' not in s; assert 'MessageService(' not in s; print('OK')"` -- no sync patterns remain
</verification>

<success_criteria>
- messages.py has zero `Depends(get_db)` occurrences
- messages.py has zero `db.query(` occurrences
- messages.py has zero `MessageRepository(`, `MessageService(`, `PatientRepository(` instantiations
- All `db.commit/flush/refresh/delete/rollback` calls are preceded by `await`
- API endpoints remain identical (same paths, same HTTP methods, same request/response shapes)
</success_criteria>

<output>
After completion, create `.planning/phases/25-api-routers-messages-quiz/25-01-SUMMARY.md`
</output>
