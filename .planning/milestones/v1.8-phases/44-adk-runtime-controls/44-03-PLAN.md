---
phase: 44-adk-runtime-controls
plan: 03
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - backend-hormonia/app/api/v2/routers/adk.py
  - backend-hormonia/app/ai/adk/runtime.py
  - backend-hormonia/app/ai/adk/session_store.py
  - backend-hormonia/tests/api/v2/test_adk.py
  - backend-hormonia/tests/unit/test_adk_tools_runtime.py
  - backend-hormonia/tests/unit/test_adk_runner_integration.py
  - .planning/phases/44-adk-runtime-controls/44-VALIDATION.md
autonomous: true
requirements:
  - ADK-09
  - ADK-10
must_haves:
  truths:
    - "Session resume retains only bounded, prioritized state and prunes the oldest low-priority data first"
    - "Closed-session reuse and oversized-resume attempts are rejected deterministically"
    - "Phase 44 behavior is locked by API, unit, and conditional runtime-real regression coverage"
  artifacts:
    - path: "backend-hormonia/app/ai/adk/session_store.py"
      provides: "Bounded-state pruning, same-tool continuity, and terminal close semantics"
    - path: "backend-hormonia/tests/api/v2/test_adk.py"
      provides: "Route-level regression coverage for auto-create/resume/close/cancel and normalized outputs"
    - path: "backend-hormonia/tests/unit/test_adk_runner_integration.py"
      provides: "Conditional runtime-real verification when `google-adk` is installed"
  key_links:
    - from: "backend-hormonia/app/ai/adk/session_store.py"
      to: "backend-hormonia/app/api/v2/routers/adk.py"
      via: "resume and close decisions reflected in public API behavior"
      pattern: "closed|resume|prune|state_size|tool_name"
    - from: "backend-hormonia/app/ai/adk/runtime.py"
      to: "backend-hormonia/tests/unit/test_adk_runner_integration.py"
      via: "conditional runner-path parity for bounded sessions"
      pattern: "Runner|session_id|context"
    - from: ".planning/phases/44-adk-runtime-controls/44-VALIDATION.md"
      to: "backend-hormonia/tests/api/v2/test_adk.py"
      via: "final validation map aligns with implemented task/test coverage"
      pattern: "ADK-09|ADK-10"
---

<objective>
Finalize bounded session-state behavior and lock the entire phase with regression coverage that proves create/resume/close, bounded growth, cancellation, and runtime-limit behavior together.

Purpose: Phase 44 is not done when the controls merely exist; it is done when bounded-state semantics and route/runtime regressions are locked well enough for execution and later verification to trust them.

Output: completed resume/close/prune semantics plus a synchronized validation map and comprehensive regression suite for the phase.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
@./.codex/get-shit-done/references/checkpoints.md
@./.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-adk-runtime-controls/44-CONTEXT.md
@.planning/phases/44-adk-runtime-controls/44-RESEARCH.md
@.planning/phases/44-adk-runtime-controls/44-VALIDATION.md
@.planning/phases/44-adk-runtime-controls/44-02-SUMMARY.md

<interfaces>
Bounded-state rules from `44-CONTEXT.md`:
- recent relevant turns + structured clinical context only
- oldest low-priority state pruned first
- most recent successful turn and clinical context have priority
- oversized resume still blocks after pruning

Current conditional integration test path:

```python
@pytest.mark.skipif(not HAS_ADK, reason=\"google-adk not installed\")
async def test_run_adk_tool_exercises_runner_path_with_domain_client(...)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize bounded state, resume, and close semantics</name>
  <files>backend-hormonia/app/ai/adk/session_store.py, backend-hormonia/app/ai/adk/runtime.py, backend-hormonia/app/api/v2/routers/adk.py, backend-hormonia/tests/api/v2/test_adk.py, backend-hormonia/tests/unit/test_adk_tools_runtime.py</files>
  <action>
    Complete the application-owned session envelope so it keeps only bounded, prioritized state and enforces the final user decisions from `44-CONTEXT.md`.

    Implement pruning of the oldest low-priority state first, protect structured clinical context plus the most recent successful turn, reject closed-session reuse, and block resume when the session still exceeds the configured budget after pruning.

    Keep session continuity same-tool-only, and make sure close semantics are terminal but cancellation still leaves the session reusable.

  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py -q</automated>
  </verify>
  <done>Bounded-state behavior, close semantics, and oversize-resume rejection all match the locked Phase 44 decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the phase with route, unit, and conditional runtime-real regressions</name>
  <files>backend-hormonia/tests/api/v2/test_adk.py, backend-hormonia/tests/unit/test_adk_tools_runtime.py, backend-hormonia/tests/unit/test_adk_runner_integration.py, .planning/phases/44-adk-runtime-controls/44-VALIDATION.md</files>
  <behavior>
    - Test 1: route-level contract covers auto-create, resume, close, cancel, timeout, and limit-hit output normalization.
    - Test 2: runtime-level coverage proves late-result discard, bounded-state pruning, and oversized-resume rejection.
    - Test 3: conditional real-ADK runner path remains compatible when `google-adk` is available, without making local feedback depend on it.
  </behavior>
  <action>
    Expand the regression suite so ADK-09 and ADK-10 are both locked by tests instead of only by implementation intent.

    Update `44-VALIDATION.md` so the validation map reflects the final task/test coverage actually delivered by the phase.

    Keep the real-runner test conditional. Do not convert the phase into a CI smoke-gate project; that belongs to Phase 47.

  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_adk.py tests/unit/test_pii_safe_adk_wrapper.py tests/unit/test_adk_tools_runtime.py tests/unit/test_adk_runner_integration.py -q</automated>
  </verify>
  <done>The full phase regression suite covers both requirements and the validation strategy matches the delivered checks.</done>
</task>

</tasks>

<verification>
Run from `backend-hormonia/`:
1. `pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py -q`
2. `pytest tests/api/v2/test_adk.py tests/unit/test_pii_safe_adk_wrapper.py tests/unit/test_adk_tools_runtime.py tests/unit/test_adk_runner_integration.py -q`
</verification>

<success_criteria>

- ADK-09 and ADK-10 are fully locked by regression tests.
- Session state remains bounded and deterministic across resume/close flows.
- Conditional runtime-real coverage exists without breaking local non-ADK environments.
- `44-VALIDATION.md` matches the delivered verification surface.
  </success_criteria>

<output>
After completion, create `.planning/phases/44-adk-runtime-controls/44-03-SUMMARY.md`
</output>
