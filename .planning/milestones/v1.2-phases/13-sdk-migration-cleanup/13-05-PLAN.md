---
phase: 13-sdk-migration-cleanup
plan: 05
type: execute
wave: 4
depends_on:
  - 13-04
files_modified:
  - backend-hormonia/app/services/enhanced_flow_engine.py
  - backend-hormonia/app/services/flow/sequential_message_handler.py
  - backend-hormonia/app/tasks/flows/batch_tasks.py
  - backend-hormonia/app/tasks/flows/flow_tasks.py
  - backend-hormonia/app/tasks/flow_automation.py
  - backend-hormonia/tests/validation/test_celery_ai_run_sync_path.py
autonomous: true
requirements:
  - SDK-03
gap_closure: true

must_haves:
  truths:
    - "Celery flow tasks that trigger AI (`process_daily_flows`, `send_flow_day_for_patient`) call run_sync-backed AI methods explicitly"
    - "AI call-chain from Celery tasks no longer relies on async agent .run() path for personalization/sentiment/empathy operations"
    - "Remaining async_to_sync in `app/tasks/flows/base.py` and `app/tasks/follow_up.py` is documented and enforced as non-AI"
  artifacts:
    - path: "backend-hormonia/app/services/enhanced_flow_engine.py"
      provides: "Celery-specific switch to sync AI methods"
      contains: "use_sync_agents"
    - path: "backend-hormonia/app/tasks/flows/batch_tasks.py"
      provides: "Daily flow task path wired to sync AI generation"
      contains: "use_sync_agents=True"
    - path: "backend-hormonia/tests/validation/test_celery_ai_run_sync_path.py"
      provides: "Permanent regression checks for SDK-03 Celery AI wiring"
  key_links:
    - from: "backend-hormonia/app/tasks/flows/batch_tasks.py"
      to: "backend-hormonia/app/services/enhanced_flow_engine.py"
      via: "generate_flow_message(..., use_sync_agents=True)"
      pattern: "use_sync_agents=True"
    - from: "backend-hormonia/app/tasks/flow_automation.py"
      to: "backend-hormonia/app/services/flow/sequential_message_handler.py"
      via: "SequentialMessageHandler(..., use_sync_agent_bridge=True)"
      pattern: "use_sync_agent_bridge=True"
    - from: "backend-hormonia/tests/validation/test_celery_ai_run_sync_path.py"
      to: "backend-hormonia/app/tasks"
      via: "AST assertions over AI vs non-AI async_to_sync usage"
      pattern: "async_to_sync"
---

<objective>
Close the remaining SDK-03 gap by wiring Celery AI execution to run_sync-backed methods and enforcing the boundary with permanent validation tests.

Purpose: Verification reported that task modules still looked async-wrapper-driven with no explicit run_sync usage in AI paths. This plan makes Celery AI call sites explicit and creates a regression gate so the gap cannot reappear.

Output: Celery AI task paths wired to sync agent methods plus an AST-based validation suite proving AI and non-AI boundaries.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sdk-migration-cleanup/13-CONTEXT.md
@.planning/phases/13-sdk-migration-cleanup/13-VERIFICATION.md
@.planning/phases/13-sdk-migration-cleanup/13-04-SUMMARY.md
@backend-hormonia/app/tasks/flows/batch_tasks.py
@backend-hormonia/app/tasks/flow_automation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Celery AI flow chain to call run_sync-backed methods</name>
  <files>backend-hormonia/app/services/enhanced_flow_engine.py, backend-hormonia/app/services/flow/sequential_message_handler.py, backend-hormonia/app/tasks/flows/batch_tasks.py, backend-hormonia/app/tasks/flows/flow_tasks.py, backend-hormonia/app/tasks/flow_automation.py</files>
  <action>
    Make run_sync-backed AI execution explicit at Celery call sites.

    Required changes:
    1. In `EnhancedFlowEngine.generate_flow_message` (and any helper it uses for sentiment/empathy in Celery paths), add a `use_sync_agents: bool = False` argument.
    2. When `use_sync_agents=True`, route AI calls through GeminiDomainClient sync APIs (`*_sync`) rather than async APIs.
       - For async contexts, call sync APIs via `asyncio.to_thread(...)` to avoid nested event-loop errors while still executing run_sync path.
    3. In `SequentialMessageHandler`, add/init `use_sync_agent_bridge` and propagate to engine calls (`generate_flow_message(..., use_sync_agents=self.use_sync_agent_bridge)`).
    4. Wire Celery entrypoints:
       - `batch_tasks.py`: call flow engine message generation with `use_sync_agents=True`.
       - `flow_automation.py` (`send_flow_day_for_patient`): instantiate `SequentialMessageHandler(..., use_sync_agent_bridge=True)`.
       - `flow_tasks.py`: update wrapper docs/comments to state Celery AI run_sync wiring is enforced in batch path.

    Constraints:
    - Keep existing retry/backoff/time_limit config unchanged.
    - Do not introduce new global feature flags or env settings.
  </action>
  <verify>
    <automated>cd /mnt/c/Meu\ Projetos/clinica-oncologica-v02-1/backend-hormonia && python3 -c "from pathlib import Path; checks={'app/services/enhanced_flow_engine.py':'use_sync_agents','app/services/flow/sequential_message_handler.py':'use_sync_agent_bridge','app/tasks/flows/batch_tasks.py':'use_sync_agents=True','app/tasks/flow_automation.py':'use_sync_agent_bridge=True'}; missing=[f'{p}:{needle}' for p,needle in checks.items() if needle not in Path(p).read_text(encoding='utf-8')]; print('missing='+str(len(missing))); [print(m) for m in missing]; raise SystemExit(1 if missing else 0)"</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Celery AI flow entrypoints explicitly invoke sync-agent path (`run_sync`) while preserving existing task retry/backoff behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add permanent SDK-03 Celery wiring and non-AI wrapper audits</name>
  <files>backend-hormonia/tests/validation/test_celery_ai_run_sync_path.py</files>
  <action>
    Add an automated validation file that enforces both sides of the verification gap.

    In `test_celery_ai_run_sync_path.py`, implement:
    1. **AI-path assertions:**
       - `batch_tasks.py` contains `use_sync_agents=True` on flow message generation path.
       - `flow_automation.py` contains `use_sync_agent_bridge=True` for `send_flow_day_for_patient` path.
       - `enhanced_flow_engine.py` exposes `use_sync_agents` branch that calls sync APIs.
    2. **Non-AI async wrapper proof:**
       - AST-parse `app/tasks/flows/base.py` and `app/tasks/follow_up.py`.
       - Fail if they import `app.ai`, `app.ai.agents`, `GeminiDomainClient`, or any `langchain*` module.
       - Keep this as the permanent proof that remaining `async_to_sync` there is outside the AI-agent call chain.

    Keep the suite deterministic and fast (no network, no live Gemini calls).
  </action>
  <verify>
    <automated>cd /mnt/c/Meu\ Projetos/clinica-oncologica-v02-1/backend-hormonia && python3 -m pytest --noconftest tests/validation/test_celery_ai_run_sync_path.py -q</automated>
    <sampling_rate>run before summary creation</sampling_rate>
  </verify>
  <done>A permanent validation test now fails on SDK-03 regressions in Celery AI wiring or non-AI wrapper boundaries.</done>
</task>

</tasks>

<verification>
Run:
1. `cd /mnt/c/Meu\ Projetos/clinica-oncologica-v02-1/backend-hormonia && python3 -m pytest --noconftest tests/validation/test_celery_ai_run_sync_path.py -q`
2. `python3 -c "from pathlib import Path; assert 'use_sync_agents=True' in Path('app/tasks/flows/batch_tasks.py').read_text(encoding='utf-8'); assert 'use_sync_agent_bridge=True' in Path('app/tasks/flow_automation.py').read_text(encoding='utf-8'); print('celery ai sync wiring present')"`
</verification>

<success_criteria>
- Celery tasks that invoke AI now use explicit run_sync-backed paths.
- Remaining async_to_sync wrappers are automatically proven non-AI.
- SDK-03 gap condition from 13-VERIFICATION.md is enforceable by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/13-sdk-migration-cleanup/13-05-SUMMARY.md`
</output>
