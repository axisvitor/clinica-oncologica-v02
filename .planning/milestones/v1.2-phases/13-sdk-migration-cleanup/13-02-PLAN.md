---
phase: 13-sdk-migration-cleanup
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - backend-hormonia/app/ai/client_domain.py
  - backend-hormonia/app/config/settings/integrations.py
  - backend-hormonia/.env.example
  - backend-hormonia/app/main.py
  - backend-hormonia/app/services/flow/sequential_message_handler.py
  - backend-hormonia/tests/unit/test_gemini_client_pii_redaction.py
  - backend-hormonia/tests/validation/detailed_import_analysis.py
  - backend-hormonia/tests/validation/import_analysis_report.json
  - backend-hormonia/tests/unit/ai/test_agents_regression.py
  - backend-hormonia/tests/no_langchain_imports.py
autonomous: true
requirements: [SDK-02]

must_haves:
  truths:
    - "grep -r 'from langchain' across all .py files in app/ returns zero results"
    - "AI_FRAMEWORK and AI_FLOW_FRAMEWORK settings are completely deleted — not in integrations.py, not in .env.example"
    - "GeminiDomainClient methods call pydantic-ai agents unconditionally — no _use_pydantic_agents() toggle"
    - "A permanent test_no_langchain_imports.py fails CI if any langchain import appears in app/"
    - "sequential_message_handler uses only the direct flow path — no legacy LangGraph branch"
  artifacts:
    - path: "backend-hormonia/app/ai/client_domain.py"
      provides: "GeminiDomainClient with unconditional pydantic-ai agent calls"
    - path: "backend-hormonia/tests/no_langchain_imports.py"
      provides: "Permanent CI gate against langchain imports"
    - path: "backend-hormonia/app/config/settings/integrations.py"
      provides: "Settings without AI_FRAMEWORK or AI_FLOW_FRAMEWORK"
  key_links:
    - from: "backend-hormonia/app/ai/client_domain.py"
      to: "backend-hormonia/app/ai/agents/"
      via: "direct agent import calls (no toggle)"
      pattern: "from app\\.ai\\.agents"
    - from: "backend-hormonia/tests/no_langchain_imports.py"
      to: "app/**/*.py"
      via: "AST walk checking for banned langchain imports"
      pattern: "langchain"
---

<objective>
Remove all LangChain references, collapse the feature-flag shim layer, and add a permanent CI guard against LangChain re-introduction.

Purpose: Complete the hard-switch from legacy to pydantic-ai. Remove AI_FRAMEWORK and AI_FLOW_FRAMEWORK settings (per user decision: deleted, not tombstoned). Make the pydantic-ai path unconditional and add a permanent regression test.

Output: Zero langchain imports in app/; no feature-flag toggles; permanent CI test; clean config files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sdk-migration-cleanup/13-RESEARCH.md
@.planning/phases/13-sdk-migration-cleanup/13-01-SUMMARY.md
@backend-hormonia/app/ai/client_domain.py
@backend-hormonia/app/config/settings/integrations.py
@backend-hormonia/app/main.py
@backend-hormonia/app/services/flow/sequential_message_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse GeminiDomainClient shim and remove feature-flag settings</name>
  <files>backend-hormonia/app/ai/client_domain.py, backend-hormonia/app/config/settings/integrations.py, backend-hormonia/.env.example, backend-hormonia/app/services/flow/sequential_message_handler.py</files>
  <action>
    **GeminiDomainClient (client_domain.py) — collapse shim:**
    1. Delete the `_use_pydantic_agents()` method entirely
    2. In each of the 4 public methods (humanize_flow_message, analyze_sentiment, generate_question_variation, generate_empathetic_message), remove the `if self._use_pydantic_agents():` / `else:` branching
    3. Keep ONLY the pydantic-ai agent call path — make it unconditional
    4. Move lazy agent imports to top-level imports (they were lazy to avoid loading when legacy mode was active; now pydantic-ai is always active)
    5. Remove any references to `settings.AI_FRAMEWORK`
    6. Clean up any now-unused imports (GeminiClient.generate_content path-specific imports)

    **IntegrationsSettings (integrations.py):**
    1. Delete `AI_FRAMEWORK: str = Field(...)` field completely
    2. Delete `AI_FLOW_FRAMEWORK: str = Field(...)` field completely
    3. These are being deleted per user decision — not tombstoned, not deprecated

    **.env.example:**
    1. Remove `AI_FRAMEWORK=legacy` line
    2. Remove `AI_FLOW_FRAMEWORK=legacy` line

    **sequential_message_handler.py — collapse flow toggle:**
    1. Remove the `_use_direct_flow()` method (checks AI_FLOW_FRAMEWORK setting)
    2. Remove all `if self._use_direct_flow():` / `else:` branching
    3. Keep ONLY the direct flow path (calls _flow_functions directly)
    4. Remove any lazy `from app.ai.langgraph...` imports in legacy branches (they are now dead)
    5. Keep all other behavior identical

    **Do NOT change:**
    - Agent implementations (base.py, sentiment_agent.py, etc.)
    - GeminiClient itself (already migrated in plan 13-01)
    - Flow function implementations (_flow_functions.py)
    - helpers.py
  </action>
  <verify>
    <automated>cd /mnt/c/Meu\ Projetos/clinica-oncologica-v02-1/backend-hormonia && python3 -c "import ast, sys; tree = ast.parse(open('app/ai/client_domain.py').read()); src = open('app/ai/client_domain.py').read(); sys.exit(1) if '_use_pydantic_agents' in src else print('shim removed')" && python3 -c "import ast, sys; src = open('app/config/settings/integrations.py').read(); sys.exit(1) if 'AI_FRAMEWORK' in src else print('settings removed')" && python3 -c "src = open('.env.example').read(); import sys; sys.exit(1) if 'AI_FRAMEWORK' in src else print('env cleaned')"</automated>
    <manual>Verify GeminiDomainClient methods call agents unconditionally without if/else toggle</manual>
  </verify>
  <done>GeminiDomainClient calls pydantic-ai agents unconditionally; AI_FRAMEWORK and AI_FLOW_FRAMEWORK deleted from settings and .env.example; sequential_message_handler uses only direct flow path</done>
</task>

<task type="auto">
  <name>Task 2: Purge remaining langchain references and add permanent CI test</name>
  <files>backend-hormonia/app/main.py, backend-hormonia/tests/validation/detailed_import_analysis.py, backend-hormonia/tests/validation/import_analysis_report.json, backend-hormonia/tests/unit/ai/test_agents_regression.py, backend-hormonia/tests/no_langchain_imports.py</files>
  <action>
    **app/main.py — LANGGRAPH_AUDIT set cleanup:**
    1. Find the set containing `"langchain_core"`, `"langchain_google_genai"` used by the LANGGRAPH_AUDIT scanner
    2. Remove `"langchain_core"` and `"langchain_google_genai"` from the set (they are no longer importable and the audit for these is obsolete)
    3. If the LANGGRAPH_AUDIT audit set becomes empty after removing all entries, remove the entire LANGGRAPH_AUDIT scanning block as dead code

    **tests/validation/detailed_import_analysis.py:**
    1. Remove `langchain_core` and `langchain_google_genai` from the known-packages dict

    **tests/validation/import_analysis_report.json:**
    1. Remove `langchain_core` and `langchain_google_genai` entries

    **tests/unit/ai/test_agents_regression.py:**
    1. Update any test fixtures that reference AI_FRAMEWORK setting to remove them (the setting no longer exists)
    2. Remove any monkeypatch of `AI_FRAMEWORK` if present — the pydantic-ai path is now unconditional
    3. Ensure all 50 scenarios still target the pydantic-ai agent path directly

    **NEW FILE: tests/no_langchain_imports.py — Permanent CI gate:**
    Create the test per the research specification:
    ```python
    """Permanent gate: zero LangChain imports anywhere in production code.

    This test uses AST parsing to detect langchain imports in any .py file
    under app/. It must NEVER be removed — it is the safety net against
    LangChain re-introduction.
    """
    import ast
    import pathlib
    import pytest

    _BANNED = {"langchain", "langchain_core", "langchain_google_genai", "langchain_google"}

    @pytest.mark.parametrize("py_file", sorted(pathlib.Path("app").rglob("*.py")))
    def test_no_langchain_imports(py_file: pathlib.Path):
        """Fail if any langchain import detected in production code."""
        try:
            tree = ast.parse(py_file.read_bytes())
        except SyntaxError:
            return  # Skip unparseable files (e.g., tombstones with raise ImportError at module level)
        for node in ast.walk(tree):
            if isinstance(node, ast.Import):
                for alias in node.names:
                    for banned in _BANNED:
                        if alias.name and alias.name.startswith(banned):
                            pytest.fail(f"{py_file}: LangChain import detected: {alias.name}")
            elif isinstance(node, ast.ImportFrom):
                mod = node.module or ""
                for banned in _BANNED:
                    if mod.startswith(banned):
                        pytest.fail(f"{py_file}: LangChain import detected: from {mod}")
    ```
    NOTE: Use `startswith` instead of `in` to avoid false positives on string matches in comments/strings that AST won't capture. The tombstoned files in `app/ai/langgraph/` raise `ImportError` at module level — `ast.parse` may succeed but they contain no import statements, so they pass cleanly.

    **Do NOT change:**
    - Tombstoned files in app/ai/langgraph/ (they contain no imports, just raise ImportError)
    - app/integrations/gemini_orchestrator.py (named LangChainOrchestrator but has zero langchain imports — cosmetic naming, not functional)
    - app/integrations/__init__.py (exports get_langchain_orchestrator — functional code, cosmetic name)
  </action>
  <verify>
    <automated>cd /mnt/c/Meu\ Projetos/clinica-oncologica-v02-1/backend-hormonia && python3 -c "import ast, pathlib, sys; banned={'langchain','langchain_core','langchain_google_genai','langchain_google'}; fails=[]; [fails.extend(f'{f}: {n.module}' for n in ast.walk(ast.parse(f.read_bytes())) if isinstance(n, ast.ImportFrom) and n.module and any(n.module.startswith(b) for b in banned)) for f in pathlib.Path('app').rglob('*.py') if not any(p in str(f) for p in ['langgraph/__init__','langgraph/graphs','langgraph/nodes','langgraph/prompts','langgraph/runtime','langgraph/state','langgraph/ai_state','langgraph/_invoke'])]; sys.exit(1) if fails else print(f'zero langchain imports in app/ (checked {len(list(pathlib.Path(\"app\").rglob(\"*.py\")))} files)')"</automated>
    <manual>Verify tests/no_langchain_imports.py exists and would catch any langchain import in app/</manual>
  </verify>
  <done>Zero langchain imports in app/ .py files (excluding tombstones); permanent CI test exists at tests/no_langchain_imports.py; LANGGRAPH_AUDIT set cleaned in main.py; validation test data cleaned; regression suite no longer references AI_FRAMEWORK</done>
</task>

</tasks>

<verification>
After both tasks:
1. `python3 -c "import ast, pathlib; [print(f) for f in pathlib.Path('app').rglob('*.py') if any('langchain' in (n.module or '') for n in ast.walk(ast.parse(f.read_bytes())) if isinstance(n, ast.ImportFrom) and n.module)]"` returns nothing (excluding tombstones)
2. `grep -r "AI_FRAMEWORK\|AI_FLOW_FRAMEWORK" backend-hormonia/app/config/ backend-hormonia/.env.example` returns zero results
3. `grep "_use_pydantic_agents\|_use_direct_flow" backend-hormonia/app/` returns zero results
4. `tests/no_langchain_imports.py` exists and contains parametrized test
5. `grep "langchain_core\|langchain_google_genai" backend-hormonia/tests/validation/detailed_import_analysis.py backend-hormonia/tests/validation/import_analysis_report.json` returns zero results
</verification>

<success_criteria>
- Zero langchain imports in any production .py file under app/ (tombstoned modules excepted)
- AI_FRAMEWORK and AI_FLOW_FRAMEWORK settings completely deleted from codebase and config
- GeminiDomainClient delegates to pydantic-ai agents unconditionally — zero branching
- sequential_message_handler uses only direct flow path — zero legacy branch
- Permanent CI test guards against future langchain re-introduction
- Validation test data and LANGGRAPH_AUDIT references cleaned
</success_criteria>

<output>
After completion, create `.planning/phases/13-sdk-migration-cleanup/13-02-SUMMARY.md`
</output>
