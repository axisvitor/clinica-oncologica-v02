---
phase: 13-sdk-migration-cleanup
plan: 04
type: execute
wave: 3
depends_on:
  - 13-03
files_modified:
  - backend-hormonia/app/ai/agents/humanize_agent.py
  - backend-hormonia/app/ai/agents/variation_agent.py
  - backend-hormonia/app/ai/agents/sentiment_agent.py
  - backend-hormonia/app/ai/agents/empathy_agent.py
  - backend-hormonia/app/ai/client_domain.py
  - backend-hormonia/tests/unit/ai/test_agent_sync_methods.py
autonomous: true
requirements:
  - SDK-03
gap_closure: true

must_haves:
  truths:
    - "Celery-safe sync APIs exist for all four agent operations (humanize, variation, sentiment, empathy) and they execute via agent.run_sync"
    - "GeminiDomainClient exposes explicit sync methods for the same four operations without introducing new framework toggles"
    - "Async domain methods remain unchanged for FastAPI/native-async callers"
  artifacts:
    - path: "backend-hormonia/app/ai/agents/humanize_agent.py"
      provides: "HumanizeAgent sync entrypoint"
      contains: "humanize_sync"
    - path: "backend-hormonia/app/ai/client_domain.py"
      provides: "GeminiDomainClient sync methods"
      contains: "humanize_flow_message_sync"
    - path: "backend-hormonia/tests/unit/ai/test_agent_sync_methods.py"
      provides: "Unit proof that sync APIs call run_sync path"
  key_links:
    - from: "backend-hormonia/app/ai/client_domain.py"
      to: "backend-hormonia/app/ai/agents/*_agent.py"
      via: "domain sync methods call agent sync methods"
      pattern: "_sync"
    - from: "backend-hormonia/app/ai/agents/*_agent.py"
      to: "pydantic_ai.Agent.run_sync"
      via: "PIISafeAgent._safe_run_sync"
      pattern: "_safe_run_sync"
---

<objective>
Prepare a direct run_sync execution path for all AI agent operations used in Celery-triggered workflows.

Purpose: The verification gap is about Celery AI-call paths still flowing through async wrappers. Before rewiring task call chains, this plan creates explicit sync APIs on agents and domain client methods so Celery paths can call agent.run_sync directly without adding new framework toggles.

Output: Sync methods on the four agent classes, sync methods in GeminiDomainClient, and targeted unit tests validating sync-path behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sdk-migration-cleanup/13-CONTEXT.md
@.planning/phases/13-sdk-migration-cleanup/13-RESEARCH.md
@.planning/phases/13-sdk-migration-cleanup/13-VERIFICATION.md
@.planning/phases/13-sdk-migration-cleanup/13-03-SUMMARY.md
@backend-hormonia/app/ai/client_domain.py
@backend-hormonia/app/ai/agents/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public sync methods to all four pydantic-ai agent wrappers</name>
  <files>backend-hormonia/app/ai/agents/humanize_agent.py, backend-hormonia/app/ai/agents/variation_agent.py, backend-hormonia/app/ai/agents/sentiment_agent.py, backend-hormonia/app/ai/agents/empathy_agent.py</files>
  <action>
    Add explicit sync methods mirroring current async methods:
    - `HumanizeAgent.humanize_sync(...)`
    - `VariationAgent.vary_sync(...)`
    - `SentimentAgent.analyze_sync(...)`
    - `EmpathyAgent.follow_up_sync(...)`

    Requirements:
    1. Reuse the exact same prompt-building helpers and post-processing behavior currently used by async methods.
    2. Call `self._safe_run_sync(..., operation=...)` internally.
    3. Keep async methods intact and backward-compatible.
    4. Keep output contracts unchanged (`SentimentResult` for sentiment sync method, `str` for others).

    Do not add framework mode flags in this step.
  </action>
  <verify>
    <automated>cd /mnt/c/Meu\ Projetos/clinica-oncologica-v02-1/backend-hormonia && python3 -c "from app.ai.agents.humanize_agent import HumanizeAgent; from app.ai.agents.variation_agent import VariationAgent; from app.ai.agents.sentiment_agent import SentimentAgent; from app.ai.agents.empathy_agent import EmpathyAgent; assert hasattr(HumanizeAgent,'humanize_sync'); assert hasattr(VariationAgent,'vary_sync'); assert hasattr(SentimentAgent,'analyze_sync'); assert hasattr(EmpathyAgent,'follow_up_sync'); print('sync methods present')"</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All four agent wrappers expose sync methods that delegate to `_safe_run_sync` while async methods remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Expose GeminiDomainClient sync methods and add sync-path unit tests</name>
  <files>backend-hormonia/app/ai/client_domain.py, backend-hormonia/tests/unit/ai/test_agent_sync_methods.py</files>
  <action>
    Extend GeminiDomainClient with explicit sync methods (no new toggles):
    - `humanize_flow_message_sync(...)`
    - `generate_varied_question_sync(...)`
    - `analyze_response_sentiment_sync(...)`
    - `create_empathetic_follow_up_sync(...)`

    Implementation rules:
    1. Each method builds deps with `_build_ai_deps()` and calls corresponding agent sync method.
    2. Keep existing async methods unchanged and still available.
    3. Preserve return shapes (sentiment sync returns `dict` via `model_dump()`).
    4. Do not introduce feature flags/settings/env toggles.

    Add `tests/unit/ai/test_agent_sync_methods.py` covering:
    - domain sync methods call agent sync methods;
    - sentiment sync path returns dict with expected keys;
    - async methods still callable and unaffected.
  </action>
  <verify>
    <automated>cd /mnt/c/Meu\ Projetos/clinica-oncologica-v02-1/backend-hormonia && python3 -m pytest --noconftest tests/unit/ai/test_agent_sync_methods.py -q</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>GeminiDomainClient has sync APIs for all four operations, unit tests pass, and no new framework toggles were added.</done>
</task>

</tasks>

<verification>
Run:
1. `cd /mnt/c/Meu\ Projetos/clinica-oncologica-v02-1/backend-hormonia && python3 -m pytest --noconftest tests/unit/ai/test_agent_sync_methods.py -q`
2. `python3 -c "from app.ai.client_domain import GeminiDomainClient; c=GeminiDomainClient(); assert hasattr(c,'humanize_flow_message_sync'); assert hasattr(c,'analyze_response_sentiment_sync'); print('domain sync methods OK')"`
</verification>

<success_criteria>
- All four agent classes expose run_sync-backed sync methods.
- GeminiDomainClient provides corresponding sync APIs without new shim/toggle fields.
- Unit tests prove sync APIs are functional and async APIs remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/13-sdk-migration-cleanup/13-04-SUMMARY.md`
</output>
