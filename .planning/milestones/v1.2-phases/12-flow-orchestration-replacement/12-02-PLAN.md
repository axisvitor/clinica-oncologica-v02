---
phase: 12-flow-orchestration-replacement
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - backend-hormonia/requirements.txt
  - backend-hormonia/app/ai/agents/helpers.py
  - backend-hormonia/app/services/flow/_flow_functions.py
  - backend-hormonia/app/services/flow/sequential_message_handler.py
autonomous: true
requirements:
  - FLOW-03

must_haves:
  truths:
    - "requirements.txt does not contain langgraph, langchain-core, langchain-google-genai, or google-ai-generativelanguage"
    - "pip check produces no dependency errors after package removal"
    - "The application imports (app.services.flow.sequential_message_handler, app.ai.agents.helpers) succeed without LangGraph installed"
  artifacts:
    - path: "backend-hormonia/requirements.txt"
      provides: "Clean dependency list without LangGraph/LangChain packages"
  key_links:
    - from: "backend-hormonia/app/ai/agents/helpers.py"
      to: "prompt and node_ai functions"
      via: "inlined function bodies (no longer re-exporting from langgraph)"
      pattern: "def build_humanization_prompt"
    - from: "backend-hormonia/app/services/flow/_flow_functions.py"
      to: "node functions"
      via: "inlined node logic (no import from app.ai.langgraph.nodes)"
      pattern: "async def load_flow_context"
---

<objective>
Remove all LangGraph and LangChain packages from requirements.txt and ensure zero import-time failures by inlining the node functions and prompt builders that previously lived in `app/ai/langgraph/` into their new permanent homes (`_flow_functions.py` and `helpers.py`).

Purpose: Eliminate the LangGraph/LangChain dependency footprint so the packages can be pip-uninstalled without breaking any runtime import.

Output: Clean `requirements.txt`, `helpers.py` with inlined prompt/node-ai function bodies, `_flow_functions.py` with inlined node logic, no remaining `from app.ai.langgraph.*` imports outside the langgraph/ directory itself.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-flow-orchestration-replacement/12-RESEARCH.md
@.planning/phases/12-flow-orchestration-replacement/12-01-SUMMARY.md
@backend-hormonia/requirements.txt
@backend-hormonia/app/ai/langgraph/prompts.py
@backend-hormonia/app/ai/langgraph/nodes_ai.py
@backend-hormonia/app/ai/langgraph/nodes.py
@backend-hormonia/app/ai/langgraph/state.py
@backend-hormonia/app/ai/langgraph/runtime.py
@backend-hormonia/app/ai/agents/helpers.py
@backend-hormonia/app/services/flow/_flow_functions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline prompt builders and node-ai helpers into helpers.py, inline node functions into _flow_functions.py, remove LangGraph imports from sequential_message_handler.py</name>
  <files>
    backend-hormonia/app/ai/agents/helpers.py
    backend-hormonia/app/services/flow/_flow_functions.py
    backend-hormonia/app/services/flow/sequential_message_handler.py
  </files>
  <action>
    **1. Inline prompt builder function bodies into `helpers.py`:**

    Currently `helpers.py` re-exports from `app.ai.langgraph.prompts` and `app.ai.langgraph.nodes_ai`. After LangGraph packages are removed, those files will be tombstoned and their imports will fail. The function bodies must be moved into `helpers.py` (or a new internal module that helpers.py owns).

    Read the full contents of `app/ai/langgraph/prompts.py` and `app/ai/langgraph/nodes_ai.py`. Copy ALL function bodies from both files into `helpers.py`, replacing the re-export `from app.ai.langgraph.prompts import ...` and `from app.ai.langgraph.nodes_ai import ...` lines. Preserve exact function signatures and docstrings. Also copy any module-level constants, regex patterns, and imports those functions depend on (e.g., `_WHITESPACE_RE`, `AIResponseValidation`, `redact_patient_context`, `PROMPT_PSEUDONYM`, `sanitize_prompt_text_for_external_ai`, `clip_text`).

    The result: `helpers.py` is fully self-contained — it imports only from standard library + `app.ai.pii_redaction`, `app.utils.text`, `app.schemas.ai_schemas`. No imports from `app.ai.langgraph.*`.

    Keep the docstring comment at the top of helpers.py updated to reflect it now OWNS (not re-exports) the prompt builders and node helpers.

    **2. Inline the four node functions into `_flow_functions.py`:**

    Currently `_flow_functions.py` (from Plan 12-01) imports `load_flow_context`, `dispatch_send_mode`, `load_response_context`, `dispatch_response_continuation` from `app.ai.langgraph.nodes`. These functions also depend on helper functions from `nodes.py`: `_require_handler`, `_looks_like_flow_state`, `_load_flow_state`, `_parse_send_mode`, `_coerce_awaiting_response`, `_build_expected_response_context`, `_collect_response_context_mismatches`, `_prepare_dispatch_execution_context`, and the `_CANONICAL_SEND_MODES` constant. They also import `validate_flow_message_state` from `state.py` and `require_configurable_thread_id` from `runtime.py`.

    Copy ALL of these into `_flow_functions.py`:
    - From `nodes.py`: ALL function bodies and the `_CANONICAL_SEND_MODES` constant
    - From `state.py`: the `validate_flow_message_state` function and the `FlowMessageState` TypedDict
    - From `runtime.py`: the `require_configurable_thread_id` function ONLY (this is the only runtime.py function the nodes use; it just reads `config["configurable"]["thread_id"]` — no LangGraph types needed)

    Replace the `RunnableConfig` type annotation with `Optional[dict]` throughout (since `langchain-core` will be gone). The runtime behavior is identical — the functions only use `.get()` on these dicts.

    Remove ALL `from app.ai.langgraph.*` imports from `_flow_functions.py`. The file should import only from standard library, `app.models.patient`, `app.utils.timezone`.

    **3. Remove top-level LangGraph imports from `sequential_message_handler.py`:**

    Remove these two lines from the top of the file:
    ```python
    from app.ai.langgraph.graphs import get_flow_message_graph, get_flow_response_graph
    from app.ai.langgraph.runtime import build_graph_config
    ```

    Move them to lazy imports INSIDE the legacy branches of `send_day_messages()` and `handle_response_and_continue()`. Wrap the legacy path's graph usage with lazy imports:
    ```python
    # Inside the else (legacy) branch:
    from app.ai.langgraph.graphs import get_flow_message_graph
    from app.ai.langgraph.runtime import build_graph_config
    ```
    This way, when `AI_FLOW_FRAMEWORK=direct`, the LangGraph imports are never executed. When legacy mode is active, the imports still work (the langgraph/ directory exists even though packages are uninstalled — the tombstone files are created in Plan 12-03, and Plan 12-02 runs BEFORE 12-03).

    **CRITICAL:** Since Plan 12-02 runs before Plan 12-03 (tombstoning), the legacy lazy imports from `app.ai.langgraph.graphs` will still work during Plan 12-02 execution because the graph files still have real code. After Plan 12-03 tombstones them, the legacy path will raise ImportError — which is the intended behavior (legacy path is no longer usable after tombstoning). The `AI_FLOW_FRAMEWORK` flag MUST be set to `"direct"` in production after Plan 12-03 completes.

    **NOTE:** Do NOT worry about `tests/unit/services/flow/test_sequential_message_handler.py` importing from `app.ai.langgraph.nodes` — that test file still works because the `.py` files still exist (they get tombstoned in Plan 12-03). The test imports will be removed in Plan 12-03.
  </action>
  <verify>
    <automated>cd backend-hormonia && python -c "from app.ai.agents.helpers import build_humanization_prompt, build_sentiment_prompt, _parse_sentiment_analysis, _coerce_recent_interactions; print('helpers inlined OK')" && python -c "from app.services.flow._flow_functions import run_flow_message, run_flow_response; print('flow_functions inlined OK')" 2>&1</automated>
    <manual>Verify that helpers.py has zero imports from app.ai.langgraph.* and _flow_functions.py has zero imports from app.ai.langgraph.*</manual>
  </verify>
  <done>
    - helpers.py contains all prompt builder and node-ai helper function bodies without any app.ai.langgraph.* imports
    - _flow_functions.py contains all four node functions + support functions without any app.ai.langgraph.* imports
    - sequential_message_handler.py has LangGraph imports moved to lazy inside legacy branches only
    - All function signatures and behavior are identical to the originals
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove LangGraph/LangChain packages from requirements.txt and verify clean dependency graph</name>
  <files>
    backend-hormonia/requirements.txt
  </files>
  <action>
    **1. Remove these 4 lines from `requirements.txt`** (approximately lines 42-45, but verify exact content before deleting):
    ```
    langchain-core>=1.2.7,<2.0.0
    langchain-google-genai>=2.1.12,<4.0.0
    google-ai-generativelanguage>=0.7.0,<1.0.0
    langgraph>=1.0.7,<2.0.0
    ```
    Also remove any comment lines immediately above them that reference these packages (e.g., `# LangChain/LangGraph` section headers). Keep surrounding packages intact.

    **2. Verify with `pip check`** — confirm zero dependency errors. Some transitive dependencies may have been pulled in by these packages; they will be auto-removed if nothing else needs them, but they do not need manual removal from requirements.txt (only explicitly pinned packages are listed there).

    **3. Verify no import failures** — run:
    ```bash
    python -c "from app.ai.agents.helpers import build_humanization_prompt; print('OK')"
    python -c "from app.services.flow._flow_functions import run_flow_message; print('OK')"
    python -c "import app.services.flow.sequential_message_handler; print('OK')"
    ```
    All three should succeed because the inlining from Task 1 removed the dependency on the langgraph/ package imports.

    **4. Also verify the LangChain tracing settings still compile** — the `AI_LANGCHAIN_ENABLE_TRACING_V2` and `AI_LANGCHAIN_API_KEY` fields in `IntegrationsSettings` are just Pydantic Field declarations (string/bool types) — they don't import anything from LangChain. Leave them as-is (they are env var declarations, not code dependencies). They can be cleaned up in a future housekeeping pass.
  </action>
  <verify>
    <automated>cd backend-hormonia && python -c "
import subprocess, sys
# Check requirements.txt has no langgraph/langchain lines
with open('requirements.txt') as f:
    content = f.read()
for pkg in ['langgraph', 'langchain-core', 'langchain-google-genai', 'google-ai-generativelanguage']:
    assert pkg not in content, f'{pkg} still in requirements.txt'
print('requirements.txt clean')
# Verify imports work
from app.ai.agents.helpers import build_humanization_prompt
from app.services.flow._flow_functions import run_flow_message
print('All imports OK')
" 2>&1</automated>
  </verify>
  <done>
    - requirements.txt has zero lines containing langgraph, langchain-core, langchain-google-genai, or google-ai-generativelanguage
    - All application imports succeed without LangGraph packages
    - No dependency errors from pip check
  </done>
</task>

</tasks>

<verification>
1. `grep -i "langgraph\|langchain-core\|langchain-google-genai\|google-ai-generativelanguage" backend-hormonia/requirements.txt` returns zero results
2. `python -c "from app.ai.agents.helpers import build_humanization_prompt, _parse_sentiment_analysis"` succeeds
3. `python -c "from app.services.flow._flow_functions import run_flow_message, run_flow_response"` succeeds
4. `grep "from app.ai.langgraph" backend-hormonia/app/ai/agents/helpers.py` returns zero results
5. `grep "from app.ai.langgraph" backend-hormonia/app/services/flow/_flow_functions.py` returns zero results
</verification>

<success_criteria>
- Zero LangGraph/LangChain packages in requirements.txt
- helpers.py is self-contained with inlined function bodies
- _flow_functions.py is self-contained with inlined node logic
- All application imports succeed without LangGraph/LangChain installed
- sequential_message_handler.py only uses lazy LangGraph imports in the legacy branch
</success_criteria>

<output>
After completion, create `.planning/phases/12-flow-orchestration-replacement/12-02-SUMMARY.md`
</output>
