---
phase: 10-preparation-scope
plan: 04
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - backend-hormonia/app/agents/communication/__init__.py
  - backend-hormonia/app/agents/communication/utils.py
  - backend-hormonia/app/agents/communication/message_composer/__init__.py
  - backend-hormonia/app/agents/communication/message_composer/composer.py
  - backend-hormonia/app/agents/communication/message_composer/context_builder.py
  - backend-hormonia/app/agents/communication/message_composer/templates.py
  - backend-hormonia/app/agents/communication/message_composer/tone_adapter.py
autonomous: true
requirements:
  - PREP-03
gap_closure: true

must_haves:
  truths:
    - "Communication modules and message composer support files have explicit one-line scope annotation headers"
    - "message_composer/composer.py explicitly documents GeminiClient delegation and non-migration-target scope"
    - "Automated full audit reports 24 total app/agents Python files and 0 missing annotations"
  artifacts:
    - path: "backend-hormonia/app/agents/communication/message_composer/composer.py"
      provides: "Composer scope annotation clarifying Gemini delegation"
      contains: "GeminiClient.generate_content()"
    - path: "backend-hormonia/app/agents/communication/utils.py"
      provides: "Communication utility scope annotation"
      contains: "DDD service agent"
    - path: "backend-hormonia/app/agents/communication/message_composer/context_builder.py"
      provides: "Composer context builder scope annotation"
      contains: "DDD service agent"
  key_links:
    - from: "backend-hormonia/app/agents/communication/message_composer/composer.py"
      to: "backend-hormonia/app/ai/client.py"
      via: "header comment clarifies delegated Gemini execution path"
      pattern: "GeminiClient.generate_content\(\)"
    - from: "backend-hormonia/app/agents/**/*.py"
      to: "Phase 11 migration boundary"
      via: "first-line scope annotation comments"
      pattern: "DDD service agent"
---

<objective>
Close the remaining Phase 10 verification gap by annotating communication modules and validating full `app/agents` scope-comment coverage.

Purpose: Finalize PREP-03 migration-boundary clarity after Plan 10-03, ensuring no `app/agents` module is ambiguous before Phase 11 implementation.

Output: Scope annotation headers added to 7 communication/message-composer files and full-project annotation audit passing.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-preparation-scope/10-CONTEXT.md
@.planning/phases/10-preparation-scope/10-RESEARCH.md
@.planning/phases/10-preparation-scope/10-VERIFICATION.md
@.planning/phases/10-preparation-scope/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scope annotations to communication modules and message composer support files</name>
  <files>
    backend-hormonia/app/agents/communication/__init__.py
    backend-hormonia/app/agents/communication/utils.py
    backend-hormonia/app/agents/communication/message_composer/__init__.py
    backend-hormonia/app/agents/communication/message_composer/composer.py
    backend-hormonia/app/agents/communication/message_composer/context_builder.py
    backend-hormonia/app/agents/communication/message_composer/templates.py
    backend-hormonia/app/agents/communication/message_composer/tone_adapter.py
  </files>
  <action>
  Add one-line header comments to all listed communication files.

  Use these exact annotations:
  - For `communication/__init__.py`, `communication/utils.py`, `message_composer/__init__.py`, `context_builder.py`, `templates.py`, and `tone_adapter.py`:
    `# DDD service agent - no LLM calls, not a pydantic-ai migration target.`
  - For `message_composer/composer.py`:
    `# DDD service agent - delegates to GeminiClient.generate_content() directly, not a pydantic-ai migration target.`

  Placement rules:
  - If a file has `from __future__ import annotations`, place the comment immediately after that line.
  - Otherwise place the comment at line 1 before module docstrings/imports.
  - Do not change executable logic, imports, or exports.

  Why: This is the remaining half of the 19-file annotation gap from verification, split from Plan 10-03 to keep file count under scope limits.
  </action>
  <verify>
    <automated>cd backend-hormonia &amp;&amp; .venv/bin/python -c "import pathlib,sys; targets=[pathlib.Path(p) for p in ['app/agents/communication/__init__.py','app/agents/communication/utils.py','app/agents/communication/message_composer/__init__.py','app/agents/communication/message_composer/composer.py','app/agents/communication/message_composer/context_builder.py','app/agents/communication/message_composer/templates.py','app/agents/communication/message_composer/tone_adapter.py']]; missing=[str(p) for p in targets if 'DDD service agent' not in p.read_text(encoding='utf-8')]; print('missing='+str(len(missing))); [print(m) for m in missing]; sys.exit(1 if missing else 0)"</automated>
    <sampling_rate>run after this task commits, before full-phase verification</sampling_rate>
  </verify>
  <done>All 7 listed communication files contain the required scope annotation headers and no non-comment behavior changed.</done>
</task>

<task type="auto">
  <name>Task 2: Run full app/agents annotation audit to close PREP-03 gap</name>
  <files>backend-hormonia/app/agents/**/*.py</files>
  <action>
  Execute a full annotation audit across all Python modules under `backend-hormonia/app/agents/` and confirm zero missing scope comments.

  If audit reports missing files, add the same one-line scope annotation pattern in those files (matching no-LLM vs Gemini delegation wording from Phase 10 decisions), then re-run until zero missing.
  </action>
  <verify>
    <automated>cd backend-hormonia &amp;&amp; .venv/bin/python -c "import pathlib,sys; files=sorted(pathlib.Path('app/agents').rglob('*.py')); missing=[str(p) for p in files if 'DDD service agent' not in p.read_text(encoding='utf-8')]; print(f'total={len(files)} missing={len(missing)}'); [print(m) for m in missing]; sys.exit(1 if missing else 0)"</automated>
    <manual>Optional spot-check: ensure `app/agents/communication/message_composer/composer.py` uses the Gemini delegation annotation rather than the no-LLM variant.</manual>
    <sampling_rate>run before phase verification and summary creation</sampling_rate>
  </verify>
  <done>Audit output shows `total=24 missing=0`, satisfying the failed truth in 10-VERIFICATION.md and fully closing Phase 10 PREP-03 scope-comment gap.</done>
</task>

</tasks>

<verification>
Run `cd backend-hormonia && .venv/bin/python -c "import pathlib,sys; files=sorted(pathlib.Path('app/agents').rglob('*.py')); missing=[str(p) for p in files if 'DDD service agent' not in p.read_text(encoding='utf-8')]; print(f'total={len(files)} missing={len(missing)}'); [print(m) for m in missing]; sys.exit(1 if missing else 0)"` and confirm output is exactly `total=24 missing=0`.
</verification>

<success_criteria>
- PREP-03 gap closure part 2: all 7 communication/message-composer files in this plan are annotated with correct scope wording.
- Phase-level failed truth from 10-VERIFICATION is resolved: all `backend-hormonia/app/agents/*.py` files have scope annotations.
</success_criteria>

<output>
After completion, create `.planning/phases/10-preparation-scope/10-04-SUMMARY.md`
</output>
