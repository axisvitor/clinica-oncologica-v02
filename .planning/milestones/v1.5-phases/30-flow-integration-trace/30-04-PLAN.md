---
phase: 30-flow-integration-trace
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/30-flow-integration-trace/30-VERIFICATION.md
  - backend-hormonia/docs/traces/30-01-onboarding-path-trace.md
  - backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md
autonomous: true
gap_closure: true
requirements:
  - TRACE-01
  - TRACE-02
  - TRACE-03
  - TRACE-04

must_haves:
  truths:
    - "Phase 30 contract language explicitly states onboarding has two independent traced paths (saga onboarding path and standalone FlowDispatcher path), not one runtime-dispatched chain"
    - "TRACE-03 wording explicitly matches implemented behavior: cancel revokes queued work and clears flow state, while saga compensation remains a separate lifecycle triggered on saga failure"
    - "TRACE-02 and TRACE-04 remain unchanged and still marked as satisfied after contract reconciliation"
    - "Phase verification report is re-run and updated to show resolved contract gaps with evidence links"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 30 goal/success criteria wording aligned with traced architecture"
      contains: "two independent onboarding paths"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRACE-01 and TRACE-03 requirement text aligned to implementation"
      contains: "cancel flow state cleanup and queued work revocation"
    - path: ".planning/phases/30-flow-integration-trace/30-VERIFICATION.md"
      provides: "Re-verification status showing gap closure outcome"
      contains: "status: passed"
    - path: "backend-hormonia/docs/traces/30-01-onboarding-path-trace.md"
      provides: "Explicit contract note for dual-path onboarding architecture"
      contains: "Path A and Path B are independent"
    - path: "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md"
      provides: "Explicit contract note for cancel vs compensation boundary"
      contains: "cancel does not trigger saga compensation"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "backend-hormonia/docs/traces/30-01-onboarding-path-trace.md"
      via: "TRACE-01 wording and evidence alignment"
      pattern: "independent.*Path A.*Path B"
    - from: ".planning/REQUIREMENTS.md"
      to: "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md"
      via: "TRACE-03 wording and evidence alignment"
      pattern: "cancel.*does not trigger.*compensation"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/30-flow-integration-trace/30-VERIFICATION.md"
      via: "phase success criteria language reconciled with verification truths"
      pattern: "Success Criteria"
---

<objective>
Close Phase 30 contract-level verification gaps by reconciling requirement and roadmap wording with traced code reality, then re-running verification to prove all TRACE truths are aligned.

Purpose: Preserve implementation truth and avoid scope creep into new runtime wiring that conflicts with Phase 30 decisions (two independent onboarding paths; cancel and compensation are independent lifecycles).

Output: Updated roadmap/requirements contracts, updated trace artifact clarifications, and refreshed `30-VERIFICATION.md` showing gaps closed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-flow-integration-trace/30-VERIFICATION.md
@.planning/phases/30-flow-integration-trace/30-01-SUMMARY.md
@.planning/phases/30-flow-integration-trace/30-02-SUMMARY.md
@.planning/phases/30-flow-integration-trace/30-03-SUMMARY.md

Required fidelity constraints from prior decisions:
- Keep FlowDispatcher and saga onboarding documented as separate execution paths.
- Keep cancel flow and saga compensation documented as independent lifecycles.
- Do not add runtime coupling changes in this gap-closure plan; reconcile contracts and evidence.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile TRACE-01 and TRACE-03 contract wording in roadmap and requirements</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
Update Phase 30 wording so contracts match the traced implementation:

1) In `.planning/ROADMAP.md` Phase 30 section:
- Revise goal/success-criteria phrasing that currently implies a single runtime chain from FlowDispatcher to SagaOrchestrator.
- Replace with explicit dual-path wording: Path A (`patients/crud.py -> OnboardingCoordinator -> SagaOrchestrator -> FlowCore`) and Path B (`FlowDispatcher -> PatientFlowService -> FlowCore`) are both traced and compared.
- Revise cancel criterion wording to state cancel revokes queued work and clears flow state, while compensation remains saga-failure scoped.

2) In `.planning/REQUIREMENTS.md`:
- Update TRACE-01 description to reflect independent traced paths rather than a single wired chain.
- Update TRACE-03 description to remove implicit cancel-triggered compensation requirement and codify the documented boundary.
- Keep TRACE IDs unchanged (`TRACE-01..TRACE-04`) and keep TRACE-02/TRACE-04 semantics intact.

3) Preserve requirement traceability table structure and phase mapping; only reconcile contract text to implementation reality.
  </action>
  <verify>
    <automated>grep -q "independent" ".planning/REQUIREMENTS.md" && grep -q "Path A" ".planning/ROADMAP.md" && grep -q "cancel" ".planning/ROADMAP.md"</automated>
  </verify>
  <done>
    ROADMAP and REQUIREMENTS no longer imply a non-existent FlowDispatcher->Saga runtime chain or cancel-triggered compensation. TRACE-01..TRACE-04 IDs and mappings remain intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit contract-clarification notes to Phase 30 trace artifacts</name>
  <files>
    backend-hormonia/docs/traces/30-01-onboarding-path-trace.md
    backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md
  </files>
  <action>
Update trace documents so they explicitly anchor the reconciled contracts:

1) In `30-01-onboarding-path-trace.md` add/update a contract note section that states:
- Path A and Path B are independent execution paths.
- FlowDispatcher does not invoke SagaOrchestrator in current implementation.
- TRACE-01 is satisfied by full traceability and comparison of both paths, not by direct path coupling.

2) In `30-02-pause-resume-cancel-trace.md` add/update a contract note section that states:
- Cancel behavior is flow/message cleanup and task revocation.
- Saga compensation is triggered during saga failure, not by cancel.
- TRACE-03 is satisfied by verifiable cancel semantics plus explicit compensation boundary.

3) Keep existing technical findings unchanged (dual pause key divergence, guard coverage notes). Only strengthen explicit contract alignment language and evidence links.
  </action>
  <verify>
    <automated>grep -q "independent" "backend-hormonia/docs/traces/30-01-onboarding-path-trace.md" && grep -q "cancel does not trigger saga compensation" "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md"</automated>
  </verify>
  <done>
    Both trace artifacts contain explicit contract reconciliation language that matches Phase 30 decisions and updated requirement wording.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-run Phase 30 verification and publish resolved gap status</name>
  <files>
    .planning/phases/30-flow-integration-trace/30-VERIFICATION.md
  </files>
  <action>
Re-verify Phase 30 after contract and trace updates:

1) Re-run the phase verification workflow for phase 30 (using project gsd tooling) and refresh `30-VERIFICATION.md`.
2) Confirm formerly failed truths now pass because contract language and evidence are aligned.
3) Ensure the report includes:
- `status: passed`
- Updated truths table with no unresolved contract mismatch for TRACE-01/TRACE-03
- Requirements coverage table showing TRACE-01..TRACE-04 satisfied
- Updated gap summary indicating closure rationale (contract reconciliation, no runtime rewiring required)

If the verifier command path under `$HOME/.claude/...` is unavailable, use repository-local `.claude/get-shit-done/bin/gsd-tools.cjs` as the execution path.
  </action>
  <verify>
    <automated>grep -q "status: passed" ".planning/phases/30-flow-integration-trace/30-VERIFICATION.md" && grep -q "TRACE-01" ".planning/phases/30-flow-integration-trace/30-VERIFICATION.md" && grep -q "TRACE-04" ".planning/phases/30-flow-integration-trace/30-VERIFICATION.md"</automated>
  </verify>
  <done>
    `30-VERIFICATION.md` is refreshed with passed status and all TRACE requirements marked satisfied with evidence.
  </done>
</task>

</tasks>

<verification>
- [ ] ROADMAP and REQUIREMENTS wording matches actual traced architecture and lifecycle boundaries
- [ ] Trace artifacts explicitly document the reconciled contracts without changing technical findings
- [ ] Re-verification report for Phase 30 shows no open contract mismatch gaps
- [ ] TRACE-01, TRACE-02, TRACE-03, TRACE-04 all appear as satisfied in verification output
</verification>

<success_criteria>
Phase 30 contract and evidence are aligned end-to-end: onboarding is documented as two independent traced paths, cancel semantics are documented with explicit compensation boundary, and verification passes with all TRACE requirements satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/30-flow-integration-trace/30-04-SUMMARY.md`
</output>
