---
phase: 30-flow-integration-trace
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md
autonomous: true
requirements:
  - TRACE-02
  - TRACE-03

must_haves:
  truths:
    - "All three pause paths (API/FlowManagement, FlowCore, Agent/TransitionHandler) are traced with their state key differences documented side-by-side"
    - "The dual pause key divergence (state_data['paused'] vs state_data['flow_paused']) is documented as a correctness finding with impact analysis"
    - "Auto-resume mechanism via Celery Beat resume_paused_flows is traced from task trigger through FlowManagementService.resume_patient_flow()"
    - "Cancel path in pause_resume.py is traced: message revocation, Celery task revocation, state cleanup, and the explicit finding that cancel does NOT trigger saga compensation"
    - "Pause guard coverage is documented: which callers check pause before dispatch, and the gap that SequentialMessageHandler has no independent pause guard"
  artifacts:
    - path: "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md"
      provides: "Complete pause/resume/cancel trace document with state transition tables and findings"
      min_lines: 120
  key_links:
    - from: "app/services/flow/management/pause_resume.py"
      to: "PatientFlowState.state_data"
      via: "pause_patient_flow() sets state_data['paused'] = True"
      pattern: "state_data.*paused.*True"
    - from: "app/agents/patient/flow_coordinator/transition_handler.py"
      to: "PatientFlowState.state_data"
      via: "pause_flow() sets state_data['flow_paused'] = True"
      pattern: "flow_paused.*True"
    - from: "app/tasks/flow_automation.py"
      to: "app/services/flow/management/pause_resume.py"
      via: "resume_paused_flows -> resume_patient_flow()"
      pattern: "resume_patient_flow"
    - from: "app/services/flow/management/pause_resume.py"
      to: "celery.result.AsyncResult"
      via: "cancel_patient_flow() revokes Celery tasks"
      pattern: "AsyncResult.*revoke"
---

<objective>
Trace all pause/resume semantics and the cancel flow path through the split flow management code, documenting state transitions, key divergences, and guard coverage gaps.

Purpose: Verify that pause/resume produces correct state transitions after the v1.3 split, that auto-resume fires correctly, and that cancel properly revokes queued work. Document the critical dual pause key divergence and the absence of saga compensation in the cancel path.

Output: A trace document at `backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md` covering three pause paths, auto-resume, cancel, and all findings.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-flow-integration-trace/30-RESEARCH.md

Key files to trace:

**Pause/Resume (TRACE-02):**
- `app/services/flow/management/pause_resume.py` -- API pause/resume/cancel (FlowManagementPauseResumeMixin), ~290 LOC
- `app/services/flow/core/transitions.py` -- FlowCore pause/resume (FlowCoreTransitionsMixin), ~262 LOC
- `app/agents/patient/flow_coordinator/transition_handler.py` -- Agent pause/resume, ~194 LOC
- `app/tasks/flow_automation.py` -- Celery Beat auto-resume task (resume_paused_flows), ~635 LOC
- `app/tasks/flows/flow_tasks.py` -- process_daily_flows (pause filter), ~200 LOC

**Cancel (TRACE-03):**
- `app/services/flow/management/pause_resume.py` -- cancel_patient_flow() implementation
- `app/api/v2/routers/flows.py` -- API endpoint wiring
- `app/services/flow_service.py` -- FlowService facade delegation
- `app/orchestration/saga_orchestrator/compensation.py` -- SagaCompensator (NOT called by cancel)

**Existing tests (reference):**
- `tests/unit/services/test_flow_cancel.py` -- 5 cancel tests
- `tests/unit/tasks/test_auto_resume_flows.py` -- 4 auto-resume tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace all three pause paths and the resume/auto-resume mechanism</name>
  <files>
    backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md
  </files>
  <action>
Read each pause implementation file in full. For EACH of the three pause paths, document:

**Path 1: API Pause (FlowManagementPauseResumeMixin.pause_patient_flow)**
- File: `app/services/flow/management/pause_resume.py`
- State keys set: `state_data["paused"]`, `state_data["pause_reason"]`, `state_data["paused_at"]`, `state_data["auto_resume_at"]`
- Status field change: `flow_state.status = "paused"`
- Parameters accepted (especially `duration_hours` for auto-resume)
- DB operations used (sync `self.db.query()` vs async `await db.execute()`)

**Path 2: FlowCore Pause (FlowCoreTransitionsMixin.pause_patient_flow)**
- File: `app/services/flow/core/transitions.py`
- State keys set
- Status field change
- How it differs from Path 1

**Path 3: Agent Pause (TransitionHandler.pause_flow)**
- File: `app/agents/patient/flow_coordinator/transition_handler.py`
- State keys set: `state_data["flow_paused"]` (DIFFERENT key!)
- Whether status field is changed (research says NO)
- Impact: Celery Beat filter will NOT detect this pause

Create a side-by-side comparison table:

```markdown
| Attribute | API/FlowManagement | FlowCore | Agent/TransitionHandler |
|-----------|-------------------|----------|------------------------|
| State key | `paused` | `paused` | `flow_paused` |
| Status field | `"paused"` | `"paused"` | NOT changed |
| Auto-resume | Yes (auto_resume_at) | ? | No |
| Blocks daily flow | Yes | Yes | NO (filter checks "paused") |
| DB mode | sync/async? | sync/async? | sync |
```

**Resume and auto-resume:**
- Trace `FlowManagementPauseResumeMixin.resume_patient_flow()` -- what state keys are cleared, what status is set
- Trace `resume_paused_flows` Celery Beat task in `app/tasks/flow_automation.py`:
  - Query: `WHERE status = 'paused' AND state_data->>'auto_resume_at' <= NOW()`
  - Action: calls `FlowManagementService.resume_patient_flow()`
  - Frequency: hourly (verify in celery_app.py beat_schedule)

**Pause guard in daily flow processing:**
- Read `app/tasks/flows/flow_tasks.py` to find the `process_daily_flows` task
- Document exactly how it filters paused flows: `if flow.state_data and flow.state_data.get("paused")`
- Document that `SequentialMessageHandler.send_day_messages()` does NOT independently check pause -- the guard is ONLY at Celery task level

Flag the dual key divergence as a MEDIUM severity finding: agent-initiated pauses via TransitionHandler may not block daily flow message dispatch because `process_daily_flows` only checks `state_data.get("paused")`, not `state_data.get("flow_paused")`.

Create the trace document at `backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md` with sections for each path, the comparison table, resume/auto-resume trace, and findings.
  </action>
  <verify>
    <automated>test -f "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md" && grep -q "flow_paused" "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md" && grep -q "auto_resume" "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md"</automated>
  </verify>
  <done>
    Trace document exists with all three pause paths documented side-by-side. The dual key divergence is flagged as a finding. Resume and auto-resume mechanisms are traced. Pause guard coverage gap (SequentialMessageHandler lacks independent check) is documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Trace the cancel flow path and verify saga compensation independence</name>
  <files>
    backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md
  </files>
  <action>
Read `app/services/flow/management/pause_resume.py` focusing on `cancel_patient_flow()`. Trace the cancel path step-by-step:

1. **Input**: patient_id, reason (optional)
2. **Flow state lookup**: How the active flow is retrieved
3. **Message cancellation**: Query pending/scheduled/queued outbound messages
   - For each message: set status = CANCELLED
   - If message_metadata has celery_task_id: revoke via `AsyncResult(task_id).revoke(terminate=False)`
   - Document the soft revocation semantics: task won't execute if queued, but won't be terminated if running
4. **State cleanup**:
   - `status = "cancelled"`
   - `completed_at = now`
   - `state_data["paused"] = False`
   - `state_data["cancelled"] = True`
   - `state_data["auto_resume_at"]` removed
   - `state_data` tracks `messages_cancelled` count and `tasks_revoked` count
5. **Commit**: verify transaction semantics

**Critical verification: Cancel vs Saga Compensation**

Read `app/orchestration/saga_orchestrator/compensation.py` briefly to confirm:
- Saga compensation is triggered ONLY during `execute_patient_onboarding_saga()` when a step fails (orchestrator.py error handling)
- Cancel does NOT touch `PatientOnboardingSaga` records
- Cancel and saga compensation are independent operations

Document this as an informational finding: "Cancel does not trigger saga compensation. The saga is a one-time onboarding transaction. Cancel operates on flow state and messages only. This is by design -- cancel happens AFTER onboarding is complete."

**Also trace the cancel API endpoint wiring:**
- Read `app/api/v2/routers/flows.py` to find the cancel endpoint
- Read `app/services/flow_service.py` (FlowService facade) to trace how it delegates to `flow_management.cancel_patient_flow()`

**Check for the sync DB issue in compensation path:**
Research identified `PatientFlowService.delete_flow()` uses sync `self.db.query()`. Verify whether `compensation_handlers.compensate_flow()` calls `delete_flow()` or inlines its own async deletion. Read `app/orchestration/saga_orchestrator/compensation_handlers.py` to answer this open question.

Append all cancel trace sections and findings to the existing trace document.
  </action>
  <verify>
    <automated>grep -q "cancel_patient_flow" "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md" && grep -q "saga compensation" "backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md"</automated>
  </verify>
  <done>
    Cancel path is fully traced with message revocation, Celery task revocation, and state cleanup documented. The saga compensation independence is verified and documented. The compensation_handlers.compensate_flow() DB pattern is clarified (open question from research answered). Cancel API endpoint wiring is traced.
  </done>
</task>

</tasks>

<verification>
- [ ] Trace document exists at `backend-hormonia/docs/traces/30-02-pause-resume-cancel-trace.md`
- [ ] All three pause paths are documented with state keys, status changes, and DB modes
- [ ] Side-by-side comparison table shows the dual key divergence
- [ ] Resume and auto-resume mechanisms are traced
- [ ] Pause guard coverage is documented (only at Celery task level)
- [ ] Cancel path is fully traced: message revocation, task revocation, state cleanup
- [ ] Saga compensation independence is verified and documented
- [ ] Open question about compensation_handlers.compensate_flow() DB pattern is answered
</verification>

<success_criteria>
1. Pause and resume semantics produce the correct state transitions: a paused flow stores `state_data.paused`, auto-resume fires at `auto_resume_at`, and no messages are dispatched while paused (TRACE-02 from ROADMAP.md)
2. Cancel flow revokes all queued Celery work, clears flow state, and the relationship to saga compensation is documented -- verifiable by inspecting cancel path logic (TRACE-03 from ROADMAP.md)
</success_criteria>

<output>
After completion, create `.planning/phases/30-flow-integration-trace/30-02-SUMMARY.md`
</output>
