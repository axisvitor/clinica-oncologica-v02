---
phase: 30-flow-integration-trace
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md
autonomous: true
requirements:
  - TRACE-04

must_haves:
  truths:
    - "The FlowCoordinatorAgent._process_daily_flow() path is traced from Celery Beat trigger through StateManager context building, DecisionEngine analysis, to action execution via TransitionHandler/SequentialMessageHandler"
    - "The indirect saga-agent relationship is documented: saga creates PatientFlowState during onboarding, agent reads and modifies it afterward -- no direct saga invocation from agent"
    - "The data flow from patient_id task payload through FlowContext construction to FlowDecision output is traced with all intermediate data shapes documented"
    - "Session type concern is documented: agent uses sync Session from Celery worker context, and any async-only paths in called code are flagged"
  artifacts:
    - path: "backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md"
      provides: "Complete agent decision engine trace with data flow verification and saga relationship documentation"
      min_lines: 80
  key_links:
    - from: "app/agents/patient/flow_coordinator/coordinator.py"
      to: "app/agents/patient/flow_coordinator/state_manager.py"
      via: "StateManager.build_flow_context()"
      pattern: "build_flow_context"
    - from: "app/agents/patient/flow_coordinator/coordinator.py"
      to: "app/agents/patient/flow_coordinator/decision_engine.py"
      via: "DecisionEngine.analyze_flow_situation() and make_flow_decision()"
      pattern: "make_flow_decision"
    - from: "app/agents/patient/flow_coordinator/coordinator.py"
      to: "app/agents/patient/flow_coordinator/transition_handler.py"
      via: "_execute_flow_decision() dispatches to TransitionHandler"
      pattern: "_execute_flow_decision"
    - from: "SagaOrchestrator (writes)"
      to: "FlowCoordinatorAgent (reads)"
      via: "PatientFlowState DB record (indirect relationship)"
      pattern: "PatientFlowState"
---

<objective>
Trace the FlowCoordinatorAgent decision engine data flow from task trigger through context building, decision-making, and action execution, documenting the indirect relationship between the saga (which creates flow state) and the agent (which manages it afterward).

Purpose: Verify that the agent has all required context from the saga-created flow state and that the data flow from agent decision to saga input is unbroken. Clarify that the agent does NOT directly invoke the saga -- the relationship is mediated through the PatientFlowState DB record.

Output: A trace document at `backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md` covering the agent execution path, data shapes, and saga-agent relationship.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-flow-integration-trace/30-RESEARCH.md

Key files to trace:

**Agent decision engine (TRACE-04):**
- `app/agents/patient/flow_coordinator/coordinator.py` -- FlowCoordinatorAgent main class, ~402 LOC
- `app/agents/patient/flow_coordinator/decision_engine.py` -- DecisionEngine (flow decisions), ~207 LOC
- `app/agents/patient/flow_coordinator/transition_handler.py` -- TransitionHandler (state changes), ~194 LOC
- `app/agents/patient/flow_coordinator/state_manager.py` -- StateManager (context building), ~200 LOC
- `app/agents/patient/flow_coordinator/models.py` -- FlowContext, FlowDecision models, ~100 LOC

**Saga-agent link (indirect):**
- `app/orchestration/saga_orchestrator/steps.py` -- step_initialize_flow() creates PatientFlowState
- `app/services/patient/flow_service.py` -- PatientFlowService used by both saga and agent paths

**Celery trigger:**
- `app/tasks/flows/flow_tasks.py` -- process_daily_flows task
- `app/celery_app.py` -- beat_schedule entry
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace FlowCoordinatorAgent execution path and data flow</name>
  <files>
    backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md
  </files>
  <action>
Read each agent file in full. Trace the complete execution path:

**1. Entry point -- Celery Beat trigger:**
- Read `app/tasks/flows/flow_tasks.py` and `app/celery_app.py` to find how `process_daily_flows` is triggered
- Document whether `FlowCoordinatorAgent` is invoked from `process_daily_flows` or from a separate task
- If the agent has its own Celery task, document its schedule and parameters

**2. FlowCoordinatorAgent._process_daily_flow():**
- Read `app/agents/patient/flow_coordinator/coordinator.py`
- Document the method signature and parameters (patient_id, current_day, db_session)
- Trace each internal call:

```
_process_daily_flow(patient_id, current_day)
  -> state_manager.build_flow_context(patient_id, current_day)
     -> reads Patient, PatientFlowState, adherence metrics, interactions from DB
     -> returns FlowContext
  -> decision_engine.analyze_flow_situation(context)
     -> returns analysis
  -> decision_engine.make_flow_decision(context, analysis)
     -> returns FlowDecision
  -> _execute_flow_decision(context, decision)
     -> switch on decision type:
        - NORMAL: SequentialMessageHandler.send_day_messages()
        - PAUSE: transition_handler.pause_flow()
        - RESUME: transition_handler.resume_flow()
        - SKIP: transition_handler.skip_day()
        - COMPLETE: transition_handler.complete_flow()
```

**3. Data shapes:**
- Read `app/agents/patient/flow_coordinator/models.py`
- Document `FlowContext` fields: what data the agent receives (patient info, flow state, adherence, interactions)
- Document `FlowDecision` fields: what the decision engine outputs (action type, reason, parameters)
- Verify that FlowContext has all fields needed for decision-making

**4. StateManager.build_flow_context():**
- Read `app/agents/patient/flow_coordinator/state_manager.py`
- Document each DB query made (Patient, PatientFlowState, etc.)
- Note the session type used (sync Session from Celery context)
- Verify that all data fetched maps to FlowContext fields

**5. DecisionEngine:**
- Read `app/agents/patient/flow_coordinator/decision_engine.py`
- Document `analyze_flow_situation()` inputs and outputs
- Document `make_flow_decision()` logic: what criteria drive which decision
- Note whether the engine is deterministic or uses AI (Pydantic AI agent vs rule-based)

**6. TransitionHandler action execution:**
- Read `app/agents/patient/flow_coordinator/transition_handler.py`
- Document each action method and what state changes it makes
- Flag the `flow_paused` vs `paused` key issue (already documented in 30-02, just cross-reference)

Create the trace document at `backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md` with:
- Section 1: Entry Point (how agent is triggered)
- Section 2: Execution Path (full call chain with handoffs)
- Section 3: Data Shapes (FlowContext, FlowDecision models)
- Section 4: Saga-Agent Relationship (indirect via PatientFlowState)
- Section 5: Findings (session type concerns, data integrity, pause key cross-reference)
  </action>
  <verify>
    <automated>test -f "backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md" && grep -q "FlowContext" "backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md" && grep -q "FlowDecision" "backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md"</automated>
  </verify>
  <done>
    Agent execution path is fully traced from Celery trigger through context building, decision-making, and action execution. FlowContext and FlowDecision data shapes are documented. Each handoff in the agent call chain has caller, callee, parameters, and return types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the indirect saga-agent relationship and verify data integrity</name>
  <files>
    backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md
  </files>
  <action>
Document the saga-agent data flow relationship by reading both sides:

**Saga side (writes):**
- Re-read `app/orchestration/saga_orchestrator/steps.py` focusing on `step_initialize_flow()`:
  - What fields are set on PatientFlowState when the saga creates it
  - What state_data keys are initialized
  - What flow_type, status, current_day values are set
- Document the PatientFlowState record shape as created by the saga

**Agent side (reads):**
- From Task 1's StateManager trace, list what fields the agent reads from PatientFlowState
- Verify field-by-field that every field the agent reads was set by the saga during creation
- Flag any fields the agent expects but the saga might not set

**Data integrity check:**
Create a table:

```markdown
| PatientFlowState Field | Set by Saga (steps.py) | Read by Agent (state_manager.py) | Status |
|----------------------|----------------------|-------------------------------|--------|
| patient_id | Yes (step_create_patient) | Yes (query filter) | OK |
| flow_type | Yes (step_initialize_flow) | Yes (FlowContext) | OK |
| status | Yes ("active") | Yes (FlowContext) | OK |
| state_data | Yes ({}) | Yes (FlowContext) | OK |
| current_day | Yes (1) | Yes (FlowContext) | OK |
| ... | ... | ... | ... |
```

**Session type analysis:**
- The saga runs in the API request context with AsyncSession (post-v1.4)
- The agent runs in Celery worker context with sync Session
- Both access PatientFlowState but at different times (saga creates during onboarding, agent reads during daily processing)
- Document that there is NO concurrent access concern -- the saga completes before the agent ever processes the patient

**Open question from research:**
Check if FlowCoordinatorAgent is registered in Celery Beat or only invoked via the agent messaging system:
```bash
grep -rn "FlowCoordinatorAgent\|flow_coordinator" backend-hormonia/app/celery_app.py backend-hormonia/app/tasks/ --include="*.py"
```
Document the finding.

Append the saga-agent relationship section and data integrity table to the trace document.
  </action>
  <verify>
    <automated>grep -q "PatientFlowState" "backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md" && grep -q "saga.*agent\|agent.*saga" "backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md"</automated>
  </verify>
  <done>
    Saga-agent indirect relationship is fully documented with data integrity table showing field-by-field coverage. Session type analysis confirms no concurrent access concern. Agent invocation mechanism (Celery Beat vs agent messaging) is clarified. All findings are documented.
  </done>
</task>

</tasks>

<verification>
- [ ] Trace document exists at `backend-hormonia/docs/traces/30-03-agent-decision-engine-trace.md`
- [ ] Agent execution path is traced from trigger through decision to action
- [ ] FlowContext and FlowDecision data shapes are documented
- [ ] Saga-agent indirect relationship is documented with data integrity table
- [ ] Session type analysis is included (AsyncSession for saga, sync Session for agent)
- [ ] Agent invocation mechanism is clarified (Celery Beat vs agent messaging)
- [ ] Pause key divergence is cross-referenced with 30-02 findings
</verification>

<success_criteria>
FlowCoordinatorAgent decision engine passes the correct patient/flow context into agent-side daily flow actions, and the saga contract is verified indirectly via PatientFlowState (saga writes during onboarding, agent reads/manages afterward) with no direct agent-to-saga invocation (TRACE-04 from ROADMAP.md).
</success_criteria>

<output>
After completion, create `.planning/phases/30-flow-integration-trace/30-03-SUMMARY.md`
</output>
