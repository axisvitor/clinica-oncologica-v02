---
phase: 43-quiz-interface-quality
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - quiz-mensal-interface/package.json
  - quiz-mensal-interface/package-lock.json
  - quiz-mensal-interface/tests/mocks/handlers.ts
  - quiz-mensal-interface/tests/mocks/server.ts
  - quiz-mensal-interface/tests/setup.ts
autonomous: true
requirements:
  - QUIZ-04
  - QUIZ-05
must_haves:
  truths:
    - "Quiz Jest suite resolves CSS module imports without missing identity-obj-proxy module errors"
    - "Quiz tests run on supported MSW v2 handler APIs (http/HttpResponse)"
    - "Automated tests pass after dependency and handler migration"
  artifacts:
    - path: "quiz-mensal-interface/package.json"
      provides: "identity-obj-proxy and msw v2-compatible dependency graph"
    - path: "quiz-mensal-interface/tests/mocks/handlers.ts"
      provides: "MSW v2 request handlers for quiz endpoints"
    - path: "quiz-mensal-interface/tests/setup.ts"
      provides: "Stable Jest/MSW lifecycle setup for v2"
  key_links:
    - from: "quiz-mensal-interface/package.json"
      to: "quiz-mensal-interface/tests/mocks/handlers.ts"
      via: "msw major-version API contract"
      pattern: "msw\"\s*:\s*\"\^2|from ['\"]msw['\"]"
    - from: "quiz-mensal-interface/package.json"
      to: "quiz-mensal-interface/jest moduleNameMapper"
      via: "identity-obj-proxy resolution"
      pattern: "identity-obj-proxy"
---

<objective>
Migrate quiz testing dependencies and handlers to a stable, supported baseline by adding missing CSS-module test dependency and upgrading MSW from v1 to v2.

Purpose: Phase 43 cannot close quality gates if test stack remains on deprecated MSW API and unresolved module mappings.

Output: Updated test dependency graph and migrated mock handlers/setup that pass Jest.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-quiz-interface-quality/43-CONTEXT.md
@.planning/phases/43-quiz-interface-quality/43-RESEARCH.md
@.planning/phases/43-quiz-interface-quality/43-01-SUMMARY.md

<interfaces>
Current mock contract from `tests/mocks/handlers.ts` uses MSW v1 API:

```typescript
import { rest } from "msw";
rest.post(`${API_BASE_URL}/access`, (req, res, ctx) => {
  return res(ctx.json(mockQuizSession));
});
```

Jest currently expects CSS module mapper package:

```json
"moduleNameMapper": {
  "\\.(css|less|scss|sass)$": "identity-obj-proxy"
}
```

MSW server wiring entry point:

```typescript
import { setupServer } from "msw/node";
import { handlers } from "./handlers";
export const server = setupServer(...handlers);
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing test dependency and upgrade MSW to v2</name>
  <files>quiz-mensal-interface/package.json, quiz-mensal-interface/package-lock.json</files>
  <action>
    Add `identity-obj-proxy` to quiz devDependencies and upgrade `msw` to v2.

    Keep Jest transform and module mapper contracts intact unless an MSW v2 compatibility fix is strictly required.

    Do not modify production quiz runtime dependencies in this task.

  </action>
  <verify>
    <automated>cd quiz-mensal-interface && npm ls identity-obj-proxy msw</automated>
  </verify>
  <done>`identity-obj-proxy` is installed and `msw` resolves to v2 in lockfile and installed tree.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate MSW handlers/setup from v1 rest/ctx API to v2 http/HttpResponse API</name>
  <files>quiz-mensal-interface/tests/mocks/handlers.ts, quiz-mensal-interface/tests/mocks/server.ts, quiz-mensal-interface/tests/setup.ts</files>
  <action>
    Rewrite mock handlers to MSW v2 conventions (`http.*`, `HttpResponse`, request.json parsing).

    Preserve existing mocked quiz scenarios (valid token, invalid token, rotated token, timeout/network/server failures) and keep setup lifecycle semantics (`listen/resetHandlers/close`) equivalent.

    Ensure no lingering `rest` or `ctx` usage remains in the quiz test directory.

  </action>
  <verify>
    <automated>cd quiz-mensal-interface && npm test -- --runInBand</automated>
  </verify>
  <done>Jest passes and test mocks run with MSW v2 APIs only.</done>
</task>

</tasks>

<verification>
Run from `quiz-mensal-interface/`:
1. `npm ls identity-obj-proxy msw`
2. `npm test -- --runInBand`
</verification>

<success_criteria>

- QUIZ-04 and QUIZ-05 are complete: missing mapper dependency added and MSW v2 migration finished.
- No `rest`/`ctx` API usage remains in quiz tests.
- Test suite is green after migration.
  </success_criteria>

<output>
After completion, create `.planning/phases/43-quiz-interface-quality/43-02-SUMMARY.md`
</output>
