---
phase: 43-quiz-interface-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quiz-mensal-interface/.prettierrc
  - quiz-mensal-interface/.prettierignore
  - quiz-mensal-interface/eslint.config.js
  - quiz-mensal-interface/.eslintrc.json
  - quiz-mensal-interface/package.json
  - quiz-mensal-interface/package-lock.json
autonomous: true
requirements:
  - QUIZ-01
  - QUIZ-02
  - QUIZ-03
must_haves:
  truths:
    - "Quiz workspace enforces Prettier formatting with the same baseline used in admin SPA"
    - "Quiz workspace runs on Next.js 15-compatible package set and builds successfully"
    - "ESLint runs with flat config on ESLint 9 without legacy .eslintrc dependency"
  artifacts:
    - path: "quiz-mensal-interface/.prettierrc"
      provides: "Deterministic formatter rules (semi false + singleQuote true baseline)"
    - path: "quiz-mensal-interface/eslint.config.js"
      provides: "ESLint 9 flat config with eslint-config-prettier last"
    - path: "quiz-mensal-interface/package.json"
      provides: "Aligned Next 15/ESLint 9 toolchain and formatter scripts"
  key_links:
    - from: "quiz-mensal-interface/package.json"
      to: "quiz-mensal-interface/eslint.config.js"
      via: "eslint/eslint-config-next/typescript-eslint version compatibility"
      pattern: "eslint\"\s*:\s*\"\^9|eslint-config-next\"\s*:\s*\"\^15"
    - from: "quiz-mensal-interface/.prettierrc"
      to: "quiz-mensal-interface/package.json"
      via: "format and format:check scripts"
      pattern: "format(:check)?"
---

<objective>
Establish quiz tooling parity baseline first: Prettier enforced, Next.js upgraded to 15, and ESLint migrated to v9 flat config without changing quiz product behavior.

Purpose: All later test migration and type-hardening work depends on a stable formatter/linter/framework baseline that matches Phase 42 decisions.

Output: Updated quiz formatter and lint configuration plus framework/toolchain versions that pass build/lint/format gates.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-quiz-interface-quality/43-CONTEXT.md
@.planning/phases/43-quiz-interface-quality/43-RESEARCH.md
@.planning/phases/42-admin-spa-quality/42-03-SUMMARY.md

<interfaces>
Admin parity decisions from Phase 42:
- Prettier baseline is `semi: false` and `singleQuote: true`.
- `eslint-config-prettier` must be the last flat-config entry.

Current quiz toolchain contracts (from `quiz-mensal-interface/package.json`):

```json
{
  "scripts": {
    "build": "next build",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.2.35",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.35"
  }
}
```

Legacy lint config currently in place:

```json
{ "extends": "next/core-web-vitals" }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and enforce Prettier baseline in quiz interface</name>
  <files>quiz-mensal-interface/.prettierrc, quiz-mensal-interface/.prettierignore, quiz-mensal-interface/package.json, quiz-mensal-interface/package-lock.json</files>
  <action>
    Add Prettier to quiz dev tooling and create `.prettierrc`/`.prettierignore` using the same baseline chosen in Phase 42 (`semi: false`, `singleQuote: true`).

    Add formatter scripts in `package.json` (`format`, `format:check`) and run formatting on quiz source/config/test files in scope for this phase.

    Do not change runtime quiz behavior in this task; this is formatter baseline only.

  </action>
  <verify>
    <automated>cd quiz-mensal-interface && npm run format:check</automated>
  </verify>
  <done>Prettier config exists, scripts exist, and `format:check` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Next/React/ESLint stack and migrate to ESLint 9 flat config</name>
  <files>quiz-mensal-interface/package.json, quiz-mensal-interface/package-lock.json, quiz-mensal-interface/eslint.config.js, quiz-mensal-interface/.eslintrc.json</files>
  <action>
    Upgrade framework/lint packages to a coherent Next 15 toolchain (`next`, `eslint-config-next`, `eslint`, and compatible React + `@types/react*`).

    Replace legacy `.eslintrc.json` with `eslint.config.js` flat config. Include explicit ignores, TS/TSX file targeting, test globals overrides, and keep `eslint-config-prettier` as the final entry.

    Preserve phase scope: no feature rewrites and no visual redesign.

  </action>
  <verify>
    <automated>cd quiz-mensal-interface && npm run lint && npm run build</automated>
  </verify>
  <done>Quiz builds on Next 15 and lint runs via ESLint 9 flat config without `.eslintrc` fallback.</done>
</task>

</tasks>

<verification>
Run from `quiz-mensal-interface/`:
1. `npm run format:check`
2. `npm run lint`
3. `npm run build`
</verification>

<success_criteria>

- QUIZ-01, QUIZ-02, and QUIZ-03 are satisfied with passing format/lint/build gates.
- Tooling baseline mirrors Phase 42 admin decisions (`semi: false`, `singleQuote: true`, flat config with prettier last).
- No runtime quiz flow behavior is changed in this plan.
  </success_criteria>

<output>
After completion, create `.planning/phases/43-quiz-interface-quality/43-01-SUMMARY.md`
</output>
