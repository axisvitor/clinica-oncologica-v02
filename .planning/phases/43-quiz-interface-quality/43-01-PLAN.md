---
phase: 43-quiz-interface-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quiz-mensal-interface/.prettierrc
  - quiz-mensal-interface/.prettierignore
  - quiz-mensal-interface/eslint.config.js
  - quiz-mensal-interface/package.json
  - quiz-mensal-interface/package-lock.json
  - quiz-mensal-interface/next.config.mjs
autonomous: true
requirements:
  - QUIZ-01
  - QUIZ-02
  - QUIZ-03
must_haves:
  truths:
    - "Quiz workspace enforces Prettier formatting with the same baseline used in admin SPA"
    - "Quiz workspace runs on a coherent Next.js 15 toolchain and `npm run build` succeeds"
    - "`eslint .` runs with ESLint 9 flat config and no module-resolution failures"
  artifacts:
    - path: "quiz-mensal-interface/.prettierrc"
      provides: "Deterministic formatter rules (semi false + singleQuote true baseline)"
    - path: "quiz-mensal-interface/eslint.config.js"
      provides: "ESLint 9 flat config aligned with admin parity and prettier override last"
    - path: "quiz-mensal-interface/package.json"
      provides: "Aligned Next 15/React 19/ESLint 9 toolchain and formatter scripts"
  key_links:
    - from: "quiz-mensal-interface/package.json"
      to: "quiz-mensal-interface/eslint.config.js"
      via: "eslint/eslint-config-next/typescript-eslint version compatibility"
      pattern: "eslint\"\s*:\s*\"\^9|eslint-config-next\"\s*:\s*\"\^15|typescript-eslint"
    - from: "quiz-mensal-interface/.prettierrc"
      to: "quiz-mensal-interface/package.json"
      via: "format and format:check scripts"
      pattern: "format(:check)?"
---

<objective>
Stabilize the quiz tooling baseline so formatter, linter, and framework gates are deterministic: Prettier parity with admin SPA, coherent Next.js 15 package set, and ESLint 9 flat config that executes reliably.

Purpose: All test migration and type-hardening work in later plans depends on a working lint/build baseline with zero config drift.

Output: Updated quiz formatter and lint configuration plus framework/toolchain versions that pass format/lint/build gates.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-quiz-interface-quality/43-CONTEXT.md
@.planning/phases/43-quiz-interface-quality/43-RESEARCH.md
@.planning/phases/42-admin-spa-quality/42-03-SUMMARY.md

<interfaces>
Admin SPA parity decisions from Phase 42:
- Prettier baseline is `semi: false` and `singleQuote: true`.
- `eslint-config-prettier` must be the final flat-config entry.

Current quiz toolchain contracts (`quiz-mensal-interface/package.json`):

```json
{
  "scripts": {
    "build": "next build",
    "lint": "eslint .",
    "format": "prettier --write ...",
    "format:check": "prettier --check ..."
  },
  "dependencies": {
    "next": "^15.5.3",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "eslint": "^9.34.0",
    "eslint-config-next": "^15.5.3",
    "prettier": "^3.6.2",
    "eslint-config-prettier": "^10.1.8"
  }
}
```

Current flat lint config import shape (`quiz-mensal-interface/eslint.config.js`):

```javascript
import nextCoreWebVitals from "eslint-config-next/core-web-vitals";
import nextTypeScript from "eslint-config-next/typescript";
import eslintConfigPrettier from "eslint-config-prettier";
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock formatter baseline and source-focused ignore scope</name>
  <files>quiz-mensal-interface/.prettierrc, quiz-mensal-interface/.prettierignore, quiz-mensal-interface/package.json</files>
  <action>
    Keep quiz Prettier behavior aligned with Phase 42 (`semi: false`, `singleQuote: true`) and ensure `.prettierignore` excludes generated/output folders (`.next`, coverage/build artifacts, and other non-source output) so formatter gates are deterministic.

    Keep `format` and `format:check` scripts scoped to source/config/test files; do not broaden to deployment/generated artifacts.

    Do not modify quiz runtime behavior in this task.

  </action>
  <verify>
    <automated>cd quiz-mensal-interface && npm run format:check</automated>
  </verify>
  <done>Prettier baseline matches admin parity and `format:check` passes against source-only scope.</done>
</task>

<task type="auto">
  <name>Task 2: Repair ESLint 9 flat config and validate Next 15 build path</name>
  <files>quiz-mensal-interface/eslint.config.js, quiz-mensal-interface/package.json, quiz-mensal-interface/package-lock.json, quiz-mensal-interface/next.config.mjs</files>
  <action>
    Keep the Next 15 + React 19 + ESLint 9 toolchain coherent in `package.json`/lockfile and resolve flat-config module import/runtime issues so `eslint .` runs cleanly from local dependencies.

    Align lint config structure with Phase 42 decisions: explicit ignores, explicit TS/test scopes, and `eslint-config-prettier` as final entry. If Next flat-config imports require extension-specific paths, fix those paths directly rather than downgrading ESLint.

    Preserve scope: tooling quality only (no feature rewrites, no UI redesign).

  </action>
  <verify>
    <automated>cd quiz-mensal-interface && npm run lint && npm run build</automated>
  </verify>
  <done>Quiz lint executes with ESLint 9 flat config and Next 15 build succeeds.</done>
</task>

</tasks>

<verification>
Run from `quiz-mensal-interface/`:
1. `npm run format:check`
2. `npm run lint`
3. `npm run build`
</verification>

<success_criteria>

- QUIZ-01, QUIZ-02, and QUIZ-03 are satisfied with passing format/lint/build gates.
- Tooling baseline mirrors Phase 42 admin decisions (`semi: false`, `singleQuote: true`, flat config with prettier last).
- No runtime quiz flow behavior is changed in this plan.
  </success_criteria>

<output>
After completion, create `.planning/phases/43-quiz-interface-quality/43-01-SUMMARY.md`
</output>
