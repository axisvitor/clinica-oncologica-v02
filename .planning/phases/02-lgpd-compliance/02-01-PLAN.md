---
phase: 02-lgpd-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/models/patient_deletion_audit.py
  - backend-hormonia/alembic/versions/lgpd01_add_patient_deletion_audit.py
  - backend-hormonia/app/services/patient/crud_service.py
  - backend-hormonia/app/api/v2/routers/patients/crud.py
autonomous: true
requirements: [LGPD-01]

must_haves:
  truths:
    - "When a patient is deleted, a record persists in patient_deletion_audit with timestamp, reason, and executor"
    - "The audit record cannot be updated or deleted via SQL (PostgreSQL rules block UPDATE/DELETE)"
    - "The audit record is written in the same transaction as the soft-delete — no window for data loss"
  artifacts:
    - path: "backend-hormonia/app/models/patient_deletion_audit.py"
      provides: "PatientDeletionAudit SQLAlchemy model"
      contains: "class PatientDeletionAudit"
    - path: "backend-hormonia/alembic/versions/lgpd01_add_patient_deletion_audit.py"
      provides: "Alembic migration creating patient_deletion_audit table + immutability rules"
      contains: "patient_deletion_audit_no_update"
    - path: "backend-hormonia/app/services/patient/crud_service.py"
      provides: "Audit record INSERT inside delete_patient() transaction"
      contains: "PatientDeletionAudit"
  key_links:
    - from: "backend-hormonia/app/services/patient/crud_service.py"
      to: "backend-hormonia/app/models/patient_deletion_audit.py"
      via: "import and INSERT in sync_transaction block"
      pattern: "PatientDeletionAudit\\("
    - from: "backend-hormonia/app/api/v2/routers/patients/crud.py"
      to: "backend-hormonia/app/services/patient/crud_service.py"
      via: "passes current_user context to delete_patient()"
      pattern: "delete_patient.*user"
---

<objective>
Create persistent, immutable patient deletion audit table and hook it into the existing delete_patient() flow.

Purpose: LGPD Art. 16/18 requires that patient deletion events are permanently recorded in a way that survives Railway log rotation. Currently, deletion evidence only exists in application logs which are ephemeral.

Output: PatientDeletionAudit model, Alembic migration with immutability rules, and service hook that writes audit record before soft-delete in the same transaction.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lgpd-compliance/02-RESEARCH.md

# Key source files to read before implementing:
@backend-hormonia/app/models/base.py
@backend-hormonia/app/models/audit_log.py
@backend-hormonia/app/services/patient/crud_service.py
@backend-hormonia/app/api/v2/routers/patients/crud.py
@backend-hormonia/alembic/versions/011_hipaa_audit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PatientDeletionAudit model and Alembic migration with immutability rules</name>
  <files>
    backend-hormonia/app/models/patient_deletion_audit.py
    backend-hormonia/alembic/versions/lgpd01_add_patient_deletion_audit.py
  </files>
  <action>
    1. Create `backend-hormonia/app/models/patient_deletion_audit.py`:
       - Import from `app.models.base` (use `BaseModel` if it provides `id`, `created_at`, `updated_at`; otherwise use SQLAlchemy `Base` directly)
       - Define `PatientDeletionAudit` class with `__tablename__ = "patient_deletion_audit"`
       - Columns: `patient_id` (UUID, NOT NULL, indexed), `deleted_by_user_id` (UUID, nullable, indexed), `deleted_by_email` (String(255), nullable), `deletion_reason` (Text, nullable), `patient_name_hash` (String(64), nullable — SHA-256 hash for verification, NOT plaintext), `deleted_at` (DateTime(timezone=True), NOT NULL)
       - Do NOT add a foreign key to `patients.id` — the audit record must survive even if the patient row is later hard-deleted
       - Add `__repr__` for debugging

    2. Create Alembic migration file `backend-hormonia/alembic/versions/lgpd01_add_patient_deletion_audit.py`:
       - Use `alembic revision --autogenerate` if available, or create manually
       - Set `down_revision` to the current Alembic head (check with `alembic heads` or inspect the most recent migration file's `revision` value)
       - `upgrade()`: Create `patient_deletion_audit` table with all columns above, add indexes on `patient_id` and `deleted_at`
       - After table creation, add immutability rules using the EXACT pattern from `011_hipaa_audit.py`:
         ```sql
         CREATE RULE patient_deletion_audit_no_update AS
             ON UPDATE TO patient_deletion_audit DO INSTEAD NOTHING;
         CREATE RULE patient_deletion_audit_no_delete AS
             ON DELETE TO patient_deletion_audit DO INSTEAD NOTHING;
         ```
       - `downgrade()`: Drop rules first (`DROP RULE IF EXISTS ...`), then drop table

    3. Register the model in the appropriate `__init__.py` or models barrel file if the project uses one (check `backend-hormonia/app/models/__init__.py`).
  </action>
  <verify>
    - `python -c "from app.models.patient_deletion_audit import PatientDeletionAudit; print(PatientDeletionAudit.__tablename__)"` prints `patient_deletion_audit`
    - Grep confirms immutability rules in migration: `grep -c "patient_deletion_audit_no_update" backend-hormonia/alembic/versions/lgpd01_*.py` returns 1
    - Migration file has valid `revision` and `down_revision` values
  </verify>
  <done>
    PatientDeletionAudit model exists with all required columns, Alembic migration creates the table with indexed columns and PostgreSQL immutability rules that prevent UPDATE and DELETE.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hook audit record into delete_patient() and pass user context from router</name>
  <files>
    backend-hormonia/app/services/patient/crud_service.py
    backend-hormonia/app/api/v2/routers/patients/crud.py
  </files>
  <action>
    1. Read `backend-hormonia/app/services/patient/crud_service.py` to find `delete_patient()` method.

    2. Modify `delete_patient()` signature to accept optional user context parameters:
       - Add `performed_by_user_id: Optional[UUID] = None` and `performed_by_email: Optional[str] = None` parameters
       - These are optional to maintain backward compatibility with any other callers

    3. Inside the existing `sync_transaction` block in `delete_patient()`, BEFORE `patient.deleted_at = now_sao_paulo()`:
       - Import `PatientDeletionAudit` from `app.models.patient_deletion_audit`
       - Import `hashlib` from stdlib
       - Create the audit record:
         ```python
         audit_record = PatientDeletionAudit(
             patient_id=patient.id,
             deleted_by_user_id=performed_by_user_id,
             deleted_by_email=performed_by_email,
             deletion_reason=reason if 'reason' param exists else "Admin deletion via API",
             patient_name_hash=hashlib.sha256(
                 (patient.name or "").encode()
             ).hexdigest(),
             deleted_at=now_sao_paulo(),
         )
         session.add(audit_record)
         ```
       - The audit INSERT must be the FIRST operation in the transaction block — before setting `patient.deleted_at`

    4. Read `backend-hormonia/app/api/v2/routers/patients/crud.py` to find the delete endpoint.

    5. Modify the delete endpoint to pass `current_user` context:
       - Pass `performed_by_user_id=current_user.id` and `performed_by_email=current_user.email` (adjust field names based on actual User model attributes)
       - If the router uses a different variable name for the authenticated user, adapt accordingly

    6. Check for other callers of `delete_patient()` (grep for `delete_patient` in the codebase) — they continue to work with the default `None` values.
  </action>
  <verify>
    - Grep confirms `PatientDeletionAudit` import in `crud_service.py`: `grep "PatientDeletionAudit" backend-hormonia/app/services/patient/crud_service.py`
    - Grep confirms user context passed from router: `grep "performed_by" backend-hormonia/app/api/v2/routers/patients/crud.py`
    - `python -c "from app.services.patient.crud_service import PatientCRUDService"` succeeds (no import errors)
    - Run existing patient tests: `pytest backend-hormonia/tests/ -k "patient" --no-header -q` — existing tests still pass
  </verify>
  <done>
    delete_patient() writes a PatientDeletionAudit record inside the same transaction as the soft-delete. The router passes current user context (user_id, email) to the service. The audit record is the FIRST insert in the transaction, guaranteeing it persists even if the process crashes after commit.
  </done>
</task>

</tasks>

<verification>
1. Model import: `python -c "from app.models.patient_deletion_audit import PatientDeletionAudit"` succeeds
2. Migration integrity: Migration file references correct down_revision and contains CREATE TABLE + CREATE RULE statements
3. Transaction ordering: `grep -n "PatientDeletionAudit\|deleted_at" backend-hormonia/app/services/patient/crud_service.py` shows audit INSERT line number < soft-delete line number
4. User context flow: Router passes user identity to service, service passes to audit record
5. Existing tests pass: `pytest backend-hormonia/tests/ -k "patient" --no-header -q`
</verification>

<success_criteria>
- PatientDeletionAudit model exists with patient_id, deleted_by_user_id, deleted_by_email, deletion_reason, patient_name_hash, deleted_at columns
- Alembic migration creates table with immutability rules (no_update, no_delete) matching audit_logs pattern
- delete_patient() inserts audit record BEFORE soft-delete, in the same transaction
- Router passes authenticated user context to the service method
- All existing patient tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-lgpd-compliance/02-01-SUMMARY.md`
</output>
