---
phase: 17-flow-core-splits
plan: 07
type: execute
wave: 6
depends_on:
  - 17-06
files_modified:
  - backend-hormonia/tests/conftest.py
  - backend-hormonia/tests/api/critical/conftest.py
  - .planning/phases/17-flow-core-splits/deferred-items.md
autonomous: true
requirements:
  - SPLIT-05
  - SPLIT-06
  - SPLIT-07
gap_closure: true

must_haves:
  truths:
    - "`GET /api/v2/notifications` returns HTTP 200 with `{items, unread_count}` for authenticated users in API contract tests"
    - "The notifications query path no longer fails with `UndefinedColumn: notifications.notification_type` during fail-fast execution"
    - "`python3 -m pytest -x` completes green after the notification schema guard is applied"
  artifacts:
    - path: "backend-hormonia/tests/conftest.py"
      provides: "Idempotent Postgres schema guard that ensures `notifications.notification_type` exists before tests run"
      contains: "_ensure_notifications_type_column"
    - path: "backend-hormonia/tests/api/critical/conftest.py"
      provides: "Critical-suite Postgres schema guard mirroring root fixture behavior for notifications"
      contains: "_ensure_notifications_type_column"
    - path: ".planning/phases/17-flow-core-splits/deferred-items.md"
      provides: "Timestamped fail-fast rerun evidence after notification guard changes"
      contains: "test_notifications_structure"
  key_links:
    - from: "backend-hormonia/tests/conftest.py"
      to: "backend-hormonia/app/api/v2/routers/notifications.py"
      via: "Session-level DB bootstrap before API contract fixture clients issue `/api/v2/notifications`"
      pattern: "notification_type column exists before list_notifications executes"
    - from: "backend-hormonia/tests/api/critical/conftest.py"
      to: "backend-hormonia/app/api/v2/routers/notifications.py"
      via: "Critical-suite test engine bootstrap uses the same additive schema guard strategy"
      pattern: "ALTER TABLE ... ADD COLUMN IF NOT EXISTS notification_type"
---

<objective>
Close the remaining Phase 17 verification gap by fixing the test-database schema mismatch that causes `tests/api/test_api_contract_fixes.py::TestNotificationsStructureFix::test_notifications_structure` to fail with `UndefinedColumn: notifications.notification_type`.

Purpose: Restore the required full-suite fail-fast gate (`python3 -m pytest -x`) so Phase 17 can be verified as complete.
Output: Updated test bootstrap guards for notifications schema + fresh fail-fast evidence.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-flow-core-splits/17-VERIFICATION.md
@.planning/phases/17-flow-core-splits/deferred-items.md

@backend-hormonia/tests/conftest.py
@backend-hormonia/tests/api/critical/conftest.py
@backend-hormonia/tests/api/test_api_contract_fixes.py
@backend-hormonia/app/api/v2/routers/notifications.py
@backend-hormonia/app/models/notification.py

<interfaces>
From backend-hormonia/app/api/v2/routers/notifications.py:
```python
async def list_notifications(...):
    query = db.query(Notification).filter(and_(*filters))
```

From backend-hormonia/app/api/v2/routers/notifications.py:
```python
def _serialize_notification(notification: Notification) -> dict:
    return {"type": notification.notification_type.value, ...}
```

From backend-hormonia/tests/conftest.py:
```python
def _ensure_patients_whatsapp_opt_out_column(engine):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idempotent notification_type schema guards to root and critical test bootstraps</name>
  <files>
    backend-hormonia/tests/conftest.py
    backend-hormonia/tests/api/critical/conftest.py
  </files>
  <action>
    Implement a new helper function `_ensure_notifications_type_column(engine)` in both conftest files, following the existing additive patch pattern used for `patients.messaging_stopped_at`.

    Required behavior in each helper:
    1. Return immediately when dialect is not PostgreSQL.
    2. Use `sa_inspect(engine)` and skip when `notifications` table does not exist.
    3. Read table columns and skip when `notification_type` already exists.
    4. If missing, run an additive patch only (no drop/recreate):
       - `ALTER TABLE notifications ADD COLUMN IF NOT EXISTS notification_type VARCHAR(50)`
       - `UPDATE notifications SET notification_type = 'INFO' WHERE notification_type IS NULL`
       - `ALTER TABLE notifications ALTER COLUMN notification_type SET NOT NULL`
       - `CREATE INDEX IF NOT EXISTS idx_notifications_notification_type ON notifications (notification_type)`
    5. Emit a clear fixture log line (same style as existing schema guards).

    Wire this helper into bootstrap flow in both files wherever `_ensure_patients_whatsapp_opt_out_column(engine)` is currently invoked, so both root API-contract and critical suites enforce the same guard before tests execute.

    Do NOT modify application router/model behavior; fix the schema bootstrap mismatch at fixture-time only.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m pytest tests/api/test_api_contract_fixes.py::TestNotificationsStructureFix::test_notifications_structure -x --tb=short</automated>
  </verify>
  <done>
    - `test_notifications_structure` returns 200 with `{items, unread_count}`.
    - No `UndefinedColumn` error for `notifications.notification_type` appears in test output.
    - Both conftest files contain `_ensure_notifications_type_column(engine)` and invoke it during engine bootstrap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run fail-fast suite and record closure evidence for Phase 17 verification</name>
  <files>
    .planning/phases/17-flow-core-splits/deferred-items.md
  </files>
  <action>
    After Task 1 lands, rerun the full backend fail-fast gate:

    `cd backend-hormonia && python3 -m pytest -x --tb=short`

    Then append a timestamped section to `.planning/phases/17-flow-core-splits/deferred-items.md` with:
    - exact command used,
    - UTC timestamp,
    - result summary (pass/fail + first failing node if any),
    - explicit statement on whether the notification-column blocker is closed.

    Goal state is a fully green fail-fast run. If a different first failure appears, log it with exact node/error class and treat this task as incomplete until that blocker is resolved.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m pytest -x --tb=short</automated>
  </verify>
  <done>
    - Full-suite fail-fast evidence is captured after notification guard changes.
    - Notification schema blocker is explicitly recorded as closed.
    - `python3 -m pytest -x --tb=short` is fully green.
  </done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/api/test_api_contract_fixes.py::TestNotificationsStructureFix::test_notifications_structure -x --tb=short` passes.
- `python3 -m pytest -x --tb=short` runs after schema guard patch and passes fully.
- Fixture logs show additive schema patching only (`ALTER TABLE ... IF NOT EXISTS` style), with no destructive table rebuild.
</verification>

<success_criteria>
- The `notifications.notification_type` missing-column failure no longer appears in fail-fast test output.
- Notifications contract test validates `{items, unread_count}` successfully.
- Phase 17 verification truth #13 has fresh fail-fast evidence reflecting post-fix state.
</success_criteria>

<output>
After completion, create `.planning/phases/17-flow-core-splits/17-07-SUMMARY.md`
</output>
