---
phase: 17-flow-core-splits
plan: 04
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
  - 17-03
files_modified:
  - backend-hormonia/tests/conftest.py
  - backend-hormonia/tests/api/critical/conftest.py
  - .planning/phases/17-flow-core-splits/deferred-items.md
autonomous: true
requirements:
  - SPLIT-05
  - SPLIT-06
  - SPLIT-07
gap_closure: true

must_haves:
  truths:
    - "The full backend suite can run through phase-17 regression gates without failing on patients.messaging_stopped_at missing-column errors"
    - "Critical API security tests execute against a schema that includes WhatsApp opt-out support"
    - "Phase-17 split validation includes fresh full-suite evidence after schema sync"
  artifacts:
    - path: "backend-hormonia/tests/conftest.py"
      provides: "Session-level test DB bootstrap that ensures required patient opt-out column exists before test execution"
      contains: "messaging_stopped_at"
    - path: "backend-hormonia/tests/api/critical/conftest.py"
      provides: "Critical API suite DB bootstrap aligned with required patient schema columns"
      contains: "messaging_stopped_at"
    - path: ".planning/phases/17-flow-core-splits/deferred-items.md"
      provides: "Updated status of previously deferred full-suite DB schema blocker"
      contains: "python3 -m pytest -x"
  key_links:
    - from: "backend-hormonia/tests/conftest.py"
      to: "backend-hormonia/tests/api/critical/test_patient_security_fixes.py"
      via: "pytest engine fixture schema bootstrap"
      pattern: "messaging_stopped_at"
    - from: "backend-hormonia/tests/api/critical/conftest.py"
      to: "patients table"
      via: "Postgres test-engine setup before session yield"
      pattern: "ALTER TABLE patients"
---

<objective>
Close the single remaining Phase 17 verification gap by making test database bootstrap idempotently enforce the `patients.messaging_stopped_at` column and by re-running the full backend regression gate.

Purpose: The split work for `_flow_functions`, `flow_core`, and `flow_management` is structurally complete, but Phase 17 cannot be marked fully done until the required full-suite gate runs cleanly.
Output: Updated test DB bootstrap fixtures for root + critical API suites and documented evidence that `python3 -m pytest -x` passes after schema sync.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-flow-core-splits/17-VERIFICATION.md
@.planning/phases/17-flow-core-splits/17-03-SUMMARY.md
@backend-hormonia/tests/conftest.py
@backend-hormonia/tests/api/critical/conftest.py
@backend-hormonia/alembic/versions/lgpd02_add_whatsapp_opt_out_flag.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idempotent schema bootstrap guard for patients.messaging_stopped_at in test fixtures</name>
  <files>
    backend-hormonia/tests/conftest.py
    backend-hormonia/tests/api/critical/conftest.py
  </files>
  <action>
    Implement a shared fixture-level schema guard in both test-engine setup paths so Postgres-backed test runs cannot proceed with a stale `patients` table.

    Required behavior:
    - In each conftest test-engine setup, inspect the `patients` table columns before yielding sessions.
    - If `messaging_stopped_at` is missing, apply an additive, idempotent schema patch for the active dialect (Postgres expected for this failure path).
    - Keep this guard non-destructive: do not drop/recreate tables, do not mutate unrelated columns/indexes, and do not touch production URLs.
    - Keep SQLite behavior intact for fast local runs while ensuring guard logic does not break SQLite fixture setup.

    Implementation note: prefer one helper per file with explicit logging/print context for when a schema patch is applied, so failures are diagnosable in CI logs.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m pytest tests/api/critical/test_patient_security_fixes.py::TestPatientSecurityFixes::test_idempotency_rbac_denies_other_doctor -x</automated>
  </verify>
  <done>The previously failing idempotency RBAC test no longer raises `UndefinedColumn` and executes the authorization assertion path.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run full backend gate and record closure of Phase 17 deferred blocker</name>
  <files>.planning/phases/17-flow-core-splits/deferred-items.md</files>
  <action>
    Re-run the phase completion command `python3 -m pytest -x` from `backend-hormonia` after Task 1 lands.

    If the command passes:
    - Update deferred notes to mark the `messaging_stopped_at` blocker as resolved and include date/time + command used.

    If the command fails for any reason:
    - Record the first failure with exact test node and error class in deferred notes.
    - Treat this task as incomplete and stop Phase 17 closure work until that new blocker is fixed.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m pytest -x</automated>
  </verify>
  <done>Full-suite fail-fast run is green (`python3 -m pytest -x`) and deferred-items records closure evidence for the `messaging_stopped_at` blocker.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/api/critical/test_patient_security_fixes.py::TestPatientSecurityFixes::test_idempotency_rbac_denies_other_doctor -x` passes without `UndefinedColumn`
- `python3 -m pytest -x` completes with a fully green run
- Fixture setup logs show schema guard executes only when column is missing (idempotent behavior)
</verification>

<success_criteria>
- Test database setup no longer allows stale `patients` schemas missing `messaging_stopped_at`
- Phase 17 requirement set (SPLIT-05/06/07) regains full-suite verification evidence instead of partial status
- Verification blocker from `17-VERIFICATION.md` is closed with a green fail-fast run
</success_criteria>

<output>
After completion, create `.planning/phases/17-flow-core-splits/17-04-SUMMARY.md`
</output>
