---
phase: 41-adk-agent-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - backend-hormonia/app/schemas/v2/adk.py
  - backend-hormonia/app/api/v2/routers/adk.py
  - backend-hormonia/app/api/v2/router.py
  - backend-hormonia/tests/api/v2/test_adk.py
autonomous: true
requirements:
  - ADK-07

must_haves:
  truths:
    - "FastAPI exposes a live ADK run endpoint under `/api/v2/adk/*`"
    - "Endpoint request triggers PIISafeADKWrapper.safe_run with synthetic payload"
    - "Endpoint returns normalized output payload with tool/session metadata"
  artifacts:
    - path: "backend-hormonia/app/api/v2/routers/adk.py"
      provides: "ADK run endpoint handler"
      exports: ["router"]
    - path: "backend-hormonia/app/schemas/v2/adk.py"
      provides: "Request/response contract models for ADK endpoint"
      contains: "class ADKRunRequest"
    - path: "backend-hormonia/tests/api/v2/test_adk.py"
      provides: "Endpoint integration tests validating wrapper path"
      contains: "/api/v2/adk/run"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/adk.py"
      to: "backend-hormonia/app/ai/adk/wrapper.py"
      via: "PIISafeADKWrapper.safe_run"
      pattern: "safe_run"
    - from: "backend-hormonia/app/api/v2/router.py"
      to: "backend-hormonia/app/api/v2/routers/adk.py"
      via: "include_router"
      pattern: "adk"
---

<objective>
Expose an operational ADK runner API route and prove end-to-end request handling through PIISafeADKWrapper with integration tests.

Purpose: ADK-07 requires a functional FastAPI endpoint that drives the ADK runner path safely and predictably.

Output: v2 ADK schema + router + router registration + endpoint integration tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-adk-agent-integration/41-RESEARCH.md
@.planning/phases/41-adk-agent-integration/41-01-SUMMARY.md

@backend-hormonia/app/api/v2/router.py
@backend-hormonia/app/api/v2/routers/hive_mind.py
@backend-hormonia/app/schemas/v2/ai.py
@backend-hormonia/app/ai/adk/wrapper.py

<interfaces>
From `backend-hormonia/app/api/v2/router.py` composition pattern:
```python
api_v2_router.include_router(ai_router, prefix="/ai", tags=["ai-v2"])
api_v2_router.include_router(hive_mind_router, prefix="/hive-mind", tags=["hive-mind-v2"])
```

From `backend-hormonia/app/ai/adk/wrapper.py` endpoint call boundary:
```python
await PIISafeADKWrapper().safe_run(prompt=..., deps=..., operation="...", context={...})
```

From Plan 41-01 expected exports:
```python
from app.ai.adk import PIISafeADKWrapper
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ADK endpoint contract and router implementation</name>
  <files>
    backend-hormonia/app/schemas/v2/adk.py
    backend-hormonia/app/api/v2/routers/adk.py
    backend-hormonia/app/api/v2/router.py
  </files>
  <behavior>
    - Test 1: `POST /api/v2/adk/run` accepts prompt + tool/session payload and returns 200 with normalized response model.
    - Test 2: endpoint invokes `PIISafeADKWrapper.safe_run` exactly once per request.
    - Test 3: endpoint returns validation error (422) for missing/invalid required fields.
  </behavior>
  <action>
    Implement endpoint wiring:
    - Create `app/schemas/v2/adk.py` with explicit request/response models (include `prompt`, `tool_name`, optional `user_id`, optional `session_id`, and normalized `status`/`output`).
    - Create `app/api/v2/routers/adk.py` with `APIRouter` and `POST /run` handler that builds `AIDeps`, delegates execution to `PIISafeADKWrapper.safe_run`, and returns normalized payload.
    - Register the new router in `app/api/v2/router.py` as `/api/v2/adk` route family.

    Keep router thin (no direct runner calls in router; wrapper is mandatory safety boundary).
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest -q tests/api/v2/test_adk.py -k "run"</automated>
  </verify>
  <done>
    ADK router is mounted under v2 and endpoint contract is importable and validated by FastAPI.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests proving end-to-end wrapper path</name>
  <files>
    backend-hormonia/tests/api/v2/test_adk.py
  </files>
  <behavior>
    - Test 1: synthetic request to `/api/v2/adk/run` returns non-empty output payload.
    - Test 2: monkeypatched wrapper confirms call path goes through `safe_run` and carries operation/context metadata.
    - Test 3: invalid request payload returns 422 and does not call wrapper.
  </behavior>
  <action>
    Create API tests with deterministic mocks:
    - Follow existing `tests/api/v2` style with `TestClient` and local monkeypatching.
    - Patch runtime/wrapper internals so tests do not require live ADK external services.
    - Verify output shape includes endpoint-level metadata (`status`, `tool_name`, `session_id`, `output`).
    - Keep tests focused and fast (<60s) with synthetic data only.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest -q tests/api/v2/test_adk.py</automated>
  </verify>
  <done>
    Endpoint behavior is validated end-to-end through wrapper path and schema validation errors are covered.
  </done>
</task>

</tasks>

<verification>
1. `pytest -q tests/api/v2/test_adk.py` passes.
2. Route appears in FastAPI v2 router composition and serves `/api/v2/adk/run`.
</verification>

<success_criteria>
- ADK-07 is satisfied with a working FastAPI endpoint wired through PIISafeADKWrapper.
- Endpoint execution is integration-tested with synthetic payloads and deterministic mocks.
</success_criteria>

<output>
After completion, create `.planning/phases/41-adk-agent-integration/41-02-SUMMARY.md`
</output>
