---
phase: 41-adk-agent-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/ai/adk/tools.py
  - backend-hormonia/app/ai/adk/runtime.py
  - backend-hormonia/app/ai/adk/wrapper.py
  - backend-hormonia/app/ai/adk/__init__.py
  - backend-hormonia/tests/unit/test_adk_tools_runtime.py
autonomous: true
requirements:
  - ADK-06

must_haves:
  truths:
    - "All four existing Pydantic AI capabilities are exposed through ADK tool contracts"
    - "ADK execution path invoked by wrapper uses typed tool selection instead of direct agent.run calls"
    - "Tool execution returns structured payloads with deterministic status/result fields"
  artifacts:
    - path: "backend-hormonia/app/ai/adk/tools.py"
      provides: "Typed FunctionTool adapter functions for sentiment, humanize, variation, empathy"
      contains: "def sentiment_tool"
    - path: "backend-hormonia/app/ai/adk/runtime.py"
      provides: "ADK runner assembly and execution helper over tool registry"
      contains: "InMemorySessionService"
    - path: "backend-hormonia/app/ai/adk/wrapper.py"
      provides: "Implemented _invoke_adk that delegates to runtime helper"
      contains: "_invoke_adk"
    - path: "backend-hormonia/tests/unit/test_adk_tools_runtime.py"
      provides: "Regression coverage for tool mapping and execution contract"
      contains: "test_"
  key_links:
    - from: "backend-hormonia/app/ai/adk/tools.py"
      to: "backend-hormonia/app/ai/client_domain.py"
      via: "GeminiDomainClient method delegation"
      pattern: "GeminiDomainClient"
    - from: "backend-hormonia/app/ai/adk/wrapper.py"
      to: "backend-hormonia/app/ai/adk/runtime.py"
      via: "_invoke_adk runtime call"
      pattern: "from app.ai.adk.runtime"
---

<objective>
Build the ADK tool/runtime foundation so existing Pydantic AI domain behaviors are callable through ADK wiring without bypassing the Phase 40 PIISafe boundary.

Purpose: ADK-06 requires wrapping the existing four agents as ADK-compatible tools while preserving LGPD-safe execution boundaries and CI run-guard compliance.

Output: typed ADK tool adapters, runtime composition helper, implemented wrapper `_invoke_adk`, and focused unit regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-adk-agent-integration/41-RESEARCH.md
@.planning/phases/40-otel-removal-adk-foundation/40-02-SUMMARY.md
@.planning/phases/40-otel-removal-adk-foundation/40-03-SUMMARY.md

@backend-hormonia/app/ai/adk/wrapper.py
@backend-hormonia/app/ai/client_domain.py
@backend-hormonia/app/ai/agents/deps.py
@backend-hormonia/scripts/check_agent_run_calls.py

<interfaces>
From `backend-hormonia/app/ai/adk/wrapper.py`:
```python
class PIISafeADKWrapper:
    async def safe_run(self, prompt: str, deps: AIDeps, *, operation: str, context: dict[str, Any] | None = None) -> Any: ...
    async def _invoke_adk(self, safe_prompt: str, deps: AIDeps, *, operation: str, context: dict[str, Any] | None = None) -> Any: ...
```

From `backend-hormonia/app/ai/client_domain.py` candidate adapters:
```python
async def humanize_flow_message(... ) -> str: ...
async def generate_varied_question(... ) -> str: ...
async def analyze_response_sentiment(... ) -> Dict[str, Any]: ...
async def create_empathetic_follow_up(... ) -> str: ...
```

From `backend-hormonia/app/ai/agents/deps.py`:
```python
@dataclass
class AIDeps:
    gemini_api_key: str
    model_name: str = "gemini-2.0-flash"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Define ADK interface contracts for downstream endpoint wiring</name>
  <files>
    backend-hormonia/app/ai/adk/tools.py
    backend-hormonia/app/ai/adk/runtime.py
    backend-hormonia/app/ai/adk/__init__.py
  </files>
  <action>
    Create interface-first skeletons before implementation:
    - In `tools.py`, declare typed tool call signatures and an exported tool registry/factory for exactly four tools: sentiment, humanize, variation, empathy.
    - In `runtime.py`, declare the runtime entrypoint used by wrapper `_invoke_adk` with explicit input contract (`prompt`, `deps`, `tool_name`, session fields).
    - Export these contracts from `app.ai.adk.__init__` so Plan 41-02 can wire endpoint imports without codebase exploration.

    Do not implement ADK execution internals in this task; contracts only.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -c "from app.ai.adk import PIISafeADKWrapper; import app.ai.adk.tools, app.ai.adk.runtime; print('ok')"</automated>
  </verify>
  <done>
    Contract files exist, export expected symbols, and import cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement FunctionTool adapters and runtime execution path</name>
  <files>
    backend-hormonia/app/ai/adk/tools.py
    backend-hormonia/app/ai/adk/runtime.py
    backend-hormonia/app/ai/adk/wrapper.py
    backend-hormonia/app/ai/adk/__init__.py
    backend-hormonia/tests/unit/test_adk_tools_runtime.py
  </files>
  <behavior>
    - Test 1: each of the four tool adapters delegates to the correct `GeminiDomainClient` method and returns `{status, result}`.
    - Test 2: wrapper `_invoke_adk` delegates to runtime helper and never raises `NotImplementedError`.
    - Test 3: runtime execution path remains CI-compliant (no direct `.run()`/`runner.run*` calls outside approved wrappers).
  </behavior>
  <action>
    Implement ADK-06 with existing behavior reuse:
    - Build `tools.py` adapters that call `GeminiDomainClient` methods for sentiment/humanize/variation/empathy, keeping explicit typed parameters for schema inference.
    - Build `runtime.py` runner/session composition around ADK primitives (including `InMemorySessionService`) and expose one async helper that executes one selected tool invocation and returns normalized output text/payload.
    - Implement `PIISafeADKWrapper._invoke_adk` to call runtime helper with sanitized prompt, preserving the existing `safe_run` boundary contract.
    - Keep safety boundary ownership in wrapper: do not invoke ADK runtime directly from routers.
    - Respect existing CI policy: if a direct run primitive is required, keep it inside approved wrapper/runtime path so `scripts/check_agent_run_calls.py` remains green.
    - Add focused unit tests in `test_adk_tools_runtime.py` using monkeypatch/mocks and synthetic data only.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest -q tests/unit/test_adk_tools_runtime.py tests/unit/test_pii_safe_adk_wrapper.py</automated>
  </verify>
  <done>
    Four adapters are callable through runtime, wrapper invokes runtime successfully, and unit regressions pass with synthetic payloads.
  </done>
</task>

</tasks>

<verification>
1. `pytest -q tests/unit/test_adk_tools_runtime.py tests/unit/test_pii_safe_adk_wrapper.py` passes.
2. `python3 scripts/check_agent_run_calls.py` remains green after ADK runtime/tool additions.
</verification>

<success_criteria>
- ADK-06 is satisfied: sentiment/humanize/variation/empathy are wrapped behind ADK tool contracts.
- PIISafeADKWrapper now performs real ADK runtime delegation instead of raising NotImplementedError.
</success_criteria>

<output>
After completion, create `.planning/phases/41-adk-agent-integration/41-01-SUMMARY.md`
</output>
