---
phase: 41-adk-agent-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/hive_mind_integration.py
  - backend-hormonia/tests/unit/test_hive_mind_langgraph_removal.py
autonomous: true
requirements:
  - ADK-08

must_haves:
  truths:
    - "HiveMind integration no longer contains LangGraph-only execution mode"
    - "Daily flow processing no longer references removed LangGraph counters/branches"
    - "HiveMind integration module imports successfully without LangGraph-only tombstone references"
  artifacts:
    - path: "backend-hormonia/app/services/hive_mind_integration.py"
      provides: "LangGraph-only branch removal while preserving remaining integration modes"
      contains: "IntegrationMode"
    - path: "backend-hormonia/tests/unit/test_hive_mind_langgraph_removal.py"
      provides: "Regression checks for removed symbols and import safety"
      contains: "LANGGRAPH_ONLY"
  key_links:
    - from: "backend-hormonia/tests/unit/test_hive_mind_langgraph_removal.py"
      to: "backend-hormonia/app/services/hive_mind_integration.py"
      via: "source and import regression assertions"
      pattern: "_process_with_langgraph"
---

<objective>
Remove LangGraph dead code from HiveMind integration so the service cannot enter a crash-prone unsupported mode.

Purpose: ADK-08 explicitly requires deleting `LANGGRAPH_ONLY` and `_process_with_langgraph()` to eliminate production crash risk from dead tombstone paths.

Output: cleaned integration mode enum/branches and regression tests proving zero residual references.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-adk-agent-integration/41-RESEARCH.md

@backend-hormonia/app/services/hive_mind_integration.py
@backend-hormonia/tests/unit/test_adk_run_guard_regression.py

<interfaces>
From `backend-hormonia/app/services/hive_mind_integration.py` current dead-path symbols:
```python
class IntegrationMode(Enum):
    LANGGRAPH_ONLY = "langgraph_only"

async def _process_with_langgraph(...): ...
```

Current branch points to remove cohesively:
```python
if self.integration_mode == IntegrationMode.LANGGRAPH_ONLY:
    ...
results["langgraph_processed"] = ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove LANGGRAPH_ONLY mode and LangGraph-only processing path</name>
  <files>
    backend-hormonia/app/services/hive_mind_integration.py
  </files>
  <behavior>
    - Test 1: `IntegrationMode` no longer contains `LANGGRAPH_ONLY`.
    - Test 2: `process_daily_flows` no longer tracks `langgraph_processed` or calls `_process_with_langgraph`.
    - Test 3: remaining modes (`FLOW_ENGINE_ONLY`, `HIVE_MIND_ONLY`, `HYBRID`, `GRADUAL_MIGRATION`) continue to route correctly.
  </behavior>
  <action>
    Perform cohesive dead-code elimination in one pass:
    - Remove `IntegrationMode.LANGGRAPH_ONLY` enum value.
    - Delete `_process_with_langgraph()` implementation and all references.
    - Remove `langgraph_processed` result field/counters and adjust total calculation accordingly.
    - Update `_should_use_agents` logic to remove LANGGRAPH_ONLY branch while preserving other mode behaviors.

    Do not change unrelated behavior in quiz conduction or swarm manager lifecycle.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -c "from app.services.hive_mind_integration import HiveMindIntegrationService, IntegrationMode; print('ok', [m.value for m in IntegrationMode])"</automated>
  </verify>
  <done>
    LangGraph-only mode and processing method are absent, and module import succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests and grep gate for dead-code removal</name>
  <files>
    backend-hormonia/tests/unit/test_hive_mind_langgraph_removal.py
  </files>
  <behavior>
    - Test 1: source scan asserts `LANGGRAPH_ONLY` and `_process_with_langgraph` are absent.
    - Test 2: import test validates module and exported service class load successfully.
    - Test 3: runtime enum assertion confirms supported mode set excludes LangGraph-only mode.
  </behavior>
  <action>
    Add focused unit regression coverage:
    - Create tests that read module text and assert no forbidden symbols remain.
    - Add import-level test using actual exported symbol names (`HiveMindIntegrationService`) to avoid roadmap alias drift.
    - Keep tests deterministic and independent from external services.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest -q tests/unit/test_hive_mind_langgraph_removal.py && rg -n "LANGGRAPH_ONLY|_process_with_langgraph" app/services/hive_mind_integration.py</automated>
  </verify>
  <done>
    Regression guard enforces permanent removal and command gate shows zero forbidden symbol matches.
  </done>
</task>

</tasks>

<verification>
1. `pytest -q tests/unit/test_hive_mind_langgraph_removal.py` passes.
2. `python3 -c "from app.services.hive_mind_integration import HiveMindIntegrationService"` succeeds.
3. `rg -n "LANGGRAPH_ONLY|_process_with_langgraph" app/services/hive_mind_integration.py` returns no matches.
</verification>

<success_criteria>
- ADK-08 is satisfied with complete dead-code removal for LangGraph-only path.
- HiveMind integration remains import-safe and mode-consistent after cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/41-adk-agent-integration/41-03-SUMMARY.md`
</output>
