---
phase: 50-pipeline-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/flow/_flow_response_flow.py
  - backend-hormonia/app/services/flow/sequential_response_gate.py
  - backend-hormonia/app/services/flow/_flow_orchestration_utils.py
  - backend-hormonia/tests/unit/services/flow/test_sequential_gate_mismatch_recovery.py
autonomous: true
requirements: [FLOW-01]
must_haves:
  truths:
    - "When sequential gate encounters a context mismatch, the flow retries with corrected context instead of waiting silently forever"
    - "After MAX_CONTEXT_MISMATCH_RETRIES consecutive mismatches, the flow resets awaiting_response=False and logs a structured warning"
    - "A single mismatch that self-corrects on the next response does not count toward the limit"
  artifacts:
    - path: "backend-hormonia/app/services/flow/_flow_response_flow.py"
      provides: "Mismatch recovery logic in load_response_context"
      contains: "context_mismatch_count"
    - path: "backend-hormonia/app/services/flow/sequential_response_gate.py"
      provides: "reset_awaiting_on_mismatch_limit helper"
      contains: "def reset_awaiting_on_mismatch_limit"
    - path: "backend-hormonia/tests/unit/services/flow/test_sequential_gate_mismatch_recovery.py"
      provides: "Tests for mismatch recovery"
      min_lines: 80
  key_links:
    - from: "backend-hormonia/app/services/flow/_flow_response_flow.py"
      to: "backend-hormonia/app/services/flow/sequential_response_gate.py"
      via: "import reset_awaiting_on_mismatch_limit"
      pattern: "reset_awaiting_on_mismatch_limit"
    - from: "backend-hormonia/app/services/flow/_flow_response_flow.py"
      to: "step_data[context_mismatch_count]"
      via: "increment and check in load_response_context"
      pattern: "context_mismatch_count"
---

<objective>
Fix silent patient stall when sequential gate encounters a context mismatch.

Purpose: Currently, when `load_response_context` detects a mismatch between the inbound response context and the pending flow state (e.g., wrong flow_day, wrong message_index), it returns `status: "waiting"` with `reason: "context_mismatch"`. The patient stays stuck forever because no recovery mechanism exists. This plan adds a counter-based recovery: after a configurable number of consecutive mismatches, the flow resets `awaiting_response=False` so the daily flow processor can re-send the prompt on the next cycle.

Output: Modified `_flow_response_flow.py` with mismatch counting and auto-reset, helper in `sequential_response_gate.py`, and comprehensive unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend-hormonia/app/services/flow/_flow_response_flow.py
@backend-hormonia/app/services/flow/sequential_response_gate.py
@backend-hormonia/app/services/flow/_flow_orchestration_utils.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/services/flow/_flow_orchestration_utils.py:
```python
class FlowMessageState(TypedDict, total=False):
    patient_id: UUID
    response_context: Optional[FlowResponseContext]
    day_number: int
    flow_kind: str
    flow_state_id: UUID
    flow_state_step_data: Dict[str, Any]
    # ... more fields

def _collect_response_context_mismatches(
    expected_context: Dict[str, Any],
    received_context: Dict[str, Any],
) -> Dict[str, Dict[str, Any]]:
    """Returns dict of field -> {expected, received} for mismatched fields."""
```

From app/services/flow/sequential_response_gate.py:
```python
def evaluate_sequential_gate(
    step_data: dict[str, Any],
    response_context: Optional[dict[str, Any]],
) -> tuple[bool, str, dict[str, Any]]:
    """Returns (allowed, reason, normalized_context)"""
```

step_data keys used for flow state:
- `awaiting_response`: bool — whether flow is waiting
- `current_flow_day`: int — current day
- `flow_kind`: str — flow type key
- `current_day_message_index`: int — message position
- `pending_response_context`: dict — expected correlation context
- `context_mismatch_count`: int (NEW) — consecutive mismatch counter
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mismatch counter and auto-reset to sequential gate recovery</name>
  <files>
    backend-hormonia/app/services/flow/_flow_response_flow.py,
    backend-hormonia/app/services/flow/sequential_response_gate.py,
    backend-hormonia/app/services/flow/_flow_orchestration_utils.py,
    backend-hormonia/tests/unit/services/flow/test_sequential_gate_mismatch_recovery.py
  </files>
  <behavior>
    - Test 1: When mismatch occurs and context_mismatch_count < MAX (default 3), response returns status=waiting with incremented count persisted in step_data
    - Test 2: When mismatch occurs and context_mismatch_count reaches MAX, awaiting_response is reset to False, pending_response_context is cleared, context_mismatch_count is reset to 0, and status=context_mismatch_reset is returned
    - Test 3: When a successful match occurs (no mismatch), context_mismatch_count is reset to 0
    - Test 4: When response_context is empty/None (auto-built from expected), existing behavior is preserved (no mismatch counted)
    - Test 5: The MAX_CONTEXT_MISMATCH_RETRIES constant is configurable (default 3)
  </behavior>
  <action>
1. In `sequential_response_gate.py`, add:
   - Constant `MAX_CONTEXT_MISMATCH_RETRIES = 3`
   - Function `reset_awaiting_on_mismatch_limit(step_data: dict, mismatches: dict, db_commit_fn) -> tuple[bool, dict]` that:
     a. Reads `step_data.get("context_mismatch_count", 0)`
     b. Increments it
     c. If count >= MAX_CONTEXT_MISMATCH_RETRIES:
        - Sets `step_data["awaiting_response"] = False`
        - Removes `step_data["pending_response_context"]`
        - Sets `step_data["context_mismatch_count"] = 0`
        - Sets `step_data["last_mismatch_reset_at"]` to ISO timestamp
        - Returns `(True, {"status": "context_mismatch_reset", "mismatches": mismatches, "reset_after": count})`
     d. Otherwise persists the incremented count and returns `(False, {"status": "waiting", "reason": "context_mismatch", "mismatches": mismatches, "mismatch_count": count})`

2. In `_flow_response_flow.py`, modify the mismatch handling block in `load_response_context` (around line 109-127):
   - After detecting mismatches, call `reset_awaiting_on_mismatch_limit(step_data, mismatches, commit_fn)`
   - If reset occurred (True returned):
     - Persist step_data to flow_state and commit via `asyncio.to_thread(handler.db.commit)`
     - Log a structured warning with patient_id, mismatch details, and reset count
     - Return the reset result dict
   - If not reset (False returned):
     - Persist the incremented count in step_data, commit
     - Return the waiting result (existing behavior but with count)

3. In the success path of `load_response_context` (around line 129 where next_index is computed), add `step_data["context_mismatch_count"] = 0` to reset the counter on successful match.

4. In `_flow_orchestration_utils.py`, no changes needed (step_data is a flexible dict).

5. Create test file with pytest-asyncio tests using mocked handler, db, and flow_state objects. Tests should:
   - Mock `handler.db.commit` as a no-op
   - Mock `handler.flow_state_repo.get_active_flow` to return a flow_state with controlled step_data
   - Mock `handler._get_day_config` to return valid day_config
   - Test counter increment on mismatch
   - Test reset on reaching MAX
   - Test counter reset on success
   - Test that default behavior (response_context auto-built from expected, thus no mismatch) is preserved
  </action>
  <verify>
    <automated>cd backend-hormonia && python -m pytest tests/unit/services/flow/test_sequential_gate_mismatch_recovery.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - context_mismatch_count is tracked in step_data and persisted on each mismatch
    - After 3 consecutive mismatches, awaiting_response resets to False so daily processor can re-send
    - Counter resets to 0 on successful match
    - All tests pass
    - Structured log emitted on reset with patient_id and mismatch details
  </done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && python -m pytest tests/unit/services/flow/test_sequential_gate_mismatch_recovery.py -x -v` passes all tests
- `cd backend-hormonia && python -m pytest tests/unit/services/flow/ -x -v` passes (no regressions in existing flow tests)
- grep confirms `context_mismatch_count` appears in both source files and test
</verification>

<success_criteria>
- FLOW-01 satisfied: sequential gate recovers from context mismatches with counter-based auto-reset
- No silent stall: after 3 mismatches, patient flow is unblocked for daily processor to re-send
- Backward compatible: existing flows without context_mismatch_count in step_data default to 0
</success_criteria>

<output>
After completion, create `.planning/phases/50-pipeline-reliability/50-01-SUMMARY.md`
</output>
