---
phase: 03-operational-stability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/tests/api/test_admin_contracts.py
  - backend-hormonia/tests/validation/test_security_comprehensive.py
autonomous: true
requirements:
  - REL-02
  - REL-03

must_haves:
  truths:
    - "grep -r 'from jose' app/ returns zero results in backend-hormonia (no production jose imports)"
    - "grep -r 'from jose' tests/ returns zero results in backend-hormonia (no test jose imports)"
    - "All JWT operations in test files use import jwt (PyJWT) instead of from jose import jwt"
  artifacts:
    - path: "backend-hormonia/tests/api/test_admin_contracts.py"
      provides: "Admin contract tests using PyJWT instead of python-jose"
      contains: "import jwt"
    - path: "backend-hormonia/tests/validation/test_security_comprehensive.py"
      provides: "Security comprehensive tests using PyJWT instead of python-jose"
      contains: "import jwt"
  key_links:
    - from: "backend-hormonia/tests/api/test_admin_contracts.py"
      to: "jwt.encode"
      via: "PyJWT encode call for test token generation"
      pattern: "import jwt.*jwt\\.encode"
    - from: "backend-hormonia/tests/validation/test_security_comprehensive.py"
      to: "jwt.decode"
      via: "PyJWT decode call for security validation test"
      pattern: "import jwt.*jwt\\.decode"
---

<objective>
Remove all remaining `from jose import jwt` imports from test files and replace with `import jwt` (PyJWT), completing the python-jose removal from the codebase. This addresses CVE-2024-23342 (python-ecdsa timing vulnerability in python-jose's dependency chain).

Purpose: python-jose has been removed from `requirements.txt` but two test files still import it. PyJWT (already in production code at `app/core/security.py`) provides an identical API for HS256 encode/decode. The package also remains installed in the venv despite being absent from requirements.txt.

Output: Two modified test files with PyJWT imports; zero `from jose` imports in the entire codebase.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-operational-stability/03-RESEARCH.md
@backend-hormonia/tests/api/test_admin_contracts.py
@backend-hormonia/tests/validation/test_security_comprehensive.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace from jose import jwt with import jwt in both test files</name>
  <files>
    backend-hormonia/tests/api/test_admin_contracts.py
    backend-hormonia/tests/validation/test_security_comprehensive.py
  </files>
  <action>
In both test files, replace the `from jose import jwt` import with `import jwt`:

**File 1: `tests/api/test_admin_contracts.py`** (line 211)
- The import is inside a test function body, not at module level
- Replace `from jose import jwt` with `import jwt`
- The call `jwt.encode({"sub": email, "exp": exp}, secret, algorithm="HS256")` is identical in PyJWT — no other code changes needed
- PyJWT `jwt.encode()` returns `str` in pyjwt >= 2.0 (same as python-jose), so no `.decode()` call needed

**File 2: `tests/validation/test_security_comprehensive.py`** (line 204)
- The import is inside a test function body, not at module level
- Replace `from jose import jwt` with `import jwt`
- The call `jwt.decode(token, "", algorithms=["none"])` works identically in PyJWT — it also raises an exception for "none" algorithm
- The `except Exception: pass` catch-all handles both jose and pyjwt exceptions, so no exception type change needed

**Important:** These are the ONLY two files in the entire codebase that import from jose. Production code (`app/`) already uses only `import jwt` (PyJWT). The report file `docs/reports/security/SECURITY_CODE_REVIEW_REPORT.md` also mentions jose in a code example but that is documentation, not executable code — leave it unchanged.
  </action>
  <verify>
Run: `cd backend-hormonia && grep -rn "from jose" tests/` — must return zero results.
Run: `cd backend-hormonia && grep -rn "from jose" app/` — must return zero results (already clean, but verify).
Run: `cd backend-hormonia && grep -n "import jwt" tests/api/test_admin_contracts.py tests/validation/test_security_comprehensive.py` — must show the new import in both files.
  </verify>
  <done>Both test files use `import jwt` (PyJWT) instead of `from jose import jwt`; zero jose imports remain in app/ and tests/.</done>
</task>

<task type="auto">
  <name>Task 2: Verify python-jose is not imported anywhere and document uninstall command</name>
  <files>
    backend-hormonia/tests/api/test_admin_contracts.py
    backend-hormonia/tests/validation/test_security_comprehensive.py
  </files>
  <action>
After Task 1 completes, run a comprehensive sweep to confirm zero jose usage:

1. Run `grep -r "from jose" backend-hormonia/app/ backend-hormonia/tests/` to confirm zero results
2. Run `grep -r "import jose" backend-hormonia/app/ backend-hormonia/tests/` to confirm zero results (catch `import jose` without `from`)
3. Verify `python-jose` is NOT in `requirements.txt`: `grep -i "python-jose\|jose" backend-hormonia/requirements.txt`

**Note on venv uninstall:** The research identified that python-jose 3.5.0 is still installed in the venv despite being absent from requirements.txt. Running `pip uninstall python-jose -y` in the venv would be ideal, but this is a runtime environment action. The executor should attempt: `cd backend-hormonia && .venv/bin/pip uninstall python-jose -y 2>/dev/null || pip uninstall python-jose -y 2>/dev/null || echo "python-jose not found or no venv available"`. If the venv is not available in the execution environment, this is acceptable — the next clean install from requirements.txt will not include python-jose.

This task is a verification and cleanup step — the primary code changes are in Task 1.
  </action>
  <verify>
Run: `cd backend-hormonia && grep -r "from jose\|import jose" app/ tests/` — must return zero results.
Run: `cd backend-hormonia && grep -i "python-jose" requirements.txt` — must return zero results.
  </verify>
  <done>Zero jose imports exist in the entire backend-hormonia codebase (app/ and tests/); python-jose is confirmed absent from requirements.txt; uninstall attempted from venv if available.</done>
</task>

</tasks>

<verification>
Run from backend-hormonia/:
1. `grep -r "from jose" app/ tests/` — must return zero results (REL-02 success criterion)
2. `grep -r "import jose" app/ tests/` — must return zero results
3. `grep -i "python-jose" requirements.txt` — must return zero results (REL-03 success criterion)
4. `python -c "import jwt; print(jwt.__version__)"` — must print PyJWT version (2.10.1 expected)
</verification>

<success_criteria>
- `grep -r "from jose" app/` returns zero results in backend-hormonia
- `grep -r "from jose" tests/` returns zero results in backend-hormonia
- Both test files use `import jwt` (PyJWT) and their JWT operations work identically
- python-jose is confirmed absent from requirements.txt
- python-jose uninstall attempted from venv (best-effort)
</success_criteria>

<output>
After completion, create `.planning/phases/03-operational-stability/03-03-SUMMARY.md`
</output>
