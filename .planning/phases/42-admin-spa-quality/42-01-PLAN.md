---
phase: 42-admin-spa-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-hormonia/src/features/whatsapp/WhatsAppDashboard.tsx
  - frontend-hormonia/src/features/admin/tabs/AdminSettingsTab.tsx
  - frontend-hormonia/src/lib/runtime-config.ts
  - frontend-hormonia/src/lib/env-validator.ts
  - frontend-hormonia/src/types/api-wave2.ts
  - frontend-hormonia/src/types/system-stats.ts
  - frontend-hormonia/src/lib/api-client/hive-mind.ts
  - frontend-hormonia/src/lib/api-client/index.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03

must_haves:
  truths:
    - "WhatsApp dashboard fetches WuzAPI session status and shows real connection state instead of 'Evolution API disabled' placeholder"
    - "Zero grep hits for VITE_ENABLE_EVOLUTION or VITE_EVOLUTION_API_URL in frontend-hormonia/src/"
    - "hive-mind.ts only exports health() and agents.list() methods that map to real backend endpoints"
    - "Dead hive-mind types (SwarmStatus, IntegrationStatus, AgentMetrics, HiveMindSystemStats, ProcessFlowsResponse, ConductQuizResponse) are removed"
    - "tsc --noEmit exits 0 after all changes"
  artifacts:
    - path: "frontend-hormonia/src/features/whatsapp/WhatsAppDashboard.tsx"
      provides: "WuzAPI-aware WhatsApp dashboard with useQuery polling"
      contains: "useQuery"
    - path: "frontend-hormonia/src/lib/api-client/hive-mind.ts"
      provides: "Trimmed hive-mind API client with only health + agents.list"
      exports: ["AgentStatus", "SystemHealthOverview", "HiveMindApi", "createHiveMindApi"]
    - path: "frontend-hormonia/src/lib/runtime-config.ts"
      provides: "RuntimeConfig without Evolution fields"
  key_links:
    - from: "frontend-hormonia/src/features/whatsapp/WhatsAppDashboard.tsx"
      to: "/api/v2/monitoring/wuzapi/session/status"
      via: "useQuery with apiClient.core.get"
      pattern: "useQuery.*wuzapi.*session.*status"
    - from: "frontend-hormonia/src/lib/api-client/hive-mind.ts"
      to: "/api/v2/hive-mind/health and /api/v2/hive-mind/agents"
      via: "client.get calls"
      pattern: "client\\.get.*hive-mind"
---

<objective>
Remove all Evolution API dead code from the admin SPA and trim the hive-mind.ts API client to only call backend endpoints that actually exist, aligning frontend types with real backend contracts.

Purpose: Physicians currently see "WhatsApp Integration Disabled — set VITE_ENABLE_EVOLUTION=true" on the WhatsApp dashboard, which is confusing because Evolution was replaced by WuzAPI in v1.6. The hive-mind API client calls 11 endpoints that 404 in the backend. Both issues erode trust in the admin panel.

Output: WhatsApp dashboard shows real WuzAPI connection status; hive-mind.ts trimmed to 2 working endpoints; all Evolution env var references gone; all dead hive-mind types removed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-admin-spa-quality/42-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend /api/v2/monitoring/wuzapi/session/status (contract):
```
GET /api/v2/monitoring/wuzapi/session/status
Response: {
  "connected": boolean,
  "logged_in": boolean,
  "timestamp": string,   // ISO8601
  "mock": boolean,       // optional
  "error": string        // optional
}
```

From frontend-hormonia/src/lib/api-client/core.ts:
```typescript
export class ApiClientCore {
  get<T>(path: string, params?: Record<string, unknown>): Promise<T>;
  post<T>(path: string, data?: unknown, params?: Record<string, unknown>): Promise<T>;
  put<T>(path: string, data?: unknown, params?: Record<string, unknown>): Promise<T>;
}
```

From frontend-hormonia/src/lib/api-client/hive-mind.ts (KEEP these types):
```typescript
export interface AgentStatus { agent_id: string; agent_type: string; status: string; capabilities: string[]; health?: { ... } }
export interface SystemHealthOverview { status: 'healthy' | 'degraded' | 'down'; timestamp: string; system_health: { ... }; system_alerts: number; }
```

From frontend-hormonia/src/lib/api-client/hive-mind.ts (REMOVE these types):
```typescript
SwarmStatus, IntegrationStatus, AgentMetrics, HiveMindSystemStats, ProcessFlowsResponse, ConductQuizResponse
```

From frontend-hormonia/src/lib/api-client/index.ts (line 136):
```typescript
export type * from "./hive-mind";  // Re-exports all types — must survive after trimming
```

Backend hive-mind endpoints that EXIST:
- GET /api/v2/hive-mind/health -> SystemHealthOverview
- GET /api/v2/hive-mind/agents -> { agents: AgentStatus[], total_agents: number, timestamp: string }

Backend hive-mind endpoints that DO NOT EXIST (all others in hive-mind.ts):
- agents.get, agents.metrics, alerts, integration.*, swarm.*, tasks.*, stats
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Evolution dead code and replace WhatsApp dashboard guard with WuzAPI status</name>
  <files>
    frontend-hormonia/src/features/whatsapp/WhatsAppDashboard.tsx,
    frontend-hormonia/src/features/admin/tabs/AdminSettingsTab.tsx,
    frontend-hormonia/src/lib/runtime-config.ts,
    frontend-hormonia/src/lib/env-validator.ts,
    frontend-hormonia/src/types/api-wave2.ts,
    frontend-hormonia/src/types/system-stats.ts
  </files>
  <action>
    **Step 1 — Remove all access sites FIRST (before interface changes):**

    1a. In `WhatsAppDashboard.tsx`:
    - Remove the `isEvolutionEnabled` state variable and its `setIsEvolutionEnabled` call
    - Remove the `config?.VITE_ENABLE_EVOLUTION === 'true'` guard block (the entire `if (!isEvolutionEnabled)` early return that shows "WhatsApp Integration Disabled")
    - Remove the `useEffect` that checks `config?.VITE_ENABLE_EVOLUTION` and sets up the old polling interval
    - Remove the `useConfig()` import if no other config fields are used (check first)
    - Add a `useQuery` import from `@tanstack/react-query`
    - Add a `WuzAPISessionStatus` interface: `{ connected: boolean; logged_in: boolean; timestamp: string; mock?: boolean; error?: string }`
    - Add a `useQuery` call to fetch WuzAPI status from `/monitoring/wuzapi/session/status` (use `apiClient` core.get — the base URL already includes `/api/v2`; check how other API calls are made in the codebase; hive-mind.ts uses full paths like `/api/v2/hive-mind/health` so use the same pattern: `/api/v2/monitoring/wuzapi/session/status`). Set `refetchInterval: 30_000` and `retry: 2`.
    - Import `apiClient` from `@/lib/api-client` if not already imported.
    - Replace the old disabled-state guard with a WuzAPI-aware status card that shows:
      - When loading: a spinner or "Checking WhatsApp connection..."
      - When connected (`wuzStatus.connected === true`): Proceed to show the full dashboard (no guard block)
      - When disconnected (`wuzStatus.connected === false && !isLoading`): Show a card saying "WhatsApp (WuzAPI) not connected" with timestamp
      - When error: Show "WhatsApp (WuzAPI) connection status unavailable — check server configuration"
    - Keep the rest of the dashboard unchanged (tabs, messaging, analytics sections)
    - Keep `loadDashboardData` and its `whatsAppService` calls — those fetch instance/queue/message data which are separate from Evolution

    1b. In `AdminSettingsTab.tsx`:
    - Remove lines 202-209 (the "Evolution API URL" label + input field and its enclosing `<div className="space-y-2">`)
    - Keep the surrounding Card ("Integracoes") and the "Atualizar Integracoes" button — just remove the Evolution URL field
    - Remove any `config?.VITE_EVOLUTION_API_URL` reference

    1c. In `runtime-config.ts`:
    - Remove `VITE_ENABLE_EVOLUTION?: string` and `VITE_EVOLUTION_API_URL?: string` from the `RuntimeConfig` interface
    - Remove the two corresponding lines from the `CONFIG` object (lines 93-94)
    - Remove the `// Evolution and Demo Configuration` comment group header (keep VITE_SHOW_DEMO_CREDENTIALS in a "Demo Configuration" group)

    1d. In `env-validator.ts`:
    - Remove the `VITE_ENABLE_EVOLUTION` validation rule (lines 266-272)
    - Remove the `VITE_EVOLUTION_API_URL` validation rule (lines 273-276)
    - Update the `// Evolution and Demo Configuration` comment to just `// Demo Configuration`

    1e. In `types/api-wave2.ts`:
    - Find `ServiceStatusMetrics` interface — remove the `evolution_api: ServiceStatus` field entirely
    - Do NOT rename to wuzapi — the backend `/admin/system-stats` endpoint does not return `evolution_api` at all per the research

    1f. In `types/system-stats.ts`:
    - Find `ServiceStatusMetrics` interface — remove the `evolution_api` field entirely

    **Run `tsc --noEmit` after EACH file change** to catch errors incrementally. If a consumer still references a removed field, fix the consumer before proceeding.
  </action>
  <verify>
    <automated>cd frontend-hormonia && grep -r "VITE_ENABLE_EVOLUTION\|VITE_EVOLUTION_API_URL" src/ | wc -l | xargs test 0 -eq && npx tsc --noEmit && npx eslint . --max-warnings 50</automated>
  </verify>
  <done>
    - Zero grep hits for VITE_ENABLE_EVOLUTION and VITE_EVOLUTION_API_URL in frontend-hormonia/src/
    - WhatsApp dashboard fetches WuzAPI session status via useQuery and shows real connection state
    - AdminSettingsTab no longer shows Evolution API URL field
    - evolution_api field removed from both ServiceStatusMetrics type definitions
    - tsc --noEmit exits 0
    - eslint exits with 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Trim hive-mind.ts to only real backend endpoints and align API client types</name>
  <files>
    frontend-hormonia/src/lib/api-client/hive-mind.ts,
    frontend-hormonia/src/lib/api-client/index.ts
  </files>
  <action>
    **Step 1 — Trim hive-mind.ts:**

    Keep ONLY these types (used by AgentSwarm.tsx and SystemHealth.tsx):
    - `AgentStatus` (used by AgentSwarm.tsx line 13)
    - `SystemHealthOverview` (used by SystemHealth.tsx line 8)

    Remove these dead types (only consumed by dead API methods):
    - `SwarmStatus`
    - `IntegrationStatus`
    - `AgentMetrics`
    - `HiveMindSystemStats`
    - `ProcessFlowsResponse`
    - `ConductQuizResponse`

    Trim `HiveMindApi` interface to ONLY:
    ```typescript
    export interface HiveMindApi {
      health: () => Promise<SystemHealthOverview>;
      agents: {
        list: () => Promise<{ agents: AgentStatus[]; total_agents: number; timestamp: string }>;
      };
    }
    ```

    Remove from HiveMindApi:
    - `agents.get`, `agents.metrics`
    - `alerts`
    - `integration` (getStatus, setMode, setMigrationPercentage)
    - `swarm` (getStatus, getAgents)
    - `tasks` (processFlows, conductQuiz)
    - `stats`

    Trim `createHiveMindApi` function to ONLY return `health` and `agents.list`:
    ```typescript
    export function createHiveMindApi(client: ApiClientCore): HiveMindApi {
      return {
        health: () => client.get("/api/v2/hive-mind/health"),
        agents: {
          list: () => client.get("/api/v2/hive-mind/agents"),
        },
      };
    }
    ```

    **Step 2 — Verify re-exports in index.ts:**

    Line 136 of `index.ts` has `export type * from "./hive-mind"`. This will continue to work — it now just exports fewer types. Verify no other file in the codebase imports the removed types.

    Search for consumers of removed types:
    - `grep -rn "SwarmStatus\|IntegrationStatus\|AgentMetrics\|HiveMindSystemStats\|ProcessFlowsResponse\|ConductQuizResponse" frontend-hormonia/src/`
    - If any consumer found, update or remove the import.

    Also search for consumers of removed API methods:
    - `grep -rn "hiveMind\.agents\.get\|hiveMind\.agents\.metrics\|hiveMind\.alerts\|hiveMind\.integration\|hiveMind\.swarm\|hiveMind\.tasks\|hiveMind\.stats" frontend-hormonia/src/`
    - If any consumer found, remove the dead call.

    **Run `tsc --noEmit` after changes** to confirm no type errors.
  </action>
  <verify>
    <automated>cd frontend-hormonia && npx tsc --noEmit && npx eslint . --max-warnings 50 && grep -c "SwarmStatus\|IntegrationStatus\|AgentMetrics\|HiveMindSystemStats\|ProcessFlowsResponse\|ConductQuizResponse" src/lib/api-client/hive-mind.ts | xargs test 0 -eq</automated>
  </verify>
  <done>
    - hive-mind.ts exports only AgentStatus, SystemHealthOverview, HiveMindApi, createHiveMindApi
    - HiveMindApi interface has only health() and agents.list()
    - Six dead types removed from hive-mind.ts
    - No consumers of removed types or methods remain anywhere in src/
    - tsc --noEmit exits 0
    - eslint exits with 0 errors
  </done>
</task>

</tasks>

<verification>
Run from frontend-hormonia/:
1. `grep -r "VITE_ENABLE_EVOLUTION\|VITE_EVOLUTION_API_URL" src/` — must return empty
2. `grep -r "SwarmStatus\|IntegrationStatus\|AgentMetrics\|HiveMindSystemStats\|ProcessFlowsResponse\|ConductQuizResponse" src/` — must return empty
3. `npx tsc --noEmit` — must exit 0
4. `npx eslint .` — must exit with 0 errors
5. `grep "useQuery" src/features/whatsapp/WhatsAppDashboard.tsx` — must match (WuzAPI polling)
</verification>

<success_criteria>
- Zero Evolution API references in frontend-hormonia/src/
- WhatsApp dashboard shows WuzAPI connection status (not "Evolution disabled" banner)
- hive-mind.ts has exactly 2 API methods (health, agents.list) matching 2 real backend endpoints
- All dead hive-mind types removed
- tsc and eslint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/42-admin-spa-quality/42-01-SUMMARY.md`
</output>
