---
phase: 05-flow-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/dispatcher.py
  - backend-hormonia/app/services/flow/__init__.py
  - backend-hormonia/app/services/flow/config.py
  - backend-hormonia/app/service_provider.py
  - backend-hormonia/app/dependencies/service_dependencies.py
  - backend-hormonia/app/services/flow/manager.py
  - backend-hormonia/app/services/flow/core/__init__.py
  - backend-hormonia/app/services/flow/core/context.py
  - backend-hormonia/app/services/flow/core/engine.py
  - backend-hormonia/app/services/flow/core/lifecycle.py
  - backend-hormonia/app/services/flow/core/manager.py
  - backend-hormonia/app/services/flow/core/state_machine.py
  - backend-hormonia/app/services/flow/errors/__init__.py
  - backend-hormonia/app/services/flow/errors/handler.py
  - backend-hormonia/app/services/flow/errors/recovery.py
  - backend-hormonia/app/services/flow/errors/retry.py
  - backend-hormonia/app/services/flow/execution/__init__.py
  - backend-hormonia/app/services/flow/execution/conditions.py
  - backend-hormonia/app/services/flow/execution/executor.py
  - backend-hormonia/app/services/flow/execution/scheduler.py
  - backend-hormonia/app/services/flow/execution/transitions.py
  - backend-hormonia/app/services/flow/integrations/__init__.py
  - backend-hormonia/app/services/flow/integrations/ai_integration.py
  - backend-hormonia/app/services/flow/integrations/base.py
  - backend-hormonia/app/services/flow/integrations/manager.py
  - backend-hormonia/app/services/flow/integrations/plugins.py
  - backend-hormonia/app/services/flow/integrations/quiz_integration.py
  - backend-hormonia/app/services/flow/validation/__init__.py
  - backend-hormonia/app/services/flow/validation/constraints.py
  - backend-hormonia/app/services/flow/validation/integrity.py
  - backend-hormonia/app/services/flow/validation/rules.py
  - backend-hormonia/app/services/flow/validation/validator.py
  - backend-hormonia/tests/services/flow/core/test_engine.py
  - backend-hormonia/tests/services/flow/core/test_error_handler.py
  - backend-hormonia/tests/services/flow/core/__init__.py
  - backend-hormonia/tests/services/flow/integrations/test_manager.py
  - backend-hormonia/tests/services/flow/integrations/test_quiz_integration.py
  - backend-hormonia/tests/services/flow/integrations/test_ai_integration.py
  - backend-hormonia/tests/services/flow/integrations/__init__.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "Production flow system (flow_core.py / EnhancedFlowEngine) is the sole canonical system -- no alternative code path exists"
    - "FlowDispatcher facade routes all flow enrollment to the production system via PatientFlowService"
    - "QW-021 package (core/, errors/, execution/, integrations/, validation/) is fully deleted from the codebase"
    - "service_provider.flow_service returns FlowDispatcher instead of deleted FlowManager"
    - "flow/__init__.py no longer imports from deleted QW-021 modules"
  artifacts:
    - path: "backend-hormonia/app/services/dispatcher.py"
      provides: "FlowDispatcher facade with patient-type routing"
      contains: "class FlowDispatcher"
    - path: "backend-hormonia/app/services/flow/__init__.py"
      provides: "Updated exports without FlowManager/FlowEngine from QW-021"
    - path: "backend-hormonia/app/service_provider.py"
      provides: "Updated flow_service property returning FlowDispatcher"
      contains: "FlowDispatcher"
  key_links:
    - from: "backend-hormonia/app/service_provider.py"
      to: "backend-hormonia/app/services/dispatcher.py"
      via: "flow_service property returns FlowDispatcher instance"
      pattern: "FlowDispatcher"
    - from: "backend-hormonia/app/services/dispatcher.py"
      to: "backend-hormonia/app/services/patient/flow_service.py"
      via: "initialize_flow delegates to PatientFlowService.initialize_default_flow"
      pattern: "PatientFlowService"
    - from: "backend-hormonia/app/services/flow/__init__.py"
      to: "backend-hormonia/app/services/flow/config.py"
      via: "FlowFeatureFlags still exported from config"
      pattern: "FlowFeatureFlags"
---

<objective>
Designate production flow system as canonical, create FlowDispatcher facade, delete QW-021 package, and update all callers.

Purpose: Eliminate the dual flow system that causes silent patient state divergence. The production system (flow_core.py, EnhancedFlowEngine) has 59 external call sites vs QW-021's 7 (all in service_provider.py). The production system is the clear canonical choice.

Output: FlowDispatcher facade at `app/services/dispatcher.py`, QW-021 package fully deleted, service_provider and flow/__init__.py updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flow-consolidation/05-RESEARCH.md

@backend-hormonia/app/service_provider.py
@backend-hormonia/app/services/flow/__init__.py
@backend-hormonia/app/services/flow/config.py
@backend-hormonia/app/services/patient/flow_service.py
@backend-hormonia/app/dependencies/service_dependencies.py
@backend-hormonia/app/services/flow/manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FlowDispatcher facade and update FlowFeatureFlags</name>
  <files>
    backend-hormonia/app/services/dispatcher.py
    backend-hormonia/app/services/flow/config.py
  </files>
  <action>
    1. Create `backend-hormonia/app/services/dispatcher.py` with `FlowDispatcher` class:
       - Constructor takes `db` (SQLAlchemy Session)
       - `async def initialize_flow(self, patient, current_user_id=None, auto_commit=True) -> Optional[PatientFlowState]`: delegates to `PatientFlowService(self.db).initialize_default_flow()` with audit logging
       - `def is_new_patient(self, patient_id: UUID) -> bool`: checks if patient has active flow state via `FlowStateRepository`
       - Feature flag: read `FLOW_CANONICAL_SYSTEM` from settings (default "production"). Log routing decision at INFO level for audit during transition period (per Claude's discretion on routing audit logging).
       - The dispatcher is ONLY for enrollment routing. It does NOT wrap advance_flow, pause, resume, or other operations -- those continue going directly to production services (EnhancedFlowEngine, FlowManagementService).
       - Use lazy imports inside methods to avoid circular import issues (same pattern as service_provider.py).

    2. Update `backend-hormonia/app/services/flow/config.py` `FlowFeatureFlags` class:
       - Replace `use_consolidated_flows` and `consolidated_flows_rollout_percentage` fields with:
         - `canonical_system: str = Field(default="production", description="Canonical flow system: 'production' (flat files)")`
         - `route_new_patients_to_canonical: bool = Field(default=True, description="Route new patients to canonical system immediately")`
         - `route_existing_patients_to_canonical: bool = Field(default=True, description="Route existing patients to canonical system")`
         - `log_dispatcher_routing: bool = Field(default=True, description="Log routing decisions for audit during migration")`
       - Keep `model_config = {"env_prefix": "FLOW_FEATURE_"}` for pydantic-settings env var binding.
       - Remove `should_use_consolidated_for_flow()` method (percentage-based routing). Per user decision: new-vs-existing patient routing only, not percentage-based.
       - If other FlowFeatureFlags fields exist that are used by production code (unrelated to QW-021), keep them.
  </action>
  <verify>
    Run: `cd backend-hormonia && python -c "from app.services.dispatcher import FlowDispatcher; print('FlowDispatcher imported OK')"` succeeds.
    Run: `cd backend-hormonia && python -c "from app.services.flow.config import FlowFeatureFlags; ff = FlowFeatureFlags(); print(ff.canonical_system)"` prints "production".
  </verify>
  <done>
    FlowDispatcher class exists with initialize_flow() and is_new_patient() methods. FlowFeatureFlags has patient-type routing fields instead of percentage-based rollout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete QW-021 package and update all callers</name>
  <files>
    backend-hormonia/app/services/flow/core/ (DELETE entire directory)
    backend-hormonia/app/services/flow/errors/ (DELETE entire directory)
    backend-hormonia/app/services/flow/execution/ (DELETE entire directory)
    backend-hormonia/app/services/flow/integrations/ (DELETE entire directory)
    backend-hormonia/app/services/flow/validation/ (DELETE entire directory)
    backend-hormonia/app/services/flow/manager.py (DELETE)
    backend-hormonia/app/services/flow/__init__.py
    backend-hormonia/app/service_provider.py
    backend-hormonia/app/dependencies/service_dependencies.py
    backend-hormonia/tests/services/flow/core/ (DELETE entire directory)
    backend-hormonia/tests/services/flow/integrations/ (DELETE entire directory)
  </files>
  <action>
    **CRITICAL: Per user decision, full code deletion -- NOT tombstone pattern. Use `git rm -r` for directories.**

    **Step 1: Delete QW-021 application code** (confirmed zero production callers outside the package itself):
    ```
    git rm -r backend-hormonia/app/services/flow/core/
    git rm -r backend-hormonia/app/services/flow/errors/
    git rm -r backend-hormonia/app/services/flow/execution/
    git rm -r backend-hormonia/app/services/flow/integrations/
    git rm -r backend-hormonia/app/services/flow/validation/
    git rm backend-hormonia/app/services/flow/manager.py
    ```

    **Step 2: Delete QW-021 test code**:
    ```
    git rm -r backend-hormonia/tests/services/flow/core/
    git rm -r backend-hormonia/tests/services/flow/integrations/
    ```
    Check if `backend-hormonia/tests/services/flow/__init__.py` still has other test subdirectories. If `tests/services/flow/` becomes empty after deletions, delete it too. If other test files remain (e.g., tests for sequential_message_handler), keep the directory.

    **Step 3: Update `backend-hormonia/app/services/flow/__init__.py`:**
    - Remove ALL imports from `.core.manager`, `.core.engine`, `.validation`, `.errors`, `.integrations`
    - Remove `FlowManager`, `FlowEngine`, `FlowValidator`, `FlowErrorHandler`, `FlowIntegrationManager`, `QuizFlowIntegration`, `AIFlowIntegration` from `__all__`
    - Remove `get_flow_manager` factory function (returns deleted FlowManager)
    - Remove `get_integration_manager` export
    - KEEP: imports from `.config` (FlowFeatureFlags, FlowConfig, etc.), `.types` (FlowType, FlowStatus, FlowContext, etc.), `.analytics` (FlowAnalytics, etc.), `.templates` (FlowTemplateManager, etc.)
    - KEEP: `FlowContext` from `.types` -- it is a Pydantic model that may be referenced by analytics/monitoring code for typing purposes. If it causes import errors (because types.py imported something from deleted modules), fix the import chain.
    - Update module docstring: remove QW-021 consolidation progress notes, state "Canonical system: production (flow_core.py / EnhancedFlowEngine)"
    - Update `__version__`, `__consolidation__`, `__status__`, `__progress__` metadata lines at the bottom.

    **Step 4: Update `backend-hormonia/app/service_provider.py`:**
    - Change `flow_service` property (lines 293-296): instead of `from app.services.flow import FlowManager; self._flow_service = FlowManager(self.db)`, change to: `from app.services.dispatcher import FlowDispatcher; self._flow_service = FlowDispatcher(self.db)`
    - Update the type annotation at line 82 from `Optional[FlowManager]` to `Optional["FlowDispatcher"]`
    - Update the TYPE_CHECKING import at line 28 from `from app.services.flow import FlowManager` to `from app.services.dispatcher import FlowDispatcher`
    - Ensure the `flow_engine` property (which returns `EnhancedFlowEngine`) is UNCHANGED -- it already points to the production system.

    **Step 5: Update `backend-hormonia/app/dependencies/service_dependencies.py`:**
    - `get_flow_service()` at line 81-83 returns `services.flow_service`. The return type docstring references FlowEngineIntegrationService. Update the docstring to reference FlowDispatcher. The function body does not need changes (it delegates to service_provider).

    **Step 6: Verify no remaining imports of deleted modules:**
    - Run `grep -rn "from app.services.flow.core" backend-hormonia/app/` -- must return zero results
    - Run `grep -rn "from app.services.flow.errors" backend-hormonia/app/` -- must return zero results
    - Run `grep -rn "from app.services.flow.execution" backend-hormonia/app/` -- must return zero results
    - Run `grep -rn "from app.services.flow.integrations" backend-hormonia/app/` -- must return zero results
    - Run `grep -rn "from app.services.flow.validation" backend-hormonia/app/` -- must return zero results
    - Run `grep -rn "from app.services.flow import FlowManager" backend-hormonia/app/` -- must return zero results
    - Also check `from app.services.flow import FlowEngine` -- if the webhook handler at `app/services/webhook/handlers/message_handler.py` line 19 imports `FlowEngine` from `app.services.flow`, this must be traced to see if it references the QW-021 engine or a re-export of `EnhancedFlowEngine`. If it is the QW-021 engine, update the import to use the correct production engine class directly.

    **Step 7: Clean up `__pycache__` directories for deleted packages (optional but recommended):**
    Remove `__pycache__/` directories under the deleted paths if they exist.
  </action>
  <verify>
    1. `ls backend-hormonia/app/services/flow/core/ 2>/dev/null` returns nothing (directory deleted)
    2. `ls backend-hormonia/app/services/flow/errors/ 2>/dev/null` returns nothing
    3. `ls backend-hormonia/app/services/flow/execution/ 2>/dev/null` returns nothing
    4. `ls backend-hormonia/app/services/flow/integrations/ 2>/dev/null` returns nothing
    5. `ls backend-hormonia/app/services/flow/validation/ 2>/dev/null` returns nothing
    6. `ls backend-hormonia/app/services/flow/manager.py 2>/dev/null` returns nothing
    7. `cd backend-hormonia && python -c "from app.services.flow import FlowType, FlowFeatureFlags; print('flow __init__ OK')"` succeeds
    8. `cd backend-hormonia && python -c "from app.service_provider import ServiceProvider; print('service_provider OK')"` succeeds
    9. `grep -rn "from app.services.flow.core" backend-hormonia/app/` returns zero results
    10. `grep -rn "FlowManager" backend-hormonia/app/services/flow/__init__.py` returns zero results
  </verify>
  <done>
    QW-021 package (5 subdirectories + manager.py) is fully deleted. flow/__init__.py exports only production-relevant symbols. service_provider.flow_service returns FlowDispatcher. No import references to deleted modules remain in app/ code. QW-021 tests are deleted.
  </done>
</task>

</tasks>

<verification>
1. `cd backend-hormonia && python -c "from app.services.dispatcher import FlowDispatcher; print('OK')"` -- FlowDispatcher importable
2. `cd backend-hormonia && python -c "from app.services.flow import FlowType, FlowConfig, FlowFeatureFlags; print('OK')"` -- flow package still works for retained exports
3. `cd backend-hormonia && python -c "from app.service_provider import ServiceProvider; print('OK')"` -- service_provider loads
4. `grep -rn "from app.services.flow.core" backend-hormonia/app/` returns 0 results -- no dangling imports
5. `grep -rn "FlowManager" backend-hormonia/app/` returns 0 results in production code -- class fully removed
6. Existing tests that do NOT depend on QW-021 still pass: `cd backend-hormonia && python -m pytest tests/ -x --ignore=tests/services/flow/core --ignore=tests/services/flow/integrations -q --no-header 2>&1 | tail -5`
</verification>

<success_criteria>
- FlowDispatcher facade exists and routes enrollment to PatientFlowService (production system)
- QW-021 package is fully deleted (not tombstoned) per user decision
- service_provider.flow_service returns FlowDispatcher
- FlowFeatureFlags uses patient-type routing (not percentage-based) per user decision
- Zero dangling imports to deleted modules in app/ code
- Retained flow package exports (FlowType, FlowConfig, analytics, templates) still function
</success_criteria>

<output>
After completion, create `.planning/phases/05-flow-consolidation/05-01-SUMMARY.md`
</output>
