---
phase: 05-flow-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend-hormonia/tests/integration/test_flow_consolidation.py
  - backend-hormonia/tests/integration/conftest.py
autonomous: true
requirements:
  - FLOW-03

must_haves:
  truths:
    - "Integration tests cover new patient onboarding through FlowDispatcher end-to-end"
    - "Integration tests cover flow advancement (day increment) on canonical system"
    - "Integration tests cover alert pipeline evaluation against real flow data"
    - "Tests run against real PostgreSQL (not SQLite or mocked) per user decision"
    - "All integration tests pass in CI with CONFIRM_REAL_DB=1 guard"
  artifacts:
    - path: "backend-hormonia/tests/integration/test_flow_consolidation.py"
      provides: "Integration test suite for unified flow system"
      contains: "class TestFlowConsolidation"
      min_lines: 80
  key_links:
    - from: "backend-hormonia/tests/integration/test_flow_consolidation.py"
      to: "backend-hormonia/app/services/dispatcher.py"
      via: "imports FlowDispatcher and calls initialize_flow"
      pattern: "FlowDispatcher"
    - from: "backend-hormonia/tests/integration/test_flow_consolidation.py"
      to: "backend-hormonia/app/services/flow_alerts.py"
      via: "imports FlowAlertsService and calls evaluate_alerts"
      pattern: "FlowAlertsService"
    - from: "backend-hormonia/tests/integration/test_flow_consolidation.py"
      to: "backend-hormonia/tests/integration/conftest.py"
      via: "uses real_db_session, cleanup_flows, cleanup_patients fixtures"
      pattern: "real_db_session"
---

<objective>
Write integration tests covering the unified flow system end-to-end: onboarding via FlowDispatcher, flow advancement, and alert pipeline evaluation.

Purpose: FLOW-03 requires integration tests that prove the consolidated flow system works end-to-end against real PostgreSQL. These tests verify the QW-021 deletion did not break any production flow behavior and that the FlowDispatcher correctly routes to the canonical system.

Output: Integration test file at `tests/integration/test_flow_consolidation.py` with coverage of onboarding, advancement, and alert pipeline scenarios.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flow-consolidation/05-RESEARCH.md
@.planning/phases/05-flow-consolidation/05-01-SUMMARY.md

@backend-hormonia/tests/integration/conftest.py
@backend-hormonia/app/services/dispatcher.py
@backend-hormonia/app/services/flow_core.py
@backend-hormonia/app/services/enhanced_flow_engine.py
@backend-hormonia/app/services/flow_alerts.py
@backend-hormonia/app/models/flow.py
@backend-hormonia/app/services/patient/flow_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for unified flow system</name>
  <files>
    backend-hormonia/tests/integration/test_flow_consolidation.py
    backend-hormonia/tests/integration/conftest.py
  </files>
  <action>
    Create `backend-hormonia/tests/integration/test_flow_consolidation.py` with a `TestFlowConsolidation` class containing the following test scenarios. All tests must use `@pytest.mark.integration` and `@pytest.mark.asyncio` decorators. Tests must run against real PostgreSQL per user decision.

    **Use existing fixtures** from `tests/integration/conftest.py`:
    - `real_db_session` — real PostgreSQL session with NullPool
    - `cleanup_patients` — tracks patient IDs for post-test cleanup
    - `cleanup_flows` — tracks flow state IDs for post-test cleanup
    - `sample_patient_data` — generates unique patient data with timestamp-based phone

    **If any required fixtures are missing** from the existing conftest (e.g., `cleanup_flows` does not exist), add them following the pattern of existing cleanup fixtures. The existing pattern is: track created IDs in a list, yield, then delete rows from the table in correct FK order.

    **Test scenarios to implement:**

    1. `test_new_patient_onboarding_via_dispatcher`:
       - Create a real Patient record in the DB
       - Call `FlowDispatcher(session).initialize_flow(patient)`
       - Assert: `PatientFlowState` record created in DB
       - Assert: `flow_state.status` is active/initialized
       - Assert: flow_metadata does NOT contain QW-021 format keys (`flow_instance_id`, `steps_completed`)
       - Covers FLOW-01 (canonical system) and FLOW-02 (FlowDispatcher routing)

    2. `test_existing_patient_detected_by_dispatcher`:
       - Create Patient + PatientFlowState records
       - Call `FlowDispatcher(session).is_new_patient(patient_id)`
       - Assert: returns False (patient has active flow)
       - Create another patient with no flow state
       - Assert: `is_new_patient()` returns True
       - Covers FLOW-02 (new-vs-existing routing)

    3. `test_flow_advancement_on_canonical_system`:
       - Create Patient + initialize flow via dispatcher
       - Use `FlowCore` or `EnhancedFlowEngine` to advance the flow by one day
       - Assert: `current_step` incremented (or next_scheduled_at updated)
       - Assert: flow state not corrupted (step_data accessible)
       - Mock Gemini AI calls since integration tests should not call real AI APIs
       - Covers FLOW-01 (canonical system works end-to-end)

    4. `test_alert_pipeline_evaluates_real_data`:
       - Create 2-3 patients with flow states in various statuses (active, completed, stuck)
       - Call `FlowAlertsService(session).evaluate_alerts()`
       - Assert: returns a list (may be empty if no thresholds triggered, but must not raise)
       - This is a smoke test proving the alert pipeline runs against real data without errors
       - Covers FLOW-03 (alert pipeline end-to-end)

    5. `test_qw021_format_absent_in_new_flows`:
       - Create Patient + initialize flow via dispatcher
       - Query `patient_flow_states` directly
       - Assert: `flow_metadata` column is either NULL or does NOT contain `flow_instance_id` key
       - This guards against the QW-021 ghost data pattern identified in research
       - Covers FLOW-01 (no QW-021 contamination)

    **Important implementation notes:**
    - Use unique phone numbers with timestamp suffix for test isolation (pattern from existing conftest)
    - Register all created patient IDs and flow state IDs immediately after creation for cleanup
    - All DB operations use `real_db_session` fixture (real PostgreSQL, not SQLite)
    - Guard tests with `CONFIRM_REAL_DB=1` environment variable check (existing pattern in conftest.py)
    - Mock external services (Gemini AI, WhatsApp) that would be called during flow operations
    - Use `from unittest.mock import patch, AsyncMock` for mocking AI calls in advancement test
    - Follow existing test naming convention: `test_<verb>_<subject>_<condition>`
  </action>
  <verify>
    1. File exists: `ls backend-hormonia/tests/integration/test_flow_consolidation.py`
    2. Correct markers: `grep -c "pytest.mark.integration" backend-hormonia/tests/integration/test_flow_consolidation.py` returns 5+ (one per test)
    3. Uses real DB fixtures: `grep "real_db_session" backend-hormonia/tests/integration/test_flow_consolidation.py` returns results
    4. Imports FlowDispatcher: `grep "FlowDispatcher" backend-hormonia/tests/integration/test_flow_consolidation.py` returns results
    5. Imports FlowAlertsService: `grep "FlowAlertsService" backend-hormonia/tests/integration/test_flow_consolidation.py` returns results
    6. No QW-021 imports: `grep "from app.services.flow.core" backend-hormonia/tests/integration/test_flow_consolidation.py` returns zero results
    7. Python syntax valid: `cd backend-hormonia && python -c "import ast; ast.parse(open('tests/integration/test_flow_consolidation.py').read()); print('Syntax OK')"`
    8. If CONFIRM_REAL_DB=1 and TEST_DATABASE_URL are available, run: `cd backend-hormonia && CONFIRM_REAL_DB=1 python -m pytest tests/integration/test_flow_consolidation.py -v --no-header 2>&1 | tail -20`
  </verify>
  <done>
    Integration test file exists with 5 test scenarios covering: new patient onboarding via FlowDispatcher, existing patient detection, flow advancement on canonical system, alert pipeline evaluation, and QW-021 format absence verification. All tests use @pytest.mark.integration, real PostgreSQL fixtures, and do not import from deleted QW-021 modules. Tests pass when run against real PostgreSQL.
  </done>
</task>

</tasks>

<verification>
1. `test_flow_consolidation.py` exists in `tests/integration/` with 5 test methods
2. All tests marked with `@pytest.mark.integration` and `@pytest.mark.asyncio`
3. Tests use `real_db_session`, `cleanup_patients`, `cleanup_flows` fixtures
4. FlowDispatcher and FlowAlertsService are tested
5. No imports from deleted QW-021 modules
6. Tests pass against real PostgreSQL (when DB is available)
</verification>

<success_criteria>
- 5 integration test scenarios implemented covering FLOW-03 requirements
- Tests run against real PostgreSQL per user decision (not mocked/in-memory)
- Tests validate FlowDispatcher routing, flow advancement, alert pipeline, and absence of QW-021 data format
- All tests pass in CI with CONFIRM_REAL_DB=1
- No references to deleted QW-021 modules in test code
</success_criteria>

<output>
After completion, create `.planning/phases/05-flow-consolidation/05-02-SUMMARY.md`
</output>
