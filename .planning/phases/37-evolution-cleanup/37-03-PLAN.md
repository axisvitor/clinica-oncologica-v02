---
phase: 37-evolution-cleanup
plan: 03
type: gap_closure
wave: 3
depends_on:
  - 37-01
  - 37-02
files_modified:
  - backend-hormonia/app/integrations/whatsapp/services/message_service.py
  - backend-hormonia/app/config/settings/__init__.py
  - backend-hormonia/app/services/unified_whatsapp_service.py
  - backend-hormonia/.env.production.example
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
  - CLEAN-06
must_haves:
  truths:
    - "App boots successfully: `TESTING=1 WHATSAPP_WUZAPI_TOKEN=dummy python3 -c 'from app.main import app; print(app.title)'` prints the app title without ImportError (CLEAN-03 unblocked)"
    - "message_service.py imports validate_and_format_phone from app.schemas.validators.phone -- not from tombstoned evolution_client (CLEAN-03)"
    - "message_service.py send_message() calls validate_and_format_phone(request.to, strict=False) and unpacks the 3-tuple (is_valid, formatted_number, error_msg) correctly (CLEAN-03)"
    - "settings/__init__.py boolean_fields list has no WHATSAPP_EVOLUTION_USE_MOCK entry (CLEAN-06)"
    - "unified_whatsapp_service.py has no getattr(settings, 'WHATSAPP_EVOLUTION_INSTANCE_NAME', ...) -- uses 'default' as instance name fallback (CLEAN-06)"
    - ".env.production.example has zero WHATSAPP_EVOLUTION_* lines and the section header is updated to WuzAPI (CLEAN-06)"
    - "grep -rn 'WHATSAPP_EVOLUTION' across backend-hormonia/app/ --include='*.py' returns zero matches outside tombstone docstrings (all CLEAN-* verified)"
  artifacts:
    - path: "backend-hormonia/app/integrations/whatsapp/services/message_service.py"
      provides: "Phone validation via canonical validate_and_format_phone (sync, 3-tuple)"
      contains: "from app.schemas.validators.phone import validate_and_format_phone"
    - path: "backend-hormonia/app/config/settings/__init__.py"
      provides: "Boolean parser without WHATSAPP_EVOLUTION_USE_MOCK"
    - path: "backend-hormonia/app/services/unified_whatsapp_service.py"
      provides: "Instance name resolved without WHATSAPP_EVOLUTION_INSTANCE_NAME fallback"
    - path: "backend-hormonia/.env.production.example"
      provides: "Production env template with WuzAPI variables only"
  key_links:
    - from: "backend-hormonia/app/integrations/whatsapp/services/message_service.py"
      to: "backend-hormonia/app/schemas/validators/phone.py"
      via: "from app.schemas.validators.phone import validate_and_format_phone"
      pattern: "validate_and_format_phone"
    - from: "backend-hormonia/app/services/unified_whatsapp_service.py"
      to: "backend-hormonia/app/config/settings/integrations.py"
      via: "No WHATSAPP_EVOLUTION_INSTANCE_NAME reference"
      pattern: "default"
---

<objective>
Fix two verification gaps that prevent Phase 37 from passing: (1) a runtime import from tombstoned evolution_client.py in message_service.py that crashes app startup, and (2) residual WHATSAPP_EVOLUTION_* references in settings parsing, unified_whatsapp_service.py, and .env.production.example.

Purpose: Phase 37 verification found 3/5 truths verified and 2 failed. Gap 1 (import crash) blocks Gap 3 (webhook 404 validation). Gap 2 (deprecated env vars in runtime) blocks CLEAN-06. Fixing these 4 files unblocks all 6 CLEAN requirements.
Output: 4 files edited, zero new files. App boots cleanly, all CLEAN-01 through CLEAN-06 pass verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-evolution-cleanup/37-RESEARCH.md
@.planning/phases/37-evolution-cleanup/37-VERIFICATION.md
@.planning/phases/37-evolution-cleanup/37-01-SUMMARY.md
@.planning/phases/37-evolution-cleanup/37-02-SUMMARY.md

Key decisions from STATE.md (LOCKED -- do not change):
- Hard cut, no dual-provider mode -- Evolution tombstoned after Phase 36
- Canonical phone validator: `app/schemas/validators/phone.py` with `validate_and_format_phone(phone, strict=False)` returning `(is_valid, formatted_phone, error_message)` -- sync function, 3-tuple
- WHATSAPP_ENABLE_SERVICE field: keep but repurpose as WuzAPI enable flag
- WHATSAPP_WUZAPI_USE_MOCK is the active mock toggle (already in boolean_fields)

<interfaces>
<!-- Canonical phone validation (from app/schemas/validators/phone.py): -->
def validate_and_format_phone(
    phone: str, default_region: str = "BR", strict: bool = True,
) -> Tuple[bool, Optional[str], Optional[str]]:
    """Returns: (is_valid, formatted_phone, error_message)"""
    # sync function, NOT async
    # strict=False returns (False, None, "error") instead of raising
    # strict=True raises PhoneValidationError on invalid

<!-- Current message_service.py import (line 16, BROKEN): -->
from .evolution_client import validate_phone_number

<!-- Current message_service.py call site (line 319): -->
is_valid, formatted_number = await validate_phone_number(request.to)
if not is_valid:
    raise ValueError(f"Invalid phone number: {formatted_number}")
# NOTE: old function was async and returned 2-tuple
# New function is sync and returns 3-tuple -- must unpack 3 values and drop await

<!-- Current settings/__init__.py boolean_fields (line 134): -->
"WHATSAPP_EVOLUTION_USE_MOCK",

<!-- Current unified_whatsapp_service.py (lines 91-93): -->
configured_instance_name = (
    resolved_instance_name
    or getattr(settings, "WHATSAPP_EVOLUTION_INSTANCE_NAME", None)
)

<!-- Current .env.production.example (lines 149-158): -->
# Webhook Security
WHATSAPP_EVOLUTION_WEBHOOK_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_VALUE

# --------------------------------------------
# WHATSAPP / EVOLUTION API - WHATSAPP_*
# --------------------------------------------
WHATSAPP_ENABLE_SERVICE=true
WHATSAPP_EVOLUTION_API_URL=https://api.evolution.production.com
WHATSAPP_EVOLUTION_INSTANCE_NAME=clinica_oncologica_prod
WHATSAPP_EVOLUTION_API_KEY=YOUR_PRODUCTION_EVOLUTION_API_KEY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix message_service.py tombstoned import -- replace with canonical phone validator</name>
  <files>backend-hormonia/app/integrations/whatsapp/services/message_service.py</files>
  <action>
**Step 1: Replace import (line 16)**

CHANGE:
```python
from .evolution_client import validate_phone_number
```
TO:
```python
from app.schemas.validators.phone import validate_and_format_phone
```

**Step 2: Update call site (line 319)**

CHANGE:
```python
        is_valid, formatted_number = await validate_phone_number(request.to)
        if not is_valid:
            raise ValueError(f"Invalid phone number: {formatted_number}")
```
TO:
```python
        is_valid, formatted_number, phone_error = validate_and_format_phone(request.to, strict=False)
        if not is_valid:
            raise ValueError(f"Invalid phone number: {phone_error}")
```

Key differences from old code:
1. **Remove `await`** -- `validate_and_format_phone` is sync, not async.
2. **Unpack 3 values** -- returns `(is_valid, formatted_phone, error_message)` instead of 2-tuple.
3. **Use `strict=False`** -- returns `(False, None, "error")` instead of raising, matching the old async function's behavior of returning a tuple with validity flag.
4. **Use `phone_error` for error message** -- the third element contains the human-readable error, not `formatted_number`. The second element is the formatted phone (used in the line below to update `request.to`).

**Step 3: Verify no other references to evolution_client in this file**

Search for any remaining `evolution_client` string in message_service.py. There should be ZERO after the import replacement. If any exist (comments, strings), remove them.
  </action>
  <verify>
    <automated>cd backend-hormonia && python -c "
errors = []

# 1. Import check
try:
    from app.integrations.whatsapp.services.message_service import WhatsAppMessageService
    print('OK: message_service.py imports without error')
except ImportError as e:
    errors.append(f'FAIL: ImportError on message_service: {e}')

# 2. App boot check (the main blocker)
import os
os.environ.setdefault('TESTING', '1')
os.environ.setdefault('WHATSAPP_WUZAPI_TOKEN', 'dummy')
os.environ.setdefault('DATABASE_URL', 'postgresql+asyncpg://x:x@localhost/x')
os.environ.setdefault('REDIS_URL', 'redis://localhost:6379/0')
try:
    from app.main import app
    print(f'OK: app boots successfully - {app.title}')
except ImportError as e:
    errors.append(f'FAIL: app boot ImportError: {e}')
except Exception as e:
    # Non-import errors (DB connect, etc.) are OK -- import chain is clean
    print(f'OK: app module loads (non-import error expected in test env: {type(e).__name__})')

if errors:
    for e in errors: print(e)
else:
    print('OK: Gap 1 resolved -- message_service uses canonical phone validator')
" 2>&1 | head -10</automated>
  </verify>
  <done>
- message_service.py imports validate_and_format_phone from app.schemas.validators.phone (not from tombstoned evolution_client)
- send_message() calls validate_and_format_phone(request.to, strict=False) and unpacks (is_valid, formatted_number, phone_error)
- App import chain no longer crashes at startup due to tombstoned evolution_client
- Zero references to evolution_client remain in message_service.py
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove residual WHATSAPP_EVOLUTION_* from settings parser, unified service, and production env template</name>
  <files>backend-hormonia/app/config/settings/__init__.py, backend-hormonia/app/services/unified_whatsapp_service.py, backend-hormonia/.env.production.example</files>
  <action>
**File 1: `app/config/settings/__init__.py`**

In the `boolean_fields` list inside the `parse_env_values` method, REMOVE line 134:
```python
            "WHATSAPP_EVOLUTION_USE_MOCK",
```

Keep `"WHATSAPP_WUZAPI_USE_MOCK",` on line 135 -- that is the active WuzAPI mock toggle.

Also update the class docstring at line 83 -- change:
```
    - IntegrationsSettings: Evolution API, Gemini AI, Celery
```
TO:
```
    - IntegrationsSettings: WuzAPI, Gemini AI, Celery
```

**File 2: `app/services/unified_whatsapp_service.py`**

CHANGE lines 91-93:
```python
        configured_instance_name = (
            resolved_instance_name
            or getattr(settings, "WHATSAPP_EVOLUTION_INSTANCE_NAME", None)
        )
```
TO:
```python
        configured_instance_name = resolved_instance_name
```

This removes the Evolution fallback. The `resolved_instance_name` comes from the `default_instance_name` constructor parameter. If it is `None`, the existing code on lines 97-98 already falls back to `"default"`, which is the correct behavior for WuzAPI (WuzAPI does not use instance names for routing; a single session is managed per container).

**File 3: `backend-hormonia/.env.production.example`**

REPLACE lines 149-158 (the entire Evolution webhook and API block):

```
# Webhook Security
WHATSAPP_EVOLUTION_WEBHOOK_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_VALUE

# --------------------------------------------
# WHATSAPP / EVOLUTION API - WHATSAPP_*
# --------------------------------------------
WHATSAPP_ENABLE_SERVICE=true
WHATSAPP_EVOLUTION_API_URL=https://api.evolution.production.com
WHATSAPP_EVOLUTION_INSTANCE_NAME=clinica_oncologica_prod
WHATSAPP_EVOLUTION_API_KEY=YOUR_PRODUCTION_EVOLUTION_API_KEY
```

WITH:

```
# Webhook Security
WHATSAPP_WUZAPI_WEBHOOK_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_VALUE

# --------------------------------------------
# WHATSAPP / WUZAPI - WHATSAPP_*
# --------------------------------------------
WHATSAPP_ENABLE_SERVICE=true
WHATSAPP_WUZAPI_BASE_URL=https://wuzapi.production.internal:8080
WHATSAPP_WUZAPI_TOKEN=YOUR_PRODUCTION_WUZAPI_TOKEN
```

Keep lines 160+ (WHATSAPP_ENABLE_ON_REGISTRATION, WHATSAPP_ENABLE_WELCOME_MESSAGE, etc.) unchanged -- those are provider-agnostic.
  </action>
  <verify>
    <automated>cd backend-hormonia && python -c "
import subprocess, sys
errors = []

# Check 1: settings/__init__.py has no WHATSAPP_EVOLUTION_USE_MOCK
with open('app/config/settings/__init__.py') as f:
    content = f.read()
if 'WHATSAPP_EVOLUTION_USE_MOCK' in content:
    errors.append('FAIL: WHATSAPP_EVOLUTION_USE_MOCK still in settings/__init__.py')
else:
    print('OK: WHATSAPP_EVOLUTION_USE_MOCK removed from boolean_fields')

# Check 2: unified_whatsapp_service.py has no WHATSAPP_EVOLUTION reference
with open('app/services/unified_whatsapp_service.py') as f:
    content = f.read()
if 'WHATSAPP_EVOLUTION' in content:
    errors.append('FAIL: WHATSAPP_EVOLUTION still in unified_whatsapp_service.py')
else:
    print('OK: WHATSAPP_EVOLUTION removed from unified_whatsapp_service.py')

# Check 3: .env.production.example has no WHATSAPP_EVOLUTION
with open('.env.production.example') as f:
    content = f.read()
if 'WHATSAPP_EVOLUTION' in content:
    errors.append('FAIL: WHATSAPP_EVOLUTION still in .env.production.example')
else:
    print('OK: WHATSAPP_EVOLUTION removed from .env.production.example')

if errors:
    for e in errors: print(e)
else:
    print('OK: Gap 2 resolved -- all WHATSAPP_EVOLUTION_* references removed')
" 2>&1 | head -10</automated>
  </verify>
  <done>
- settings/__init__.py boolean_fields no longer includes WHATSAPP_EVOLUTION_USE_MOCK
- settings class docstring references WuzAPI instead of Evolution API
- unified_whatsapp_service.py no longer reads WHATSAPP_EVOLUTION_INSTANCE_NAME as fallback
- .env.production.example has WHATSAPP_WUZAPI_* variables instead of WHATSAPP_EVOLUTION_*
- Zero WHATSAPP_EVOLUTION_* references remain in active runtime code
  </done>
</task>

</tasks>

<verification>
1. `cd backend-hormonia && grep -rn "from .evolution_client" app/integrations/whatsapp/services/message_service.py` returns zero matches (Gap 1 import fixed)
2. `cd backend-hormonia && grep -n "validate_and_format_phone" app/integrations/whatsapp/services/message_service.py` returns the import line and the call site (Gap 1 replacement confirmed)
3. `cd backend-hormonia && TESTING=1 WHATSAPP_WUZAPI_TOKEN=dummy python3 -c "from app.main import app; print('BOOT OK')"` prints "BOOT OK" without ImportError (Gap 1 + Gap 3 unblocked)
4. `cd backend-hormonia && grep -n "WHATSAPP_EVOLUTION_USE_MOCK" app/config/settings/__init__.py` returns zero matches (Gap 2a)
5. `cd backend-hormonia && grep -n "WHATSAPP_EVOLUTION" app/services/unified_whatsapp_service.py` returns zero matches (Gap 2b)
6. `grep -n "WHATSAPP_EVOLUTION" backend-hormonia/.env.production.example` returns zero matches (Gap 2c)
7. `grep -rn "WHATSAPP_EVOLUTION" backend-hormonia/app/ --include="*.py" | grep -v "TOMBSTONED\|tombstoned\|has been decommissioned\|Phase 37"` returns zero matches (full sweep)
8. `cd backend-hormonia && python -c "from app.schemas.validators.phone import validate_and_format_phone; ok, num, err = validate_and_format_phone('+5511987654321', strict=False); print(f'valid={ok} num={num}')"` prints valid=True with formatted number (canonical validator works)
</verification>

<success_criteria>
- App boots without ImportError: the message_service.py import chain to tombstoned evolution_client is severed (Gap 1 + Gap 3 resolved)
- WHATSAPP_EVOLUTION_USE_MOCK removed from settings boolean parser (Gap 2a)
- WHATSAPP_EVOLUTION_INSTANCE_NAME fallback removed from UnifiedWhatsAppService (Gap 2b)
- .env.production.example has zero WHATSAPP_EVOLUTION_* lines (Gap 2c)
- All 6 CLEAN requirements (CLEAN-01 through CLEAN-06) are now fully satisfiable -- re-verification should score 5/5 truths
</success_criteria>

<output>
After completion, create `.planning/phases/37-evolution-cleanup/37-03-SUMMARY.md`
</output>
