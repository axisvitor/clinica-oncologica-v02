---
phase: 37-evolution-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/integrations/evolution/__init__.py
  - backend-hormonia/app/integrations/evolution/client.py
  - backend-hormonia/app/integrations/evolution/message_sender.py
  - backend-hormonia/app/integrations/evolution/request_handler.py
  - backend-hormonia/app/integrations/evolution/webhook_handler.py
  - backend-hormonia/app/integrations/evolution/rate_limiter.py
  - backend-hormonia/app/integrations/evolution/validators.py
  - backend-hormonia/app/integrations/evolution/models.py
  - backend-hormonia/app/integrations/__init__.py
  - backend-hormonia/app/orchestration/saga_orchestrator/orchestrator.py
  - backend-hormonia/app/api/v2/routers/patients/crud.py
  - backend-hormonia/app/api/v2/routers/health/service_health.py
  - backend-hormonia/app/resilience/circuit_breaker/enhanced.py
  - backend-hormonia/tests/integrations/evolution/test_client_comprehensive.py
  - backend-hormonia/tests/fixtures/saga_fixtures.py
autonomous: true
requirements:
  - CLEAN-01
must_haves:
  truths:
    - "Importing from app.integrations.evolution raises ImportError with tombstone message mentioning Phase 37 and WuzAPI"
    - "All 8 files in app/integrations/evolution/ are tombstoned: __init__.py, client.py, message_sender.py, request_handler.py, webhook_handler.py, rate_limiter.py, validators.py, models.py"
    - "app/integrations/__init__.py no longer exports EvolutionClient, EvolutionAPIError, WebhookEvent, MessageType, get_evolution_client, or close_evolution_client"
    - "SagaOrchestrator.__init__ no longer accepts evolution_client parameter and does not import EvolutionClient"
    - "patients/crud.py no longer imports EvolutionClient and does not pass evolution_client to SagaOrchestrator"
    - "service_health.py no longer has Evolution API health check block"
    - "enhanced.py WHATSAPP enum value is 'wuzapi' not 'whatsapp_evolution_api'"
    - "test_client_comprehensive.py is tombstoned (pytest.mark.skip)"
    - "saga_fixtures.py no longer has mock_evolution_client fixture and saga_orchestrator fixture omits evolution_client arg"
  artifacts:
    - path: "backend-hormonia/app/integrations/evolution/__init__.py"
      provides: "ImportError tombstone sentinel for Stack A"
      contains: "raise ImportError"
    - path: "backend-hormonia/app/integrations/__init__.py"
      provides: "Clean integrations namespace without Evolution exports"
    - path: "backend-hormonia/app/orchestration/saga_orchestrator/orchestrator.py"
      provides: "SagaOrchestrator without Evolution dependency"
    - path: "backend-hormonia/app/api/v2/routers/patients/crud.py"
      provides: "Patient crud without Evolution client instantiation"
    - path: "backend-hormonia/app/resilience/circuit_breaker/enhanced.py"
      provides: "Circuit breaker enum with wuzapi label"
  key_links:
    - from: "backend-hormonia/app/orchestration/saga_orchestrator/orchestrator.py"
      to: "backend-hormonia/app/integrations/evolution/__init__.py"
      via: "Import removed -- no longer references evolution"
      pattern: "class SagaOrchestrator"
    - from: "backend-hormonia/app/api/v2/routers/patients/crud.py"
      to: "backend-hormonia/app/orchestration/saga_orchestrator/orchestrator.py"
      via: "SagaOrchestrator(db=db, redis_client=...) without evolution_client"
      pattern: "SagaOrchestrator\\(db="
---

<objective>
Tombstone all Stack A Evolution files (8 files in app/integrations/evolution/) and clean all callers that import from Stack A, so that any import of Evolution code fails immediately with ImportError.

Purpose: Stack A contains the original Evolution API client, message sender, request handler, webhook handler, rate limiter, validators, and models. All outbound messaging now flows through WuzAPI (Phase 36 complete). These files must be tombstoned so no code path can accidentally use the old Evolution client. All callers must be cleaned first to prevent startup failures.
Output: 8 tombstoned files in app/integrations/evolution/, cleaned integrations/__init__.py, cleaned orchestrator, cleaned crud.py, cleaned service_health.py, renamed circuit breaker label, tombstoned Evolution test, cleaned saga fixtures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-evolution-cleanup/37-RESEARCH.md

Key decisions from STATE.md (LOCKED -- do not change):
- Hard cut, no dual-provider mode -- Evolution tombstoned in single commit after Phase 36 passes
- Phase 37 tombstone must come AFTER Phase 36 (IdempotentMessageSender updated) to avoid Celery worker ImportError on startup
- Tombstone pattern: module-level docstring + bare `raise ImportError(...)` on first executable line (identical to Phase 12 LangGraph pattern)

<interfaces>
<!-- Tombstone pattern (from app/ai/langgraph/__init__.py): -->
"""
TOMBSTONED -- Phase 37 (Evolution Cleanup)

This module has been decommissioned. WuzAPI is the sole WhatsApp provider.
All outbound messaging: use app.integrations.wuzapi.
All inbound events: routed through /api/v2/webhooks/wuzapi.

Do not import from this module.
"""
raise ImportError(
    "app.integrations.evolution has been tombstoned in Phase 37 (Evolution Cleanup). "
    "Use app.integrations.wuzapi for WhatsApp messaging."
)

<!-- SagaOrchestrator current signature (line 56-64 of orchestrator.py): -->
class SagaOrchestrator:
    def __init__(
        self, db: Session, redis_client=None,
        evolution_client: Optional[EvolutionClient] = None,
        ...
    ):
        self.evolution_client = evolution_client

<!-- patients/crud.py (lines 754-757): -->
from app.integrations.evolution import EvolutionClient
saga = SagaOrchestrator(
    db=db, redis_client=_get_sync_redis_client(), evolution_client=EvolutionClient()
)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tombstone all 8 Stack A files and clean integrations/__init__.py</name>
  <files>backend-hormonia/app/integrations/evolution/__init__.py, backend-hormonia/app/integrations/evolution/client.py, backend-hormonia/app/integrations/evolution/message_sender.py, backend-hormonia/app/integrations/evolution/request_handler.py, backend-hormonia/app/integrations/evolution/webhook_handler.py, backend-hormonia/app/integrations/evolution/rate_limiter.py, backend-hormonia/app/integrations/evolution/validators.py, backend-hormonia/app/integrations/evolution/models.py, backend-hormonia/app/integrations/__init__.py</files>
  <action>
**For EACH of the 8 files in `backend-hormonia/app/integrations/evolution/`**, replace the entire file content with this tombstone (adjust the module path in the ImportError message for each file):

```python
"""
TOMBSTONED -- Phase 37 (Evolution Cleanup)

This module has been decommissioned. WuzAPI is the sole WhatsApp provider.
All outbound messaging: use app.integrations.wuzapi.
All inbound events: routed through /api/v2/webhooks/wuzapi.

Do not import from this module.
"""
raise ImportError(
    "app.integrations.evolution has been tombstoned in Phase 37 (Evolution Cleanup). "
    "Use app.integrations.wuzapi for WhatsApp messaging."
)
```

The tombstone text is IDENTICAL for all 8 files. The ImportError message references the package, not the individual module.

Files to tombstone:
1. `app/integrations/evolution/__init__.py`
2. `app/integrations/evolution/client.py`
3. `app/integrations/evolution/message_sender.py`
4. `app/integrations/evolution/request_handler.py`
5. `app/integrations/evolution/webhook_handler.py`
6. `app/integrations/evolution/rate_limiter.py`
7. `app/integrations/evolution/validators.py`
8. `app/integrations/evolution/models.py`

**Then clean `backend-hormonia/app/integrations/__init__.py`:**

1. REMOVE the entire import block:
```python
from .evolution import (
    EvolutionClient,
    EvolutionAPIError,
    WebhookEvent,
    MessageType,
    get_evolution_client,
    close_evolution_client,
)
```

2. REMOVE these items from the `__all__` list:
- `"EvolutionClient"`
- `"EvolutionAPIError"`
- `"WebhookEvent"`
- `"MessageType"`
- `"get_evolution_client"`
- `"close_evolution_client"`

3. Update the module docstring to remove any reference to "Evolution API" -- replace with "WuzAPI" or simply remove the Evolution mention.

4. Keep ALL other imports and __all__ entries intact (e.g., any wuzapi-related exports).
  </action>
  <verify>
    <automated>cd backend-hormonia && python -c "
try:
    from app.integrations.evolution import EvolutionClient
    print('FAIL: import should have raised ImportError')
except ImportError as e:
    if 'tombstoned' in str(e):
        print('OK: Stack A tombstoned')
    else:
        print(f'FAIL: wrong ImportError: {e}')
" 2>&1 | head -5</automated>
  </verify>
  <done>
- All 8 files in app/integrations/evolution/ contain tombstone with ImportError
- app/integrations/__init__.py no longer imports or exports any Evolution symbols
- `from app.integrations.evolution import EvolutionClient` raises ImportError with tombstone message
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean Stack A callers (orchestrator, crud, service_health, circuit breaker, tests)</name>
  <files>backend-hormonia/app/orchestration/saga_orchestrator/orchestrator.py, backend-hormonia/app/api/v2/routers/patients/crud.py, backend-hormonia/app/api/v2/routers/health/service_health.py, backend-hormonia/app/resilience/circuit_breaker/enhanced.py, backend-hormonia/tests/integrations/evolution/test_client_comprehensive.py, backend-hormonia/tests/fixtures/saga_fixtures.py</files>
  <action>
**File 1: `backend-hormonia/app/orchestration/saga_orchestrator/orchestrator.py`**

1. DELETE the import at line 26:
   ```python
   from app.integrations.evolution import EvolutionClient
   ```

2. In the `__init__` method, REMOVE the `evolution_client` parameter:
   - DELETE: `evolution_client: Optional[EvolutionClient] = None,` from the signature
   - DELETE: `self.evolution_client = evolution_client` from the body
   - If `Optional` is no longer used after this removal, remove the `Optional` import too (check first)

3. VERIFY: `evolution_client` is NOT referenced anywhere else in the file beyond lines 26, 60, 64. Do a full file search before editing.

**File 2: `backend-hormonia/app/api/v2/routers/patients/crud.py`**

1. DELETE the import at line 754 (inside a function body):
   ```python
   from app.integrations.evolution import EvolutionClient
   ```

2. At line 757, CHANGE:
   ```python
   db=db, redis_client=_get_sync_redis_client(), evolution_client=EvolutionClient()
   ```
   TO:
   ```python
   db=db, redis_client=_get_sync_redis_client()
   ```
   (Simply remove the `evolution_client=EvolutionClient()` keyword argument)

**File 3: `backend-hormonia/app/api/v2/routers/health/service_health.py`**

1. DELETE the entire Evolution API health check block (lines ~161-195):
   ```python
   # Check Evolution API if enabled
   if hasattr(settings, "ENABLE_EVOLUTION") and settings.WHATSAPP_ENABLE_SERVICE:
       try:
           from app.integrations.evolution import get_evolution_client
           ...
       except Exception as e:
           ...
   ```
   Delete from the `# Check Evolution API if enabled` comment through the end of the `except` block that handles it. Be careful to preserve the indentation and surrounding code.

**File 4: `backend-hormonia/app/resilience/circuit_breaker/enhanced.py`**

1. At line 53, CHANGE:
   ```python
   WHATSAPP = "whatsapp_evolution_api"
   ```
   TO:
   ```python
   WHATSAPP = "wuzapi"
   ```

2. Update the comment on line 8 (or nearby) that says `WhatsApp/Evolution API` to say `WhatsApp/WuzAPI`.

**File 5: `backend-hormonia/tests/integrations/evolution/test_client_comprehensive.py`**

Replace the entire file with a test tombstone:
```python
"""TOMBSTONED -- Phase 37: tested app.integrations.evolution which is now tombstoned."""
import pytest

pytestmark = pytest.mark.skip(reason="Evolution API tombstoned in Phase 37")
```

**File 6: `backend-hormonia/tests/fixtures/saga_fixtures.py`**

1. REMOVE the `mock_evolution_client` fixture (entire function, ~lines 64-78):
   ```python
   @pytest.fixture
   def mock_evolution_client():
       """Mock Evolution API client for WhatsApp messaging."""
       ...
   ```

2. UPDATE the `saga_orchestrator` fixture:
   - REMOVE `mock_evolution_client` from the fixture's parameter list (line ~83)
   - REMOVE the `mock_evolution_client` parameter from the docstring (line ~91)
   - REMOVE `evolution_client=mock_evolution_client,` from the `SagaOrchestrator(...)` call (line ~99)

3. REMOVE `"mock_evolution_client"` from the `__all__` list (line ~332).
  </action>
  <verify>
    <automated>cd backend-hormonia && python -c "
from app.orchestration.saga_orchestrator.orchestrator import SagaOrchestrator
import inspect
sig = inspect.signature(SagaOrchestrator.__init__)
params = list(sig.parameters.keys())
if 'evolution_client' in params:
    print('FAIL: evolution_client still in SagaOrchestrator.__init__')
else:
    print('OK: SagaOrchestrator cleaned')
" 2>&1 | head -5 && python -c "
from app.api.v2.routers.patients import crud
print('OK: crud imports clean')
" 2>&1 | head -5 && python -c "
from app.resilience.circuit_breaker.enhanced import CircuitBreakerName
if CircuitBreakerName.WHATSAPP.value == 'wuzapi':
    print('OK: circuit breaker label updated')
else:
    print(f'FAIL: label is {CircuitBreakerName.WHATSAPP.value}')
" 2>&1 | head -5</automated>
  </verify>
  <done>
- SagaOrchestrator no longer imports EvolutionClient and no longer accepts evolution_client parameter
- patients/crud.py no longer imports EvolutionClient and passes no evolution_client to SagaOrchestrator
- service_health.py no longer has Evolution API health check block
- CircuitBreakerName.WHATSAPP value is "wuzapi"
- test_client_comprehensive.py is tombstoned with pytest.mark.skip
- saga_fixtures.py has no mock_evolution_client fixture and saga_orchestrator fixture omits evolution_client
  </done>
</task>

</tasks>

<verification>
1. `cd backend-hormonia && python -c "from app.integrations.evolution import EvolutionClient"` raises ImportError with tombstone message
2. `cd backend-hormonia && python -c "from app.integrations.evolution.client import EvolutionClient"` raises ImportError with tombstone message
3. `cd backend-hormonia && python -c "from app.integrations.evolution.models import MessageType"` raises ImportError with tombstone message
4. `cd backend-hormonia && python -c "from app.integrations import EvolutionClient"` raises ImportError or AttributeError (not exported)
5. `cd backend-hormonia && python -c "from app.orchestration.saga_orchestrator.orchestrator import SagaOrchestrator; print('OK')"` succeeds
6. `cd backend-hormonia && python -c "from app.api.v2.routers.patients.crud import router; print('OK')"` succeeds
7. `cd backend-hormonia && python -c "from app.api.v2.routers.health.service_health import check_service_health; print('OK')"` succeeds (or the function name)
8. `grep -rn "EvolutionClient" backend-hormonia/app/orchestration/ --include="*.py"` returns zero matches
9. `grep -rn "EvolutionClient\b" backend-hormonia/app/api/v2/routers/patients/crud.py` returns zero matches
10. `grep -rn "whatsapp_evolution_api" backend-hormonia/app/resilience/ --include="*.py"` returns zero matches
</verification>

<success_criteria>
- All 8 files in app/integrations/evolution/ are tombstoned with ImportError sentinel (CLEAN-01)
- app/integrations/__init__.py has zero Evolution exports
- SagaOrchestrator has no evolution_client parameter or import
- patients/crud.py has no EvolutionClient import or instantiation
- service_health.py has no Evolution health check block
- CircuitBreakerName.WHATSAPP value is "wuzapi" (not "whatsapp_evolution_api")
- test_client_comprehensive.py is tombstoned
- saga_fixtures.py has no mock_evolution_client fixture
- All modified files import cleanly without errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-evolution-cleanup/37-01-SUMMARY.md`
</output>
