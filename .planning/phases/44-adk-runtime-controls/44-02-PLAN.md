---
phase: 44-adk-runtime-controls
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - backend-hormonia/app/api/v2/routers/adk.py
  - backend-hormonia/app/ai/adk/runtime.py
  - backend-hormonia/app/ai/adk/wrapper.py
  - backend-hormonia/app/ai/adk/session_store.py
  - backend-hormonia/tests/api/v2/test_adk.py
  - backend-hormonia/tests/unit/test_adk_tools_runtime.py
  - backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py
autonomous: true
requirements:
  - ADK-09
must_haves:
  truths:
    - "Per-invocation timeout and LLM-call budget are enforced for both runner and direct-handler fallback paths"
    - "Explicit operator cancellation transitions the invocation to a terminal cancelled state and blocks late result write-back"
    - "Runtime controls are honored without bypassing the existing PIISafeADKWrapper boundary"
  artifacts:
    - path: "backend-hormonia/app/ai/adk/runtime.py"
      provides: "Unified execution boundary for timeout, limit accounting, and cancellation-aware status transitions"
    - path: "backend-hormonia/app/api/v2/routers/adk.py"
      provides: "Canonical explicit cancel/run entry semantics with normalized output"
    - path: "backend-hormonia/tests/unit/test_adk_tools_runtime.py"
      provides: "Regression coverage for timeout, call-budget exhaustion, cancellation, and late-result discard"
  key_links:
    - from: "backend-hormonia/app/api/v2/routers/adk.py"
      to: "backend-hormonia/app/ai/adk/runtime.py"
      via: "operator runtime overrides carried to execution boundary"
      pattern: "timeout|max_llm_calls|cancel|invocation_id"
    - from: "backend-hormonia/app/ai/adk/runtime.py"
      to: "backend-hormonia/app/ai/adk/session_store.py"
      via: "terminal invocation status blocks late completion writes"
      pattern: "cancelled|completed|running|timeout|limit"
    - from: "backend-hormonia/app/ai/adk/wrapper.py"
      to: "backend-hormonia/app/ai/adk/runtime.py"
      via: "safe_run still mediates every execution path"
      pattern: "safe_run|_invoke_adk|run_adk_tool"
---

<objective>
Implement the hard runtime controls for Phase 44: enforce timeout and `max_llm_calls`, and add explicit operator cancellation with deterministic terminal behavior.

Purpose: this is the risk-heavy async-control slice that turns the Phase 44 contract into real runtime guarantees without regressing the ADK safety boundary.

Output: runtime control enforcement across both real-runner and fallback paths, plus regression tests for timeout, budget exhaustion, cancellation, and late-result suppression.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
@./.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-adk-runtime-controls/44-CONTEXT.md
@.planning/phases/44-adk-runtime-controls/44-RESEARCH.md
@.planning/phases/44-adk-runtime-controls/44-VALIDATION.md
@.planning/phases/44-adk-runtime-controls/44-01-SUMMARY.md

<interfaces>
Current execution boundary in `backend-hormonia/app/ai/adk/runtime.py`:

```python
events = executor.run_async(
    user_id=request.user_id,
    session_id=request.session_id,
    new_message=message,
)
runner_output = await _extract_runner_output(events)
```

Current wrapper guarantee in `backend-hormonia/app/ai/adk/wrapper.py`:

```python
safe_prompt = sanitize_prompt_text_for_external_ai(prompt)
result = await self._invoke_adk(safe_prompt, deps, operation=operation, context=context)
```

Current backend timeout pattern in `backend-hormonia/app/api/v2/routers/flows.py`:

```python
return await asyncio.wait_for(service.start_patient_flow(...), timeout=FLOW_SERVICE_TIMEOUT_SECONDS)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce timeout and LLM call-budget at the runtime boundary</name>
  <files>backend-hormonia/app/ai/adk/runtime.py, backend-hormonia/app/api/v2/routers/adk.py, backend-hormonia/app/ai/adk/wrapper.py, backend-hormonia/tests/api/v2/test_adk.py, backend-hormonia/tests/unit/test_adk_tools_runtime.py</files>
  <action>
    Add a unified execution boundary around both the ADK runner branch and direct-handler fallback so timeout and `max_llm_calls` behave the same regardless of runtime availability.

    Use safe server defaults with per-request overrides. Timeout must fail explicitly instead of hanging the API. LLM call-budget exhaustion must stop execution immediately with an explicit normalized limit-hit result.

    Keep this phase narrow: do not broaden into the full deterministic error taxonomy planned for Phase 45. Only timeout and explicit budget exhaustion should become first-class outcomes here.

  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py -q</automated>
  </verify>
  <done>Timeout and `max_llm_calls` are enforced over both runtime paths and tests prove the API does not hang on budget/timeout failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit cancel flow with terminal invocation-state guarding</name>
  <files>backend-hormonia/app/api/v2/routers/adk.py, backend-hormonia/app/ai/adk/runtime.py, backend-hormonia/app/ai/adk/session_store.py, backend-hormonia/tests/api/v2/test_adk.py, backend-hormonia/tests/unit/test_adk_tools_runtime.py, backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py</files>
  <action>
    Implement explicit operator cancellation for in-flight ADK invocations using the invocation metadata introduced in Plan 44-01.

    A cancel request must move the invocation to a terminal `cancelled` state, stop further processing, confirm cancellation to the operator, and prevent any late completion from writing output back into the terminal state.

    Leave the ADK session open after cancellation. Reuse an existing Redis/pubsub pattern only if the runtime needs cross-instance signaling; otherwise keep the solution as small as possible.

    Preserve the PIISafeADKWrapper contract: cancellation support cannot create a direct runtime path that bypasses prompt sanitization.

  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py tests/unit/test_pii_safe_adk_wrapper.py -q</automated>
  </verify>
  <done>Explicit cancellation works, late results are discarded, the session remains usable, and the wrapper remains the only entry to execution.</done>
</task>

</tasks>

<verification>
Run from `backend-hormonia/`:
1. `pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py -q`
2. `pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py tests/unit/test_pii_safe_adk_wrapper.py -q`
</verification>

<success_criteria>

- ADK-09 is satisfied for timeout, budget exhaustion, and explicit cancellation.
- Runtime controls apply equally to the runner path and the direct-handler fallback path.
- Cancellation is terminal at the invocation level and does not silently leak late output.
- No execution path bypasses `PIISafeADKWrapper.safe_run()`.
  </success_criteria>

<output>
After completion, create `.planning/phases/44-adk-runtime-controls/44-02-SUMMARY.md`
</output>
