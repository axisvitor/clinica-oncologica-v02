---
phase: 44-adk-runtime-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/schemas/v2/adk.py
  - backend-hormonia/app/api/v2/routers/adk.py
  - backend-hormonia/app/ai/adk/runtime.py
  - backend-hormonia/app/ai/adk/wrapper.py
  - backend-hormonia/app/ai/adk/session_store.py
  - backend-hormonia/tests/api/v2/test_adk.py
  - backend-hormonia/tests/unit/test_adk_tools_runtime.py
autonomous: true
requirements:
  - ADK-09
  - ADK-10
must_haves:
  truths:
    - "`/api/v2/adk/run` accepts explicit runtime/session control payloads without ceasing to be a thin route"
    - "ADK session and invocation metadata have an application-owned store with deterministic status, tool binding, TTL, and size accounting"
    - "Closed sessions are terminal and same-tool session continuity is enforced before runtime execution begins"
  artifacts:
    - path: "backend-hormonia/app/schemas/v2/adk.py"
      provides: "Validated operator contract for run/create/resume/close/cancel controls"
    - path: "backend-hormonia/app/ai/adk/session_store.py"
      provides: "Redis-backed session/invocation metadata primitives with inactivity and size metadata"
    - path: "backend-hormonia/app/api/v2/routers/adk.py"
      provides: "Canonical thin route delegating lifecycle-aware runtime requests"
  key_links:
    - from: "backend-hormonia/app/schemas/v2/adk.py"
      to: "backend-hormonia/app/api/v2/routers/adk.py"
      via: "validated request fields for runtime/session controls"
      pattern: "runtime|session|invocation|action"
    - from: "backend-hormonia/app/api/v2/routers/adk.py"
      to: "backend-hormonia/app/ai/adk/session_store.py"
      via: "lifecycle resolution before execution"
      pattern: "create|resume|close|cancel|session_id|invocation_id"
    - from: "backend-hormonia/app/ai/adk/runtime.py"
      to: "backend-hormonia/app/ai/adk/session_store.py"
      via: "runtime request carries session/invocation metadata"
      pattern: "session_id|invocation_id|state_size|tool_name"
---

<objective>
Define the Phase 44 contract and lifecycle foundation: explicit runtime/session controls on the canonical ADK route plus a Redis-backed application store for ADK session and invocation metadata.

Purpose: later timeout, cancellation, and bounded-state work cannot be implemented safely until route/schema/runtime semantics are explicit and lifecycle state survives across requests.

Output: validated request/response contract, route/runtime request plumbing, and an ADK session store module that owns lifecycle status, TTL, tool binding, and size-accounting metadata.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
@./.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-adk-runtime-controls/44-CONTEXT.md
@.planning/phases/44-adk-runtime-controls/44-RESEARCH.md
@.planning/phases/44-adk-runtime-controls/44-VALIDATION.md

<interfaces>
Current public contract in `backend-hormonia/app/schemas/v2/adk.py`:

```python
class ADKRunRequest(BaseModel):
    prompt: constr(min_length=1, max_length=4000)
    tool_name: constr(strip_whitespace=True, min_length=1, max_length=64)
    user_id: str | None = None
    session_id: str | None = None
    context: dict[str, Any] | None = None
```

Current route is intentionally thin in `backend-hormonia/app/api/v2/routers/adk.py`:

```python
raw_result = await PIISafeADKWrapper().safe_run(
    prompt=payload.prompt,
    deps=deps,
    operation="adk_endpoint_run",
    context=context,
)
```

Current runtime request object in `backend-hormonia/app/ai/adk/runtime.py`:

```python
@dataclass(frozen=True)
class ADKToolRunRequest:
    prompt: str
    tool_name: str
    deps: AIDeps
    user_id: str
    session_id: str
    context: dict[str, Any] | None = None
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the canonical ADK route contract for runtime and lifecycle controls</name>
  <files>backend-hormonia/app/schemas/v2/adk.py, backend-hormonia/app/api/v2/routers/adk.py, backend-hormonia/app/ai/adk/runtime.py, backend-hormonia/tests/api/v2/test_adk.py</files>
  <action>
    Extend the request/response and runtime request models so `/api/v2/adk/run` can express explicit runtime/session intent instead of hiding control information in generic `context`.

    Keep the route thin: it may validate, normalize, and delegate, but it must not absorb runtime orchestration logic.

    Introduce explicit operator-facing fields for runtime/session/invocation control, and validate impossible combinations early (for example, lifecycle actions without the identifiers they require). Preserve the established external response envelope `{status, tool_name, session_id, output}`.

    Do not implement timeout/cancel/limit behavior in this task beyond carrying the control data into the runtime contract.

  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_adk.py -q</automated>
  </verify>
  <done>Route/schema/runtime request objects carry explicit control metadata and route tests lock the public contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add Redis-backed ADK session and invocation store primitives</name>
  <files>backend-hormonia/app/ai/adk/session_store.py, backend-hormonia/app/ai/adk/runtime.py, backend-hormonia/app/api/v2/routers/adk.py, backend-hormonia/tests/unit/test_adk_tools_runtime.py</files>
  <action>
    Create a dedicated ADK session store module that owns session and invocation metadata separately from the live runner object.

    The store must support: session create/get/touch/close, invocation register/get/update, same-tool binding, terminal closed-state rejection, inactivity expiry metadata, and explicit state-size accounting fields needed by later pruning logic.

    Reuse project Redis/session patterns rather than inventing a new persistence subsystem. Keep this as application-owned metadata; do not attempt DB-backed persistent ADK sessions in this phase.

    Wire the runtime/route only enough to resolve lifecycle metadata before execution. Full timeout/cancel enforcement is deferred to Plan 44-02.

  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/test_adk_tools_runtime.py -q</automated>
  </verify>
  <done>Redis-backed session/invocation primitives exist, are test-covered, and the route/runtime can resolve lifecycle state before execution begins.</done>
</task>

</tasks>

<verification>
Run from `backend-hormonia/`:
1. `pytest tests/api/v2/test_adk.py -q`
2. `pytest tests/unit/test_adk_tools_runtime.py -q`
</verification>

<success_criteria>

- ADK-09 and ADK-10 have an explicit public contract on `/api/v2/adk/run`.
- Session and invocation lifecycle metadata survive across requests via the application store.
- Closed sessions are rejected and same-tool continuity is enforced before runtime work starts.
- The route remains a thin adapter over `PIISafeADKWrapper.safe_run()`.
  </success_criteria>

<output>
After completion, create `.planning/phases/44-adk-runtime-controls/44-01-SUMMARY.md`
</output>
