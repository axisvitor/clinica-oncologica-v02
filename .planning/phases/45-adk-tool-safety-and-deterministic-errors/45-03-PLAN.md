---
phase: 45-adk-tool-safety-and-deterministic-errors
plan: 03
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - backend-hormonia/tests/api/v2/test_adk.py
  - backend-hormonia/tests/unit/test_adk_tools_runtime.py
  - backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py
  - backend-hormonia/tests/unit/test_adk_runner_integration.py
  - .planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md
autonomous: true
requirements:
  - ADK-11
  - ADK-12
must_haves:
  truths:
    - "Repeated `policy_block`, `tool_error`, and `upstream_error` scenarios stay deterministic across the final regression suite"
    - "New deterministic classes do not regress existing timeout, cancel, limit, or session-control behaviors from Phase 44"
    - "`45-VALIDATION.md` matches the delivered automated/manual coverage for the phase"
  artifacts:
    - path: "backend-hormonia/tests/api/v2/test_adk.py"
      provides: "Route-level regression coverage for the final deterministic taxonomy"
    - path: "backend-hormonia/tests/unit/test_adk_tools_runtime.py"
      provides: "Runtime-level repeated-scenario and no-regression coverage"
    - path: ".planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md"
      provides: "Phase 45 validation map synchronized to the implemented coverage"
  key_links:
    - from: "backend-hormonia/tests/unit/test_adk_tools_runtime.py"
      to: "backend-hormonia/app/ai/adk/runtime.py"
      via: "repeated-scenario regression coverage proves deterministic outcomes"
      pattern: "policy_block|tool_error|upstream_error|timeout"
    - from: "backend-hormonia/tests/api/v2/test_adk.py"
      to: "backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py"
      via: "route contract and wrapper boundary remain aligned under the new taxonomy"
      pattern: "policy_block|tool_error|upstream_error"
    - from: ".planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md"
      to: "backend-hormonia/tests/unit/test_adk_runner_integration.py"
      via: "validation map preserves conditional runner coverage without upgrading scope to CI smoke gating"
      pattern: "test_adk_runner_integration.py|ADK-11|ADK-12"
---

<objective>
Finalize Phase 45 by locking deterministic regressions across the whole route/runtime/wrapper surface and synchronizing the Nyquist validation contract with the delivered coverage.

Purpose: the phase is only complete when repeated scenarios stay deterministic, legacy runtime controls remain stable, and the validation map reflects the actual automated and manual checks executors must keep green.

Output: full regression suite for the new taxonomy plus an updated `45-VALIDATION.md` aligned with the implemented tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
@./.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-RESEARCH.md
@.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md
@.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-02-SUMMARY.md
@backend-hormonia/tests/api/v2/test_adk.py
@backend-hormonia/tests/unit/test_adk_tools_runtime.py
@backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py
@backend-hormonia/tests/unit/test_adk_runner_integration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand repeated-scenario regressions for deterministic classes and no-regression guarantees</name>
  <files>backend-hormonia/tests/api/v2/test_adk.py, backend-hormonia/tests/unit/test_adk_tools_runtime.py, backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py, backend-hormonia/tests/unit/test_adk_runner_integration.py</files>
  <behavior>
    - Test 1: the same blocked payload returns `policy_block` on repeated runs and never executes the tool.
    - Test 2: the same tool-side failure returns `tool_error` on repeated runs in both execution branches.
    - Test 3: the same runner/bootstrap failure returns `upstream_error` on repeated runs and never falls through to tool execution.
    - Test 4: existing `timeout`, `cancelled`, `limit_exceeded`, and session-error behaviors remain unchanged while the new taxonomy is active.
  </behavior>
  <action>
    Expand and consolidate the route/unit/conditional-runner regressions so the deterministic taxonomy is locked at the end of the phase, not just proven by isolated branch tests. Keep the real-ADK path conditional and avoid introducing CI-smoke-gate work that belongs to Phase 47.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py tests/unit/test_pii_safe_adk_wrapper.py tests/unit/test_adk_runner_integration.py -q</automated>
  </verify>
  <done>The final regression suite proves deterministic repeated outcomes and preserves all shipped Phase 44 control/status behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize `45-VALIDATION.md` with the delivered coverage</name>
  <files>.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md</files>
  <action>
    Update the validation map, wave assignments, quick/full commands, and manual-only notes so `45-VALIDATION.md` reflects the actual Phase 45 test surface after implementation. Keep Nyquist focused on this phase only: conditional runner coverage stays conditional, and CI smoke gating or release-blocking workflow changes remain deferred to Phase 47.
  </action>
  <verify>
    <automated>rg -n "45-01-01|45-02-01|45-03-01|policy_block|tool_error|upstream_error" .planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md</automated>
  </verify>
  <done>The validation file mirrors the final automated/manual coverage and is ready for execution-time status updates.</done>
</task>

</tasks>

<verification>
Run from `backend-hormonia/`:
1. `pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py tests/unit/test_pii_safe_adk_wrapper.py tests/unit/test_adk_runner_integration.py -q`
2. `rg -n "policy_block|tool_error|upstream_error" ../.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md`
</verification>

<success_criteria>

- Repeated scenarios always return the same deterministic class.
- Phase 44 runtime/session statuses remain intact.
- `45-VALIDATION.md` matches the delivered regression surface.
  </success_criteria>

<output>
After completion, create `.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-03-SUMMARY.md`
</output>
