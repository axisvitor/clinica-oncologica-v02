---
phase: 45-adk-tool-safety-and-deterministic-errors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/ai/adk/runtime.py
  - backend-hormonia/app/ai/adk/wrapper.py
  - backend-hormonia/tests/api/v2/test_adk.py
  - backend-hormonia/tests/unit/test_adk_tools_runtime.py
  - backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py
autonomous: true
requirements:
  - ADK-11
  - ADK-12
must_haves:
  truths:
    - "Unsafe ADK tool calls are blocked before the registered handler or domain client executes"
    - "The canonical `/api/v2/adk/run` route returns `policy_block` without inventing a new response envelope"
    - "PIISafeADKWrapper remains the single entry to ADK execution even when policy metadata is evaluated"
  artifacts:
    - path: "backend-hormonia/app/ai/adk/runtime.py"
      provides: "App-owned pre-tool guardrail and normalized `policy_block` payload"
    - path: "backend-hormonia/app/ai/adk/wrapper.py"
      provides: "Safety boundary that still sanitizes prompts while forwarding policy context"
    - path: "backend-hormonia/tests/unit/test_adk_tools_runtime.py"
      provides: "Regression coverage that blocked requests never execute tool code"
  key_links:
    - from: "backend-hormonia/app/ai/adk/wrapper.py"
      to: "backend-hormonia/app/ai/adk/runtime.py"
      via: "safe_run constructs the runtime request that carries policy metadata"
      pattern: "safe_run|_invoke_adk|context"
    - from: "backend-hormonia/app/ai/adk/runtime.py"
      to: "backend-hormonia/tests/unit/test_adk_tools_runtime.py"
      via: "blocked scenarios are asserted before any handler/domain side effect"
      pattern: "policy_block|before_tool|blocked"
    - from: "backend-hormonia/tests/api/v2/test_adk.py"
      to: "backend-hormonia/app/api/v2/routers/adk.py"
      via: "route normalization preserves the established `{status, tool_name, session_id, output}` contract"
      pattern: "policy_block|output|tool_name"
---

<objective>
Install the Phase 45 safety foundation: an app-owned pre-tool guardrail that blocks unsafe ADK tool calls before any side effect and surfaces a normalized `policy_block` result through the existing route contract.

Purpose: ADK-11 cannot be trusted until policy evaluation happens before the tool handler or domain client runs, and ADK-12 needs the `policy_block` branch in place before broader failure classification is layered on top.

Output: runtime-owned pre-tool policy guardrail, wrapper-to-runtime policy context continuity, and regression coverage that proves blocked calls never execute the tool.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-RESEARCH.md
@.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md
@backend-hormonia/app/ai/adk/runtime.py
@backend-hormonia/app/ai/adk/wrapper.py
@backend-hormonia/app/ai/adk/tools.py
@backend-hormonia/tests/api/v2/test_adk.py
@backend-hormonia/tests/unit/test_adk_tools_runtime.py
@backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py

<interfaces>
Current runtime seam in `backend-hormonia/app/ai/adk/runtime.py`:

```python
if HAS_ADK_RUNTIME and Agent is not None and Runner is not None:
    ...
    events = executor.run_async(**run_kwargs)
    runner_output = await _extract_runner_output(events)
```

Current wrapper seam in `backend-hormonia/app/ai/adk/wrapper.py`:

```python
result = await self._invoke_adk(
    safe_prompt,
    deps,
    operation=operation,
    context=context,
)
```

Current route contract in `backend-hormonia/app/api/v2/routers/adk.py`:

```python
return ADKRunResponse(
    status=str(normalized.get("status", "success")),
    tool_name=payload.tool_name,
    session_id=str(normalized.get("session_id") or context.get("session_id") or "") or None,
    output=normalized.get("result", normalized),
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a runtime-owned pre-tool guardrail that returns deterministic `policy_block` results</name>
  <files>backend-hormonia/app/ai/adk/runtime.py, backend-hormonia/app/ai/adk/wrapper.py</files>
  <action>
    Introduce an app-owned guardrail at the runtime boundary, not inside the tool handlers. The ADK-enabled path should attach a `before_tool_callback` (or equivalent app-owned pre-tool interception) to the single-tool Agent created in `_execute_request()`, evaluate the requested tool name, arguments, and request context, and return a structured `{"status": "policy_block", "result": {...}}` payload when the call is unsafe.

    Reuse the same policy decision in the direct-handler compatibility branch so ADK-unavailable execution cannot bypass the block. Keep the implementation narrow: use explicit policy metadata and deterministic reasons, not regex parsing over model text. Do not break the outer `PIISafeADKWrapper.safe_run()` contract; prompt sanitization must still happen before any runtime evaluation.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/test_pii_safe_adk_wrapper.py tests/unit/test_adk_tools_runtime.py -q</automated>
  </verify>
  <done>Blocked requests return `policy_block` before any tool/domain side effect and the wrapper remains the mandatory entry to execution.</done>
</task>

<task type="auto">
  <name>Task 2: Lock route/runtime regression coverage for blocked tool requests</name>
  <files>backend-hormonia/tests/api/v2/test_adk.py, backend-hormonia/tests/unit/test_adk_tools_runtime.py, backend-hormonia/tests/unit/test_pii_safe_adk_wrapper.py</files>
  <action>
    Add tests that prove blocked requests preserve the canonical `/api/v2/adk/run` response envelope, return `policy_block` exactly, and never execute the registered tool handler or domain client. Cover both prompt/context-driven block conditions if the implementation supports both, but keep assertions deterministic and explicit.

    Also add a wrapper-focused regression that ensures policy metadata forwarding does not create any execution path that bypasses prompt sanitization or calls `_invoke_adk()` more than once.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py tests/unit/test_pii_safe_adk_wrapper.py -q</automated>
  </verify>
  <done>API, runtime, and wrapper tests prove ADK-11 and the `policy_block` branch of ADK-12 without regressing the existing route contract.</done>
</task>

</tasks>

<verification>
Run from `backend-hormonia/`:
1. `pytest tests/unit/test_pii_safe_adk_wrapper.py tests/unit/test_adk_tools_runtime.py -q`
2. `pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py tests/unit/test_pii_safe_adk_wrapper.py -q`
</verification>

<success_criteria>

- Unsafe tool requests are blocked before handler/domain execution.
- `policy_block` flows through the existing route envelope without a new contract.
- Wrapper prompt sanitization remains mandatory for all ADK executions.
  </success_criteria>

<output>
After completion, create `.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-01-SUMMARY.md`
</output>
