---
phase: 45-adk-tool-safety-and-deterministic-errors
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - backend-hormonia/app/ai/adk/runtime.py
  - backend-hormonia/app/ai/adk/tools.py
  - backend-hormonia/tests/api/v2/test_adk.py
  - backend-hormonia/tests/unit/test_adk_tools_runtime.py
  - backend-hormonia/tests/unit/test_adk_runner_integration.py
autonomous: true
requirements:
  - ADK-12
must_haves:
  truths:
    - "Tool-side failures classify as `tool_error` in both runner-enabled and direct-handler execution branches"
    - "Runner/bootstrap/model failures outside tool execution classify as `upstream_error` and never fall through to execute the tool afterward"
    - "Existing Phase 44 statuses (`timeout`, `cancelled`, `limit_exceeded`, session errors) remain unchanged while Phase 45 removes ambiguous generic runtime failures"
  artifacts:
    - path: "backend-hormonia/app/ai/adk/runtime.py"
      provides: "Source-aware deterministic classifier and safe fallback boundary"
    - path: "backend-hormonia/app/ai/adk/tools.py"
      provides: "Explicit tool-origin markers or wrappers that do not rely on parsing exception text"
    - path: "backend-hormonia/tests/unit/test_adk_runner_integration.py"
      provides: "Parity coverage for deterministic classes when the real ADK path is available"
  key_links:
    - from: "backend-hormonia/app/ai/adk/tools.py"
      to: "backend-hormonia/app/ai/adk/runtime.py"
      via: "tool-origin failure metadata feeds the runtime classifier"
      pattern: "tool_error|source|raise"
    - from: "backend-hormonia/app/ai/adk/runtime.py"
      to: "backend-hormonia/tests/unit/test_adk_tools_runtime.py"
      via: "deterministic class mapping and no-silent-fallback behavior are locked by tests"
      pattern: "tool_error|upstream_error|runtime_error"
    - from: "backend-hormonia/tests/unit/test_adk_runner_integration.py"
      to: "backend-hormonia/app/ai/adk/runtime.py"
      via: "runner-enabled branch proves parity with the direct-handler path"
      pattern: "Runner|tool_error|upstream_error"
---

<objective>
Replace the ambiguous Phase 44 generic runtime error path with a deterministic, source-aware classifier and a strict fallback boundary that preserves parity between ADK-enabled and ADK-unavailable execution.

Purpose: ADK-12 is not satisfied until the runtime can distinguish tool failures from upstream ADK failures without silently re-dispatching the tool after a runner failure.

Output: runtime-owned failure classifier, explicit tool/upstream failure markers, and regression coverage for `tool_error` / `upstream_error` parity.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-RESEARCH.md
@.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-VALIDATION.md
@.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-01-SUMMARY.md
@backend-hormonia/app/ai/adk/runtime.py
@backend-hormonia/app/ai/adk/tools.py
@backend-hormonia/tests/api/v2/test_adk.py
@backend-hormonia/tests/unit/test_adk_tools_runtime.py
@backend-hormonia/tests/unit/test_adk_runner_integration.py

<interfaces>
Current broad fallback risk in `backend-hormonia/app/ai/adk/runtime.py`:

```python
try:
    ...
    if wrapped_result is not None:
        return _normalize_result(wrapped_result)
except Exception:  # noqa: BLE001
    pass
```

Current terminal status mapping in `backend-hormonia/app/ai/adk/runtime.py`:

```python
_TERMINAL_INVOCATION_STATUSES = {
    "cancelled",
    "completed",
    "error",
    "limit_exceeded",
    "timeout",
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the ambiguous runtime fallback with a source-aware deterministic classifier</name>
  <files>backend-hormonia/app/ai/adk/runtime.py, backend-hormonia/app/ai/adk/tools.py</files>
  <action>
    Remove the broad `except Exception: pass` behavior from `_execute_request()` and replace it with a small runtime-owned classifier that can tell whether a failure came from the tool/domain side or from the ADK runner/bootstrap/model side. Use explicit source markers or typed local exceptions; do not parse arbitrary exception strings as the source of truth.

    Keep fallback only for genuine capability absence before execution starts. Once the runner has started, the callback has run, or the tool dispatch has begun, failures must classify deterministically instead of falling through to the direct handler. Preserve existing Phase 44 terminal statuses exactly: `timeout`, `cancelled`, `limit_exceeded`, and session-related errors must not be renamed or collapsed into the new taxonomy.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/unit/test_adk_tools_runtime.py -q</automated>
  </verify>
  <done>No covered Phase 45 failure path returns generic `runtime_error`, and post-start silent fallback is eliminated.</done>
</task>

<task type="auto">
  <name>Task 2: Add parity coverage for `tool_error` and `upstream_error` across both execution branches</name>
  <files>backend-hormonia/tests/api/v2/test_adk.py, backend-hormonia/tests/unit/test_adk_tools_runtime.py, backend-hormonia/tests/unit/test_adk_runner_integration.py</files>
  <action>
    Add tests for tool handler/domain client exceptions, runner/bootstrap/model failures, and repeated same-scenario classification. Cover both ADK-enabled and ADK-unavailable execution where applicable, and assert that runner failures do not execute the direct handler afterward.

    Keep the real-ADK runner test conditional when `google-adk` is not installed. The goal is deterministic parity, not making local feedback depend on a production-like ADK environment.
  </action>
  <verify>
    <automated>cd backend-hormonia && pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py tests/unit/test_adk_runner_integration.py -q</automated>
  </verify>
  <done>`tool_error` and `upstream_error` are locked by tests in both execution branches, and repeated scenarios map to the same class every time.</done>
</task>

</tasks>

<verification>
Run from `backend-hormonia/`:
1. `pytest tests/unit/test_adk_tools_runtime.py -q`
2. `pytest tests/api/v2/test_adk.py tests/unit/test_adk_tools_runtime.py tests/unit/test_adk_runner_integration.py -q`
</verification>

<success_criteria>

- Tool-side failures classify as `tool_error`.
- ADK runner/bootstrap/model failures classify as `upstream_error`.
- No runner-started failure silently re-executes the direct handler.
- Phase 44 terminal statuses remain stable.
  </success_criteria>

<output>
After completion, create `.planning/phases/45-adk-tool-safety-and-deterministic-errors/45-02-SUMMARY.md`
</output>
