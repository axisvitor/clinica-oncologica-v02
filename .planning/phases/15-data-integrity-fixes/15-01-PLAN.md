---
phase: 15-data-integrity-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/agents/patient/flow_coordinator/constants.py
  - backend-hormonia/app/utils/template_variables.py
  - backend-hormonia/app/tasks/flows/batch_tasks.py
  - backend-hormonia/app/domain/quizzes/quiz_trigger_policy.py
  - backend-hormonia/app/domain/quizzes/integration/flow_integration/trigger_service.py
  - backend-hormonia/tests/unit/agents/patient/flow_coordinator/test_constants_consolidation.py
autonomous: true
requirements:
  - FIX-05
  - FIX-06

must_haves:
  truths:
    - "All flow phase constants (ONBOARDING_END_DAY, DAILY_FOLLOWUP_END_DAY, MONTHLY_CYCLE_START_DAY, MONTHLY_CYCLE_DAYS) resolve to identical values regardless of which module imports them"
    - "The monthly quiz cycle number computed for any given days_since_enrollment is identical whether called from flow_coordinator, sequential_message_handler, batch_tasks, quiz_trigger_policy, or template_variables"
    - "No module defines its own copy of phase boundary constants or cycle calculation logic"
  artifacts:
    - path: "backend-hormonia/app/agents/patient/flow_coordinator/constants.py"
      provides: "Canonical phase constants AND compute_cycle_number function"
      contains: "def compute_cycle_number"
    - path: "backend-hormonia/tests/unit/agents/patient/flow_coordinator/test_constants_consolidation.py"
      provides: "Test coverage for consolidated constants and cycle calculation"
      min_lines: 40
  key_links:
    - from: "backend-hormonia/app/utils/template_variables.py"
      to: "backend-hormonia/app/agents/patient/flow_coordinator/constants.py"
      via: "import MONTHLY_CYCLE_START_DAY, MONTHLY_CYCLE_DAYS"
      pattern: "from app\\.agents\\.patient\\.flow_coordinator\\.constants import"
    - from: "backend-hormonia/app/domain/quizzes/quiz_trigger_policy.py"
      to: "backend-hormonia/app/agents/patient/flow_coordinator/constants.py"
      via: "import compute_cycle_number"
      pattern: "from app\\.agents\\.patient\\.flow_coordinator\\.constants import.*compute_cycle_number"
---

<objective>
Consolidate all phase constants and cycle calculation logic to a single canonical source.

Purpose: Eliminate constant duplication across 4 modules and divergent cycle calculation implementations that can produce different results for the same input date, causing patients to receive messages on wrong days or miss quiz triggers.

Output: Single canonical `constants.py` with all phase boundaries and a `compute_cycle_number()` function; all consumers import from this source.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend-hormonia/app/agents/patient/flow_coordinator/constants.py
@backend-hormonia/app/utils/template_variables.py
@backend-hormonia/app/tasks/flows/batch_tasks.py
@backend-hormonia/app/domain/quizzes/quiz_trigger_policy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compute_cycle_number to canonical constants and redirect all consumers</name>
  <files>
    backend-hormonia/app/agents/patient/flow_coordinator/constants.py
    backend-hormonia/app/utils/template_variables.py
    backend-hormonia/app/tasks/flows/batch_tasks.py
    backend-hormonia/app/domain/quizzes/quiz_trigger_policy.py
    backend-hormonia/app/domain/quizzes/integration/flow_integration/trigger_service.py
  </files>
  <action>
    1. In `constants.py`, add a `compute_cycle_number(days_since_enrollment: int) -> tuple[int, int]` function that returns `(monthly_cycle, day_in_cycle)`. The logic must match the existing behavior in `QuizTriggerPolicy.calculate_monthly_cycle`: if days < 45, return (0, days); else compute `days_in_monthly = days - 45`, `cycle = days_in_monthly // 30 + 1`, `day = days_in_monthly % 30 + 1`.

    2. In `template_variables.py`, remove the class-level constants `MONTHLY_PHASE_START_DAY = 46`, `MONTHLY_CYCLE_DAYS = 30`, `MONTHLY_QUIZ_DAY_IN_CYCLE = 30`. Replace them with imports from the canonical source:
       ```python
       from app.agents.patient.flow_coordinator.constants import (
           MONTHLY_CYCLE_START_DAY,
           MONTHLY_CYCLE_DAYS,
       )
       ```
       Update all references in the class methods: `cls.MONTHLY_PHASE_START_DAY` becomes `MONTHLY_CYCLE_START_DAY`, `cls.MONTHLY_CYCLE_DAYS` becomes `MONTHLY_CYCLE_DAYS`, `cls.MONTHLY_QUIZ_DAY_IN_CYCLE` becomes `DEFAULT_QUIZ_TRIGGER_DAY` (already in constants as 30).

    3. In `quiz_trigger_policy.py`, replace `QuizTriggerPolicy.calculate_monthly_cycle` body with a delegation to `compute_cycle_number`:
       ```python
       from app.agents.patient.flow_coordinator.constants import compute_cycle_number
       ```
       Keep the method signature and docstring intact. The body becomes `return compute_cycle_number(days_since_enrollment)`. Remove the hardcoded `45` and `30` from the method body. Also update `is_quiz_day` to use `compute_cycle_number` instead of inline arithmetic (replace `days_in_monthly_phase = days_since_enrollment - 45; day_in_current_cycle = (days_in_monthly_phase % 30) + 1` with a call to `compute_cycle_number`).

    4. In `trigger_service.py`, ensure the cycle calculation in `_check_quiz_due` delegates to `QuizTriggerPolicy.calculate_monthly_cycle` (which now calls canonical `compute_cycle_number`). Verify no inline arithmetic duplicates the cycle logic.

    5. In `batch_tasks.py`, the `_normalize_template_day` function already imports from canonical constants — verify it uses `MONTHLY_CYCLE_START_DAY` and `MONTHLY_CYCLE_DAYS` from the canonical import and does NOT redefine them locally.

    IMPORTANT: Do NOT change the numerical values (15, 45, 46, 30). Only consolidate WHERE they are defined. The canonical source is `flow_coordinator/constants.py`.
  </action>
  <verify>
    <automated>cd backend-hormonia && python -c "
from app.agents.patient.flow_coordinator.constants import compute_cycle_number, MONTHLY_CYCLE_START_DAY, MONTHLY_CYCLE_DAYS
assert compute_cycle_number(44) == (0, 44), 'Pre-monthly phase failed'
assert compute_cycle_number(45) == (1, 1), 'First day of cycle 1 failed'
assert compute_cycle_number(74) == (1, 30), 'Last day of cycle 1 failed'
assert compute_cycle_number(75) == (2, 1), 'First day of cycle 2 failed'
assert MONTHLY_CYCLE_START_DAY == 46
assert MONTHLY_CYCLE_DAYS == 30
print('All assertions passed')
"</automated>
    <manual>Verify no module defines its own MONTHLY_CYCLE_DAYS or MONTHLY_PHASE_START_DAY (search via grep)</manual>
  </verify>
  <done>
    A single `compute_cycle_number` function exists in constants.py; `template_variables.py` no longer defines MONTHLY_PHASE_START_DAY or MONTHLY_CYCLE_DAYS; `quiz_trigger_policy.py` delegates to the canonical function; all consumers produce identical cycle numbers for every input.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for consolidated constants and cycle calculation</name>
  <files>
    backend-hormonia/tests/unit/agents/patient/flow_coordinator/test_constants_consolidation.py
  </files>
  <action>
    Create test file with the following test cases:

    1. `test_compute_cycle_number_pre_monthly_phase` — days 0-44 return (0, days)
    2. `test_compute_cycle_number_first_cycle` — days 45-74 return (1, 1) through (1, 30)
    3. `test_compute_cycle_number_second_cycle` — day 75 returns (2, 1)
    4. `test_compute_cycle_number_boundary_day_45` — exactly day 45 returns (1, 1)
    5. `test_compute_cycle_number_boundary_day_74` — exactly day 74 returns (1, 30)
    6. `test_quiz_trigger_policy_delegates_to_canonical` — `QuizTriggerPolicy.calculate_monthly_cycle(50)` returns same as `compute_cycle_number(50)`
    7. `test_template_variables_uses_canonical_constants` — import `TemplateVariableProcessor` and verify it does NOT have `MONTHLY_PHASE_START_DAY` as a class attribute (should use module-level import from constants)
    8. `test_resolve_flow_type_and_day_consistency` — for days 46-75, `resolve_flow_type_and_day` returns cycle_day matching `compute_cycle_number` day_in_cycle

    Use standard pytest patterns. All tests should be unit tests (no DB, no external deps).
  </action>
  <verify>
    <automated>cd backend-hormonia && python -m pytest tests/unit/agents/patient/flow_coordinator/test_constants_consolidation.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 8 test cases pass, confirming constants consolidation is complete and consistent across all consumers.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "MONTHLY_PHASE_START_DAY\|MONTHLY_CYCLE_DAYS = 30" backend-hormonia/app/ --include="*.py" | grep -v constants.py | grep -v __pycache__` returns NO results (no duplicates outside canonical source)
2. `python -m pytest tests/unit/agents/patient/flow_coordinator/test_constants_consolidation.py -x` passes
3. `python -c "from app.domain.quizzes.quiz_trigger_policy import QuizTriggerPolicy; from app.agents.patient.flow_coordinator.constants import compute_cycle_number; assert QuizTriggerPolicy.calculate_monthly_cycle(100) == compute_cycle_number(100)"` passes
</verification>

<success_criteria>
- Zero duplicate constant definitions outside `flow_coordinator/constants.py`
- Single `compute_cycle_number` function used by all cycle calculation paths
- All 8 unit tests pass
- No behavioral change in cycle calculation results
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-integrity-fixes/15-01-SUMMARY.md`
</output>
