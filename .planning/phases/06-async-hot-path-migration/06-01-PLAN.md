---
phase: 06-async-hot-path-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/flow/sequential_message_handler.py
  - backend-hormonia/app/services/webhook/handlers/message_handler.py
  - backend-hormonia/app/services/response_processor/processor.py
  - backend-hormonia/app/agents/patient/flow_coordinator/coordinator.py
  - backend-hormonia/app/services/hive_mind_integration.py
autonomous: true
requirements:
  - ASYNC-01

must_haves:
  truths:
    - "All 12 TODO(async-migration) annotations in sequential_message_handler.py are resolved"
    - "Webhook message handling does not block the event loop during DB operations"
    - "Celery flow_automation.py continues to work with sync Session unchanged"
  artifacts:
    - path: "backend-hormonia/app/services/flow/sequential_message_handler.py"
      provides: "AsyncSession-based sequential message handler"
      contains: "AsyncSession"
    - path: "backend-hormonia/app/services/webhook/handlers/message_handler.py"
      provides: "Caller passes AsyncSession to SequentialMessageHandler"
    - path: "backend-hormonia/app/services/response_processor/processor.py"
      provides: "Caller passes AsyncSession to SequentialMessageHandler"
  key_links:
    - from: "backend-hormonia/app/services/flow/sequential_message_handler.py"
      to: "database"
      via: "await self.db.execute(select(...))"
      pattern: "await self\\.db\\.(execute|commit|flush)"
    - from: "backend-hormonia/app/services/webhook/handlers/message_handler.py"
      to: "backend-hormonia/app/services/flow/sequential_message_handler.py"
      via: "SequentialMessageHandler(self.db) where self.db is AsyncSession"
      pattern: "SequentialMessageHandler\\(self\\.db\\)"
---

<objective>
Migrate `sequential_message_handler.py` from sync `Session` to `AsyncSession` for all 12 annotated TODO(async-migration) instances. Update all FastAPI-path callers to pass `AsyncSession`.

Purpose: The webhook handler is the highest-throughput hot path -- every incoming WhatsApp message flows through `SequentialMessageHandler`. Sync DB calls block the event loop, creating latency under load.

Output: `sequential_message_handler.py` uses `await self.db.execute(select(...))` and `await self.db.commit()` for all 12 annotated sites. Callers in webhook/response processor/coordinator pass `AsyncSession`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-async-hot-path-migration/06-RESEARCH.md

Key reference files:
@backend-hormonia/app/database.py (get_async_db, get_async_session_factory)
@backend-hormonia/app/integrations/whatsapp/services/message_service.py (canonical AsyncSession pattern)
@backend-hormonia/app/utils/transaction_manager.py (async_transaction context manager)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert SequentialMessageHandler to AsyncSession</name>
  <files>
    backend-hormonia/app/services/flow/sequential_message_handler.py
  </files>
  <action>
Convert `SequentialMessageHandler` from sync `Session` to `AsyncSession`. This file has 12 `TODO(async-migration)` annotations that must all be resolved.

**Step 1: Change type annotation**
- Change `def __init__(self, db: Session, ...)` to `def __init__(self, db: AsyncSession, ...)`
- Update import: add `from sqlalchemy.ext.asyncio import AsyncSession`, add `from sqlalchemy import select`
- Remove `from sqlalchemy.orm import Session` if no longer needed

**Step 2: Convert all 12 annotated sync operations to async**

Follow the research catalog exactly (06-RESEARCH.md "File 1" section):

1. Line ~232 `_get_flow_day_config`: `self.db.execute(text(...)).fetchone()` -> `result = await self.db.execute(text(...)); row = result.fetchone()`
2. Line ~268 `_get_or_create_flow_state`: `self.flow_state_repo.get_active_flow(...)` -> Inline as `await self.db.execute(select(PatientFlowState).filter(...))` with `result.scalar_one_or_none()`. Add comment `# Inlined from FlowStateRepository.get_active_flow() for async compat`
3. Line ~274 `_get_or_create_flow_state`: `self.db.execute(text(...)).fetchone()`, `self.db.add(...)`, `self.db.commit()` -> `await self.db.execute(...)`, `self.db.add(...)`, `await self.db.commit()`
4. Line ~363 `_send_flow_message`: `self.db.add(message)`, `self.db.commit()` -> `self.db.add(message)`, `await self.db.commit()`
5. Line ~387 `_inject_quiz_link_if_needed`: `self.db.query(QuizTemplate).filter(...).all()` -> `result = await self.db.execute(select(QuizTemplate).filter(...)); templates = result.scalars().all()`
6. Line ~425 `_inject_quiz_link_if_needed`: `self.db.query(QuizSession).filter(...).first()` -> `result = await self.db.execute(select(QuizSession).filter(...)); session = result.scalar_one_or_none()`
7. Line ~450 `_inject_quiz_link_if_needed`: Same pattern as #6
8. Line ~459 `_inject_quiz_link_if_needed`: `self.db.commit()` -> `await self.db.commit()`
9. Line ~529 `_send_all_sequential`: `self.db.commit()` -> `await self.db.commit()`
10. Line ~657 `_send_wait_each_with_auto_advance`: `self.db.commit()` -> `await self.db.commit()`
11. Line ~846 (send wait all remaining): `self.db.commit()` -> `await self.db.commit()`
12. Line ~1154 `_set_flow_progress`: `self.db.commit()` -> `await self.db.commit()`

**Step 3: Update factory function**
- Line ~1159: `def get_sequential_message_handler(db: Session) -> SequentialMessageHandler:` -> change `Session` to `AsyncSession`

**Step 4: Remove all 12 `TODO(async-migration)` comments** after converting each site.

**IMPORTANT:**
- `self.db.add(obj)` is NOT a coroutine on AsyncSession -- do NOT await it
- `await self.db.delete(obj)` IS a coroutine on AsyncSession -- DO await it
- `await self.db.commit()`, `await self.db.flush()`, `await self.db.refresh(obj)` are all coroutines
- Do NOT modify `FlowStateRepository` or `MessageRepository` classes -- inline specific queries instead
- The `flow_automation.py` Celery caller is OUT OF SCOPE -- it stays sync with its own sync session
  </action>
  <verify>
Run: `cd backend-hormonia && grep -c "TODO(async-migration)" app/services/flow/sequential_message_handler.py` -- must return 0.
Run: `cd backend-hormonia && grep -c "await self.db" app/services/flow/sequential_message_handler.py` -- must return >= 12.
Run: `cd backend-hormonia && grep "AsyncSession" app/services/flow/sequential_message_handler.py` -- must show import and type hint.
Run: `cd backend-hormonia && python -c "from app.services.flow.sequential_message_handler import SequentialMessageHandler; print('import OK')"` -- must not raise ImportError.
  </verify>
  <done>All 12 TODO(async-migration) sites in sequential_message_handler.py use AsyncSession async operations. Factory function accepts AsyncSession. No sync Session references remain in the handler class for DB operations.</done>
</task>

<task type="auto">
  <name>Task 2: Update FastAPI-path callers to pass AsyncSession</name>
  <files>
    backend-hormonia/app/services/webhook/handlers/message_handler.py
    backend-hormonia/app/services/response_processor/processor.py
    backend-hormonia/app/agents/patient/flow_coordinator/coordinator.py
    backend-hormonia/app/services/hive_mind_integration.py
  </files>
  <action>
Update all callers that instantiate `SequentialMessageHandler(self.db)` in the FastAPI request path to ensure they pass `AsyncSession`. These callers receive `self.db` from upstream injection.

**Caller 1: message_handler.py (line ~756)**
- `handler = SequentialMessageHandler(self.db)` -- The `MessageHandler` class receives `db` in its constructor. Trace the caller chain upward to verify that `self.db` is already an `AsyncSession` (from webhook route injection). If the webhook route uses `get_db` (sync), change it to `get_async_db`. Update the `MessageHandler.__init__` type hint from `Session` to `AsyncSession` if present.

**Caller 2: processor.py (line ~679)**
- `self._sequential_handler = SequentialMessageHandler(self.db)` -- `ResponseProcessor` takes `db: Any`. Verify its callers pass async session. If `ResponseProcessor` is instantiated from the webhook handler (which receives `AsyncSession`), no change needed beyond confirming the chain.

**Caller 3: coordinator.py (line ~283)**
- `handler = SequentialMessageHandler(self.db_session)` -- `FlowCoordinator` uses `self.db_session`. Verify the injection path. If it receives sync session from Celery tasks, wrap with a note that this path should use sync SequentialMessageHandler variant or ensure async session is passed.

**Caller 4: hive_mind_integration.py (line ~483)**
- `handler = SequentialMessageHandler(self.db_session)` -- Same pattern as coordinator.

**For each caller:**
1. Trace `self.db` / `self.db_session` to its origin (constructor parameter)
2. If origin is FastAPI `Depends(get_db)`, change to `Depends(get_async_db)` and update import
3. If origin is Celery task with sync session, add `# Note: Celery path passes sync Session; SequentialMessageHandler supports both via db: Any` -- do NOT break Celery callers
4. Update type hints from `Session` to `AsyncSession` where the caller is exclusively in async FastAPI context

**IMPORTANT:** Do NOT change callers in `backend-hormonia/app/tasks/flow_automation.py` -- that is a Celery task using sync `get_db_session()` and is out of scope.
  </action>
  <verify>
Run: `cd backend-hormonia && grep -n "SequentialMessageHandler" app/services/webhook/handlers/message_handler.py app/services/response_processor/processor.py app/agents/patient/flow_coordinator/coordinator.py app/services/hive_mind_integration.py` -- verify instantiation lines exist.
Run: `cd backend-hormonia && python -c "from app.services.webhook.handlers.message_handler import MessageHandler; print('import OK')"` -- must not raise.
  </verify>
  <done>All FastAPI-path callers of SequentialMessageHandler pass AsyncSession. Celery callers are unchanged and documented. The webhook message handling chain from HTTP request to DB commit is fully async.</done>
</task>

</tasks>

<verification>
1. Zero `TODO(async-migration)` annotations remain in `sequential_message_handler.py`
2. `sequential_message_handler.py` imports `AsyncSession` and uses `await` for all DB operations
3. Webhook handler chain (message_handler -> SequentialMessageHandler) passes AsyncSession end-to-end
4. `flow_automation.py` Celery task is untouched and still uses sync Session
5. Python import check passes for all modified files
</verification>

<success_criteria>
- `grep -c "TODO(async-migration)" sequential_message_handler.py` returns 0
- All 12 sync DB operations converted to async equivalents
- Factory function `get_sequential_message_handler` accepts AsyncSession
- FastAPI callers inject AsyncSession; Celery callers unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-async-hot-path-migration/06-01-SUMMARY.md`
</output>
