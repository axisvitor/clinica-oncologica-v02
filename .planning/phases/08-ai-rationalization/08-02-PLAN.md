---
phase: 08-ai-rationalization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/ai/client.py
  - backend-hormonia/tests/unit/ai/test_circuit_breaker_exception.py
autonomous: true
requirements:
  - AI-04

must_haves:
  truths:
    - "When Gemini circuit breaker is open, generate_content() raises FeatureNotAvailableError instead of GeminiAPIError"
    - "Callers that already catch FeatureNotAvailableError (e.g. enhanced_flow_engine.py) handle the circuit-open case without code changes"
    - "FeatureNotAvailableError carries graph_name='gemini' and operation='generate_content' for structured error handling"
  artifacts:
    - path: "backend-hormonia/app/ai/client.py"
      provides: "Circuit-open path raises FeatureNotAvailableError"
      contains: "FeatureNotAvailableError"
    - path: "backend-hormonia/tests/unit/ai/test_circuit_breaker_exception.py"
      provides: "Test that circuit-open raises FeatureNotAvailableError"
      contains: "FeatureNotAvailableError"
  key_links:
    - from: "backend-hormonia/app/ai/client.py"
      to: "app.core.exceptions.FeatureNotAvailableError"
      via: "raise FeatureNotAvailableError on used_fallback=True"
      pattern: "raise FeatureNotAvailableError"
    - from: "backend-hormonia/app/services/enhanced_flow_engine.py"
      to: "backend-hormonia/app/ai/client.py"
      via: "catches FeatureNotAvailableError (already in place)"
      pattern: "except FeatureNotAvailableError"
---

<objective>
Fix the circuit breaker exception type: when Gemini circuit breaker opens, raise FeatureNotAvailableError (canonical exception) instead of GeminiAPIError (generic).

Purpose: Enable structured error handling across all Gemini callers. FeatureNotAvailableError is already caught by enhanced_flow_engine.py and defined in app/core/exceptions.py. This change makes the circuit-open signal canonical and distinguishable from actual API errors (rate limits, bad requests, etc.).

Output: 2-line fix in client.py + unit test confirming the behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-rationalization/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix circuit breaker exception type in GeminiClient.generate_content()</name>
  <files>
    backend-hormonia/app/ai/client.py
  </files>
  <action>
In `app/ai/client.py`, locate the circuit breaker fallback handling (line ~601-602):

```python
if used_fallback:
    raise GeminiAPIError("Gemini circuit breaker fallback used")
```

Replace with:

```python
if used_fallback:
    raise FeatureNotAvailableError(
        "Gemini circuit breaker open — feature unavailable",
        "gemini",
        "generate_content",
    )
```

Add the import at the top of the file (near existing exception imports):
```python
from app.core.exceptions import FeatureNotAvailableError
```

IMPORTANT: Use positional args for FeatureNotAvailableError, NOT keyword args. The constructor signature is `__init__(self, message: str, graph_name: str, operation: Optional[str] = None)` — `graph_name` is positional.

Do NOT change any other part of the circuit breaker pipeline. The `AIServiceCircuitBreaker.call_gemini()` method, the `_circuit_breaker` attribute, and the `_generate_content_internal` method all remain unchanged.

Verify that `FeatureNotAvailableError` is not already imported (it may be — check first to avoid duplicate imports).
  </action>
  <verify>
Grep for `FeatureNotAvailableError` in `client.py` — should find the import and the raise statement.
Grep for `GeminiAPIError.*circuit breaker` in `client.py` — should return 0 matches (old error removed).
Verify the import does not break: `cd backend-hormonia && python -c "from app.ai.client import GeminiClient; print('OK')"` should succeed.
  </verify>
  <done>
`generate_content()` raises `FeatureNotAvailableError("Gemini circuit breaker open — feature unavailable", "gemini", "generate_content")` when the circuit breaker is open, instead of the generic `GeminiAPIError`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit test for circuit breaker FeatureNotAvailableError</name>
  <files>
    backend-hormonia/tests/unit/ai/test_circuit_breaker_exception.py
  </files>
  <action>
Create a focused unit test file `tests/unit/ai/test_circuit_breaker_exception.py` with:

1. **Test: `test_generate_content_raises_feature_not_available_on_circuit_open`**
   - Monkeypatch `GeminiClient._circuit_breaker.call_gemini` to return `("fallback text", True)` (simulating circuit open with used_fallback=True)
   - Call `await client.generate_content("test prompt")`
   - Assert it raises `FeatureNotAvailableError` (not `GeminiAPIError`)
   - Assert `error.graph_name == "gemini"`
   - Assert `error.operation == "generate_content"`

2. **Test: `test_generate_content_returns_normally_when_circuit_closed`**
   - Monkeypatch `GeminiClient._circuit_breaker.call_gemini` to return `("valid response", False)` (normal operation)
   - Also monkeypatch the guardrail normalization/validation to pass through the response
   - Call `await client.generate_content("test prompt")`
   - Assert it returns the response text without raising

3. **Test: `test_feature_not_available_error_is_catchable_as_ai_service_error`**
   - Verify `FeatureNotAvailableError` is a subclass of `AIServiceError` — so existing broad `except AIServiceError` handlers still work
   - `assert issubclass(FeatureNotAvailableError, AIServiceError)`

Use `pytest` with `@pytest.mark.asyncio` for async tests. Use monkeypatching (not mocking) to avoid import issues with the GeminiClient singleton.
  </action>
  <verify>
Run `cd backend-hormonia && python -m pytest tests/unit/ai/test_circuit_breaker_exception.py -x -v --tb=short 2>&1 | tail -20` — all 3 tests pass.
  </verify>
  <done>
Unit tests confirm that (1) circuit-open raises FeatureNotAvailableError with correct attributes, (2) normal operation returns response without error, (3) FeatureNotAvailableError inherits from AIServiceError for backward-compatible catch blocks.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "GeminiAPIError.*circuit breaker" backend-hormonia/app/ai/client.py` returns 0 matches
2. `grep -n "FeatureNotAvailableError" backend-hormonia/app/ai/client.py` returns the import and the raise
3. `python -m pytest tests/unit/ai/test_circuit_breaker_exception.py -v` — all 3 tests pass
4. `grep -rn "except FeatureNotAvailableError" backend-hormonia/app/services/enhanced_flow_engine.py` still catches correctly (no code change needed in callers)
</verification>

<success_criteria>
- GeminiAPIError no longer raised on circuit-open condition
- FeatureNotAvailableError raised with graph_name="gemini", operation="generate_content"
- 3 unit tests passing
- Existing FeatureNotAvailableError catchers (enhanced_flow_engine.py) work without changes
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-rationalization/08-02-SUMMARY.md`
</output>
