---
phase: 38-tests-ci-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/tests/integrations/wuzapi/test_wuzapi_webhook.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "WuzAPIClient unit tests pass for text send, media types, auth header, retry on 5xx, and rate limiting"
    - "Webhook handler test for unknown event type returns {status: ignored, type: <event_type>}"
    - "HMAC missing header test confirms 403 when x-hmac-signature header is absent but secret is configured"
  artifacts:
    - path: "backend-hormonia/tests/integrations/wuzapi/test_wuzapi_webhook.py"
      provides: "Unknown event type test and missing HMAC header test"
      contains: "test_unknown_event_type_returns_ignored, test_missing_hmac_header_returns_403"
  key_links:
    - from: "tests/integrations/wuzapi/test_wuzapi_webhook.py"
      to: "app/integrations/wuzapi/webhook.py"
      via: "httpx ASGITransport integration test"
      pattern: "httpx.ASGITransport"
---

<objective>
Close TEST-01/02/03 test gaps: confirm existing WuzAPIClient tests pass, add unknown-event-type webhook test, and add missing-HMAC-header test.

Purpose: TEST-01 is already fully covered by 22 tests in test_wuzapi_client.py and test_wuzapi_media.py. TEST-02 is missing one test case (unknown event type returns `{"status": "ignored"}`). TEST-03 is missing one test case (absent `x-hmac-signature` header returns 403 when secret is configured). Both gaps are small additions to the existing `test_wuzapi_webhook.py`.

Output: Two new test functions in `test_wuzapi_webhook.py`; full wuzapi test suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-tests-ci-validation/38-RESEARCH.md

@backend-hormonia/tests/integrations/wuzapi/test_wuzapi_webhook.py
@backend-hormonia/app/integrations/wuzapi/webhook.py

<interfaces>
<!-- Existing test infrastructure in test_wuzapi_webhook.py -->

Fixtures:
- app() -> FastAPI  (mounts wuzapi router at /webhooks, overrides get_async_db)
- secret() -> str  (returns "test-webhook-secret")
- fake_redis()  (fakeredis.aioredis.FakeRedis instance)

Helpers:
- compute_hmac(body: bytes, secret: str) -> str  (HMAC-SHA256 hex digest)
- message_payload(*, event_id, sender, text) -> dict
- receipt_payload(*, event_id, sender, receipt_type) -> dict
- post_payload(client, payload, secret=None) -> httpx.Response

Webhook endpoint behavior (from webhook.py):
- Unknown event type: returns {"status": "ignored", "type": event_type}  (line 74-75)
- HMAC with secret configured but no header: signature = "" -> validate_signature returns False -> HTTPException(403)  (lines 37-42)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm TEST-01 existing coverage and add TEST-02/TEST-03 gap tests</name>
  <files>backend-hormonia/tests/integrations/wuzapi/test_wuzapi_webhook.py</files>
  <action>
1. Run the existing WuzAPIClient test suite to confirm TEST-01 is fully satisfied:
   ```
   cd backend-hormonia && .venv/bin/pytest tests/integrations/wuzapi/test_wuzapi_client.py tests/integrations/wuzapi/test_wuzapi_media.py -x -q
   ```
   All 22 tests must pass. If any fail, diagnose and fix before proceeding.

2. Add `test_unknown_event_type_returns_ignored` to `test_wuzapi_webhook.py`:
   - Construct a payload with `{"type": "PresenceUpdate", "event": {"Info": {"ID": "UNK-1"}}}` (a type that is not "Message" or "ReadReceipt")
   - Patch `get_async_redis_client` to return `fake_redis` (for idempotency to pass)
   - POST to `/webhooks/wuzapi` via httpx ASGITransport (no HMAC secret needed — default is no secret configured)
   - Assert `response.status_code == 200`
   - Assert `response.json()["status"] == "ignored"`
   - Assert `response.json()["type"] == "PresenceUpdate"`

3. Add `test_missing_hmac_header_returns_403` to `test_wuzapi_webhook.py`:
   - Build a valid JSON body bytes: `json.dumps({"type": "Message", "event": {"Info": {"ID": "HMAC-MISS-1"}}}).encode()`
   - Patch `app.integrations.wuzapi.webhook.os.environ.get` to return the `secret` fixture value (simulates secret being configured)
   - POST to `/webhooks/wuzapi` with `content=body` and headers containing ONLY `content-type: application/json` — do NOT include `x-hmac-signature` header at all
   - Assert `response.status_code == 403`

4. Run the full webhook test file to confirm all tests pass including the two new ones:
   ```
   cd backend-hormonia && .venv/bin/pytest tests/integrations/wuzapi/test_wuzapi_webhook.py -v
   ```
  </action>
  <verify>
    <automated>cd backend-hormonia && .venv/bin/pytest tests/integrations/wuzapi/test_wuzapi_webhook.py -x -q</automated>
  </verify>
  <done>
    - test_unknown_event_type_returns_ignored passes (returns status=ignored, type=PresenceUpdate)
    - test_missing_hmac_header_returns_403 passes (returns 403 when x-hmac-signature header is absent with secret configured)
    - All pre-existing webhook tests continue to pass (no regressions)
    - TEST-01 confirmed green (22 tests in test_wuzapi_client.py + test_wuzapi_media.py pass)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full wuzapi integration test suite as regression gate</name>
  <files>backend-hormonia/tests/integrations/wuzapi/test_wuzapi_webhook.py</files>
  <action>
Run the complete wuzapi integration test suite across all test files:
```
cd backend-hormonia && .venv/bin/pytest tests/integrations/wuzapi/ -v --tb=short
```

This validates:
- TEST-01: test_wuzapi_client.py + test_wuzapi_media.py (WuzAPIClient text/media/auth/retry/rate-limit)
- TEST-02: test_wuzapi_webhook.py including the new unknown event type test + test_wuzapi_extractor.py
- TEST-03: test_wuzapi_webhook.py including both existing HMAC tests and the new missing-header test

All tests must pass. If any failures, diagnose and fix the root cause — do NOT skip or mark xfail.
  </action>
  <verify>
    <automated>cd backend-hormonia && .venv/bin/pytest tests/integrations/wuzapi/ -x -q</automated>
  </verify>
  <done>
    - Full wuzapi integration suite passes (all files: test_wuzapi_client.py, test_wuzapi_media.py, test_wuzapi_mock.py, test_wuzapi_extractor.py, test_wuzapi_webhook.py, test_wuzapi_session.py)
    - Zero failures, zero errors
    - TEST-01, TEST-02, TEST-03 fully satisfied
  </done>
</task>

</tasks>

<verification>
- `cd backend-hormonia && .venv/bin/pytest tests/integrations/wuzapi/ -v` passes with 0 failures
- `test_unknown_event_type_returns_ignored` and `test_missing_hmac_header_returns_403` appear in test output
- TEST-01 coverage: text send, media send (all types), auth header, retry on 5xx, rate limiting all covered by existing tests
</verification>

<success_criteria>
1. All wuzapi integration tests pass (expected: ~75 tests across 6 files)
2. Unknown event type test confirms `{"status": "ignored", "type": "PresenceUpdate"}` response
3. Missing HMAC header test confirms 403 when header is absent but secret is configured
4. No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/38-tests-ci-validation/38-01-SUMMARY.md`
</output>
