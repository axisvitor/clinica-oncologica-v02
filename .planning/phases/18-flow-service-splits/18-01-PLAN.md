---
phase: 18-flow-service-splits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/flow_monitoring.py
  - backend-hormonia/app/services/flow_monitoring_pkg/__init__.py
  - backend-hormonia/app/services/flow_monitoring_pkg/models.py
  - backend-hormonia/app/services/flow_monitoring_pkg/metrics.py
  - backend-hormonia/app/services/flow_monitoring_pkg/health.py
  - backend-hormonia/app/services/flow_monitoring_pkg/alerting.py
  - backend-hormonia/app/services/flow_monitoring_pkg/trends.py
  - backend-hormonia/app/services/flow_monitoring_pkg/service.py
  - backend-hormonia/tests/unit/services/test_flow_monitoring_split_contract.py
autonomous: true
requirements:
  - SPLIT-04

must_haves:
  truths:
    - "FlowMonitoringService callers can still import from app.services.flow_monitoring without code changes"
    - "AlertSeverity is re-exported by the shim so callers in critical_error_escalation_pkg continue to work"
    - "Prometheus metrics (Gauge, Counter, Histogram) are defined exactly once in metrics.py to avoid double-registration"
    - "No file in the flow_monitoring_pkg split exceeds 500 lines"
    - "Metrics collection, health checks, alert lifecycle, and performance trends are in separate modules"
  artifacts:
    - path: "backend-hormonia/app/services/flow_monitoring_pkg/models.py"
      provides: "HealthStatus, PerformanceMetrics, SystemAlert dataclasses and related types"
      exports: ["HealthStatus", "PerformanceMetrics", "SystemAlert"]
    - path: "backend-hormonia/app/services/flow_monitoring_pkg/metrics.py"
      provides: "Prometheus metric definitions and collect_performance_metrics with all metric sub-methods"
      exports: ["FLOW_MESSAGES_SENT", "FLOW_ERRORS", "FLOW_PROCESSING_TIME", "FLOW_ACTIVE_PATIENTS"]
    - path: "backend-hormonia/app/services/flow_monitoring_pkg/health.py"
      provides: "System health checks: database, Redis, flow processing, message delivery, data integrity, external services"
      contains: "run_health_checks"
    - path: "backend-hormonia/app/services/flow_monitoring_pkg/alerting.py"
      provides: "Alert lifecycle: check_and_create_alerts, get_active_alerts, resolve_alert"
      contains: "check_and_create_alerts"
    - path: "backend-hormonia/app/services/flow_monitoring_pkg/trends.py"
      provides: "Performance trend analysis: message volume, error rate, response time"
      contains: "_get_performance_trends"
    - path: "backend-hormonia/app/services/flow_monitoring_pkg/service.py"
      provides: "Composed FlowMonitoringService class reassembling all mixin methods"
      contains: "class FlowMonitoringService"
    - path: "backend-hormonia/app/services/flow_monitoring.py"
      provides: "Compatibility shim re-exporting FlowMonitoringService, AlertSeverity, and data models"
      contains: "from app.services.flow_monitoring_pkg import"
  key_links:
    - from: "backend-hormonia/app/services/flow_monitoring.py"
      to: "backend-hormonia/app/services/flow_monitoring_pkg/service.py"
      via: "legacy import path re-export"
      pattern: "FlowMonitoringService"
    - from: "backend-hormonia/app/services/critical_error_escalation_pkg/models.py"
      to: "backend-hormonia/app/services/flow_monitoring.py"
      via: "AlertSeverity import"
      pattern: "from app.services.flow_monitoring import AlertSeverity"
---

<objective>
Split `app/services/flow_monitoring.py` (923 LOC) into a focused `flow_monitoring_pkg/` package with separate modules for data models, Prometheus metrics collection, health checks, alert lifecycle, and performance trends, while preserving all existing import paths via a compatibility shim.

Purpose: Isolate system health monitoring concerns from metrics collection and alert management so each responsibility can be maintained and tested independently.
Output: Six focused modules in `flow_monitoring_pkg/` plus a compatibility shim at `flow_monitoring.py` and a contract test file.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-flow-core-splits/17-13-SUMMARY.md
@backend-hormonia/app/services/flow_monitoring.py
@backend-hormonia/app/services/template_loader_pkg/__init__.py
@backend-hormonia/app/services/flow_core.py
@backend-hormonia/tests/unit/services/test_flow_core_split_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract flow_monitoring.py into focused modules in flow_monitoring_pkg/</name>
  <files>
    backend-hormonia/app/services/flow_monitoring.py
    backend-hormonia/app/services/flow_monitoring_pkg/__init__.py
    backend-hormonia/app/services/flow_monitoring_pkg/models.py
    backend-hormonia/app/services/flow_monitoring_pkg/metrics.py
    backend-hormonia/app/services/flow_monitoring_pkg/health.py
    backend-hormonia/app/services/flow_monitoring_pkg/alerting.py
    backend-hormonia/app/services/flow_monitoring_pkg/trends.py
    backend-hormonia/app/services/flow_monitoring_pkg/service.py
  </files>
  <action>
    Create `app/services/flow_monitoring_pkg/` with six focused modules:

    1) `models.py` — Extract `HealthStatus`, `PerformanceMetrics`, `SystemAlert` dataclasses and any shared type definitions. Keep them independent (no imports from sibling modules).

    2) `metrics.py` — Extract the four Prometheus metric definitions (`FLOW_MESSAGES_SENT` Gauge, `FLOW_ERRORS` Counter, `FLOW_PROCESSING_TIME` Histogram, `FLOW_ACTIVE_PATIENTS` Gauge) at module level (define exactly ONCE here — never duplicate). Extract `collect_performance_metrics` and its private helpers: `_update_flow_metrics`, `_get_average_response_time`, `_calculate_error_rate`, `_get_queue_depth`, `_get_redis_memory_usage`, `_get_database_connection_count`, `_count_stale_flows`, `_calculate_corruption_rate`. Import from `.models` for dataclass types.

    3) `health.py` — Extract `get_system_health`, `run_health_checks`, `_determine_health_status`, `_check_database_connectivity`, `_check_redis_connectivity`, `_check_flow_processing_health`, `_check_message_delivery_health`, `_check_data_integrity`, `_check_external_services`, `_get_component_health`. Import from `.models` for HealthStatus.

    4) `alerting.py` — Extract `check_and_create_alerts`, `get_active_alerts`, `resolve_alert`, `_create_alert`, `_send_critical_alert_notification`. Import from `.models` for SystemAlert.

    5) `trends.py` — Extract `_get_performance_trends`, `_get_message_volume_trend`, `_get_error_rate_trend`, `_get_response_time_trend`.

    6) `service.py` — Compose `FlowMonitoringService` by assembling methods from the mixin modules. If the original class has a single `__init__`, keep it in service.py. Use mixin pattern (matching `flow/core/service.py` convention) or direct method reassembly.

    Create `__init__.py` with exhaustive re-exports and explicit `__all__`:
    - Re-export `FlowMonitoringService` from `.service`
    - Re-export `HealthStatus`, `PerformanceMetrics`, `SystemAlert` from `.models`
    - Re-export `AlertSeverity` from `app.models.alert` (currently re-exported by flow_monitoring.py — callers in critical_error_escalation_pkg depend on this)
    - Re-export Prometheus metrics from `.metrics`

    Convert `app/services/flow_monitoring.py` into a thin shim with explicit named imports (not wildcard) matching the `flow_core.py` shim pattern:
    ```python
    """Shim — canonical code lives in flow_monitoring_pkg/. See Phase 18."""
    from app.services.flow_monitoring_pkg import (
        FlowMonitoringService,
        HealthStatus,
        PerformanceMetrics,
        SystemAlert,
        AlertSeverity,
    )
    __all__ = [
        "FlowMonitoringService",
        "HealthStatus",
        "PerformanceMetrics",
        "SystemAlert",
        "AlertSeverity",
    ]
    ```

    CRITICAL constraints:
    - Prometheus metrics must be defined EXACTLY ONCE in `metrics.py` — never copy definitions
    - `AlertSeverity` is imported from `app.models.alert` and re-exported — do not redefine it
    - Cross-module imports within the package must use relative imports (`from .models import ...`)
    - Keep all method signatures, return types, and business logic identical — this is a structural refactor only
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -c "from app.services.flow_monitoring import FlowMonitoringService, HealthStatus, PerformanceMetrics, SystemAlert, AlertSeverity; assert FlowMonitoringService is not None; print('shim exports OK')"</automated>
    <sampling_rate>run after task completion before committing task changes</sampling_rate>
  </verify>
  <done>FlowMonitoringService is split into focused modules under flow_monitoring_pkg/ with the original import path preserved as a shim and AlertSeverity correctly re-exported.</done>
</task>

<task type="auto">
  <name>Task 2: Add flow_monitoring split contract tests and verify line budgets</name>
  <files>
    backend-hormonia/tests/unit/services/test_flow_monitoring_split_contract.py
  </files>
  <action>
    Create a contract test file following the established pattern in `test_flow_core_split_contract.py`:

    1) `test_shim_resolves_to_canonical()` — Assert `from app.services.flow_monitoring import FlowMonitoringService` is the same object as `from app.services.flow_monitoring_pkg.service import FlowMonitoringService`.

    2) `test_alert_severity_reexported()` — Assert `from app.services.flow_monitoring import AlertSeverity` is the same object as `from app.models.alert import AlertSeverity`.

    3) `test_split_files_under_500_lines()` — Assert every file in `flow_monitoring_pkg/` (models.py, metrics.py, health.py, alerting.py, trends.py, service.py) has fewer than 500 lines.

    4) `test_responsibilities_split_by_module()` — Verify key methods have the expected `__module__` attribute confirming they live in the correct split module (metrics collection in metrics, health checks in health, alert lifecycle in alerting).

    Run the contract test and verify no import errors across the test suite.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m pytest tests/unit/services/test_flow_monitoring_split_contract.py -x -q</automated>
    <sampling_rate>run after this task commits, before next plan begins</sampling_rate>
  </verify>
  <done>Contract tests pass confirming shim identity, AlertSeverity re-export, line budgets, and module responsibility separation for SPLIT-04.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/unit/services/test_flow_monitoring_split_contract.py -x` passes
- `python3 -c "from app.services.flow_monitoring import FlowMonitoringService, AlertSeverity"` succeeds
- `wc -l` confirms all files in `flow_monitoring_pkg/` are under 500 lines
- No Prometheus `ValueError: Duplicated timeseries` at import time
</verification>

<success_criteria>
- `flow_monitoring.py` is a compatibility shim, not a monolith
- Metrics, health checks, alerting, trends, and data models are physically separated
- AlertSeverity is correctly re-exported for critical_error_escalation_pkg callers
- Prometheus metrics are defined exactly once
- SPLIT-04 is fully covered with automated test evidence
</success_criteria>

<output>
After completion, create `.planning/phases/18-flow-service-splits/18-01-SUMMARY.md`
</output>
