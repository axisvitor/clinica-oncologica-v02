---
phase: 18-flow-service-splits
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-hormonia/app/services/flow_dashboard.py
  - backend-hormonia/app/services/flow_dashboard_pkg/__init__.py
  - backend-hormonia/app/services/flow_dashboard_pkg/models.py
  - backend-hormonia/app/services/flow_dashboard_pkg/analytics.py
  - backend-hormonia/app/services/flow_dashboard_pkg/trends.py
  - backend-hormonia/app/services/flow_dashboard_pkg/risk.py
  - backend-hormonia/app/services/flow_dashboard_pkg/alerts.py
  - backend-hormonia/app/services/flow_dashboard_pkg/optimization.py
  - backend-hormonia/app/services/flow_dashboard_pkg/service.py
  - backend-hormonia/tests/unit/services/test_flow_dashboard_split_contract.py
autonomous: true
requirements:
  - SPLIT-03

must_haves:
  truths:
    - "FlowDashboardService callers can still import from app.services.flow_dashboard without code changes"
    - "get_flow_dashboard_service factory function is re-exported by the shim"
    - "DashboardTimeframe and TrendDirection enums are accessible from the original import path"
    - "No file in the flow_dashboard_pkg split exceeds 500 lines"
    - "Dashboard analytics, trend analysis, risk detection, alerting, and optimization are in separate modules"
  artifacts:
    - path: "backend-hormonia/app/services/flow_dashboard_pkg/models.py"
      provides: "DashboardTimeframe and TrendDirection enums plus any shared dashboard types"
      exports: ["DashboardTimeframe", "TrendDirection"]
    - path: "backend-hormonia/app/services/flow_dashboard_pkg/analytics.py"
      provides: "Dashboard overview and flow-type breakdown methods"
      contains: "get_dashboard_overview"
    - path: "backend-hormonia/app/services/flow_dashboard_pkg/trends.py"
      provides: "Patient engagement trend analysis with daily metrics, distribution, and insights"
      contains: "get_patient_engagement_trends"
    - path: "backend-hormonia/app/services/flow_dashboard_pkg/risk.py"
      provides: "At-risk patient dashboard with risk factor analysis and intervention recommendations"
      contains: "get_at_risk_patient_dashboard"
    - path: "backend-hormonia/app/services/flow_dashboard_pkg/alerts.py"
      provides: "Real-time alert checks: no-response, sentiment, performance, engagement"
      contains: "get_real_time_alerts"
    - path: "backend-hormonia/app/services/flow_dashboard_pkg/optimization.py"
      provides: "Flow optimization recommendations: timing, content, dropoffs, prioritization"
      contains: "get_flow_optimization_recommendations"
    - path: "backend-hormonia/app/services/flow_dashboard_pkg/service.py"
      provides: "Composed FlowDashboardService class reassembling all mixin methods"
      contains: "class FlowDashboardService"
    - path: "backend-hormonia/app/services/flow_dashboard.py"
      provides: "Compatibility shim re-exporting FlowDashboardService, enums, and factory"
      contains: "from app.services.flow_dashboard_pkg import"
  key_links:
    - from: "backend-hormonia/app/services/flow_dashboard.py"
      to: "backend-hormonia/app/services/flow_dashboard_pkg/service.py"
      via: "legacy import path re-export"
      pattern: "FlowDashboardService"
    - from: "backend-hormonia/app/api/v2/flows/analytics.py"
      to: "backend-hormonia/app/services/flow_dashboard.py"
      via: "DashboardTimeframe import"
      pattern: "from app.services.flow_dashboard import DashboardTimeframe"
---

<objective>
Split `app/services/flow_dashboard.py` (946 LOC) into a focused `flow_dashboard_pkg/` package with separate modules for data models, dashboard analytics, trend analysis, risk detection, alerting, and optimization recommendations, while preserving all existing import paths via a compatibility shim.

Purpose: Separate six distinct analytical concerns (overview, trends, risk, alerts, optimization, models) that currently share a single 946-line file so each can be extended independently.
Output: Seven focused modules in `flow_dashboard_pkg/` plus a compatibility shim at `flow_dashboard.py` and a contract test file.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-flow-core-splits/17-13-SUMMARY.md
@backend-hormonia/app/services/flow_dashboard.py
@backend-hormonia/app/services/template_loader_pkg/__init__.py
@backend-hormonia/app/services/flow_core.py
@backend-hormonia/tests/unit/services/test_flow_core_split_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract flow_dashboard.py into focused modules in flow_dashboard_pkg/</name>
  <files>
    backend-hormonia/app/services/flow_dashboard.py
    backend-hormonia/app/services/flow_dashboard_pkg/__init__.py
    backend-hormonia/app/services/flow_dashboard_pkg/models.py
    backend-hormonia/app/services/flow_dashboard_pkg/analytics.py
    backend-hormonia/app/services/flow_dashboard_pkg/trends.py
    backend-hormonia/app/services/flow_dashboard_pkg/risk.py
    backend-hormonia/app/services/flow_dashboard_pkg/alerts.py
    backend-hormonia/app/services/flow_dashboard_pkg/optimization.py
    backend-hormonia/app/services/flow_dashboard_pkg/service.py
  </files>
  <action>
    Create `app/services/flow_dashboard_pkg/` with seven focused modules:

    1) `models.py` — Extract `DashboardTimeframe` and `TrendDirection` enums and any shared dashboard type definitions. No imports from sibling modules.

    2) `analytics.py` — Extract `get_dashboard_overview`, `_get_flow_type_breakdown`, `_get_date_range` and related helpers. Import from `.models` for enums.

    3) `trends.py` — Extract `get_patient_engagement_trends`, `_calculate_trends`, `_get_trend_direction`, `_get_daily_engagement_metrics`, `_get_engagement_distribution`, `_get_peak_engagement_times`, `_generate_engagement_insights`. Import from `.models` for TrendDirection.

    4) `risk.py` — Extract `get_at_risk_patient_dashboard`, `_analyze_risk_factors`, `_generate_intervention_recommendations`, `_get_risk_trends`.

    5) `alerts.py` — Extract `get_real_time_alerts`, `_get_recent_alerts`, `_check_no_response_alerts`, `_check_sentiment_alerts`, `_check_performance_alerts`, `_check_engagement_alerts`.

    6) `optimization.py` — Extract `get_flow_optimization_recommendations`, `_analyze_message_timing`, `_analyze_content_effectiveness`, `_analyze_flow_dropoffs`, `_generate_optimization_recommendations`, `_prioritize_recommendations`.

    7) `service.py` — Compose `FlowDashboardService` by assembling methods from mixin modules. Keep `__init__` in service.py. Preserve the `get_flow_dashboard_service()` factory function here (or in `__init__.py` — whichever keeps the function accessible from both the package and the shim).

    Create `__init__.py` with exhaustive re-exports and explicit `__all__`:
    - Re-export `FlowDashboardService` from `.service`
    - Re-export `DashboardTimeframe`, `TrendDirection` from `.models`
    - Re-export `get_flow_dashboard_service` from `.service`

    Convert `app/services/flow_dashboard.py` into a thin shim with explicit named imports (matching `flow_core.py` pattern):
    ```python
    """Shim — canonical code lives in flow_dashboard_pkg/. See Phase 18."""
    from app.services.flow_dashboard_pkg import (
        FlowDashboardService,
        DashboardTimeframe,
        TrendDirection,
        get_flow_dashboard_service,
    )
    __all__ = [
        "FlowDashboardService",
        "DashboardTimeframe",
        "TrendDirection",
        "get_flow_dashboard_service",
    ]
    ```

    CRITICAL constraints:
    - `get_flow_dashboard_service()` factory function MUST be re-exported — `app/api/v2/flows/analytics.py` and `app/api/v2/routers/flows.py` import it
    - Cross-module imports within the package must use relative imports (`from .models import ...`)
    - Keep all method signatures, return types, and business logic identical — structural refactor only
    - `FlowAnalyticsService`, `EventType`, `RiskLevel`, `PatientRisk` are external imports from `app.services.analytics` — keep them as external imports in whichever split modules need them
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -c "from app.services.flow_dashboard import FlowDashboardService, DashboardTimeframe, TrendDirection, get_flow_dashboard_service; assert FlowDashboardService is not None; assert callable(get_flow_dashboard_service); print('shim exports OK')"</automated>
    <sampling_rate>run after task completion before committing task changes</sampling_rate>
  </verify>
  <done>FlowDashboardService is split into focused modules under flow_dashboard_pkg/ with the original import path preserved as a shim and factory function correctly re-exported.</done>
</task>

<task type="auto">
  <name>Task 2: Add flow_dashboard split contract tests and verify line budgets</name>
  <files>
    backend-hormonia/tests/unit/services/test_flow_dashboard_split_contract.py
  </files>
  <action>
    Create a contract test file following the established pattern in `test_flow_core_split_contract.py`:

    1) `test_shim_resolves_to_canonical()` — Assert `from app.services.flow_dashboard import FlowDashboardService` is the same object as `from app.services.flow_dashboard_pkg.service import FlowDashboardService`.

    2) `test_factory_function_reexported()` — Assert `from app.services.flow_dashboard import get_flow_dashboard_service` is callable and is the same function as `from app.services.flow_dashboard_pkg import get_flow_dashboard_service`.

    3) `test_enums_reexported()` — Assert `from app.services.flow_dashboard import DashboardTimeframe, TrendDirection` are the same objects as the canonical package versions.

    4) `test_split_files_under_500_lines()` — Assert every file in `flow_dashboard_pkg/` (models.py, analytics.py, trends.py, risk.py, alerts.py, optimization.py, service.py) has fewer than 500 lines.

    5) `test_responsibilities_split_by_module()` — Verify key public methods have the expected `__module__` confirming they reside in the correct split module.

    Run the contract test to confirm no import errors.
  </action>
  <verify>
    <automated>cd backend-hormonia && python3 -m pytest tests/unit/services/test_flow_dashboard_split_contract.py -x -q</automated>
    <sampling_rate>run after this task commits, before next plan begins</sampling_rate>
  </verify>
  <done>Contract tests pass confirming shim identity, factory re-export, enum re-export, line budgets, and module responsibility separation for SPLIT-03.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/unit/services/test_flow_dashboard_split_contract.py -x` passes
- `python3 -c "from app.services.flow_dashboard import FlowDashboardService, DashboardTimeframe, get_flow_dashboard_service"` succeeds
- `wc -l` confirms all files in `flow_dashboard_pkg/` are under 500 lines
</verification>

<success_criteria>
- `flow_dashboard.py` is a compatibility shim, not a monolith
- Dashboard analytics, trends, risk, alerts, optimization, and models are physically separated
- get_flow_dashboard_service factory is correctly re-exported
- DashboardTimeframe and TrendDirection enums accessible from original path
- SPLIT-03 is fully covered with automated test evidence
</success_criteria>

<output>
After completion, create `.planning/phases/18-flow-service-splits/18-02-SUMMARY.md`
</output>
